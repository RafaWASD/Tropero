# Sesión actual

> Este archivo se vacía al cerrar cada sesión y su resumen se mueve a `history.md`.
> Mientras trabajás, **mantenelo actualizado en tiempo real**, no al final.

### ✅ CIERRE ADMINISTRATIVO spec-03 MODO MANIOBRAS — FEATURE `done` (2026-06-19)
Cierre formal de la feature 03 tras la **Puerta 2 (código) aprobada por Raf** (commit `fd36a4f`). Las entradas de abajo predatan los commits finales — el estado real quedó cerrado por la tanda `3186215`→`7f5181c`:
- `3186215` **R8.4** — preview de transición de categoría offline en el resumen de maniobra.
- `0a99fe0` **R9.x** — asignación de lote opcional/manual desde el wizard de maniobra.
- `baf1436` reconciliación del **ledger de tasks.md** + cierre de **Gate 2 (seguridad)** + **T2.12** (re-check find-or-create cross-spec).
- `fd36a4f` **Puerta 2 (código) APROBADA por Raf** — spec-03 funcionalmente cerrada.
- `7f5181c` los **2 MEDIUM** de maxLength UX en inputs de texto libre (último cabo del Gate 2).

**Acción de este cierre**: `feature_list.json` feature 03 `in_progress`→**`done`** (notas reescritas al estado DONE, lo viejo a HISTÓRICO) + reconciliación de `plan.md` (C.2 + tabla de estado global + changelog) + esta entrada + entrada en `history.md`. **Ningún feature queda `in_progress`** (08 SIGSA sigue `spec_ready`, lo lleva la otra terminal). `check.mjs` rojo al cierre = flake conocido `animals_tag_unique` (23505) por la suite backend de la otra terminal en paralelo, NO regresión (spec 03 es frontend). **PENDIENTE no-bloqueante** (fuera del alcance de spec 03, NO la reabre): sync NATIVO de las tablas de maniobra (spec 15-nativo) + bastón spp-android real (spec 04) — ambos gateados por el dev build Android de Raf.

---

### CIERRE 2026-06-19 — spec-03 MODO MANIOBRAS: M6 + snap + recorte CE + M4.3 COMMITEADOS + PUSHEADOS (leader-orquestado, Opus)
Sesión de cierre del cliente de MODO MANIOBRAS. Todo commiteado y **pusheado a origin/main** (la otra terminal commiteó cut-ficha `53f0f27` primero → después yo, surgical index-only, sin pisar su WIP de spec-08):
- **`81bd66f`** — M6 circunferencia escrotal (backend gateado 0098-0100 + suite 12/12 + stream `ev_scrotal_measurements`; frontend rueda inercial + ficha) **+ el LOCK del snap de la rueda** (en react-native-web ni `snapToInterval` ni los handlers de momentum lockean → snap determinístico sobre `onScroll` settle + handlers nativos; reviewer APROBADO) **+ fixes M5 custom** (título no se recorta al crecer / sheet header-fijo+body-scroll+footer-fijo / validación scroll-al-campo+borde-rojo+inline). ⚠️ El commit `53f0f27` de la otra terminal se llevó mi M6-C.2 (ficha) entrelazado en `animal/[id].tsx`; mi commit aporta los módulos que importa (`scrotal.ts`/`event-timeline`/`wheel-picker`) → la punta compila (commit intermedio roto, inofensivo).
- **`3e2c5c0`** — fix de RECORTE del campo editable de CE (Raf lo cazó en vivo): el input estaba clavado en 88px y "40,5"/"49,5" se cortaban → auto-fit medido al valor más ancho del rango (~104px estable) + maxLength. reviewer APROBADO (tras fix-loop: 2 líneas `left/top` crudas del medidor que rompían anti-hardcode → eliminadas). e2e anti-recorte 5/5 (mide `scrollWidth ≤ clientWidth` a 360/412).
- **`f00837f`** — M4.3 verificación offline (test-only): E2E `maniobra-offline.spec.ts` (1 passed) — jornada completa 100% offline (createSession → identificación manual → 2 maniobras [sanitary+weight] → close) → reconexión → oráculo server-side. Cubre R10.1/R10.2/R10.3 (gating offline desde cache de `rodeo_data_config`)/R10.7 (orden de cierre: el oráculo cruza el `session_id` FK + sesión `closed` → si el close subiera antes, el tenant-check 0056 rechazaría). reviewer APROBADO. Gate 1 N/A.
- **Skill `design-review` + memoria `feedback_ux_basicos_sheets_forms`**: +3 reglas UX (título no se recorta al CRECER el contenido / patrón de sheet / validación scroll-al-campo+rojo+inline) — codificadas con OK de Raf tras que se me escaparan en M5.
**PENDIENTE de spec-03 (NO bloquea el uso del core en la manga)**: R8.4 (preview de transición de categoría offline en resumen/ficha, M4.1 `[~]`, add-on diferido) · T2.12 (re-check find-or-create cross-spec, atado a spec-09) · auditoría/limpieza del ledger de `tasks.md` (varios chunks M1-M3 con nota "pendiente reviewer+Gate2" aunque el Gate 2 transversal se cerró el 2026-06-17; M5-B figura `[~]` aunque está aplicado) · Puerta 2 humana (código) de la feature. Feature 03 sigue `in_progress` (no `done`).

### CUT-FICHA DONE + COMMITEADO + PUSHEADO (leader-orquestado, Opus, 2026-06-19) — marcar/quitar CUT (descarte) desde la ficha + indicador amarillo — feature CERRADA (otra terminal: este chunk es de spec 02, NO toca spec 03/08)
Delta de **spec 02** (modelo-animal), frontend puro (sin schema/RLS/Edge → Gate 1 N/A), pedido por Raf esta sesión. Afordancia en la ficha para **marcar/quitar CUT** (descarte) en **hembras activas ≠ ternera**, reusando los builders existentes (`buildSetCutUpdate`/`buildUnsetCutUpdate`/`resolveCutCategory`); el desmarcado resetea `is_cut` y **suprime la card 'Quitar fijación' genérica cuando `isCut`** (RCUT.5.7, hueco que el leader cazó cuestionando la spec). Badge de categoría CUT en **AMARILLO** (par de tokens amber nuevo `cutText #855300`/`cutBg #FBE6AE`, contraste 5.27:1 / 6.49:1) — verde intacto para el resto vía `CategoryBadge`. Gate de cliente por `dientes` (no ofrecer lo que el server `0054` rechazaría). SDD completo: Gate 0 (`context-cut-ficha.md`, aprobado por Raf) → spec (`{requirements,design,tasks}-cut-ficha.md`) → implementer (núcleos puros `cut-eligibility.ts`/`cut-service-core.ts`) → **reviewer APROBADO** (`review_02-cut-ficha.md`) + **Gate 2 security PASS 0 HIGH** (`security_code_02-cut-ficha.md`) → **veto de diseño del leader PASS** (render `design/veto-cut-badge/`) → puerta de código de Raf. Unit 141+282 verdes, typecheck + anti-hardcode limpios. **E2E `cut-ficha.spec.ts` 1 passed** (marcar→badge amarillo→quitar→verde, web táctil; el exit 127 + assertion libuv al final = crash de teardown de Node en Windows, NO fallo de test). **Commiteado + pusheado: `53f0f27` en origin/main** (slice QUIRÚRGICO index-only: `tamagui.config.ts` grupo color + `run-tests.mjs` solo mis 2 tests; NO pisé el WIP sin commitear de spec-03/08 de la otra terminal). Spec 02 sigue `deferred` (delta, no cierra la feature). Plan/mapa: `progress/impl_02-cut-ficha.md`.

### M6-C.2 DONE (implementer Opus, 2026-06-18) — tarjeta de tendencia de CE en la ficha + CE en el timeline (R14.14) — pendiente reviewer + Gate 2 (= N/A a confirmar)
Feature `03-modo-maniobras` (in_progress). Chunk **M6-CLIENTE / M6-C.2** (ÚLTIMO de M6). **Frontend PURO, display read-only** (reusa `fetchScrotalHistory`/`buildScrotalHistoryQuery` de C.1; NO toca la vista server `animal_timeline` — composición de la CE en el CLIENTE, DM6 §12.6 opción (a)). Construido: (1) `event-timeline.ts` kind `'scrotal'` (`TimelineKind`+variante `TimelineItem`) + `isDateOnlyKind('scrotal')` + helpers PUROS `scrotalRowsToTimelineItems`/`describeScrotalTimeline` ("36,5 cm · 24 meses", reusa `formatCmAR`)/`formatAgeMonthsAR` (no snapea — snapshot R14.8) + **`sortTimelineItems` EXTRAÍDO de `parseTimeline`** para mergear la CE compuesta con el riel server; (2) `TimelineEvent.tsx` `case 'scrotal'` (ícono Ruler $primary, "Circunferencia escrotal"); (3) `maneuver-applicability.ts` predicado PURO `isBullEntire(categoryCode,isCastrated)` (única fuente del set {torito,toro}; `appliesToAnimal('circunferencia_escrotal')` lo reusa — DRY); (4) `animal/[id].tsx` `scrotalHistory` (load blando SOLO si `isBullEntire`) + `composedTimeline` (memo, merge+sort → CE en el riel) + `ScrotalTrendSection` (mini-tendencia de BARRAS con tokens `$greenLight`/`$primary`, sin lib de charts, orden cronológico, la más reciente resaltada + serie cm es-AR + "edad·fecha" + lista capeada/peek/fade `scrollFades` + empty cálido); (5) `e2e/helpers/admin.ts` `seedScrotalMeasurement`. **typecheck + anti-hardcode 0 + 1559 client unit (incluye +12 nuevos) VERDES**; e2e `ficha-circunferencia-escrotal.spec.ts` **4/4** (toro 3 CE → tarjeta+serie es-AR+mini-tendencia+CE en timeline; toro 7 CE → lista scrollea peek/fade; hembra → sin tarjeta; castrado con CE histórica → sin tarjeta). Capturas `tests/modo-maniobra/ficha-ce-{tarjeta,timeline,serie-larga}-{360,412}.png` (título sin recorte verificado). check.mjs rojo = SOLO flake `animals_tag_unique` (23505, otra terminal spec-08, `reference_check_red_rate_limit`) — NO regresión (frontend display). Reconciliado design §12.6 (AS-BUILT, opción (a)) + tasks M6-C.2 ([x]); requirements R14.14 NO tocado (se cumple tal cual). **Gate 2 = N/A** (display read-only; sin cambio de data-path/inputs/auth/schema; lectura local de tabla ya gateada por RLS + WAL). NO `git add -A`, NO spec-08, NO feature_list. **NO marqué la feature done.** Plan/mapa R→test/autorrevisión/reconciliación: `progress/impl_03-m6-c2.md`.

### M6-C.1 DONE (implementer Opus, 2026-06-18) — cablear la CIRCUNFERENCIA ESCROTAL al flujo real de MODO MANIOBRAS — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M6-CLIENTE / M6-C.1**. Frontend puro + write-path local. **Backend M6 YA APLICADO al remoto** (0098/0099/0100 live; `supabase/tests/scrotal/run.cjs` 12/12 verde = oráculo server). Componente aprobado por Raf (`CircunferenciaEscrotalStep.tsx`/`WheelPicker.tsx`/`wheel-picker.ts`) → se ENCHUFA, no se rediseña. Gate 1 N/A (frontend); Gate 2 después. Plan: T1 `maneuver-gating.ts` (ManeuverKind+REQS); T2 `maneuver-step-kind.ts` (StepKind 'rueda'); T3 `maneuver-applicability.ts` (isCastrated + regla R14.2/R14.3); T4 `maneuver-sequence.ts` (StepValue 'scrotal' + describe); T5 `maneuver-event-query.ts` (case 'scrotal'); T6 `scrotal.ts` + builders `local-reads.ts`; T7 `carga.tsx` case 'rueda' (initialCm última medida o 36 + edad prefill birth_date); T8 polish outline campo editable verde/$primary; T9 e2e + capturas 360/412 web táctil + fila server; T10 autorrevisión + reconciliación design §12/tasks M6-C.1. ⚠️ NO toco spec-08 ni feature_list. Plan/baseline/mapa: `progress/impl_03-m6-c1.md`.

**Reconciliación post-seed M6 (2026-06-18) — plantilla de cría 26 → 27.** El seed `0099` (R14.18, live) habilitó `circunferencia_escrotal` por defecto en cría → catálogo global activo 26→**27**, `system_default_fields`/`rodeo_data_config` de cría 26→**27** filas (enabled 23→**24**; OFF siguen siendo `inseminacion`/`peso_nacimiento`/`tuberculosis`). Regresión REAL del seed M6 (distinta del flake). Reconciliados los conteos VIVOS: `supabase/tests/animal/run.cjs` (13 conteos + 5 comentarios, nota `+1 por circunferencia_escrotal`) + comentario `app/src/utils/rodeo-template.ts:13` (27 filas) + comentario STALE del header `CircunferenciaEscrotalStep.tsx:3-6` (ya cableado en M6-C.1) + nota AS-BUILT `design.md §12.4`. Referencias HISTÓRICAS (0018, CONTEXT/04, specs/progress de spec-02, "23 chars" de 0099) dejadas intactas (describen el origen, no el conteo vivo). `check.mjs`: regresiones `27!=26`/`24!=23` ARREGLADAS; queda SOLO el flake `animals_tag_unique` (23505, tag literal `'9'×64`, otra terminal en spec-08) = 107 pass / 2 fail (leaf `R2 INPUT-1` + su suite padre por cascada). Detalle: `progress/impl_03-m6-c1.md`.

### M5-CUSTOMFIELDSHEET-FIX DONE (implementer Opus, 2026-06-18) — el sheet de CREAR dato/maniobra custom recortaba el título + error a nivel de campo (Raf lo cazó EN VIVO) — pendiente reviewer; Gate 2 = N/A
Feature `03-modo-maniobras` (in_progress). Chunk **M5-CLIENTE** (`CustomFieldSheet.tsx`, M5-C.2, ya en prod). Frontend PURO de presentación, **NO toca backend ni `validateCustomFieldDraft`** (solo CÓMO se muestra el error + el layout). Causa raíz: el `ScrollView` del form NO tenía `flex:1` y el `FormError` era un banner entre el scroll y los botones → al crecer el contenido o aparecer el error, el sheet (anclado abajo, `maxHeight:90%`) clippeaba el TOPE → el título "Nueva maniobra" se recortaba. **FIX 1 (layout robusto)**: header fijo (`flexShrink:0`) + cuerpo `ScrollView flex:1 minHeight:0` + footer fijo → título SIEMPRE completo. **FIX 2 (error a nivel de campo)**: helper PURO nuevo `customFieldErrorTarget(draft)` (mapea draft→campo culpable `'label'|'options'|null`, misma precedencia que la validación, sin re-decidir; test de consistencia) → scroll al campo (Nombre `scrollTo({y})` / Opciones `scrollToEnd` diferido 1 frame) + borde `$terracota` (token) + mensaje INLINE en ese campo; banner del fondo ELIMINADO; limpieza al editar. **FIX 3 (sweep)**: `ManeuverConfigSheet` tocado (mismo patrón); `Exit/NuevaJornadaConfirm/OtherRodeo/SavePreset/SyncRechazo/CandidatePicker/age-sheet-CE` auditados y YA OK (contenido fijo o ScrollView hard-capeado → título no clipeable). typecheck + anti-hardcode 0 + `custom-field.test.ts` 30 (23+7) verdes; e2e nuevo `maniobra-customfield-validacion.spec.ts` 1/1 (web táctil 360/412: reposo completo + error enum_multi-0-opciones reproducido con título completo + editor resaltado/scrolleado + mensaje inline; título-no-recortado por bounding-box); regresión `maniobra-custom`/`maniobra-wizard`(ManeuverConfigSheet)/`custom-bugfix`/`custom-render`(enum/numeric) verde. **Nota de entorno**: la otra terminal tiene spec-08 + un delta spec-03 (custom-attributes ficha-render) sin commitear → `maniobra-custom-render.spec.ts:195` (propiedad "Manchado" en la ficha) falla por ESE delta ajeno, NO usa ninguno de los 3 archivos de este fix → no es regresión mía. Capturas `tests/modo-maniobra/customfield-{reposo,error}-{360,412}.png`. Reconciliado design §11.6 (AS-BUILT) + tasks.md (`[x]`). **Gate 2 = N/A** (presentación; sin cambio de data-path/inputs/auth/schema). Plan/causa-raíz/sweep/mapa R→test/autorrevisión/reconciliación: `progress/impl_03-m5-customfieldsheet-fix.md`. **NO marqué la feature done.**

### M5-CUSTOM-BUGFIX EN CURSO (implementer Opus, 2026-06-18) — 2 defectos visuales de la maniobra CUSTOM cazados EN VIVO por Raf — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M5-CLIENTE** (custom maneuvers, ya en prod). Frontend PURO, NO se toca backend. **Bug 1**: el título del paso de maniobra custom se RECORTA (gotcha de truncado/lineHeight). **Bug 2**: en las listas enum_single/enum_multi no se nota que se puede SCROLLEAR para ver más opciones. Plan: T1 reproducir ambos (e2e nuevo título largo + enum 8-12 opciones, captura del estado roto); T2 fix título (length-aware step-down + wrap 2 líneas + lineHeight matching en `carga.tsx`); T3 fix affordance de scroll (fade + peek + ScrollView en enum_single, que hoy no scrollea); T4 capturas 360/412 web táctil; T5 check + autorrevisión + reconciliar design §11.6. ⚠️ NO toco spec-08 ni feature_list. **Nota de entorno**: typecheck rojo por debris ajeno (`app/src/utils/cut-eligibility.ts` UNTRACKED con `</content>` pegado, otra terminal) — no lo introduje ni lo toco. Plan/baseline: `progress/impl_03-m5-custom-bugfix.md`.

### M6 — Circunferencia escrotal: Gate 0 ✅ + spec ✅ + Gate 1 ✅ + Puerta 1 ✅ + M6-BACKEND ✅ (reviewer mérito + Gate 2 PASS) — PENDIENTE DEPLOY de Raf (2026-06-17)
Maniobra nueva de spec-03 (chunk **M6**), pedida por Raf. **Gate 0** cerrado (`context-m6-circunferencia-escrotal.md`, research de CE integrado: rueda 20–50/0,5 cm, longitudinal, clasificación diferida) + aprobado por Raf + 4 decisiones (aplica toritos/toros enteros; edad auto+manual; carga=solo nº; tarjeta de tendencia en ficha). **spec_author (Opus)** foldó **US-14 (R14.1–R14.18)** + `design.md` §12 + `tasks.md` chunk M6 (**M6-BACKEND** B.1–B.5 tabla typed `scrotal_measurements` + data_key + seed cría + gating capa 2 + RLS + sync; **M6-CLIENTE** C.0 design spike de la rueda → C.1 StepKind+write-path → C.2 tarjeta ficha). **Veto del leader**: resolví **DM6-6** (divergencia: el as-built guarda año-solo como `birth_date='AAAA-07-01'`, indistinguible de fecha exacta → la edad se **prellena + es siempre ajustable** por rueda de meses, R14.6/R14.7 reescritas); acepté DM6-1..DM6-5 + 2 sub-divergencias menores (reuso `tg_event_session_tenant_check` 0056; timeline compuesto en cliente). **Gate 1 (security spec) PASS 0 HIGH** (`security_spec_03-m6-circunferencia.md`): RLS espeja post-M5 `custom_measurements` (no IDOR), anti-spoof `0077`/`0043`, gating fail-closed `0054`, seed global sin camino de cliente, frontera WAL paridad `ev_*`. **2 MEDIUM foldeados** (M6-SEC-01 test loop gating→surfacing R10.8; M6-SEC-02 test no-bypass `session_id` en M6-B.5 caso (i)). **Puerta 1 APROBADA por Raf (2026-06-17).** **M6-BACKEND implementado** (`0098/0099/0100` + suite no-bypass a–i + delta sync; `progress/impl_03-m6-backend.md`): **reviewer ✅ en el mérito** (su CHANGES_REQUESTED es SOLO por el flake `animals_tag_unique` de la otra terminal, no código M6) + **Gate 2 (code) ✅ PASS 0 HIGH**. 2 menores a foldear en el apply (M6-CODE-01 sub-assert UPDATE-path en test (b); LOW measured_at → backlog). **PENDIENTE: autorización de deploy de Raf** (aplicar 0098-0100 + deployar `rafaq.yaml` + descomentar hook → suite scrotal verde post-apply). **Design spike de la rueda (C.0): ✅ pasó veto del leader + mostrado a Raf, esperando su OK visual.** El leader cazó/cerró en fix-loop (sin molestar a Raf) un recorte de los valores `.5` por las líneas de selección (causa raíz: `$8` size-scale=84px desbordaba la celda de 64 → token uniforme `$wheelValueText=26` + drum opacidad-only) + la redundancia de dos heroes (un solo readout). Limpio a 360/412 (`tests/modo-maniobra/rueda-ce-*`). Pendiente: OK visual de Raf → **M6-C.1** (StepKind+write-path real a `scrotal_measurements`) + **M6-C.2** (tarjeta ficha). Detalle: `progress/impl_03-m6-c0-spike.md`.

### M6-BACKEND EN CURSO (implementer Opus, 2026-06-17) — delta backend de la CE (US-14): tabla typed + data_key + gating + RLS + sync — Gate 1 PASS → transcripción del SQL diseñado, NO se aplica DDL ni se deploya YAML
Feature `03-modo-maniobras` (in_progress). Chunk **M6-BACKEND** (B.1–B.5). Gate 1 (security spec) ya **PASS** (`progress/security_spec_03-m6-circunferencia.md`, 0 HIGH) → transcribo design §12.3/§12.4/§12.5, honro los 2 MEDIUM (M6-SEC-01 surfacing R10.8; M6-SEC-02 test no-bypass `session_id` caso (i)). Reabre schema: tabla typed nueva `scrotal_measurements` + data_key global `circunferencia_escrotal` + seed cría + trigger gating capa 2 + RLS + sync rules. **Numeración** re-confirmada: 0098/0099/0100 LIBRES (último as-built 0097, 0092 saltada spec-08, sin migraciones sin commitear de la otra terminal). Plan: T1 `0098_scrotal_measurements.sql`; T2 `0099_scrotal_data_key_and_seed.sql`; T3 `0100_scrotal_gating.sql`; T4 sync (`rafaq.yaml`+`schema.ts`+`upload-rejections.ts`); T5 suite no-bypass `supabase/tests/scrotal/run.cjs` (casos a–i) + hook COMENTADO. ⚠️ **NO se aplica DDL** (deploy lo gatea el leader post-Gate 2). La suite scrotal queda **roja-hasta-apply** (esperado). Plan/baseline/mapa: `progress/impl_03-m6-backend.md`.

### ✅ GATE 2 (security code) CERRADO para TODO el delta cliente pendiente (2026-06-17) — SUPERSEDE los "pendiente reviewer + Gate 2" de abajo
Auditoría de seguridad (security_analyzer modo `code`, Opus) sobre los chunks que tenían reviewer APPROVED pero les **faltaba Gate 2** — hueco detectado al retomar (los reviewers decían "Pasa a Gate 2" pero no existían los `security_code_*`). Scopeado a los **commits** de spec 03 (no al working-tree: la otra terminal tiene spec-08/SIGSA sin commitear → excluido explícitamente).
- **Gate 2 — M5-CLIENTE** (`progress/security_code_03-m5-client.md`): **PASS 0 HIGH** sobre `cba703f..a03e593` (C.1+C.2+C.3). El cliente NUNCA es la autoridad: audit/tenant forzados server-side (triggers `force_audit` 0094/0095 pisan `establishment_id`/`recorded_by`), IDOR por PK compuesta `a:f` cerrado (UPDATE parametrizado + RLS evalúa la fila existente), jsonb parametrizado (sin inyección; server re-valida `jsonb_typeof` 0096), owner-only de los dos `+` = defensa en profundidad sobre el guard `tg_field_definitions_custom_guard`, todo input con cota autoritativa server-side. **1 MEDIUM no bloqueante (M-1)**: el `text` ui_component no tiene `maxLength` client-side (el cap server `value_size<4096` existe; es paridad UX). M5-BACKEND ya tenía su Gate 2 — no se re-auditó el SQL.
- **Gate 2 — M4-reanudación + R10.6 + M4.2** (`progress/security_code_03-m4-m4.2.md`): **PASS 0 HIGH** sobre `9d8962a`/`279a10f`/`7cfbea7`/`a9eff93`. `closeActiveSessions` scopeado por `establishment_id` (no cierra jornadas de otro tenant), fail-closed; el store de rechazos NO persiste `opData` (solo `{id,table,op,code,at}`, cap 50 + dedup); el never-throw es notificación pura (no fail-open, el descarte ya ocurrió en el connector); `rejectionReason` sin info-disclosure. **1 MEDIUM** (MED-1: close-all+insert no transaccional — race teórica inter-proceso, irrelevante en el modelo single-device de R10.6) + **1 LOW** (`console.warn` no gateado por `__DEV__`).
- **Estado de gates de spec 03**: M1→M5 TODOS con reviewer APPROVED + Gate 2 PASS. Los 2 MEDIUM + 1 LOW son defensa-en-profundidad no explotables → candidatos a `docs/backlog.md` (no toco el backlog ahora: está modificado en el working-tree de la otra terminal).
- **PENDIENTE de spec 03 (NO está done)**: **M4.3** (offline PowerSync + cierre, verificación R10.1/R10.2/R10.3/R10.7/R12.1 — `tasks.md:361`, `[ ]`), **R8.4** (preview de transición, M4.1 `[~]`), **R9.2** (lote opcional). + Acciones de Raf: deploy de `sync-streams/rafaq.yaml` a PowerSync (destraba el sync LIVE de las tablas custom de M5 + la verificación de M4.3) + matriz de aplicabilidad para Facundo (ya escrita en `CONTEXT/07-pendientes.md`). + Puerta 2 humana (código) de la feature.

### M5-C.3 DONE (implementer Opus, 2026-06-17) — render genérico del paso desde `ui_component` + propiedad custom en alta/ficha (R13.8/R13.10) — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M5-C.3** (ÚLTIMO de M5). Frontend puro (backend M5-BACKEND aplicado+verde; C.1/C.2 hechos). Gate 1 N/A por chunk; reviewer + Gate 2 después.
**PARTE A (R13.8) hecha**: config jsonb suma clave PARALELA `customManiobras:[fieldDefId]` (las 12 de fábrica IDÉNTICAS) → `extractCustomManiobras`/`toggleCustomManiobra`/`buildJornadaConfig(…, customManiobras?)`; secuencia DISCRIMINADA `SequenceItem` (`source:'factory'|'custom'`) en `maneuver-sequence.ts`; helper PURO `custom-render.ts`; renderer `CustomManeuverStep.tsx` por `ui_component` REUSANDO idioms lockeados (keypad/stepper/bloques/multi-select/input/Sí-No/fecha es-AR); `CustomManeuverSection` del wizard ahora SELECCIONABLE (toggle ✓); `carga.tsx` dispatcher discrimina source → `addCustomMeasurement` (value tipado, session_id).
**PARTE B (R13.10) hecha**: `CustomFieldInput.tsx` (input por ui_component, form) + `CustomPropertiesSection.tsx` (`CustomPropertiesForm` alta ref-based post-create soft-fail + `CustomPropertiesFicha` ver/editar in-place → `setCustomAttribute`); services `fetchEnabledCustomProperties`/`fetchCustomAttributes`; `fetchEnabledCustomManeuvers` enriquecido con ui_component/options.
**GOTCHA "cannot UPSERT a view" cazado en e2e + resuelto**: PowerSync expone las tablas como VIEW → `INSERT … ON CONFLICT(id)` falla. Revertido el upsert latente que C.1 puso en `custom_attributes` → `setCustomAttribute` UPDATE-luego-INSERT-si-0-filas (`runLocalWriteCount`); `custom_measurements` plain INSERT + `buildUpdateCustomMeasurement` para la corrección R5.9. Connector (PK natural) sin cambios. Supersede el AS-BUILT de C.1.
**Verificación**: typecheck + 1455 client unit + RLS 22 + Edge 42 VERDES; e2e `maniobra-custom-render.spec.ts` 3/3 (enum_single→bloques→custom_measurements [oráculo server session_id]; numeric→keypad→número jsonb; propiedad alta→custom_attributes + ficha ver/editar); regresión VERDE (carga 3/3, elegir 2/2, wizard 1/1, custom 2/2, animals 14/14). check.mjs rojo = flake `animals_tag_unique` (23505) de la suite backend animal (terminales paralelas), NO este chunk (frontend puro). 8 capturas 360/412 en `tests/modo-maniobra/custom-render-*` + `custom-prop-*`. Reconciliado design §11.6/§11.7 + requirements R13.8/R13.10 + tasks M5-C.3 ([x]). Plan/baseline/mapa R→test/autorrevisión/reconciliación: `progress/impl_03-m5-c3.md`. **NO marco la feature done.**

### M5-C.2 DONE (implementer Opus, 2026-06-17) — UI de CREACIÓN de datos/maniobras custom (R13.5–R13.9) — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M5-C.2**. Frontend puro (backend M5-BACKEND aplicado + verde `0093-0097`; services de captura M5-C.1 hechos). Gate 1 N/A por chunk. Construido: (1) helper PURO `app/src/utils/custom-field.ts` (`slugifyDataKey` único [a-z0-9_]/≤64 + `validateCustomFieldDraft` caps label≤80/options≤50·≤60 + `UI_COMPONENT_OPTIONS` los 7 es-AR + `buildCreateCustomFieldPayload` shape EXACTO de 0093); (2) service `app/src/services/custom-fields.ts` (`createCustomField` CRUD-plano offline INSERT a field_definitions con establishment_id del contexto + config_schema {options} enum; `enableCustomFieldInRodeo` REUSA `enqueueSetRodeoConfig`/outbox; `fetchCustomDataKeys`/`fetchEnabledCustomManeuvers`) + builders en `local-reads.ts`; (3) form `app/app/maniobra/_components/CustomFieldSheet.tsx` (sheet idiom + guard tap-through doble-rAF; modo `classify` = pregunta propiedad/maniobra primero R13.6 / modo `maniobra` = sin pregunta data_type fijo R13.7; label + picker de los 7 + editor de opciones enum); (4) los 2 `+` owner-only: `editar-plantilla.tsx` (config de datos del rodeo → classify) y `jornada.tsx` etapa 2 (lista de maniobras → maniobra + habilita en rodeo); (5) tweak M1: `fetchEnabledCustomManeuvers` + `CustomManeuverSection` (lista = fábrica + custom maniobra enabled). **GOTCHA jsonb cazado en autorrevisión**: `field_definitions.config_schema` sumado a `JSONB_TEXT_COLUMNS` (`upload-classify.ts`) → el connector lo decodifica a jsonb nativo antes de subir (sin él el guard 0093 `config_schema -> 'options'` daría NULL → 23514). **DECISIÓN DE SCOPE (reconciliada en design §11.6/§11.7 + requirements R13.9)**: el `+` CREA+HABILITA la maniobra custom y la lista la EXHIBE; la SELECCIÓN/secuencia + RENDER per-animal va a **M5-C.3** (renderer genérico desde ui_component; `buildJornadaConfig` filtra a ManeuverKind conocidos → sumarlas sin renderer las dropearía en silencio). Tests: `custom-field.test.ts` (23), `maneuver-reads.test.ts` (+6), `upload-classify.test.ts` (+2), e2e `maniobra-custom.spec.ts` 2/2 (oráculo server `waitForServerCustomField`: offline→sync→CRUD-plano→0093, config_schema jsonb nativo). **typecheck + 1243 client unit VERDES**; el rojo de check.mjs = flake `animals_tag_unique` (23505 dup) de la suite backend spec-13 (terminales paralelas) — NO este chunk (frontend puro). 10 capturas web táctil 360/412 en `tests/modo-maniobra/custom-{config-plus,classify,form,enum-options,maneuver-plus}-{360,412}.png`. Reconciliado `design.md §11.6` (AS-BUILT) + `requirements.md` (nota R13.9) + `tasks.md M5-C.2` ([x]). Plan/baseline/mapa R→test/autorrevisión: `progress/impl_03-m5-c2.md`. Pendiente reviewer + Gate 2. NO marqué la feature done.

### M5-C.1 DONE (implementer Opus, 2026-06-17) — services de captura CUSTOM (custom-measurements + custom-attributes) — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M5-C.1** (R13.11/R13.12/R13.13/R13.16). Frontend puro (write-path local + connector + unit; backend M5-BACKEND ya aplicado + verde). Construido: `app/src/services/custom-measurements.ts` (`addCustomMeasurement`, append-only, id REAL, espeja `events.ts`) + `app/src/services/custom-attributes.ts` (`setCustomAttribute`, current-value upsert) + helper PURO `app/src/utils/custom-value.ts` (`serializeCustomValue`/`CustomValue`, único origen — número como número JSON, bool, string, array; rechaza NaN/Inf/array mal tipado) + builders `buildAddCustomMeasurementInsert`/`buildSetCustomAttributeUpsert` en `local-reads.ts`. **DOS gotchas de CRUD-plano de PowerSync cazados en autorrevisión y resueltos** (`connector.ts` + helpers puros `buildCrudUpsert`/`buildCrudPatch`/`decodeJsonbColumns` en `upload-classify.ts`): (1) **PK COMPUESTA** de `custom_attributes` (sin columna `id` real; la stream emite id sintético `a:f`) → el connector por default subiría/filtraría un `id` inexistente = 42703 → upsert por la PK natural (PUT `onConflict`; PATCH de re-edición decodifica `a:f` y filtra por `(animal_profile_id, field_definition_id)`). (2) **TIPO jsonb del `value`** (lección "config jsonb" de M2.2): el value serializado a JSON-TEXT subido como string JS quedaría double-encodeado (`"385"`) y `assert_custom_value_valid` (0096, valida `jsonb_typeof`) lo rechazaría → `decodeJsonbColumns` parsea a nativo antes de subir. Sin RPC nueva, sin tocar DB/deploy. Tests: `custom-value.test.ts` (12), `maneuver-reads.test.ts` (+8 builders), `upload-classify.test.ts` (+18 connector). **typecheck + client unit VERDES**; el rojo de check.mjs = flake `animals_tag_unique` de la suite backend spec-02 (terminales paralelas) — NO este chunk (frontend puro). Reconciliado `design.md §11.6` (AS-BUILT) + `tasks.md M5-C.1` ([x]). Plan/baseline/mapa R→test/autorrevisión: `progress/impl_03-m5-c1.md`. Pendiente reviewer + Gate 2. NO marqué la feature done.

### M5-BACKEND EN CURSO (implementer Opus, 2026-06-17) — delta schema/RLS/gating/sync de datos custom (US-13) — SECURITY-CRÍTICO, Gate 1 PASS → transcripción del SQL diseñado, NO se aplica DDL
Feature `03-modo-maniobras` (in_progress). Chunk **M5-BACKEND** (M5-B.1..B.6). Reabre la RLS de `field_definitions` (catálogo global de spec 02) + 2 tablas nuevas (`custom_measurements`/`custom_attributes`) + triggers SECURITY DEFINER (gating genérico + validación de value + audit forzado + guard de inmutabilidad) + frontera WAL del sync. Gate 1 (security spec) ya **PASS** (`progress/security_spec_03-m5-custom.md`, 5 findings cerrados) → se TRANSCRIBE design §11, no se re-decide. **Numeración**: 0090/0091 ya tomadas (sanitary), 0092 reservada spec-08 → M5 arranca en **0093** (0093 field_definitions ALTER, 0094 custom_measurements, 0095 custom_attributes, 0096 gating, 0097 check_grants+sync). Plan: T1 escribir 5 migraciones; T2 suite no-bypass `supabase/tests/custom/run.cjs` (casos a-m del fix-loop); T3 sync delta `rafaq.yaml` + `schema.ts`; T4 autorrevisión M5-SEC-01..05 + check parse. ⚠️ **NO se aplica ninguna migración** (deploy lo hace el leader post-Gate 2). Plan/baseline/mapa: `progress/impl_03-m5-backend.md`. Pendiente apply + tests remoto + reviewer + Gate 2.

### M4.2-RECHAZOS-SYNC EN CURSO (implementer Opus, 2026-06-17) — R10.8: surfacing de rechazos de sync (que un rechazo no se pierda en silencio) — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk **M4.2 — R10.8**. Frontend + un toque al connector PowerSync (sin schema; Gate 1 N/A). GAP: una maniobra cargada offline y RECHAZADA al sincronizar (gating capa 2 `23514`, tenant-check, RLS `42501`) se descarta en `connector.uploadData` y `surfaceUploadRejection` HOY solo `console.warn` → dead-letter silencioso. Plan: T1 store observable `app/src/services/powersync/upload-rejections.ts` (record/ack/cap50/dedup, NO opData) + helper PURO `rejectionReason` es-AR + filtro a las 5 tablas de maniobra + test; T2 connector llama `recordUploadRejection` best-effort (NUNCA throw); T3 banner terracota en `maniobra.tsx` → `SyncRechazoSheet` (idiom ExitJornadaSheet + guard tap-through) + "Entendido"; T4 e2e con hook SOLO-E2E (patrón maneuver-e2e-fault) → banner→sheet→Entendido→desaparece + capturas 360/412; T5 check + reconciliación (cierra M4.2 + M4). Plan/baseline/tasks: `progress/impl_03-m4.2-rechazos-sync.md`.

### M4-REANUDACION EN CURSO (implementer Opus, 2026-06-16) — landing ofrece RETOMAR la jornada abierta (R10.5/R10.6) — pendiente reviewer + Gate 2
Feature `03-modo-maniobras` (in_progress). Chunk M4 reanudación, frontend puro (backend sesiones DONE 0050-0057; `getActiveSession`/`closeSession` ya existen, M1; Gate 1 N/A). GAP: el landing `maniobra.tsx` no chequea si hay jornada ABIERTA → "Salir sin terminar" del ExitJornadaSheet (M2.1-exit-hero) deja la sesión `active` pero el landing no la ofrece retomar. Plan: T1 chequear `getActiveSession` en el `useFocusEffect` + resolver rodeo via RodeoContext; T2 tarjeta "Retomar la jornada de hoy" ARRIBA de "Tus rutinas" (rodeo + maniobras + N animales + fecha si no es hoy) → `/maniobra/identificar?sessionId`; T3 "Nueva jornada" CON abierta → `NuevaJornadaConfirmSheet` (idiom ExitJornadaSheet + guard tap-through doble-rAF): "Empezar una nueva"→closeSession→wizard (fail-closed) / "Retomar la abierta"→identificar / cancelar·scrim; SIN abierta → wizard directo; T4 e2e `maniobra-reanudar.spec.ts`; T5 check + capturas 360/412 + reconciliación. ⚠️ R10.8 FUERA DE SCOPE. Plan/baseline/tasks: `progress/impl_03-m4-reanudacion.md`.

### MANIOBRA-LABEL-LARGO DONE (implementer Opus, 2026-06-16) — robustez de la LÍNEA DE MANIOBRA a labels largos — pendiente veto leader + reviewer + Gate 2
Iteración de VETO del leader sobre el rename de `tacto_vaquillona` → "Tacto de aptitud reproductiva (vaquillonas)" (~42 chars). Frontend puro (layout/robustez de render; NO toca aplicabilidad ni data_keys). **Fix** en la LÍNEA DE MANIOBRA de `carga.tsx` (~497-503): label `<Text>` = `flex={1}`+`minWidth={0}`+`numberOfLines={1}` (elipsa "…"; en RN-web `numberOfLines` no elipsa sin ancho constreñido) + contador "· N de M" = `flexShrink={0}` (nunca se recorta, siempre visible). **Auditoría** de los OTROS call-sites de `maneuverLabel(`: las filas del wizard (etapa 2 `ManeuverReorderList`, etapa 3 `jornada.tsx`) y el resumen por animal (`AnimalSummary`) YA estaban constreñidas (`flex/minWidth:0`+`numberOfLines`) → no tocadas; `SilentSanitaryStep`/`PlaceholderStep` nunca reciben el label largo (tacto_vaquillona routea a `TactoVaquillonaStep`). tsc 0, unit 58/58 (maneuver-wizard/sequence/step-kind), e2e capture `maniobra-label-largo.capture.ts` 2/2 (360+412, hasTouch+mobile). check.mjs rojo = flake `animals_tag_unique` de terminales paralelas (backend, NO regresión). 4 capturas web táctil en `tests/modo-maniobra/{maniobra-line,wizard}-vaquillona-{360,412}.png` (label elipsa + contador visible, sin overflow). Reconciliado design.md (nota AS-BUILT). Plan/baseline/mapa/autorrevisión: `progress/impl_03-maniobra-label-largo.md`.

### M2.2-CONTINUE-MANIOBRA EN CURSO (implementer Opus, 2026-06-16) — alta desde la manga → continúa la carga de la maniobra (Raf lo cazó en vivo)
Feature `03-modo-maniobras` (in_progress). Tarea acotada de FLUJO/navegación, frontend puro (sin backend/schema). Cierra el TODO de R4.1 anotado en `onDarDeAlta` ("M2.2 puede encadenar el retorno directo a la carga"). Bug: alta desde MODO MANIOBRAS → wizard `/crear-animal` → al crear, happy-path iba a la ficha `/animal/[id]` = **dead-end de la jornada** (sin volver a cargar la maniobra; back→pantalla stale "Animal nuevo"). Fix: el contexto de maniobra (`sessionId`) viaja al alta; al crear (o soft-fail) en contexto maniobra rutea a `/maniobra/carga` (carga del nuevo animal) en vez de la ficha. Alta normal (sin sessionId) → ficha, sin regresión. Plan/tasks/baseline: `progress/impl_03-m2.2-continue-maniobra.md`. Pendiente reviewer + Gate 2.

## Sesión 26 — 2026-06-13 — arranca el CLIENTE de spec 03 (MODO MANIOBRAS)

**Decisión de Raf**: seguir por MODO MANIOBRAS. El leader cuestionó (no es trivial: 03 es la cima del stack) y Raf eligió empujarla igual aceptando la prep previa.

**Estado de partida**:
- Backend de spec 03 **done** (migraciones 0050-0057, Puerta 2 aprobada s18): `sessions`, `maneuver_presets`, FK `session_id`, triggers gating capa 2, enum `tacto_vaquillona`, tenant-check. NO se toca.
- Falta SOLO el **cliente** (Fase 3/4). La spec del cliente es **provisional** (header de requirements US-3/4/5/10/12) y su context/design (s18) **predatan el as-built** de specs 02-Tier2 / 04 (BLE adapters) / 09 (FindOrCreateOverlay + useBusyMode) / 10 (ops masivas) / 11 (transfer) / 15 (PowerSync web outbox/overlay). El `design.md` describe `app/src/features/maneuvers/` que NO existe (el front vive en `app/app/**` + `app/src/{services,hooks,utils}`).
- Cliente = **frontend puro** (backend done) → Gate 1 probablemente N/A por chunk; Gate 2 sí.

**Gate 0 del cliente — decisiones lockeadas por Raf (2026-06-13)**:
1. **Balanza FUERA del MVP** → peso manual (acción explícita "pesar"); spec 05 diferida; interfaz del bastón extensible (`WeightCapableStickAdapter` futuro). Cierra D6.
2. **Botones GIGANTES que llenan la pantalla** → supersede el "60-80px" de R5.2/R12.2; bloques full-width que se reparten el alto, cero espacio muerto, header de identidad mínimo siempre visible. Memoria: `feedback_maniobras_botones_gigantes.md`.
3. **Arranque del cliente = DESIGN SPIKE de la pantalla de manga (M2) primero** (visual antes de plomería), vetado con skill `design-review`.

**En curso**: `spec_author` (opus, background) reconciliando JIT `requirements/design/tasks/context` de spec 03 contra el as-built (re-point a la API real de 04/09/15/02, balanza-out, botones gigantes, settle s18) + decomposición en chunks **M1 (inicio+config jornada) → M2 (carga rápida, arranca por design spike) → M3 (10 maniobras) → M4 (offline/reanudación)**.

**Reconciliación DONE + vetada por el leader** (RC=0): nombres de API verificados reales (04/09/15/02), botones gigantes foldeados (R5.2/R12.2 + R12.5 NFR medible), balanza-out, s18 settled, decomposición M1-M4 en tasks.md (M2.0 = design spike primero). Cabo C1 limpiado por el leader. **Puerta 1 presentada a Raf** — esperaba su OK.

**NUEVO SCOPE (Raf, Gate 0 sesión 26): MANIOBRAS/DATOS CUSTOM = chunk M5.** Raf quiere un `+` para crear datos/maniobras personalizadas por campo (diferenciador: medir cosas que competidores no permiten, ej. ángulo de pezuñas). Hallazgo: el modelo as-built (`field_definitions`, `0018`) YA tiene `data_type` (propiedad|maniobra → distingue "1 carga" vs "medición repetida") + `ui_component` + `config_schema` → resuelve el ruido de "nombre vs ángulo". Modelo: dato custom = fila de `field_definitions` con `establishment_id`; los 2 `+` (config de rodeo / lista de maniobras) convergen en el mismo objeto; storage genérico `custom_measurements` (append-only, seguimiento) + `custom_attributes` (current-value). Gate 0 cerrado en `context-m5-custom-maniobras.md`. **2 decisiones lockeadas de Raf**: (1) custom DENTRO del MVP como chunk M5; (2) alcance = mediciones repetibles + atributos de una carga (ambos). Reabre schema (field_definitions RLS + establishment_id + 2 tablas genéricas + gating data-driven + sync rules) → **Gate 1 obligatorio**. NO bloquea M2.0.

**M5 FOLDADO + VETADO por el leader (PASA) + Gate 1 corrido = FAIL → fix-loop en curso.** spec_author foldó US-13 (R13.1-R13.24) + design §11 (delta schema: field_definitions+establishment_id, custom_measurements, custom_attributes, gating genérico, sync WAL) + tasks M5. Veto del leader: citas reales (ADR-026 existe), modelo fiel; cabos corregidos (path stale §5). Gate 1 (security modo spec, `security_spec_03-m5-custom.md`) FAIL 2 HIGH (SEC-01 validación de value fail-open sin else + sin CHECK de ui_component; SEC-02 establishment_id mutable → fuga WAL al moverla de campo) + 3 MEDIUM (caps INPUT-1 faltantes en description/category/data_key/data_type/options/notes). **Decisión del leader sobre SEC-02**: establishment_id/data_type/data_key/ui_component INMUTABLES post-creación (patrón tag/idv). check.mjs RC=0 confirmado (el RC=1 previo era 502 de Supabase = flake).

**Fix-loop DONE + re-Gate-1 PASS** (`security_spec_03-m5-custom.md`): los 5 findings cerrados (SEC-01 value fail-closed + CHECK ui_component; SEC-02 inmutabilidad establishment_id/data_type/data_key/ui_component [decisión leader]; SEC-03/04/05 caps INPUT-1), sin findings nuevos, verificado contra las 25 globales de `0018`. R13.25-R13.27 + tests M5-B.6(g-m) agregados. check RC=0 verde. Decisiones menores resueltas: DM5-1 (unique parcial doble), DM5-2 (EDIT propiedad = cualquier rol, Gate 1 OK), DM5-3 (corrección de captura = cualquier rol, Gate 1 no la flageó). Nota no-bloqueante: editar opciones de enum post-creación con valores cargados = data-integrity, lo maneja la UI de M5-CLIENTE.

**PUERTA 1 APROBADA por Raf (2026-06-13)** (cliente completo: core reconciliado M1-M4 + M5 custom, todo gateado). **M2.0 DESIGN SPIKE EN CURSO** (implementer Opus): pantallas de manga con botones gigantes (carga rápida binaria Tacto + pesaje numérico), mock data, capturadas con Playwright a 412×915. El leader las vetea con skill `design-review` (Fitts/jerarquía/densidad ≥60%/contraste + recorte de descendentes) antes de mostrarle a Raf. Después M1/M2.1/M2.2/M3/M4/M5 por chunk (Gate 2 c/u; M5-BACKEND ya tiene Gate 1 PASS). NOTA: feature_list 03 sigue `deferred`; flip a `in_progress` al arrancar M1 (el spike es exploratorio/visual).

**Notas técnicas vigentes**: pnpm.cmd en PowerShell; implementer/reviewer/security_analyzer SIEMPRE opus; testear con Playwright (no a mano); design-review veto antes de mostrar a Raf; botones gigantes en toda pantalla de manga.

---

### Chunk M1-UI (implementer Opus, DONE 2026-06-13) — wizard de config de jornada — pendiente reviewer + Gate 2
Cliente del wizard de MODO MANIOBRAS (inicio + 3 etapas, task M1.4), consumiendo M1-SERVICIOS (done). Backend done (0050-0057). Frontend → Gate 1 N/A; reviewer + Gate 2 después. Detalle/mapa R→test/autorrevisión: `progress/impl_03-m1-ui.md`.

**Iteración UX 2 (implementer Opus, DONE 2026-06-14) — 4 arreglos del wizard** (NO toca servicios M1 ni backend). (1) **Registro central de iconos** `app/src/theme/icons.ts` (single source of truth: `RodeoIcon=Boxes`, `LoteIcon=Layers`, `CampoIcon=Building2`, `AnimalIcon=PawPrint`, `MiembroIcon=Users`); migrados a consumirlo: `jornada.tsx` + `(tabs)/index.tsx` + `(tabs)/mas.tsx` + `lotes.tsx` (solo swap de iconos, idéntico). (2) **Ícono de rodeo** etapa 1 + resumen `Group`→`RodeoIcon`(=`Boxes`) (era el bug de Raf: no era el ícono de rodeo as-built). (3) **Etapa 2 SCROLL roto → fix**: `Animated.ScrollView` (reanimated, web-safe) → swipe scrollea (8 maniobras → 9na+pool+preconfig+CTA alcanzables); drag **grip-gated** (`activeOffsetY`/`failOffsetX`) para no robar el swipe; **auto-scroll** (`useFrameCallback`+`scrollTo`, viewport medido y guardado); **bounds** clamp del ítem a la región de seleccionadas. (4) **Etapa 3 SCROLL** con CTA pinneado. check.mjs **verde**; e2e **1 passed**; **6 capturas** 412×915 (+`etapa2-scroll.png` con 8 maniobras scrolleado al pool+CTA). Detalle/autorrevisión/reconciliación (design §6.bis.1 as-built v3, tasks M1.4 as-built v3): `progress/impl_03-m1-ui.md`. Pendiente: veto design-review del leader + reviewer + Gate 2.

**Iteración UX 3 (implementer Opus, DONE + VETADA por el leader 2026-06-14) — preconfig de tanda inline + bottom sheet** (NO toca servicios M1 ni backend; opción B elegida por Raf). Sacó la sección huérfana "Detalle de la tanda" del fondo de etapa 2; el preconfig pasó a **inline** en la fila de cada maniobra configurable (Vacunación multi / Inseminación single): 2da línea = hint "Tocá para elegir vacuna" (vacío) o el valor (botella, coma-join multi). Nuevo `app/app/maniobra/_components/ManeuverConfigSheet.tsx` (patrón as-built `BulkConfirmSheet`: scrim + sheet anclado + grip + safe-area): input grande `$searchBarLg`(56) + autocompletar "Usadas antes" (`filterAutocomplete`, sembrado de presets DM1-UI-1) + chips multi-vacuna con × ; helpers puros `splitMultiPreconfig`/`joinMultiPreconfig` (+6 tests). Zonas de toque en fila seleccionada (3 GestureDetector disjuntos): badge ✓=quitar, cuerpo=abrir sheet, grip=drag. `ROW_HEIGHT` 64→80 (entra la 2da línea; drag/scroll/bounds intactos). **Veto del leader (design-review, MEDIDO con Pillow)**: inline value glifo botella `(30,90,62)` = 4,55:1 vs card (PASA AA); **cacé bug funcional** → `canSave` deshabilitaba Guardar en multi vacío → no se podía LIMPIAR una vacuna ya seteada (contradecía el comentario "vacío=limpiar") + hero con Guardar lavado `(139,168,154)`. **Fix-loop (implementer)**: Guardar siempre habilitado (multi y single), vacío→`onSave('')`→caller borra la clave→fila revierte al hint; e2e sumó el round-trip de limpiar (la 1ra corrida cazó el bug con `aria-disabled`). **Re-veto**: Guardar=botella `(30,90,62)` habilitado (texto 8,12:1), chips multi OK, "Usadas antes: Aftosa" distinta a la agregada, jerarquía Guardar>Cancelar correcta → **PASA**. Capturas finales `design/maniobra-wizard/{etapa2,etapa2-sheet}.png` mostradas a Raf. Detalle/reconciliación: `progress/impl_03-m1-ui.md`.

### ✅ M1 CERRADO (2026-06-14) — los 3 filtros verdes
**Raf aprobó las capturas finales (OK 2026-06-14).** M1 (M1-SERVICIOS + M1-UI, wizard completo + preconfig sheet) queda cerrado:
- **design-review (leader)**: PASA (medido con Pillow; cacé y cerré el bug de canSave en el sheet).
- **reviewer** (`progress/review_03-m1.md`): **APRUEBA** — trazabilidad R→test completa, offline-first/CRUD-plano, multi-tenant, cero hardcode, descendentes OK, tests reales verdes (104 unit + 26 wizard + e2e real del gating). Sin cambios requeridos. check.mjs RC=1 = SOLO flake de rate-limit ajeno (suites backend; M1 es frontend puro).
- **Gate 2 / security code** (`progress/security_code_03-m1.md`): **PASS** 0 HIGH. Único input editable (preconfig jsonb `config`) con cota autoritativa server-side (`CHECK octet_length < 16384`, 0050). SQL parametrizado, RLS/multi-tenant firme, RPC 0057 sin IDOR. **2 MEDIUM defensa-en-profundidad** (`work_lot_label`/`maneuver_presets.name` sin cap server-side, NO explotables hoy — sin call-site en UI) → **anotados en `docs/backlog.md`** como hallazgo para Gate 1 puntual ANTES de cablear esos inputs (M5 / "lote de trabajo").

### M2 EN CURSO (2026-06-14) — identify-first (pantalla CRÍTICA 🔴)
M2.0 (design spike de carga rápida: Tacto binario + Pesaje numérico) ya aprobado por Raf. La pantalla de **IDENTIFICACIÓN** (scan bastón BLE + manual + find-or-create) NO estuvo en M2.0 y es UI nueva 🔴 → se spikea primero (visual antes de plomería), igual que M2.0. Flujo (R3.x/R4.x): bastón BLE (`tag_electronic`) primario + manual (`idv`/`visual_id_alt`) fallback; MODO MANIOBRAS suspende el listener BLE global de spec 09 (`useBusyMode`, R3.2); feedback inmediato visual+vibración (R3.4); fallback a manual sin perder sesión si se cae el bastón (R3.6); desconocido → find-or-create inline reusando `app/app/_components/FindOrCreateOverlay.tsx` (spec 09, R4.1) con identificador precargado + rodeo de sesión; identificado → carga rápida (R5.1). **Dirección de diseño del leader (Idea 3, scan-first)**: header de sesión slim (rodeo + cola de maniobras + contador procesados) / hero de escaneo dominante 2/3 (estado "escuchando" BLE + feedback al leer) / entrada manual en el tercio inferior (thumb zone, R3.5). **Design spike del identify lanzado al implementer** (Opus, background) → veto del leader → mostrar a Raf. Después se cablea M2.1 (BLE spec 04 + manual spec 09 + useBusyMode) y M2.2 (frame carga rápida + resumen + progreso).

**✅ Spike del identify VETADO (leader, medido Pillow) + APROBADO por Raf (2026-06-14).** 4 estados (`design/maniobra-identify/{identify-listening,identify-found,identify-manual,identify-unknown}.png`): scan-first (hero de escaneo dominante "escuchando" + manual en thumb zone + header de sesión slim con rodeo+cola+contador). Medido: targets ≥56px (Buscar 71, Dar de alta 56, pill 56, input 56), contraste OK (subtítulo 5,74:1, badge "12 hoy" 4,5:1 botella-sobre-greenLight), caravana dominante en "encontrado", descendentes OK. La autorrevisión del implementer cazó/arregló "dos heroes compitiendo" en manual (hero se atenúa, cede al input). Tokens provisionales agregados (`tamagui.config.ts`: `heroScan=200`/`heroRing=8`/`heroIcon=80` + `StickIcon=ScanLine` en `theme/icons.ts`) → **canonizados al aprobar la dirección**. Detalle: `progress/impl_03-m2.1-spike-identify.md`. **DECISIÓN DE FLUJO (default del leader, Raf no objetó): auto-avanzar** a carga rápida tras flash de confirmación ~0,8s en match único (el resumen por animal R5.9 + desambiguación R4.2/R4.3 cubren el error). Opinables dejados como están: ícono `ScanLine`, copy "12 hoy".

**✅ M2.1-CORE DONE (implementer Opus, 2026-06-14) — pendiente reviewer + Gate 2.** Cableó el spike
`identificar.tsx` a real (mantiene scan-first aprobado): sesión por `sessionId` + BLE manga-owned
(`useBleStickListener`) + **suspensión del listener global por RUTA** (`FindOrCreateOverlay` → `BLE_OWNED_ROUTES
= {asignar-caravanas, maniobra}`, R3.2; NO `disableListener()` que apagaría el transporte) + lookup
(`lookupByTag`/`searchAnimals`) + feedback del provider (R3.4) + manual idv/visual (R3.5) +
disconnect/reconnect (chip `BleConnectionChip`, R3.6/R3.7) + find-or-create inline → `/crear-animal` con id
precargado + rodeo de contexto (R4.1) + otro-establecimiento→avisar+saltar (R4.5) + **auto-avance** a carga
en match único (~0,8s). Lógica PURA en `app/src/utils/maniobra-identify.ts` (9 tests). Wizard `jornada.tsx`
re-apunta a `/maniobra/identificar`; la pantalla SALE de DEV_WEB_ROUTES (ahora autenticada real).
**check.mjs verde**; **e2e `maniobra-identify.spec.ts` 5/5** (found→auto-avance, unknown→alta precargada,
otro-campo→saltar, manual→found, disconnect→manual) con adapter-mock/BleE2EBridge. **DIFERIDO M2.1-edge**:
R4.2 (desambiguación manual multi-candidato — outcome `ambiguous` seguro, sin UI de selección) + R4.7 (rodeo
de jornada mal elegido) + R4.4-UI (pasar/saltar otro-rodeo-mismo-sistema). Specs reconciliadas
(tasks split core/edge, design §1.1/§5 R3.2 as-built, nota R4.1, T2.12). Detalle: `progress/impl_03-m2.1-core.md`.

**M2.1-CORE (registro previo) — plomería del identify** (implementer Opus, background). Cablea el spike `identificar.tsx` a las piezas reales: StickReader (spec 04 `ble/stick.ts`+`contract.ts`+`adapter-mock.ts`+`parser-rs420.ts`), suspensión del listener global (R3.2, espejando `FindOrCreateOverlay`/busyMode de spec 09), parseo+resolución por `tag_electronic` (`lookupByTag`/`tag-lookup.ts`), feedback visual+vibración (R3.4, `ble/feedback.ts`), manual idv/visual_id_alt (R3.5), disconnect/reconnect (R3.6/R3.7, `connection-status.ts`), find-or-create inline (R4.1, `FindOrCreateOverlay` con identificador precargado + rodeo de sesión), BLE dup→auto por tag (R4.3), otro establecimiento→avisar+saltar (R4.5), **auto-avance a carga rápida en match único**. e2e con `adapter-mock`/`BleE2EBridge`. Frontend puro → Gate 1 N/A; Gate 2 después. **DIFERIDO a M2.1-EDGE** (anotado): R4.2 (UI de desambiguación manual de visual_id_alt multi-candidato) + R4.7 (heurística de rodeo de jornada mal elegido). Gate 2 de M2 verifica el contrato del find-or-create cross-spec (T2.12/D9).

### ✅ M2.1-core LOCKEADO (2026-06-14) — los gates verdes
- **implementer** (`progress/impl_03-m2.1-core.md`): cableó `identificar.tsx` a real. R3.2 resuelto con **supresión-por-ruta** del `onTagRead` del `FindOrCreateOverlay` (early-return en `segments[0]==='maniobra'`) + listener manga-owned, NO `disableListener()` (mantiene el transporte vivo, un solo consumidor efectivo). Nuevo módulo PURO `app/src/utils/maniobra-identify.ts` (decisión found/other/unknown/ambiguous + gate auto-avance). Reusó BLE/lookup/find-or-create/crear-animal. e2e 5/5 + regresión baston 4/4.
- **reviewer** (`progress/review_03-m2.1-core.md`): **APRUEBA**. R3.2 un solo consumidor verificado (el caso peligroso find-or-create-desde-manga lo cubre `useBusyWhileMounted` de crear-animal → apaga el fan-out); auto-avance 1 sola vez (`router.replace`+clearTimeout+dedup+guards); R4.5 sin fuga (lookup ve solo SQLite local scopeado por `has_role_in`). Observación menor no-bloqueante: CTAs secundarios `$touchMin=56`; el piso ≥60px de R5.2 aplica a los bloques gigantes de carga rápida (M2.2/M3), no a esta scan-first.
- **Gate 2** (`progress/security_code_03-m2.1-core.md`): **PASS** 0 HIGH/0 MEDIUM. find-or-create cross-spec (D9/T2.12) cerrado (`created_by` forzado trigger 0043, `create_animal` 0083 anti-IDOR + UNIQUE spec 02, precargado con cap server-side CHECK≤64 0070). R4.5 sin info-disclosure (sync-stream `est_animal_profiles` scopeado). Sin doble-INSERT (identify nunca INSERTa).
- **DIFERIDO a M2.1-edge** (anotado, estado seguro): R4.2 (desambiguación manual multi-candidato), R4.7 (rodeo de jornada mal elegido), R4.4-UI (pasar/saltar a otro rodeo del mismo establecimiento).
- **⚠️ NOTA DE ENTORNO (no es mi chunk)**: `check.mjs` rojo por **desalineamiento test↔migración de spec 12** (`import_rodeo_bulk`, `supabase/tests/import/run.cjs`, commits `e2ee997`/`c008085`) — NO el flake de rate-limit, NO regresión de M2.1-core. Lo reconcilia la terminal dueña de spec 12 antes de cualquier cierre que dependa de check verde. El leader NO lo toca (colisión paralela).

### M2.2 EN CURSO (implementer Opus, 2026-06-14) — frame de carga rápida + resumen por animal + progreso
Arranca el FRAME que secuencia las maniobras de la sesión (en orden de `config.maniobras`) como pantallas de botones gigantes, con identidad del animal (R5.1), resumen por animal corregible (R5.9), avance + contador de progreso (R5.10), y vínculo `session_id` (R5.11). Vertical slice demoable: identify → carga rápida → **tacto + pesaje** (los 2 spikes M2.0 aprobados, cableados a persistencia real reusando servicios de evento de spec 02 + session_id) → resumen → siguiente animal. El renderer per-maniobra es GENÉRICO (M3 lo llena con las 10 + nuevas + gating delta M3.0-BACKEND). El esqueleto del orquestador de eventos de maniobra (maneuver-events.ts) se inicia acá; M3.1 lo generaliza.

### ✅ M2.2 LOCKEADO EN LO TÉCNICO (2026-06-14) — reviewer + Gate 2 verdes (falta OK de diseño de Raf)
- **implementer** (`progress/impl_03-m2.2.md`): frame `carga.tsx` + pasos `{TactoStep,PesajeStep,PlaceholderStep,AnimalSummary}.tsx` + lógica pura `{maneuver-sequence,maneuver-step-kind,maneuver-event-query}.ts` + orquestador-esqueleto `services/maneuver-events.ts` + builders session_id/UPDATE-corrección en `local-reads.ts`. Cazó 4 bugs (el gordo: **doble-encoding del config jsonb** = raíz de "sin maniobras tras sync"; + eventId no-UUID, corrección upsert→split INSERT/UPDATE, remount stale del keypad). Construyó tacto paso 2 (tamaño CABEZA/CUERPO/COLA, R6.2) aunque estaba scopeado M3. **+ fix-loop de jerarquía de identidad** (leader veto): caravana visual `visual_id_alt||idv` dominante + tag electrónico muted (consistente con identify-found), prop opcional `tagElectronic` en `SpikeIdentityHeader`; 4 capturas limpias re-hechas (`design/maniobra-carga/*.png`, identidad "0385"/"Cría hembras"/"Vaquillona").
- **reviewer** (`progress/review_03-m2.2.md`): **APRUEBA**. Trazabilidad R5.1/5.8/5.9/5.10/5.11/5.12/5.14/R6.2/R6.9/R10.1/R12.1/R12.4 + dispatcher/seam → cada R con ≥1 test. Focos cerrados (doble-encoding tolerante 3 formas; session_id+created_by server-side; corrección split sin duplicar; seam M3 limpio; offline; identidad). check.mjs **RC=0 run limpio** (1174 unit + backend; el rojo del 1er run = flake rate-limit, spec-12 no reapareció). **OBS-1 (LOW)**: CTAs full-width `touchMin=56` vs piso EARS ≥60 → tensión token-canónico vs EARS, mitigada (bloques dominantes flex:1 >60), **al backlog**. **OBS-2 (INFO)**: keypad no capea decimales tras la coma, cosmético.
- **Gate 2** (`progress/security_code_03-m2.2.md`): **PASS** 0 HIGH. created_by forzado (0043), session_id cross-tenant bloqueado (0056), UPDATE corrección sin IDOR (RLS `is_owner_of OR created_by`), peso con cota server-side `numeric(7,2) CHECK>0`, jsonb fix sin inyección, offline valida al subir.
- **Demo end-to-end mostrado a Raf** (identify→carga→tacto 2 pasos→pesaje→resumen→siguiente). **Esperando OK de diseño de Raf** + **decisión D10** (antiparasitario interno/externo) para destrabar M3.

### ✅ M2.1-edge DONE (implementer Opus, 2026-06-14) — edge cases diferidos del identify — pendiente reviewer + Gate 2
**R4.2** (desambiguación manual multi-candidato → `CandidatePicker` sheet manga-friendly, **N° interno idv como DESEMPATE** cuando el visual está duplicado —cacé que sin él dos animales con mismo visual+rodeo+categoría eran indistinguibles—) + **R4.4-UI** (animal de otro rodeo del MISMO campo → `OtherRodeoSheet`: **cambiar el rodeo de la JORNADA** [`setSessionRodeo`, no mover el animal — reconciliación del EARS R4.4, dirección del leader] si es del mismo sistema / saltar; rodeo de origen visible) + **R4.7** (tracker PURO de racha, umbral 3 configurable → `RodeoMismatchBanner` NO-bloqueante). Lógica PURA `app/src/utils/maniobra-edge.ts` (16 tests). Service nuevo `setSessionRodeo` (CRUD-plano offline). Frontend puro → Gate 1 N/A; reviewer + Gate 2 después. No tocó M2.2 ni backend.
**check.mjs RC=0** (sin rojos ajenos esta vez). **e2e `maniobra-identify` 8/8** (5 core + 3 edge f/g/h) + **`maniobra-carga` 3/3** (regresión M2.2). 3 capturas 412×915 en `design/maniobra-identify/{candidate-picker,other-rodeo-sheet,rodeo-mismatch-warning}.png`.
Autorrevisión cazó/cerró: bug de diseño del picker (idv desempate), manual-expandido tras saltar (colapsa). Reconciliación: R4.4 cambiar-jornada vs mover-animal (requirements nota + design §2.3), riesgo residual R4.4/R4.7↔R10.8 documentado. Detalle: `progress/impl_03-m2.1-edge.md`.
**Baseline Gate 2**: `f518ea56b8dec3db34ec5e8427a6f1b95b0a858b`.

### ✅ M2.1-edge LOCKEADO (2026-06-15) + ✅✅ M2 CERRADO COMPLETO + M3 ARRANCANDO
**OJO — SUPERSEDE el registro de arriba (line ~88)**: el R4.4 quedó como "cambiar la jornada" ahí, pero **Raf decidió R4.4 = "pasar el animal a este rodeo"** (honrar el EARS; mi dirección de la divergencia fue revertida en fix-loop). Decisiones de Raf (2026-06-14/15): (1) **R4.4 = pasar el animal** (EARS); (2) **D10 = NO distinguir interno/externo** del antiparasitario (nota libre) → reconciliado en `requirements.md` R6.14 + tabla de mapeo; (3) **carga rápida APROBADA**; (4) el prefijo "e2e_…" que Raf notó en las capturas era el RUN_TAG del seed de tests en el NOMBRE del rodeo (no IDs, no bug) → re-capturado limpio.
- **Fix-loop R4.4** (`progress/impl_03-m2.1-edge.md`): `moveAnimalToRodeo`/`buildMoveAnimalToRodeoUpdate` (UPDATE `animal_profiles.rodeo_id` al rodeo de la SESIÓN, reusa idiom de `buildAssignAnimalToGroupUpdate`, validado server-side por trigger 0047 same-system + 0021 + RLS; muestra rodeo de ORIGEN; NO carga eventos hasta mover). `setSessionRodeo` queda SOLO en R4.7. `RodeoMismatchBanner` botones apilados + nombre largo truncado (robustez real). 3 capturas limpias re-hechas.
- **reviewer** (`progress/review_03-m2.1-edge.md`): **APRUEBA**. R4.4 honra EARS (e2e oráculo `waitForServerProfileRodeo`); `setSessionRodeo` solo R4.7; R4.2 no auto-elige; R4.7 tracker OK; offline; nombres largos OK; specs no mienten. **2 LOW housekeeping** (comentarios stale: `maneuver-reads.test.ts:166,168` atribuye `setSessionRodeo` a "R4.4"→es R4.7; `maniobra-edge.ts:18` docblock `evaluateMisconfiguredRodeo`→`shouldWarnMisconfiguredRodeo`) → **foldear en el 1er chunk de M3**.
- **Gate 2** (`progress/security_code_03-m2.1-edge.md`): **PASS** 0 HIGH. R4.4 move-animal sin IDOR (RLS+0021+0047, destino=rodeo de sesión no spoofeable); R4.7 setSessionRodeo (RLS+0050); R4.2 picker puro + searchAnimals scopeado + cap 64 en builder + escape LIKE.

**✅✅ M2 CERRADO**: M2.0 (spike) + M2.1-core (plomería identify) + M2.1-edge (R4.2/R4.4/R4.7) + M2.2 (frame carga rápida + resumen + progreso). Todo con reviewer + Gate 2 verdes + diseño aprobado por Raf. Flujo demoable identify→carga→tacto(2 pasos)→pesaje→resumen→siguiente + edge cases del identify, completo.

### M3 ARRANCANDO (2026-06-15) — las 10 maniobras + 2 nuevas sanitarias
Orden: **M3.0-BACKEND** (delta de gating capa 2 `tg_sanitary_events_gating` 0054 → gatear `deworming` [**OR** de `antiparasitario_interno`/`externo`, D10] + `treatment` [`antibiotico`] fail-closed; toca trigger de spec 02 → **Gate 1 OBLIGATORIO** + **deploy a la DB compartida GATEADO por Raf**) → **M3.1** (generalizar el orquestador `maneuver-events.ts` a las 10 + nuevas) → **M3.2** (las 8 pantallas de maniobra restantes + tacto vaquillona R6.3/R5.13 + gating per-animal R6.12 raspado-solo-machos / R6.8 prompt CUT). **Foldear los 2 LOW de housekeeping de M2.1-edge en el 1er chunk de M3.** **M3.0-BACKEND lanzado: implementer DRAFTea la migración + tests (NO aplica) → Gate 1 → presento a Raf para autorizar deploy.**
### ✅✅ M3.0-BACKEND CERRADO + DEPLOYADO + VERIFICADO (2026-06-15)
**Gate 1 (security, `progress/security_spec_03-m3.0-backend.md`): PASS** 0 HIGH/0 MEDIUM (fail-closed real, OR correcta `v_have<1`, no-bypass BEFORE INSERT, vacunación byte-idéntica a 0054, REVOKE EXECUTE en el helper nuevo en la propia 0091, sin inyección; 1 LOW: el smoke-check 0055 no lista el helper → futuro `check_grants`). **reviewer (`progress/review_03-m3.0-backend.md`): código+tests APROBADOS**; su CHANGES_REQUESTED eran 4 specs stale "con sub-elección" (D10) → **reconciliadas por el leader** (R6.13:197, R7.7:221, tasks M3.2:260, design:618 0054→0091, + 3 más en la matriz/changelog 405/504/506; grep confirma cero "sub-elección" stale). **Raf AUTORIZÓ el deploy (2026-06-15).** **0091 APLICADA vía MCP** (verifiqué antes con `pg_get_functiondef` que la función viva era 0054 puro, sin cambio posterior que pisar). Post-deploy verificado: función viva con las 3 ramas + helper creado + `authenticated` sin EXECUTE; **suite backend "All tests passed"** (T2.4c deworming/treatment ahora VERDE). El gating de antiparasitario/antibiótico está activo en la DB compartida.

### ✅ M3.1 LOCKEADO (2026-06-15) — orquestador de eventos para TODAS las maniobras
- **implementer** (`progress/impl_03-m3.1.md`): generalizó `maneuver-gating.ts` (`ManeuverKind` 12, `MANEUVER_DATA_KEY_REQS` con `match` all/**any**), `maneuver-step-kind.ts` (StepKind real, sin placeholder), `maneuver-event-query.ts` (`buildManeuverEventQueries` array/multi-write), `maneuver-events.ts` (`persistManeuverEvent`), nuevo `maneuver-applicability.ts` (R6.12 raspado-solo-machos + R6.8 CUT-no-terneros puros), 14 builders en local-reads. Antiparasitario con `match:'any'` (OR, NO `.every()`) → `deworming` escribe **sin `route`** (verificado node:sqlite). 233/233 unit + 14/14 backend (T2.4c verde = 0091 deployada).
- **Gate 2** (`progress/security_code_03-m3.1.md`): **PASS** 0 HIGH. Todos los inputs con cota server-side (condición `CHECK` discreto, product_name ≤160, tube_number ≤64, pajuela/notes ≤4000); session_id cross-tenant bloqueado (0052/0056); **dientes/CUT sin IDOR** (RLS `has_role_in` de la fila + CUT gateado por `dientes` 0054 + categoría de otro sistema rechazada 0021); gating OR autoritativo server-side (0091). SQL parametrizado.
- **reviewer** (`progress/review_03-m3.1.md`): código APROBADO; CHANGES_REQUESTED por **1 hallazgo de doc (H1)**: `requirements.md` tenía el mapeo de tamaño de preñez INVERTIDO (small=cabeza) vs el as-built `small=Cola/medium=Cuerpo/large=Cabeza` (dominio Facundo, `event-timeline.ts`). **Reconciliado por el leader** (R6.2:173 + Refinamiento#2:501; grep confirma sin otra mención stale — el resto es orden de display CABEZA/CUERPO/COLA, correcto). M3.1 lockeado.

### ✅✅ M3.2a LOCKEADO (2026-06-15) — pantallas de "elegir" — los 3 filtros + diseño mostrado a Raf
**Veto del leader (design-review, MEDIDO Pillow): PASA** — DIFERIDA ámbar `(154,98,6)` texto blanco 5,09:1 AA (distinto de verde/terracota, no se lee deshabilitado); 3 bloques tacto entran gigantes (213px); stepper − 88px; dientes 8 bloques ~80px (medianos porque 8 opciones); prompt CUT terracota + copy de consecuencia + no-terneros. **Gate 2** (`security_code_03-m3.2a.md`): **PASS** 0 HIGH (CUT UPDATE sin IDOR: RLS `has_role_in` + check categoría 0021 + gating dientes 0054 fail-closed; `resolveCutCategory` no spoofeable; teeth_state parametrizado; condición `CHECK` server-side). **reviewer** (`review_03-m3.2a.md`): **APRUEBA**. **4 capturas mostradas a Raf** (`design/maniobra-elegir/*.png`). Detalle abajo ↓.

### ✅✅✅ M3.2b LOCKEADO (2026-06-15) → M3 COMPLETO (las 12 maniobras renderizables)
**Fix-loop (espacio muerto + inseminación) + los 3 filtros verdes.** Las 4 pantallas no-keypad ahora envuelven el contenido en una **card dominante** (`flex:1`, figura-fondo, patrón del stepper) que LLENA el alto + CTA gigante disjunto → **cero espacio muerto** (Gate 0). **Inseminación (R6.5)** agregada (`InseminacionStep`, reusa `SilentSanitaryStep` parametrizado; 1 pajuela→confirma, >1→selector; write-path M3.1 ya existía; **divergencia popup→confirma reconciliada bajo R6.5**, dirección del leader). **Veto del leader (re-medido): PASA** (card llena el alto en las 5; inseminación "Toro 123" consistente). **Gate 2** (`security_code_03-m3.2b.md`): **PASS** 0 HIGH (writes parametrizados; cotas server-side product_name≤160/tube≤64/notes≤4000 todas VALIDATED 0070; soft-delete sin IDOR `is_owner_of OR created_by`; gating server-side). **reviewer** (`review_03-m3.2b.md`): **APRUEBA**. **5 capturas mostradas a Raf** (`design/maniobra-sanitaria/{sanitaria-silent,vacunacion,sangrado,raspado,inseminacion}.png`). Bug cazado en autorrevisión: el frame nunca aplicaba `filterByAnimalApplicability` → raspado NO se saltaba para hembras (R6.12) → cerrado.

**🎉 M3 COMPLETO**: M3.0-BACKEND (gating deworming/treatment deployado+verde) + M3.1 (orquestador 12 maniobras) + M3.2a (tacto vaquillona/condición/dientes+CUT) + M3.2b (vacunación/antiparasitario/antibiótico/sangrado/raspado/pesaje ternero/inseminación). Las **12 maniobras** renderizan, persisten con `session_id` y gating server-side, offline. Núcleo de MODO MANIOBRAS end-to-end: config jornada → identificar → carga rápida → cualquier maniobra → resumen → siguiente.

**PENDIENTE (consultar prioridad a Raf)**: **M4** (offline/reanudación R10.x + surfacing de rechazos R10.8 + preview de transición R8.4 + lote opcional R9.2) y **M5** (custom maniobras/datos — backend Gate 1 PASS, falta cliente: UI de creación + renderer genérico + services de captura). + Cabos menores: el flake del keypad-remount de M2.2 (`maniobra-carga` test5, pre-existente); el smoke-check 0055 no lista `assert_any_data_key_enabled` (housekeeping, REVOKE ya aplicado).

### M2.1-EXIT-HERO DONE (implementer Opus, 2026-06-16) — back→ExitJornadaSheet + hero adaptativo por conexión — pendiente reviewer + Gate 2
Pantalla `identificar.tsx` (🔴 manga). Diseño ya decidido por leader+Raf, se cableó. **PARTE A**: el botón ‹ del `SpikeSessionHeader` no hacía nada (faltaba `onBack`) → ahora abre un `ExitJornadaSheet` (NUEVO; cierre de jornada): "Llevás N animales hoy" + Terminar jornada (`closeSession`, R10.7, → confirmación "Procesaste N animales" + "Listo" → `router.dismissAll()` fuera del flujo, fallback `replace('/(tabs)')`; fail-closed si `ok:false`) / Salir sin terminar (fuera SIN cerrar, reanudable R10.5/R10.6) / Seguir en la jornada (cancelar/scrim). NADA rojo. Guard tap-through `readyToDismissRef` doble-rAF (regla `reference_rn_web_pitfalls`); scrim phase-aware (en confirmación también navega fuera — cacé que dismissar la confirmación dejaba al usuario en una sesión cerrada). **PARTE B**: hero del estado "escuchando" (outcome===null) ADAPTATIVO por conexión, decisión PURA `app/src/utils/maniobra-listen-state.ts` — CONECTADO→ScanHero / DESCONECTADO+conectable (`transport!=null`)→`ConnectHero` (NUEVO, disco = botón activo `connect-stick-disc` + badge Bluetooth, tap→`connect()`) / desconectado+`transport==null`→`ManualPromptHero` (NUEVO, sin disco) + `ManualEntry` `promoted`. **Cacé una regresión propia**: el hero adaptativo cambió el estado inicial (mock conectable → arranca ConnectHero) → 5 specs e2e (carga/elegir/sanitaria/tacto-bugfix/wizard) que asertaban "Acercá el bastón" tras "Arrancar jornada" → arreglados (conectan el mock en sus helpers; wizard sin flag de mock → asierta ConnectHero, su estado real). Verificación: tsc 0, anti-hardcode 0, unit 52/52 (incl. `maniobra-listen-state` 4), e2e `maniobra-identify` 18/18 (i/j/k/l/m nuevos) + carga 3/3 + elegir 2/2 + sanitaria 11/11 + wizard + tacto-bugfix 3/3; check.mjs rojo = flake `animals_tag_unique` de terminales paralelas (backend, NO regresión). Flag E2E secundario `__RAFAQ_BLE_E2E_MANUAL__` (doble-gateado, sin prod) para capturar el sub-estado manual. 10 capturas web táctil 360/412 en `tests/modo-maniobra/`. Frontend puro → Gate 1 N/A; reviewer + Gate 2 después. Plan/baseline/mapa→test/autorrevisión/reconciliación: `progress/impl_03-m2.1-exit-hero.md`.

### BUGFIX-GATING-RELOAD DONE (implementer Opus, 2026-06-16) — la recarga blanquea la carga rápida y pierde el peso tecleado, Raf lo cazó en vivo — pendiente reviewer + Gate 2
Tecleando el peso, ~1s después la pantalla "se puso en blanco y volvió a cargar" → perdió lo tecleado. Causa: `useManeuverGating.load()` hace `setLoading(true)` en CADA invocación, y se dispara en focus + en CADA avance de sync (`lastSyncedMs`) → `carga.tsx` cae al spinner full-screen → `PesajeStep` se DESMONTA → su `useState` local `peso` se pierde. **Fix stale-while-revalidate (root cause en el hook)**: `loading=true` SOLO en la carga INICIAL del rodeo (`loadedRodeoRef`); revalidación background del MISMO rodeo = silenciosa (mantiene config, no flipea loading); cambio de rodeo = sí loading; `rodeoId=null` resetea. Decisión extraída a módulo PURO `app/src/utils/maneuver-gating-load.ts` (`shouldShowLoadingForLoad`/`initialLoadingFor`, node:test, 12 tests). `load` no lee `config` → deps `[rodeoId]`, sin loop; `reqIdRef` conservado. **+ defensa en `carga.tsx`** (`hasRenderedContentRef`: una vez renderizado un paso, un flip transitorio del gating NO vuelve al spinner). **Strict improvement para los 2 consumidores** (verificado): carga rápida `carga.tsx` (rodeo del animal, estable → revalidación silenciosa) + wizard `jornada.tsx` (rodeo cambia al elegir → loading correcto, antes parpadeaba en cada sync). Verificación: 12/12 nuevo + 152/152 unit de maniobras + typecheck OK + e2e `maniobra-carga` 3/3. check.mjs RC=1 = flake de terminales paralelas (edge/rls rate-limit + animal duplicate-key seed concurrente), NO regresión (este bugfix es frontend puro). Frontend puro → Gate 1 N/A; reviewer + Gate 2 después. Detalle/mapa→test/autorrevisión: `progress/impl_03-bugfix-gating-reload.md`.

### BUGFIX-SILENT-PRODUCT-TRUNCADO EN CURSO (implementer Opus, 2026-06-16) — nombre de producto hero overflow/cortado (web), Raf lo cazó en vivo
Mismo gotcha que el bugfix-cc pero en `SilentSanitaryStep.tsx` (paso silent_apply: Antiparasitario/Antibiótico/Inseminación-single): un nombre de producto largo se renderiza a `$11`=64px con `adjustsFontSizeToFit` (NO-OP en web) → no se encoge → overflow horizontal saliéndose de la pantalla. Evidencia `tests/modo-maniobra/antibiotico-cortado.png`. Diferencia con CC: el nombre es **texto libre de longitud arbitraria** (no fija) → fix = helper PURO **step-down length-aware** (bucket de longitud → token de fontSize, emulando adjustsFontSizeToFit web-safe) + **wrap/word-break** para el caso patológico sin espacios + `numberOfLines` con elipsis último recurso. Plan/baseline/tasks en `progress/impl_03-bugfix-silent-product-truncado.md`. Frontend puro → Gate 1 N/A; reviewer + Gate 2 después.

### ✅ BUGFIX-CC-TRUNCADO LOCKEADO (2026-06-15) — valor de condición corporal recortado "4..." (web), Raf lo cazó en vivo
Causa: `adjustsFontSizeToFit` (NO-OP en react-native-web, ver memoria `reference_rn_web_pitfalls`) no achicaba + `numberOfLines={1}` truncaba el valor hero entre los botones − / +. Fix (`CondicionCorporalStep.tsx`): valor **full-width en su propia línea arriba** (sin numberOfLines/adjustsFontSizeToFit/minimumFontScale) + − / + lado a lado debajo. Verificado en web a 412 y 360 (1,00–5,00 completos). reviewer **APRUEBA** + Gate 2 **N/A** (cero superficie de seguridad). check.mjs RC=0. **3 bugs de Raf (tacto / config-sheet / cc-truncado) todos lockeados.** NOTA: `current.md` está inflado (WARN de check.mjs) → limpiar al cerrar sesión (mover a history.md).

### ✅ BUGFIX-CONFIG-SHEET LOCKEADO (2026-06-15) — race del bottom sheet de preconfig (web), Raf lo cazó en vivo
Etapa 2 del wizard: tocar Vacunación abría el `ManeuverConfigSheet` y se cerraba al instante (~1ms). **Causa (confirmada por repro táctil + logging)**: el cuerpo abre con `Gesture.Tap()` (resuelve en `pointerup`); en web TÁCTIL el browser emula `touch→mouse→click` y dispara un `click` ~20ms después, re-hit-testeado contra el `$scrim` (`Pressable onPress=onClose`) ya montado → lo cierra. **Fix** (`ManeuverConfigSheet.tsx`): guard `readyToDismissRef` armado en el próximo frame (doble `requestAnimationFrame` + fallback `setTimeout(0)`) → el scrim ignora el click huérfano del open, pero el backdrop deliberado posterior SIGUE cerrando; SOLO el scrim pasa por el guard (Cancelar/Guardar/chips/input intactos). **Autorrevisión del implementer**: había puesto el guard también en `DientesStep` (CutPromptSheet) por analogía → lo REVIRTIÓ tras verificar que ese abre con `onPress` de Tamagui (consume el click, sin race). Nuevo e2e `maniobra-config-sheet-race.spec.ts` (`hasTouch:true` + `touchscreen.tap()`, fiel a la repro real). **reviewer APRUEBA (probó fail-without/pass-with empíricamente) + Gate 2 PASS 0 HIGH** (superficie de seguridad nula). check.mjs RC=0. **Esperando re-test de Raf** (tocar Vacunación → sheet queda abierto → escribir vacuna). LECCIÓN: el e2e viejo no lo agarraba (`locator.click()` = mouse sintético sin re-hit-test + Desktop Chrome `hasTouch:false`); reproducir bugs táctiles de web requiere `hasTouch:true` + `touchscreen.tap()`.

### ✅ BUGFIX-TACTO LOCKEADO (2026-06-15) — 2 bugs que Raf cazó en testing en vivo (web)
Raf reportó: la pantalla de tacto mostraba "otra caravana" (tipeó "1212", apareció "ARG 4721") + no avanzaba al tapear PREÑADA/VACÍA. **El leader dio 2 hipótesis EQUIVOCADAS** (display-inconsistency mismo-animal + gating-rechaza-tacto) → corregidas. **Causas reales** (`progress/impl_03-bugfix-tacto.md`): (1) **auto-avance manual sobre match por SUBSTRING** — `searchAnimals` hace `LIKE '%texto%'`; `resolveManualIdentify` trataba CUALQUIER match único como `found` y auto-cargaba sin confirmar (tecleo "1212" → cargó un animal que lo contenía). **Fix**: auto-avance SOLO con match EXACTO (idv/visual/tag === texto); no-exacto → `ambiguous` → `CandidatePicker` (confirmar) o alta. (2) **error de persistencia TRAGADO** — `captureAndAdvance` con `void` sin try/catch ni chequeo de `ServiceResult` → tapear no hacía nada sin feedback. **Fix**: fail-closed (try/catch + chequea `res.ok`); ante fallo NO avanza, superficia `ManeuverErrorBanner` (terracota, es-AR, R5.7/R10.8); `setCaptured` post-write; guard `capturingRef`. Nota: el rechazo de gating server-side es ASÍNCRONO (al sync, R10.8), no bloquea el write local → la hipótesis del leader era falsa; binding capa1↔capa2 del tacto verificado idéntico (no se tocó backend). Nuevo `maneuver-e2e-fault.ts` (inyector de fallo e2e-only, gateado como `ble-e2e-flag`). **reviewer APRUEBA + Gate 2 PASS 0 HIGH** (`review_03-bugfix-tacto.md` / `security_code_03-bugfix-tacto.md`). check.mjs **RC=0 sin flake**. e2e `maniobra-tacto-bugfix.spec.ts` 3/3 (substring→picker / exacto→auto-avance / fallo→banner+no-avanza→reintento→ok). **Esperando que Raf re-pruebe con "1212"** (la dist ya está rebuildeada). LECCIÓN: los tests felices no agarraban estos 2 (substring auto-load + error tragado) — testing en vivo de Raf los cazó.

### M3.2b FIX-LOOP + INSEMINACIÓN (registro previo) — el leader veteó las capturas
**2 cambios** (frontend puro, NO toca backend): (1) **ESPACIO MUERTO** — las pantallas no-keypad nuevas (SilentSanitaryStep / SilentVaccinationStep / LabSampleStep / LabDoubleStep) tienen ~50-60% de pantalla vacía → viola Gate 0 "cero espacio muerto". Dirección del leader: llenar el alto con card dominante (patrón CondicionCorporalStep/superficie). (2) **INSEMINACIÓN R6.5** — falta renderizada (el leader no la scopeó en M3.2b); agregar reusando el silent_apply SINGLE arreglado. El write-path M3.1 (`buildAddManeuverInseminationInsert`, service ai, semenName en notes) + StepKind `inseminacion` + StepValue + data_key `inseminacion` YA existen → solo falta UI + dispatcher case.
Plan: T1 SilentSanitaryStep card dominante (reusada por inseminación) → T2 InseminacionStep (1 pajuela confirma / >1 selector) + dispatcher case → T3 SilentVaccinationStep sin vacío → T4 Lab single/double centrados con peso → T5 e2e inseminación + re-capturas (5: sanitaria-silent/vacunacion/sangrado/raspado/inseminacion) → T6 check.mjs + autorrevisión + reconciliación. Detalle: `progress/impl_03-m3.2b.md` (sección fix).

**FIX-LOOP DONE (técnico, 2026-06-15) — pendiente reviewer + Gate 2 + veto diseño + OK Raf.** (1) **Card dominante** (patrón CondicionCorporalStep figura-fondo, `flex={1}` sobre `$surface`) en los 4 pasos no-keypad → cero espacio muerto; "Cambiar producto"/"Cambiar pajuela" dentro de la card, disjunto del CTA (sin mis-tap). Lab inputs centrados verticalmente. (2) **Inseminación** `InseminacionStep.tsx` (NUEVO) + `case 'inseminacion'`: el write-path M3.1 YA existía (`buildAddManeuverInseminationInsert` → reproductive_events service ai, pajuela en notes; data_key/StepKind/StepValue mapeados) → solo UI + case. 1 pajuela → confirmar de un toque (reusa SilentSanitaryStep parametrizado); >1 → selector + "Otra pajuela". Helper PURO `pajuelasFor` (1 vs >1) + `preconfigHistory` enriquecido. **Inseminación nace deshabilitada en cría (0018) → e2e prende el data_key (`setRodeoDataKey`); server re-valida (capa 2, 0054).** **Divergencia R6.5 reconciliada** (1 pajuela = 1 toque, no "popup informativo" — dirección del leader). **check.mjs RC=0**; **e2e `maniobra-sanitaria` 5/5** (3 previas + inseminación 1 pajuela → service ai server + >1 selector → elige Toro 456 → server); regresión `maniobra-elegir` 2/2 + `maniobra-carga` 4/5 (el 5to = flake pre-existente del keypad M2.2, PASA aislado). **5 capturas re-hechas (alto LLENO)**: sanitaria-silent/vacunacion/sangrado/raspado/inseminacion en `design/maniobra-sanitaria/`. Specs reconciliadas (requirements nota R6.5, design §6.bis.5, tasks M3.2b fix + M3.2c sin inseminación). Detalle: `progress/impl_03-m3.2b.md` (sección FIX-LOOP).

### M3.2b DONE (implementer Opus, 2026-06-15) — sanitarias silent_apply + tubos + pesaje ternero — pendiente reviewer + Gate 2 + veto diseño + OK Raf
4 pantallas nuevas enchufadas al dispatcher de `carga.tsx` (SEAM M3.1; el frame NO se reescribió) → **las 12 maniobras renderizables**. **SilentSanitaryStep** (antiparasitario→`deworming` SIN route D10 / antibiótico→`treatment`): hero del producto preconfig + "Aplicar y seguir" (1 toque); sin preconfig → input + autocompletar "Usadas antes" (R1.8); CTA siempre habilitado (silent: la maniobra se aplicó aunque no se nombre). **SilentVaccinationStep** (R6.1, multi): chips Aftosa/Mancha con × + input + "Agregar"; CTA deshabilitado con 0 vacunas (escribe 1 fila POR vacuna). **LabSampleStep** (sangrado R6.4): 1 nº de tubo → `lab_samples` blood. **LabDoubleStep** (raspado R6.11): 2 nº de tubo (tricho+campylo) → 2 `lab_samples` scrape_*. **tube_number = INPUT DE TEXTO grande ($tubeText=24), NO keypad** (la columna es `text`, los códigos de lab son alfanuméricos A-104/TR-1; SIN es-AR = código de máquina); REQUERIDO (NOT NULL + R5.7 → CTA bloqueado si vacío). **Pesaje ternero** (R6.10): reusa PesajeStep (cae en `case 'pesaje'`); categoría ternera autocompletada por el espejo C6 (header "Cría · Ternera"). **FIX cazado en autorrevisión (R6.12)**: el frame NO aplicaba `filterByAnimalApplicability` → el raspado NO se saltaba para hembras; cerrado (la secuencia = orden ∩ gating rodeo ∩ aplicabilidad atributos). Multi-write: UUIDs estables (`extraIdsFor`) para los writes extra (raspado 2 / vacunación N) + soft-delete de huérfanos al corregir vacunación con menos (`softDeleteManeuverEvents`). Helpers PUROS nuevos `preconfigStringFor`/`preconfigHistory` (+8 tests). **check.mjs RC=0**. **e2e `maniobra-sanitaria.spec.ts` 3/3** (macho con las 5 → server-side deworming SIN route/treatment/2 vaccination/blood/2 scrape_*; hembra → raspado saltado + 0 scrape_*; ternera → pesaje + categoría) + regresión maniobra-elegir 2/2 + maniobra-carga 3/3. **5 capturas** 412×915 en `design/maniobra-sanitaria/` (sanitaria-silent [hero antiparasitario], vacunacion, sangrado, raspado, pesaje-ternero). Specs reconciliadas (tasks M3.2b `[x]` + split M3.2c diferido a M4; design §6.bis.4 as-built). **Diferido a M3.2c→M4**: inseminación R6.5 + preview transición R8.4 + lote R9.x + label timeline. Detalle/mapa R→test/autorrevisión: `progress/impl_03-m3.2b.md`.

### M3.2a (detalle as-built) — pantallas de "elegir" (reviewer + Gate 2 ya verdes)
Las 3 pantallas de paso de las maniobras de "elegir un valor", enchufadas al `switch` del dispatcher `ManeuverStep` de `carga.tsx` (SEAM de M3.1, el frame NO cambió). **TactoVaquillonaStep** (R6.3/R5.13): 3 bloques gigantes APTA (verde `$primary` ✓) / NO APTA (terracota ✗) / DIFERIDA (ámbar `$amber` ⏲ — leader propuso "neutra/ámbar"; elegí **ámbar** porque el gris se leería como deshabilitado sobre 2 bloques vivos) → `reproductive_events` tacto_vaquillona + heifer_fitness. **CondicionCorporalStep** (R6.6): stepper gigante (valor hero `$11` "3,25" + −/+ `$stepperBtn`=88 ≥80, step 0,25, 1,00–5,00, default 3,00, límites deshabilitados) + pista de escala visual (5 marcas, activa resaltada) en card de superficie (densidad R12.5) → `condition_score_events`. Aritmética pura en `condition-stepper.ts` (sin drift de coma flotante). **DientesStep** (R6.7/R6.8): 8 bloques del enum `teeth_state_enum` (0020) en orden de boca (`teeth-options.ts`) → UPDATE propiedad; **prompt CUT** (sheet patrón ManeuverConfigSheet, si 1/2·1/4·sin_dientes vía `CUT_PROMPT_TEETH` + NO terneros vía `shouldOfferCutPrompt`) → UPDATE is_cut/category('cut')/override reusando el camino de categoría as-built; `resolveCutCategory` (NUEVO en animals.ts) resuelve code='cut'→id (SET) + categoría derivada (revert, reusa `resolveRevertCategory`). Tokens JIT provisionales `$amber`/`$amberPress` + `$stepperBtn`. **check.mjs RC=0** (typecheck + anti-hardcode + unit incl. condition-stepper 18 + teeth-options 9 + backend verdes). **e2e `maniobra-elegir.spec.ts` 2/2** (las 3 → apta+3,25+1/2→CUT verificado server-side heifer_fitness/score/teeth_state+is_cut+categoría='cut'; + ternero → NO prompt CUT R6.8). **4 capturas** 412×915 en `design/maniobra-elegir/`. Autorrevisión cazó/cerró: truncado del valor hero ("3,..."→"3,25"), densidad del stepper (card + escala visual), seed del ternero sin birth_date (espejo derivaba vaquillona→prompt aparecía; fix: birth_date<1año=ternera real). Specs reconciliadas (tasks split M3.2a `[x]`/M3.2b, design §6.bis.3 as-built). Detalle/mapa R→test/autorrevisión: `progress/impl_03-m3.2a.md`. **Diferido a M3.2b**: sanitarias silent_apply (vacunación/antiparasitario/antibiótico) + tubos (sangrado/raspado) + inseminación + pesaje ternero + frame (preview transición R8.4, lote opcional R9.2).

### M3.1 (registro previo) — orquestador de eventos para TODAS las maniobras
Generaliza `maneuver-events.ts` + `buildManeuverEventQuery` + `stepKindFor` para escribir el evento correcto de cada maniobra (vacunación→`vaccination`, tacto vaquillona→`tacto_vaquillona`+`heifer_fitness`, sangrado→`lab_samples` blood, condición→`condition_score_events`, dientes→UPDATE `teeth_state`+prompt CUT, pesaje ternero→`weight_events`, raspado→2×`lab_samples`, antiparasitario→`deworming`, antibiótico→`treatment`) con `session_id`, reusando servicios de spec 02. + aplicabilidad per-animal PURA (R6.12 raspado solo machos, R6.8 CUT no-terneros). NO construye pantallas (M3.2). Frontend puro → Gate 1 N/A; reviewer + Gate 2 después.

### M3.0-BACKEND (registro previo, ahora SUPERSEDED por el cierre de arriba) — delta de gating capa 2
Migración **`0091_sanitary_gating_deworming_treatment.sql`** (escrita, **NO aplicada** — deploy a la DB compartida GATEADO por el leader tras **Gate 1 + OK de Raf**, memoria `project_supabase_mcp_write`). Extiende `tg_sanitary_events_gating` (0054, que hoy solo gatea `vaccination`) para gatear fail-closed por rodeo real las 2 maniobras sanitarias nuevas: **`treatment` (Antibiótico)** → `['antibiotico']` (single key, vía `assert_data_keys_enabled`, igual que vaccination); **`deworming` (Antiparasitario)** → **OR pura** de `antiparasitario_interno`/`antiparasitario_externo` (D10 CERRADO Raf 2026-06-14: NO distingue interno/externo; basta uno) vía un helper nuevo `assert_any_data_key_enabled` (mismo fail-closed que el AND pero `v_have >= 1`). Rama `vaccination` EXACTA; `test`/`other` no se gatean; ambos helpers SECURITY DEFINER + search_path + EXECUTE revocado. **2 LOW de housekeeping de M2.1-edge foldeados** (comentarios stale: `maneuver-reads.test.ts:166,168` R4.4→R4.7; `maniobra-edge.ts:18` `evaluateMisconfiguredRodeo`→`shouldWarnMisconfiguredRodeo`). Specs reconciliadas (design §3/§4.bis/§9-D10 RESUELTO; tasks M3.0-BACKEND `[x]` + M3.1/M3.2 sin sub-elección estructurada). **check.mjs FAIL exit 1 = ROJO ESPERADO**: el único test rojo es `T2.4c` de la suite de maniobras (`actual: null` en "treatment disabled → reject" = el insert se aceptó porque el trigger viejo no gatea treatment = estado pre-deploy, NO bug del test); typecheck + 1195 client unit + RLS/Edge/Animal verdes; no es flake de rate-limit ni spec-12. Pasa a verde POST-DEPLOY de 0091. Detalle/mapa R→test/autorrevisión SQL: `progress/impl_03-m3.0-backend.md`. **NO done**: espera Gate 1 (security spec del delta) + reviewer + Gate 2 + OK de Raf para deploy.

**As-built**: inicio `app/app/maniobra.tsx` (presets al tope R2.2 + "Nueva jornada"); wizard `app/app/maniobra/jornada.tsx` (3 etapas: rodeo R1.3 / maniobras gateadas R1.4/R1.5 + toggle + drag-reorder R1.12 + preconfig+autocompletar R1.7/R1.8 / resumen R1.9 → createSession con config {maniobras:[orden],preconfig} R1.13); drag a mano `ManeuverReorderList.tsx` (gesture-handler `Gesture.Pan` + reanimated, handles `GripVertical` visibles — NO había lib draggable); lógica pura `maneuver-wizard.ts`(+test 16 casos, incl. round-trip de orden). check.mjs **verde**; e2e `maniobra-wizard.spec.ts` 1 passed + 4 capturas 412×915 en `design/maniobra-wizard/`. Decisión menor **DM1-UI-1** (autocompletar R1.8 sembrado de presets del campo, no nuevo distinct-query) reconciliada en design §6.bis.1 + tasks M1.4.

### Chunk M1-SERVICIOS (implementer Opus, DONE 2026-06-13) — pendiente reviewer + Gate 2
Lógica pura + servicios de M1 (NO UI del wizard — esa es M1-UI aparte). Backend done (0050-0057). Frontend → Gate 1 N/A; reviewer + Gate 2 después.
Plan:
- **M1.1** `app/src/utils/maneuver-gating.ts` (PURO: mapeo `MANEUVER_DATA_KEYS` + resolución por rodeo real, required/optional) + `app/src/hooks/useManeuverGating.ts` (combina con `fetchRodeoConfig`/`fetchFieldCatalog`/`fetchSystemDefaults`) + unit tests.
- **M1.2** `app/src/services/sessions.ts` (CRUD-plano offline: createSession/closeSession/getActiveSession/setWorkLotLabel + contadores app-maintained; IDs cliente; config jsonb pass-through) + SQL builders en local-reads.ts + tests.
- **M1.3** `app/src/services/maneuver-presets.ts` (CRUD-plano; soft-delete vía RPC 0057; loadPreset filtra maniobras gateadas OFF + lista omitidas R2.3) + SQL builders + tests.
AppSchema: `sessions` + `maneuver_presets` YA estaban (chunk previo) — solo verifico columnas. No toco UI/backend.
