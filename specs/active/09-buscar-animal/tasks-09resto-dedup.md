# Spec 09 — chunk "09 resto · dedup A/B (asignación de caravana)" — Tasks

> Fases en orden de DEPENDENCIA: backend (RPC) primero → tests backend → service+outbox+mapping → UI opción A → UI opción B → E2E → cierre. Cada task con su aceptación + los `RD<n>` que cubre. El implementer marca `[x]`.
> **Gate 1 APLICA** (RPC SECURITY DEFINER nuevo) → corre ANTES de la Fase 1 (lo lanza el leader: `security_analyzer` modo spec sobre el RPC de `design §1`). **Deploy gated**: la migración a la DB compartida = autorización de Raf en sesión.
> Consumir las firmas EXACTAS de spec 04 (`useBleStickListener`/`useBusyWhileMounted`/`useStickListenerControls`) y spec 15 (outbox `enqueue*`/`mapIntentToRpc`/`classifyIntentUploadError`) — NO redefinir.
> Si una task descubre un hueco no cubierto por el Gate 0 (ver §9 del design + DA-1/DA-2/DA-3 del requirements) → **parar y reportar al leader**, no improvisar (especialmente: mecanismo de idempotencia, authz, tabla de audit).

## Fase 0 — Gate 1 (precondición, lo corre el leader)

- [ ] **F0.1** — Gate 1 (security_analyzer modo spec) sobre el contrato del RPC `assign_tag_to_animal` (`design §1`): anti-IDOR (derivar de la fila real), authz D-d (ratificar/endurecer, DA-2), guard `IS NULL`, idempotencia (DA-1: estado vs columna), validación de formato, revoke/grants, search_path, sin tablas nuevas al sync. **Gate 1 PASS es precondición de la Fase 1.** Cubre: superficie de seguridad de RD1.

## Fase 1 — Backend: migración RPC (TOCA LA DB COMPARTIDA — deploy gated)

> **⚠ DEPLOY GATED**: el SQL se escribe acá, pero aplicarlo al remoto (Management API / MCP) requiere autorización de Raf en sesión (clasificador / [[project_supabase_mcp_write]]). Hasta aplicarlo, la suite backend (Fase 2) FALLA con "función inexistente" (PGRST202) — ESPERADO (patrón 0075-0087).

- [x] **F1.1** — `supabase/migrations/0089_assign_tag_to_animal_rpc.sql` (o el siguiente número libre ≥0089): RPC `assign_tag_to_animal(p_profile_id uuid, p_tag_electronic text, p_client_op_id uuid)` SECURITY DEFINER + `set search_path = public`, según `design §1.2`: (a) derivar `animal_id`+`establishment_id` de la fila real del perfil activo (anti-IDOR, `23503` si no existe); (b) `has_role_in(v_est)` (D-d / lo que Gate 1 aprobó, `42501`); (c) validación formato `^\d{15}$` (`23514`); (d) idempotencia por estado ya aplicado → `replay:true` (o lo que Gate 1 fijó en DA-1); (e) UPDATE con guard `AND tag_electronic IS NULL`; (f) `not found` → `23514` "ese animal ya tiene caravana"; unicidad global → `23505`. Cubre: **RD1.1–RD1.7, RD1.9**.
  - _Aceptación_: el SQL compila local (review del leader del SQL); refleja exactamente los controles de `design §1.3` aprobados por Gate 1. NO se aplica al remoto sin autorización de Raf. **AS-BUILT**: migración **0089** escrita (número libre ≥0089 confirmado: el último as-built es 0088). Orden (a)→(f) implementado TAL CUAL design §1.2; `p_client_op_id` recibido pero NO referenciado en ninguna query (passthrough, MED-2). `not found` tras el UPDATE guardado = race → 23514 (verificado: `IF EXISTS` en (d) NO resetea `FOUND`; lo setea el UPDATE de (e)).
- [x] **F1.2** — Cierre de superficie en la misma migración: `revoke execute ... from public, anon` + `grant execute ... to authenticated` firma tipada `(uuid, text, uuid)` + smoke-check fail-closed (estilo 0087) + `notify pgrst, 'reload schema'`. Cubre: **RD1.8**.
  - _Aceptación_: la migración incluye el bloque de cierre idéntico en estructura a 0087/0075. **AS-BUILT**: bloque de cierre idéntico en estructura a 0087 (revoke/grant `(uuid,text,uuid)` + `do $$` que itera anon/public y `raise` si tienen EXECUTE + `notify pgrst`). Función NUEVA → sin firma vieja que dropear.
- [ ] **F1.3 (GATED)** — Aplicar la migración al remoto vía Management API/MCP **tras autorización explícita de Raf en sesión**. Cubre: deploy de RD1.
  - _Aceptación_: la RPC existe en el remoto (`PGRST202` desaparece); la Fase 2 puede correr.

## Fase 2 — Tests backend (node:test contra el RPC deployado) — los 6 escenarios

> Depende de F1.3 (RPC deployado). Sin el deploy, esta suite FALLA esperadamente.

- [x] **F2.1** — Suite `assign_tag_to_animal` (molde de las suites RPC de spec 11/02), los 6 escenarios de `design §8.1`: (1) NULL→valor OK + propagación a `animal_profiles.animal_tag_electronic` (trigger 0079) + `replay:false`; (2) valor→valor rebota (`23514`, trigger 0036); (3) anti-IDOR (perfil de otro campo → `42501`/`23503`, no toca el animal ajeno); (4) rol sin acceso → `42501`; (5) idempotencia (mismo `client_op_id`/TAG aplicado → `replay:true`); (6) dup global → `23505`. Cubre: **RD1 (verificación)**.
  - _Aceptación_: 6/6 verdes contra el RPC deployado. **AS-BUILT**: suite escrita en `supabase/tests/animal/run.cjs` (los 6 escenarios + 3 extra: grants anon fail-closed, formato no-15-díg → 23514, perfil inexistente → 23503). Guard `assertRpcExists` (PGRST202 → `assert.fail`) para no pasar por la razón equivocada. EID de 15 díg único por invocación (los `${RUN_TAG}_...` de la suite base no cumplen `^\d{15}$`). **PENDIENTE DE DEPLOY**: hoy FALLA con PGRST202 (RPC 0089 no aplicada) — ESPERADO; va verde tras F1.3 (deploy del leader). El resto de la suite Animal (spec 02/11/13/15) sigue verde = cero regresión.

## Fase 3 — Service offline: outbox + RPC-mapping + clasificación (sin DB compartida)

> Frontend/cliente puro. NO toca la DB compartida (solo el SQLite local + la outbox).

- [x] **F3.1** — `outbox.ts`: `enqueueAssignTag({ params: { p_profile_id, p_tag_electronic } })` → `buildOpIntentInsert(clientOpId, 'assign_tag_to_animal', JSON.stringify(params), createdAt)` + `enqueue(intent, [], db)` (SIN overlay — `animals` no está local, `design §2.1`). Cubre: **RD2.2**.
  - _Aceptación_: encola un `op_intent` con el op_type y los params correctos; sin overlay; éxito offline. **AS-BUILT**: `enqueueAssignTag` + `EnqueueAssignTagInput` agregados, molde de `enqueueExitAnimal` pero `enqueue(intent, [], db)` (overlay vacío). No tiene unit propio (consistente con as-built: `outbox.ts` importa `react-native` vía `./database` → fuera del grafo node:test, igual que `enqueueExitAnimal`/`enqueueRegisterBirth`, sin unit). La verificación del shape pasa por el typecheck + el unit del mapeo + la suite backend.
- [x] **F3.2** — `upload.ts`: agregar `assign_tag_to_animal` a `RPC_OP_TYPES` + extender la rama de `p_client_op_id` en `mapIntentToRpc` (`opType === 'register_birth' || opType === 'assign_tag_to_animal'`) → `{ rpcName:'assign_tag_to_animal', args:{ ...params, p_client_op_id: op.id } }` (`design §2.2`, opción (a) — op_type = nombre de la RPC). Cubre: **RD2.3**.
  - _Aceptación_: `mapIntentToRpc` de un intent `assign_tag_to_animal` arma `{ rpcName, args con p_client_op_id }`; un intent desconocido sigue dando `PermanentIntentError`. **AS-BUILT**: `assign_tag_to_animal` agregado a `RPC_OP_TYPES`; rama `p_client_op_id` extendida a `register_birth || assign_tag_to_animal`. `op_type === rpcName` → mapeo genérico sin case especial (fold MED-1).
- [x] **F3.3** — `upload.test.ts`: unit del mapeo `assign_tag_to_animal`. Verificar que `classifyIntentUploadError` NO cambió para los casos existentes y que `23505`/`23514`/`42501`/`23503` de `assign_tag_to_animal` caen en `permanent_reject` (default, `design §2.3`). Cubre: **RD2.3, RD2.4**.
  - _Aceptación_: unit verde; sin regresión en los casos existentes del clasificador. **AS-BUILT**: 2 tests nuevos en `upload.test.ts` — (1) mapeo `assign_tag_to_animal` → `{ rpcName, args con p_profile_id/p_tag_electronic/p_client_op_id }`; (2) clasificación: 23505/23514/42501/23503 → `permanent_reject` + red → `transient` + el 23505 de assign_tag NO cae en el idempotent_discard de register_birth. 23/23 verdes; cero regresión.
- [x] **F3.4** — `animals.ts`: service público `assignTagToAnimal(profileId, tag)` → `enqueueAssignTag(...)` (`design §2.4`). Confirmar (verificación §9.3 del design) que un 2xx con `{replay:true}` se trata como ACK en `uploadData` (clearOverlay), no como error. Cubre: **RD2.1, RD2.5**.
  - _Aceptación_: el service encola offline al instante; el replay idempotente no se trata como error. **AS-BUILT**: `assignTagToAnimal(profileId, tag): Promise<OutboxResult>` (thin sobre `enqueueAssignTag`, contrato literal de design §2.4). Verificación §9.3 **CONFIRMADA**: el clasificador as-built (`classifyIntentUploadError`) solo se invoca cuando `supabase.rpc` devuelve `error`; un 2xx con cuerpo `{replay:true}` devuelve `data` SIN error → no entra al clasificador → ACK normal (clearOverlay). No requiere case `idempotent_discard` para `assign_tag_to_animal` → `classifyIntentUploadError` queda SIN cambios para esta op (design §2.3 / RD2.4).

## Fase 4 — UI opción A: modo `assign_or_create` del bottom-sheet (sin DB compartida)

> Depende de Fase 3 (service). Frontend puro sobre el host BLE global ya construido.

- [x] **F4.1** — Decisión pura `resolveCreateOrAssign(noTagCandidateCount)` (`design §3.2`, módulo `tag-lookup.ts` o `assign-candidates.ts`) + unit (0 → `create`, ≥1 → `assign_or_create`). Cubre: **RD8.1, RD8.2**.
  - _Aceptación_: función pura testeada sin SDK/DOM (molde `resolveTagLookup`). **AS-BUILT (Run 2)**: `resolveCreateOrAssign(count): { mode:'assign_or_create' } | { mode:'create' }` + `CreateOrAssignResult` agregados a `tag-lookup.ts` (junto a `resolveTagLookup`, mismo módulo puro sin I/O). Regla `count > 0` → `assign_or_create`, si no `create` (defensivo: count ≤0 cae a create, nunca intermedia vacía). 4 tests en `tag-lookup.test.ts` (0/1/≥2/negativo).
- [x] **F4.2** — `local-reads.ts`: variante de candidatos `noTag` ordenada por `updated_at DESC` (opción `orderBy` en `buildAnimalsListQuery` o builder dedicado, `design §3.4`) + test del builder (SQL/args + integración SQLite). Cubre: **RD3.3, RD5.2**.
  - _Aceptación_: el builder devuelve candidatos `noTag` activos del campo, `updated_at DESC`; test verde. **AS-BUILT (Run 2)**: opción `orderBy?: 'created_at' | 'updated_at'` en `buildAnimalsListQuery` (default `created_at` → cero regresión). Con `updated_at`: la rama synced proyecta `ap.updated_at` REAL; la overlay (`pending_animal_profiles` NO tiene `updated_at`) proyecta `pap.created_at AS updated_at` (frescura del alta optimista) — column-aligned con el UNION vía helper `injectProjection` (inserta el alias ANTES del ` FROM`, no al final del SELECT que ya incluye FROM/JOINs). ADEMÁS: `buildNoTagCandidatesCountQuery(establishmentId)` para el conteo del host (COUNT noTag synced [oculta exits] + overlay → 1 fila, no degrada). Tests en `local-reads.test.ts`: SQL/args (string-match) + 2 de COMPORTAMIENTO contra node:sqlite (orden por updated_at mezclando synced+overlay; conteo excluye tagged/otro-campo/exited). 99/99 verdes.
- [x] **F4.3** — `FindOrCreateOverlay.tsx`: en la rama `create` de `lookupByTag`, computar el conteo de candidatos `noTag` (lectura local) → `resolveCreateOrAssign` → si `assign_or_create`, render del nuevo `AssignOrCreateBody`; si `create`, comportamiento actual (directo a CREATE, RD3.2). Live-rescan / cambio de campo / cierre reusan la maquinaria del host (RB3.5/RB2.4/RB3.4 = RD4.2/RD4.3/RD4.4). Cubre: **RD3.1, RD3.2, RD4.1–RD4.5**.
  - _Aceptación_: bastoneo sin match con candidatos → intermedia; sin candidatos → CREATE directo. **AS-BUILT (Run 2)**: `onTagRead` reescrito a `async` IIFE — tras `lookupByTag`, SOLO la rama `mode:'create'` corre `runLocalQuerySingle(buildNoTagCandidatesCountQuery(establishmentId))` → `resolveCreateOrAssign(count)` (RD3.8: edit/transfer NO cambian; solo BLE). El guard de secuencia `seqRef`/`ticket` se RE-CHEQUEA tras AMBOS awaits (lookup + count) → live-rescan/cierre/cambio-de-campo descartan el resultado tardío SIN tocar `setState` (RD4.2/RD4.3/RD4.4 reusados sin romperlos). `OverlayState.ready.result` pasó de `TagLookupResult` a `ResolvedBody = TagLookupResult | { mode:'assign_or_create' }` (sub-estado del host, NO toca el type puro del service). Fail-safe: count read falla → `count=0` → CREATE directo (nunca bloquea el alta).
- [x] **F4.4** — `AssignOrCreateBody` (cuerpo del sheet, `design §3.3`): encabezado EID + título + buscador (`searchAnimals` scopeado a `noTag`, RD3.4) + lista scrollable de candidatos (idv/visual/category/sex/rodeo, RD3.5) + CTA "Es un animal nuevo → dar de alta" (RD3.7). Tocar candidato → confirmar → `assignTagToAnimal(profileId, eid)` → cerrar + `/animal/[id]` (RD3.6). Cubre: **RD3.3–RD3.7**.
  - _Aceptación_: elegir candidato asigna (offline) y aterriza en la ficha; "es nuevo" navega a `/crear-animal?tag=`. La puerta manual sigue directo a CREATE (RD3.8 — no se toca). **AS-BUILT (Run 2)**: `AssignOrCreateBody` (hermano de Edit/Create/TransferBody) + helpers `CandidateSearchBar`/`CandidateRow`/`CandidateSummary`. Título "¿Es uno de tus animales sin caravana?"; buscador con debounce 250ms + guard `searchSeq` (idiom de la tab Animales); lista: término vacío → `fetchAnimals({ noTag:true, orderBy:'updated_at' })`, con término → `searchAnimals` filtrado client-side a `tagElectronic == null` (design §3.4); `ScrollView maxHeight $candidateListMax` (300, JIT token) → buscador y CTA "es nuevo" (`Button secondary fullWidth` ≥`$touchMin`) SIEMPRE visibles, PINNED fuera del scroll (Fitts). Cada `CandidateRow` = idv/visual + `CategoryBadge` + sexo + rodeo (RD3.5). Tocar candidato → paso de CONFIRMACIÓN ("Le vas a asignar la caravana `<EID legible>` a este animal" + `CandidateSummary`) → `assignTagToAnimal(profileId, eid)` (offline) → al éxito del ENCOLADO: `onClose()` + `router.push('/animal/[id]', { id: profileId })` (RD3.6). "Es nuevo" → `onClose()` + `/crear-animal?tag=<eid>` (idéntico a `CreateBody`, RD3.7). `fetchAnimals` + `FetchAnimalsFilter` extendidos con `orderBy` (passthrough al builder). Cero hardcode (lint 0 violaciones); `$candidateListMax` token nuevo en `tamagui.config.ts`.
    - **AS-BUILT (fix de diseño post-veto del leader, Run 2)**: cada `CandidateRow` ahora lleva un **chevron derecho** (`ChevronRight` de lucide) como afford de tap explícito (patrón Jakob de fila tappable iOS/Android) — en la manga (una mano, <1s, pleno sol) deja inequívoco que tocar el candidato asigna la caravana. Único cambio: el interior del `Card` se reestructuró a un `XStack alignItems="center"` con `[YStack flex={1}` (hero/visual/categoría/sexo/rodeo, igual que antes) `][View flexShrink={0}` (chevron) `]` → el hero queda left-aligned y el chevron NO lo empuja ni lo recorta (ADR-027: decoración lateral DERECHA sobre contenido NO centrado → sin riesgo de descentrado). El chevron va dentro de un `View` no-Pressable → NO roba área de tap (toda la fila sigue tappable, verificado en la captura de confirmación). Tamaño `getTokenValue('$navIcon','size')` (24, idéntico a `AnimalRow`) + color `getTokenValue('$textMuted','color')` + `strokeWidth={2}` (cero hardcode, ADR-023 §4). NO se tocó: `resolveCreateOrAssign`, el builder de candidatos, `assignTagToAnimal`, la lógica 0-candidatos→CREATE, el `key={eid}` del live-rescan, ni el CTA "es nuevo". Re-captura del veto: `design/veto-dedup-opcionA/assign-or-create.png` (lista con chevrons) regenerado + `design/veto-dedup-opcionA/assign-confirm.png` (estado de confirmación tras tocar un candidato) NUEVO; el spec de captura `app/e2e/dedup-screenshot.spec.ts` se extendió para emitir ambos. `node scripts/check.mjs` verde end-to-end (sin cambios de tests: ningún snapshot/medida de layout depende de la estructura del `CandidateRow`).
- [ ] **Veto de diseño del leader** (skill `design-review`) sobre el modo `assign_or_create` ANTES de mostrar a Raf: EID legible, lista escaneable a pleno sol, CTA "es nuevo" ≥56px siempre visible, una decisión por pantalla, operable con una mano. **[PENDIENTE — es del leader, no del implementer]**

## Fase 5 — UI opción B: `BulkTagAssignmentScreen` (sin DB compartida)

> Depende de Fase 3 (service) + Fase 4.2 (builder de candidatos). Frontend puro.

- [x] **F5.1** — `BulkTagAssignmentScreen` (`app/app/asignar-caravanas.tsx`) + registro de la ruta en `_layout.tsx`: consume el listener BLE en modo asignación; cola de sesión (estado local); para el EID en cabeza muestra candidatos `noTag` + buscador (RD5.4). Cubre: **RD5.2, RD5.4, RD5.7**.
  - _Aceptación_: la pantalla bastonea en serie sin abrir el overlay global; muestra candidatos del EID actual. **AS-BUILT (Run 3)**: `app/app/asignar-caravanas.tsx` (`BulkTagAssignmentScreen` default export) + ruta `asignar-caravanas` registrada en `app/app/_layout.tsx` (`<Stack.Screen>` + agregada a `ANIMAL_DESTINATIONS` para que el gate no la expulse de 'active'). Cola de sesión vía `useReducer` (`queue: string[]`, cabeza = EID actual; dedup defensivo de re-bastoneo). El listener es `useBleStickListener({ enabled, onTagRead })` PROPIO con `enabled = est.active && rodeo.active` (RB2.1). **ANTI-STACKING — reconciliación de design §4.2**: el `useBusyWhileMounted()` que el sketch sugería era **INVIABLE** — el provider de spec 04 gatea `listening = enabled && !busy` ANTES de entregar a CUALQUIER suscriptor (`BleStickListenerProvider.handleReading` early-return si `!listening`), así que `busy=true` mataría TAMBIÉN el listener de esta pantalla (no recibiría tags). Mecanismo as-built que NO toca `ble/*` (gateado): el `FindOrCreateOverlay` global se hizo ROUTE-AWARE (`useSegments()` → si el top-segment es `asignar-caravanas`, su `onTagRead` no abre nada + cierra cualquier overlay stale). Verificado E2E (mock): el bastoneo en la masiva muestra "¿A cuál de tus animales sin caravana?" (la pantalla) y NO "¿Es uno de tus animales sin caravana?" (el overlay global) → no se apila ni se procesa dos veces.
- [x] **F5.2** — Asignación 1×1 + contador: elegir candidato → confirmar → `assignTagToAnimal` → quitar el candidato de la lista de sesión (client-side, RD2.5) + avanzar la cola + incrementar el contador "X caravanas asignadas" (RD5.5). Cerrar la pantalla no rollbackea (cada intent es independiente). Cubre: **RD5.3, RD5.5**.
  - _Aceptación_: asignar saca el candidato de la lista; el contador sube; cerrar conserva lo encolado. **AS-BUILT (Run 3)**: tocar candidato → paso de CONFIRMACIÓN ("Le vas a asignar la caravana `<EID legible>` a este animal" + `CandidateSummary`) → `assignTagToAnimal(profileId, eid)` (offline, Run 1) → al ÉXITO del encolado: `dispatch({type:'assigned', profileId})` → avanza la cola (`queue.slice(1)`) + `assignedCount + 1` + `assignedProfileIds.add(profileId)`. `visibleCandidates` filtra `excludedProfileIds` (RD2.5: el candidato no re-aparece aunque el denorm local siga NULL hasta el sync). El contador (`SessionCounter`) vive en el header del screen (NO se desmonta al avanzar la cola). NO se cierra la pantalla: el `key={currentEid}` del `BulkEidBody` lo REMONTA limpio con el próximo EID (resetea búsqueda + `confirming`). Cerrar la pantalla unmonta el estado de sesión PERO los `op_intent` quedan en la outbox (independientes, RD5.5) — no rollbackea.
- [x] **F5.3** — CTA "Bastoneé un animal nuevo, no está en la lista" → `/crear-animal?tag=<eid>` (RD5.6); al volver, la sesión sigue. Entry points (RD5.1 / D-c): CTA "Asignar caravanas en masa" en la tab Animales con filtro `noTag` + ítem en la tab Más. Cubre: **RD5.1, RD5.6**.
  - _Aceptación_: "es nuevo" da de alta sin perder la sesión; ambos entry points navegan a la pantalla. **AS-BUILT (Run 3)**: CTA "Bastoneé un animal nuevo, no está en la lista" → `dispatch({type:'skipHead'})` (saca el EID de la cola, se va a dar de alta CON él) + `router.push('/crear-animal', {tag:eid})`. crear-animal usa `router.push` → la `BulkTagAssignmentScreen` SIGUE montada en el stack debajo con su estado de sesión intacto → al volver (back / tras crear → ficha → back) la cola con los EIDs restantes + el contador persisten (RD5.6). **Entry points**: (a) tab Animales (`animales.tsx`) — con el filtro "Sin caravana" (`onlyNoTag`) activo aparece un `Button primary fullWidth` "Asignar caravanas en masa" en el header fijo → `/asignar-caravanas`; (b) tab Más (`mas.tsx`) — `ActionRow` "Asignar caravanas en masa" (ícono `Radio`) en la sección "Campo activo", visible a TODOS los roles (D-d: cualquier rol activo caravanea, no owner-only). Ambos verificados E2E (mock).
- [x] **F5.4 (Run 4 — prevención client-side, RD6 reconciliada por el leader)** — Prevención de dup/race en la masiva (`design §5`/§5.0): `onTagRead` corre `lookupByTag` ANTES de encolar; un EID que ya tiene caravana (`mode:'edit'`/`'transfer'`) NO se encola y avisa in-sesión sin perder progreso; el residual al sync (negligible/auto-sanante) lo maneja la maquinaria existente (NO se inventa canal de sync nuevo). Cubre: **RD6.1, RD6.2, RD6.3, RD9.3**.
  - _Aceptación_: un EID ya asignado avisa accionablemente y la sesión sigue; un EID nuevo entra a la cola normal. **AS-BUILT (Run 4)**: **decisión del leader (RD6 reconciliada, post-Run-3)** — el canal de rechazos de sync as-built (`connector.ts::surfaceUploadRejection`) es solo `console.warn` y `assign_tag_to_animal` no tiene overlay → NO se puede surfacear el rechazo *al sincronizar* sin inventar un canal nuevo (PROHIBIDO por RD6.3). La defensa correcta es **PREVENIR el dup en el momento del bastoneo (client-side)**. Implementado en `asignar-caravanas.tsx`: `onTagRead` pasó a `async` → corre `lookupByTag(eid, establishmentId)` (lectura LOCAL, ya existente) ANTES de encolar. Solo `mode:'create'` (EID nuevo) → `dispatch({type:'enqueue'})`; `mode:'edit'` (ya tiene caravana en este campo) / `mode:'transfer'` (en otro campo del usuario) → estado `dupNotice` + `DupNoticeBanner` ("Esa caravana ya está asignada · ese TAG ya está asignado a otro animal de tus campos, no se puede reasignar") **sin encolar**, sin tocar la cola/contador (no se pierde la sesión). Fallo de la lectura local (raro) → fail-CLOSED (banner "No pudimos verificar" + no encola). El `DupNoticeBanner` reusa el patrón `Card`+`Button` de `FieldChangedNotice` (componente ya vetado) con acento `$terracota` → banner menor (cero hardcode, voseo). Re-chequeo del campo tras el await del lookup (si cambió → descarta). El banner se limpia al descartar / al bastonear un EID válido nuevo / al cambiar de campo. El **residual** (un assign que igual rebote al sync: race auto-sanante o dup cross-tenant casi imposible) lo maneja la maquinaria existente (`classifyIntentUploadError → permanent_reject → descarte + log`), SIN canal/toast de sync nuevo — **LIM documentada** (RD6.2/RD6.3). En la opción A esto ya estaba cubierto por el motor (un EID con match va a `edit`/`transfer`, nunca entra a `assign_or_create`). **Test**: `dedup-screenshot.spec.ts::"opción B: bastonear un EID ya asignado NO encola y avisa (RD6.1)"` — bastonea un EID sembrado CON caravana → banner "Esa caravana ya está asignada" + la cola NO avanza (sigue en "Bastoneá para empezar", NO en la lista de candidatos) + un EID nuevo después SÍ entra a la cola. Captura `design/veto-dedup-opcionB/bulk-dup-warning.png` para el leader (banner menor — capturada por cortesía, no obligatoria por reusar componente vetado).
- [x] **F5.5** — Re-escopeo de sesión al cambiar de establishment (RD7.3 / DA-3): aplicar el default acordado con el leader (reiniciar la sesión al nuevo campo con aviso) — **confirmar la UX con el leader/Raf antes de cerrar**. Cubre: **RD7.1, RD7.3**.
  - _Aceptación_: cambiar de campo nunca muestra candidatos del campo anterior. **AS-BUILT (Run 3)**: `prevEstablishmentRef` (arranca con el campo inicial → un primer render NO dispara reset) + `useEffect([establishmentId])` → al CAMBIAR el campo activo: `dispatch({type:'reset'})` (vacía cola + contador + `assignedProfileIds`) + `setFieldChangedNotice(true)` (banner "Cambiaste de campo · reiniciamos la sesión · las caravanas que ya asignaste se guardaron"). Invariante DURA (RD7.3) cumplida: al resetear, `currentEid` → null → `EmptyQueueState` → NUNCA se muestran candidatos del campo anterior; además los loaders de candidatos están scopeados al `establishmentId` activo (RD7.1). El default acordado (reiniciar + avisar) es el confirmado por el leader en DA-3. _(El veto visual del aviso lo hace el leader.)_
- [ ] **Veto de diseño del leader** (skill `design-review`) sobre la `BulkTagAssignmentScreen` ANTES de mostrar a Raf: contador visible, lista escaneable, ritmo bastón→asignar→siguiente fluido, una decisión por pantalla. **[PENDIENTE — es del leader, no del implementer]**

## Fase 6 — E2E Playwright (bastón mock) — los 4 escenarios de §3.5 del Gate 0

> Reusa el wiring de mock del chunk BLE global (`window.__rafaqBle.tagRead` bajo flag E2E). Frontend puro.

- [x] **F6.1** — `app/e2e/baston-dedup.spec.ts` (o extender `baston.spec.ts`), los 4 escenarios (`design §8.3`): (a) BLE sin-match con candidatos → intermedia → elegir candidato → asignar → ficha con la caravana (al sincronizar en el mock); (b) intermedia → "es nuevo" → CREATE con el EID precargado; (c) masiva: bastonear 2 → asignar a 2 candidatos → contador en 2 → ambos salen de la lista; (d) dup-TAG → copy accionable, sesión no se pierde. Seed con candidatos `noTag` + un TAG ya usado para el dup. Cubre: **RD3/RD5/RD6 (E2E)**.
  - _Aceptación_: los 4 escenarios VERDES sobre PowerSync local con el mock. **AS-BUILT (Run 4)**: archivo NUEVO `app/e2e/baston-dedup.spec.ts` con **5** escenarios de COMPORTAMIENTO (los 4 del Gate 0 + el directo-a-CREATE RD3.2): (a) opción A asignar a candidato → ficha del candidato correcto + **caravana puesta verificada en el SERVER** (oráculo `waitForServerTagAssigned` nuevo en `helpers/admin.ts` — pollea `animals.tag_electronic` Y `animal_profiles.animal_tag_electronic`, probando la cadena offline→sync→RPC→propagación 0079; NO se verifica en la ficha porque es lectura LOCAL no-reactiva, staleness offline-first documentada); (b) opción A "es nuevo" → `/crear-animal` con EID precargado read-only ("Creando: [EID]"); (a') 0 candidatos noTag → CREATE directo, NO abre la intermedia (RD3.2); (c) opción B masiva: 2 bastoneos de EIDs distintos → asignar a 2 candidatos → contador "2 caravanas asignadas" + ambos salen de la lista de sesión (un 3er bastoneo no tiene candidatos); (d) opción B EID ya asignado → banner "Esa caravana ya está asignada" sin encolar + sesión no se pierde + un EID nuevo después SÍ entra (RD6.1). Reusa el harness mock del bastón (`window.__rafaqBle.tagRead` bajo `__RAFAQ_BLE_E2E__`). Seed espera el first-sync (candidatos visibles en la lista) antes de bastonear (no pasar por la razón equivocada). **5/5 VERDES** (`pnpm exec playwright test e2e/baston-dedup.spec.ts`). Además, `dedup-screenshot.spec.ts` se endureció: EIDs hardcodeados → `makeEid()` único por corrida (un EID fijo leakeaba el unique global de `animals` entre runs interrumpidos). La suite BLE+dedup completa (12 tests: baston + baston-dedup + dedup-screenshot) corre VERDE junta, sin regresión de `baston.spec.ts`.

## Fase 7 — Cierre y verificación

- [x] **F7.1** — Autorrevisión adversarial del implementer (paso 8 de su agente). **AS-BUILT (Run 4)**: pasada adversarial sobre el E2E formal — busqué tests que pasen por la razón equivocada y los cerré: (1) el escenario (a) verificaba la caravana en la FICHA (lectura LOCAL no-reactiva via `useFocusEffect` → muestra "—" hasta re-focus) → el test FALLÓ por timeout; el verde fácil habría sido subir el timeout (test verde-falso), el correcto fue mover la verificación al SERVER (oráculo `waitForServerTagAssigned` atado al `profileId` del candidato + el EID único → no puede pasar por el animal equivocado); (2) destapé un leak REAL del `dedup-screenshot.spec.ts` (EID hardcodeado choca el unique global de `animals` entre runs) → lo endurecí a EID único (NO es código del chunk; es un test). El RPC/host/masiva/sync (código del chunk) NO los toqué — ya gateados en Runs 1-3 (reviewer APPROVED + Gate 2 PASS en `review_/security_code_09resto-dedup.md`). NO descubrí ningún bug del código del chunk al testear (los 5 escenarios + el oráculo confirman el comportamiento esperado end-to-end).
- [x] **F7.2** — `node scripts/check.mjs` exit 0 end-to-end (lint anti-hardcode 0 violaciones, unit app + builders + mapeo, suites backend con el RPC deployado). Cubre: **criterios de aceptación del chunk**. **AS-BUILT (Run 4)**: check.mjs VERDE end-to-end ("All tests passed" / "Entorno listo"), exit 0. Las modificaciones del Run 4 son E2E-only (`e2e/` está excluido del `tsc` del cliente; Playwright transpila aparte) → cero impacto en el typecheck/unit. Suite E2E dedup (Playwright): 5/5 + screenshot 3/3 + baston 4/4 = 12/12 verdes.
- [x] **F7.3** — Reconciliación specs↔as-built al cerrar el chunk. **AS-BUILT (Run 4)**: actualizadas las notas AS-BUILT del `requirements.md` BASE (R7/R8/R12) a los deltas FINALES, corrigiendo 2 afirmaciones que habían quedado stale del plan: (1) **R8** decía "anti-stacking con `useBusyWhileMounted`" → CORREGIDO a "route-aware del overlay global" (el `useBusyWhileMounted` era inviable, reconciliado en Run 3 design §4.5); (2) **R8.4** decía "el dup-TAG se surfacea por el canal de status existente al sincronizar" → CORREGIDO a "prevención CLIENT-SIDE al bastonear (RD6.1) + LIM del residual al sync (RD6.2/RD6.3)" (el canal as-built es solo `console.warn`, reconciliado en Run 4 RD6). Además: R7/R12 actualizados a "Gate 1 RATIFICÓ idempotencia state-based + authz cualquier rol activo" (ya no "a confirmar") + cita de la migración **0089** deployada + la staleness de la ficha + referencia al E2E formal. Corregidos los headers Status de `requirements-09resto-dedup.md` y `design-09resto-dedup.md` (decían "Draft / pendiente de aprobación + Gate 1" → ambos YA pasaron + deploy hecho). Tabla de archivos del design ampliada con `baston-dedup.spec.ts` + el oráculo `waitForServerTagAssigned` + el endurecimiento del screenshot spec. **Nunca quedan specs contradictorias con el código.** Cierre de 09 (con opción C diferida) = del leader tras reviewer + Gate 2 + Puerta 2.

---

## Notas de scope (del Gate 0)

- **DENTRO**: RPC `assign_tag_to_animal` (≥0089), service offline (outbox + mapping), modo `assign_or_create` del sheet (opción A), `BulkTagAssignmentScreen` (opción B), manejo de race/dup, E2E mock.
- **DIFERIDO** (NO en este chunk): opción C (detección automática + merge, R9 base), `spp-android` real (gated ADR-024 §4), audit granular de asignación (R12.4 base — upgrade post-MVP).
- **Gates**: **Gate 1 SÍ** (RPC SECURITY DEFINER, lo corre el leader antes de Fase 1). **Deploy gated** (F1.3, autorización de Raf). **Gate 2 SÍ** (RPC + host BLE + masiva + sync, lo corre el implementer). Veto de diseño del leader sobre el modo `assign_or_create` y la `BulkTagAssignmentScreen`. Puerta 2 (Raf prueba en `pnpm web` con mock/web-serial) para `done` del chunk.
- **Fases que tocan la DB compartida**: SOLO la **Fase 1** (migración del RPC) → F1.3 gated. La Fase 2 (tests backend) la LEE (corre contra el RPC deployado). Fases 3–6 son cliente/local (SQLite + outbox + UI + E2E mock) — sin tocar la DB compartida.

## DECISIONES ABIERTAS heredadas del requirements (para el leader / Raf)

- **DA-1** — mecanismo de idempotencia del RPC: estado ya aplicado (`design §1.2 d`) vs columna+índice. A ratificar en Gate 1. Si endurece a columna/tabla de audit → parar y consultar (alcance mayor).
- **DA-2** — authz D-d ("cualquier rol activo"): Gate 1 ratifica o endurece a owner-or-creator.
- **DA-3** — UX del re-escopeo de sesión en la masiva ante cambio de campo (F5.5): confirmar con el leader/Raf el default (reiniciar + avisar).
