# Spec 04 — DELTA «multivendor + selección + demo» — Tasks

**Status**: `spec_ready` (delta-spec ADR-028 Nivel B — mini-ciclo propio; **NO reabre el core aprobado de spec 04, ni toca su `tasks.md` original**).
**Fecha**: 2026-07-20 (sesión bastón).
**Fuente**: `requirements-multivendor.md` (RMV) + `design-multivendor.md`. Cada `RMV<n>` mapea a ≥1 task; cada task referencia ≥1 `RMV<n>`.

> **Reglas** (`docs/specs.md`): pasos discretos en orden, cada uno con `[ ]` + los `RMV<n>` que cubre. El implementer marca `[x]`; el reviewer rechaza `[ ]` sin justificación. Cada `RMV<n>` mapea a ≥1 test.

> **Orden de despacho para el leader.** Las fases están ordenadas para despachar implementers por fase, respetando dependencias:
> 1. **Fases MV.0–MV.4 = buildable-hoy sin hardware** (registro + selección + simulador + UI). Todo unit puro / mock / simulador. Se puede cerrar entero sin ningún bastón.
> 2. **Fase MV.5 = `adapter-spp-android` escrito** (código + tests puros hoy; device-test GATED por hardware). Depende del registro (MV.1) + veto del config plugin.
> 3. **Fase MV.6 = gated / fuera de este delta** (MFi/EA, GATT, device-validation real). Arquitectura preparada, sin implementar.

| Fase | Buildable hoy | Gated por hardware | Gated por negocio (MFi) |
|---|---|---|---|
| MV.0 — Setup (uniones aditivas + tipos) | ✅ | — | — |
| MV.1 — Registro de drivers (RMV1) | ✅ (puro) | — | — |
| MV.2 — Motor de selección (RMV2) | ✅ (puro) | matching device→driver real | — |
| MV.3 — Simulador + gate demo (RMV4) | ✅ (dev/demo-gated) | — | — |
| MV.4 — Pantalla de conexión + indicador (RMV3) | ✅ (UI + mock/web-serial/sim) | conexión real SPP/HID | — |
| MV.5 — `adapter-spp-android` (RMV5) | ✅ **código + tests + dep nativa instalada + montado en Android** (2026-07-29) + **stream/dedup/reconexión con el emulador ESP32** (T-MV.5.7, **corrido en device el 2026-07-30**) | solo las **idiosincrasias** del RS420 físico (T-MV.5.6) | — |
| MV.6 — MFi/EA + GATT (RMV6) | arquitectura declarable | — | ✅ adapter EA/MFi |

---

## Fase MV.0 — Setup (buildable hoy)

- [x] T-MV.0.1 — Confirmar el core as-built intacto: `contract.ts`, `stick-adapter.ts`, `adapter-selection.ts`, `parser-rs420.ts`, `line-framer.ts`, `remembered-device.ts`, `permissions.ts`, `BleStickListenerProvider.tsx`. **NO reescribir** ninguno. Cubre: prerrequisito (RMV1–RMV5).
- [x] T-MV.0.2 — Extensiones **aditivas** de union (04-owned): `stick-adapter.ts` `StickAdapter['kind']` += `'simulator'`; `adapter-selection.ts` `AdapterKind` += `'simulator'`, `ProviderMode` += `'demo'`. Completar los switches exhaustivos que el typecheck marque (`permissions.ts` → `case 'simulator': {kind:'none'}`; `instantiateTransport`). Cubre: RMV2.7 (aditividad), RMV4.1 (kind).
- [x] T-MV.0.3 — `driver-types.ts`: `TransportKind`, `TransportCapability` (discriminada), `FrameParser`, `DeviceMatcher`, `DiscoveredDevice`, `DiscoveryChannel`, `ReaderDriver`/`ReaderProfile`. PURO (sin RN). Cubre: RMV1.1, RMV1.2, RMV6.1 (params `mfi`).

## Fase MV.1 — Registro de drivers (buildable hoy, puro)

- [x] T-MV.1.1 — `driver-rs420.ts`: `RS420_DRIVER` con transportes `spp` (`SPP_UUID`, pin `'1234'`) + `serial` (`DEFAULT_BAUD`), `frameParser` = `{ parse: parseRs420Line }` (reuso, **no** reimplementa), `deviceMatch` (`/RS\s?420|allflex/i` + `[SPP_UUID]`), `streaming:true`. Cubre: RMV1.3.
- [x] T-MV.1.2 — `driver-registry.ts`: `DRIVER_REGISTRY = [RS420_DRIVER]`, `driverByVendorId(id)`, `findDriverForDevice(device)` (cruza `deviceMatch` con nombre/UUIDs). Cubre: RMV1.4, RMV1.5, RMV1.7.
- [x] T-MV.1.3 — `driver-registry.test.ts` (node:test): RS420 matchea un device `{name:'RS420…'}` / uno con `advertisedServiceUuids:[SPP_UUID]`; un device desconocido → `findDriverForDevice`=`null` (RMV1.7); `driverByVendorId('allflex-rs420')` OK / `null` para inexistente; **test de aditividad** (RMV1.6): registrar un driver sintético en una copia del registry y resolverlo por selección NO requiere editar `contract.ts`/`stick-adapter.ts`/adapters. Cubre: RMV1.3, RMV1.4, RMV1.5, RMV1.6, RMV1.7.

## Fase MV.2 — Motor de selección por capacidad (buildable hoy, puro)

- [x] T-MV.2.1 — `selection-priority.ts`: `platformTransportPriority(os)` (ios `['ble-hid','ble-gatt','mfi']`; android `['spp','ble-gatt','ble-hid']`; web `['serial']`; otro `[]`); `adapterForTransport(kind,os)` (spp+android→`spp-android`; serial+web→`web-serial`; ble-hid→`hid-wedge`; ble-gatt/mfi→`null`). Cubre: RMV2.1, RMV2.2, RMV6.2, RMV6.3.
- [x] T-MV.2.2 — `selection-priority.ts`: `selectReaderBinding(env)` → `{adapterKind,transportKind,driver,available}` eligiendo el transporte de mayor prioridad soportado con adapter mapeado; `available = env.builtAdapters.includes(adapterKind)`; sin transporte alcanzable → `null` (RMV2.5). Puro, entradas inyectadas (RMV2.6). Cubre: RMV2.3, RMV2.4, RMV2.5, RMV2.6.
- [x] T-MV.2.3 — `adapter-selection.ts`: rama `if (env.mode === 'demo') return 'simulator';` **antes** de la lógica de plataforma; `auto`/`mock`/`manual` **sin cambios** de resultado. Cubre: RMV2.7, RMV4.3.
- [x] T-MV.2.4 — `selection-priority.test.ts` (node:test): RS420 en android→`{spp-android,spp}`; en web→`{web-serial,serial,available:true}`; **RS420 en ios→`null`** (no declara ble-hid/gatt/mfi → no alcanzable → manual, RMV2.5); **driver HID genérico en ios→`{hid-wedge,ble-hid,available:false}`**; driver solo-HID en android→`{hid-wedge,ble-hid,available:false}`; ambigüedad (device SPP+HID en android)→`spp` gana determinístico (RMV2.8); driver sin transporte alcanzable→`null` (RMV2.5); `available` con `builtAdapters` inyectado (RMV2.4); **regresión** `selectTransportAdapter` (`auto`/`mock`/`manual` idénticos al as-built) (RMV2.7). Cubre: RMV2.1–RMV2.8.

## Fase MV.3 — Simulador + gate demo (buildable hoy, dev/demo-gated)

- [x] T-MV.3.1 — `demo-gate.ts`: `isDemoMode()` puro = `globalThis.__RAFAQ_BLE_DEMO__ === true && isDemoBuildAllowed()`, donde `isDemoBuildAllowed() = (__DEV__ === true) || isExplicitDemoBuild() || isE2eDemoAllowed()` (`isExplicitDemoBuild()` lee `Constants.expoConfig?.extra?.demoBuild === true` con require perezoso; `isE2eDemoAllowed()` = `globalThis.__RAFAQ_BLE_E2E__ === true`, contexto no-prod de E2E/captura), todo envuelto en try/catch. `__DEV__` leído con `typeof` (safe en node). No seteable desde UI/input. Cubre: RMV4.4. **[Reconciliación as-built 2026-07-20: la redacción original decía `&& !isProduction()` (`NODE_ENV==='production' || !__DEV__`); se reconcilió a `&& isDemoBuildAllowed()` para alinear con RMV4.4 (ampliado por el leader pre-Puerta 1) + design §5 Guard 2 — "dev O build de demo explícito", que habilita una demo standalone sin romper la garantía prod-safe. El requirement y el design ya traían la fórmula nueva; solo esta task quedaba con la vieja.]**
- [x] T-MV.3.2 — `adapter-simulator.ts`: `SimulatorAdapter` (`kind:'simulator'`, `StickAdapter`): `connect`→`'connected'`; `emit(eid?)` empuja un EID sintético **válido** (`isValidTag`) por `onTagRead`; respeta `enable/disable`; opcional auto-play (`startAutoPlay(intervalMs)`/`stop`). Cubre: RMV4.1, RMV4.2.
- [x] T-MV.3.3 — `BleStickListenerProvider.tsx` `instantiateTransport`: `case 'simulator': return isDemoMode() ? new SimulatorAdapter() : null;` (triple-guard 3). El simulador entra por `handleReading(value, isRawStream=false)` (mismo contrato: validate+dedup+confirm pre-commit+feedback). Cubre: RMV4.5, RMV4.8.
- [x] T-MV.3.4 — `demo-gate.test.ts` + `adapter-simulator.test.ts` (node:test): sin marca→`isDemoMode()` false; en prod (`__DEV__` false / ausente)→false; con marca+dev→true (RMV4.4); `emit()` de EID válido dispara el pipeline (candidato→commit→tag_read); `disable`→no emite; EID sintético siempre pasa `isValidTag` (RMV4.2); `selectTransportAdapter(mode:'auto')` **nunca** `'simulator'` (RMV4.3). Cubre: RMV4.1, RMV4.2, RMV4.3, RMV4.4.

## Fase MV.4 — Pantalla de conexión + indicador (buildable hoy; conexión real SPP/HID gated)

- [x] T-MV.4.1 — `connection-view.ts` (PURO): `ConnectionStatus → {label,hint,cta}` (apagado/permiso/buscando/conectando/conectado/desconectado, RMV3.4); `ReaderBinding → estado de fila` (available true/false, RMV3.7; driver=null → "no reconocido", RMV3.8); marca "DEMO" para lecturas del simulador (RMV4.6). Cubre: RMV3.4, RMV3.7, RMV3.8, RMV4.6. **[as-built: `app/src/features/ble-stick/connection-view.ts` — `connectionStatusView`, `deviceRowView` (incluye el estado `recognized-unreachable` para RMV2.5: RS420 en iOS, driver reconocido sin transporte alcanzable), `readingBadge`.]**
- [x] T-MV.4.2 — `features/ble-stick/screens/StickConnectionScreen.tsx`: consume el provider global (`useBleProviderApi` + `useBleConnectionStatus`), NO monta provider propio (RMV3.1); flujo descubrir→listar→elegir→conectar específico por adaptador del binding (SPP listar/elegir/olvidar; web-serial requestPort/getPorts; HID instrucción+scan) (RMV3.2); elegir device → `writeRememberedDevice` (RMV3.3); estados con CTA desde `connection-view.ts` (RMV3.4); no bloqueante (RMV3.6). Monta en "Más" (ADR-018). Cubre: RMV3.1, RMV3.2, RMV3.3, RMV3.4, RMV3.6. **[as-built: la sub-UI por adaptador (`TransportInstructions`) se deriva del `ReaderBinding`; la LISTA de web-serial es el diálogo NATIVO de puertos del navegador (la interfaz `StickAdapter` del core NO expone `getPorts` — ver reconciliación en design §7). `writeRememberedDevice(driver.vendorId)` como marcador de reconexión.]**
- [x] T-MV.4.3 — `features/ble-stick/components/StickDeviceRow.tsx` + `StickStatusIndicator.tsx`: fila de device (reconocido/no-reconocido/available RMV3.7/3.8); indicador global en el chrome alimentado por `useBleConnectionStatus()` (RMV3.5). Cubre: RMV3.5, RMV3.7, RMV3.8. **[as-built: el indicador se monta en `app/app/_layout.tsx` (`BleHost`, hermano del stack), `pointerEvents="box-none"` no bloqueante + auto-oculto en 'off'. NO tocó ningún archivo de spec 09.]**
- [x] T-MV.4.4 — `features/ble-stick/components/DemoControls.tsx`: "Simular lectura" + (opcional) auto-play, montado **solo** bajo `isDemoMode()` (re-chequeo, triple-guard 3); marca las lecturas del simulador como "DEMO" (RMV4.6). Cubre: RMV4.5, RMV4.6. **[as-built: re-chequeo `isDemoMode() && transport instanceof SimulatorAdapter` → null si no es demo.]**
- [x] T-MV.4.5 — **Frontera host-level (coordinar con el leader, colisión-safe):** `app/app/_layout.tsx` → prop `mode` del provider raíz `… : (isDemoMode() ? 'demo' : 'auto')` (1 línea, análoga a `isBleE2E`); entrada de ruta a `StickConnectionScreen` en "Más" (ADR-018); montaje de `StickStatusIndicator` en el chrome. **Si montar el indicador exigiera tocar un contrato de spec 09 → parar y reportar.** NO tocar archivos de teléfono/auth. Cubre: RMV3.1 (nav), RMV3.5 (montaje), RMV4.4 (wiring del gate). **[as-built: precedencia demo en el `mode` + `<Stack.Screen name="baston" />` + route file `app/app/baston.tsx` + `<StickStatusIndicator/>` en `BleHost`. La FILA de "Más" (`(tabs)/mas.tsx`) que navega a `/baston` queda PENDIENTE de coordinación: `mas.tsx` es de la otra terminal (feature 19/20) — colisión-safe, no se tocó; la ruta ya es alcanzable por deep-link. Snippet de la fila en `progress/impl_04-multivendor-ui.md`.]** **[CERRADO 2026-08-05 por T-MV.4.7: la fila existe.]**
- [x] T-MV.4.6 — `connection-view.test.ts` (node:test) + component/E2E con simulador/mock: mapeo estado→vista; binding available=false → CTA manual sin conectar (RMV3.7); device sin driver → "no reconocido" (RMV3.8); lectura demo marcada "DEMO" (RMV4.6). Cubre: RMV3.4, RMV3.7, RMV3.8, RMV4.6. **[as-built: `connection-view.test.ts` (10 casos node:test) verde + registrado en `scripts/run-tests.mjs`. El component/E2E con simulador va en el run APARTE de Gate 2.5 (E2E + capturas), fuera de este run buildable-hoy — ver T-MV.7.2.]**
- [x] T-MV.4.7 — **ACCESO IN-APP a la pantalla de conexión** *(2026-08-05; cierra la deuda que T-MV.4.5 dejó abierta y el ítem 3 de la entrada del 2026-07-30 de `docs/backlog.md`)*. Disparador: Raf en device — abrió la app, el chip global quedó ciclando "Conectando…" (reconexión R6.4 con el bastón apagado) y **no tuvo cómo llegar a `/baston`** (ruta deep-link-only). (a) `(tabs)/mas.tsx`: sección **"Bastón"** + `ActionRow` a `/baston`, **entre la card de Perfil y el bloque "Campo activo"** (el bastón es del TELÉFONO, no del campo — y ese bloque está gateado por `activeField != null`, así que la fila desaparecería sin campo resuelto), **sin gate de rol** (es trabajo de manga), ícono `StickIcon` del registro central. (b) **Trailing con el estado EN VIVO**, derivado por `connectionRowStatus(status, env)` — función PURA nueva de `connection-view.ts`, NO inline en `mas.tsx` (es el defecto que ese archivo cerró el 2026-07-29). Copy corto exhaustivo sobre `ConnectionStatus` + `hasTransport:false` + `autoConnectExhausted`. Sin transporte la fila **no se oculta** (es el único camino in-app a la pantalla, que es la que explica la salida manual): dice "No disponible". (c) `toneColorToken` se exporta de `connection-view.ts` para que el call site nuevo no invente una 4ª traducción tono→color. (d) `StickConnectionScreen`: `backOr(router, '/(tabs)/mas')` en el chevron (era el ÚLTIMO `router.back()` pelado de la app; sin entrada in-app se llegaba con el stack vacío, o sea justo donde `back()` es no-op). **Tests**: `connection-view.test.ts` **29 → 37** casos (incluye el invariante "el tono de la fila nunca contradice al de la card", el techo de largo del trailing y la prohibición de repetir "Bastón"); falsificado con un mutante → 5 rojos. **E2E** `baston-multivendor.spec.ts` (e) Más → fila → `/baston` + vuelta por el chevron, y (f) contrafáctico sin transporte (`__RAFAQ_BLE_E2E_MANUAL__`) → **6/6 verde**. **Capturas**: `07-indicador-global-chrome` **restaurada** + `08-mas-fila-baston` + `09-fila-mas-baston-conectado`. Cubre: RMV3.1 (el punto de entrada, que hasta hoy el EARS afirmaba y no existía), RMV3.4 (copy corto), RMV3.5 (evidencia visual).
- [x] T-MV.4.8 — **EL FAB LE ROBA LOS TAPS A LA BANDA DE ARRIBA DEL NAV** *(bugfix 🔴, 2026-08-06)*. Disparador: Raf en device — *"el pill de «Conectando…» que se ve arriba del rayo de modo maniobra, si lo clickeo me lleva al modo maniobra"*. **(a) LA CAUSA ERA EL FAB, NO EL PILL**: `app/(tabs)/_layout.tsx` le daba al Pressable del FAB `hitSlop={{ top: $fabRaise }}` → el target se extendía 26 dp sobre el círculo pintado y se comía el **48 % inferior** del pill. Medido dos veces: web (cajas del DOM: solape 16 dp) y device A07 (techo pintado y=1324 vs. techo táctil y=1276 por barrido de `adb shell input tap`). Se sacó el `top`; el `bottom` (hace tocable el label "Maniobra", crece hacia dentro del nav) se conserva. Ojo con el razonamiento fácil: que el pill sea `pointerEvents="none"` **no evitaba** el bug — el toque lo atravesaba y caía en el target inflado del FAB, que era lo que estaba debajo. El comentario que justificaba el `top` **afirmaba lo contrario y era falso**: verificaba el paquete de web (donde `hitSlop` es no-op) y generalizaba a nativo sin medir; y su premisa —que el ancestro recorta— también cae (el target dispara 86 px por encima del techo de la barra → el tabBar no recorta → sacar el `top` no crea zona muerta). `docs/backlog.md` 2026-07-18, cerrada como "no se reproduce", corregida: su hipótesis (a) era la correcta, y el error de método fue prever **un solo** modo de falla (target de menos) cuando el que se dio fue el opuesto. **(b) GEOMETRÍA**: gap del pill `$2`→`$4` ⇒ **20 dp** de separación efectiva (antes 9-10) — aire visual **y** margen contra un slop futuro del FAB. **(c) a11y**: `connectionRowA11yLabel` + `STICK_ROW_ACTION` puros en `connection-view.ts` (`mas.tsx` dejó de armar el label inline). **(d) "Más": el título de sección "Bastón" → "Dispositivos"** (con una sola fila homónima el título era una tautología; escala a balanza/impresora y espeja la sección de `/baston`) + `testID="stick-devices-section"` para desambiguar la E2E. **GUARDS (el entregable central, sobre el INVARIANTE y no sobre la instancia)**: `src/utils/nav-target-bands.ts` + `.test.ts` (**27 casos**: bandas desde los tokens, con el contrafáctico de `1f1c002`, más el **RESOLVEDOR** que traduce un fuente de producción a números en dp y es fail-closed), `src/utils/tap-target-collision-guard.test.ts` (**22 casos**: (A) el valor de todo `hitSlop` legible de UN solo lugar · (A-fix) el target del FAB no excede su círculo salvo hacia abajo **resolviendo el VALOR real** (no contando claves) · (A-fix bis) el resolvedor es fail-closed · (B1) anclajes por MENCIÓN de la reserva, con vocabulario derivado por archivo · (B1-bis) si el valor sale de la reserva, la prop tiene que ser una que el guard sepa medir — `bottom` o `padding`, todo lo demás en rojo · (B-banda) la coordenada real de cada superficie registrada despeja el FAB en las 4 reservas · (B2) tokens de geometría del nav · (C)/(D) espejos contra tokens reales y el E2E sin espejo · (E) el pill no puede volver a ser tocable · balanceo de llaves con la línea culpable), y `e2e/fab-target-geometry.spec.ts` (2 casos: hit-test MUESTREADO con `elementFromPoint` sobre la franja del slop —no intersección de rects, que da falso positivo por ignorar el z-order— con auto-falsificación in-place, más la aserción de que el pill no intercepta el toque). Los dos primeros **registrados en `scripts/run-tests.mjs`**; los tres **falsificados con mutantes** (**36 en total** a lo largo de tres rondas de review: los 2 con los que el reviewer burló la v1, los 2 que seguían vivos en la v2 —el `const` en UNA línea y el alias destructurado de la reserva—, los 14 de gramática del `hitSlop`, y los del anclaje: `Math.max(insets.bottom, 86)` sin un solo `+`, `insetBlockEnd`, `style={fn(reserva)}`, `transform` como template string, un hand-off sin registrar y la reintroducción de la trampa de la llave en un regex). **Ninguna E2E de comportamiento en web podía cazar este bug** (`hitSlop` es no-op en RNW): esa es la razón de que los guards sean geométricos. **Capture Gate 2.5**: `e2e/captures/fab-hitslop-pill.capture.ts`. **⏸ PENDIENTE**: el fix del `hitSlop` **no se puede verificar en device sin un build de EAS** (gate de builds de CLAUDE.md). Cubre: RMV3.5, RMV3.1 (el rótulo de la sección).
  **[REVERSIÓN dentro de la misma unidad — el pill NO quedó tocable.** El delta de UX que acompañaba al bugfix (tocar el pill → `/baston`) se implementó y **se revirtió el mismo día** cuando la medición mostró que el pill se queda con los toques de CTAs de manga: en el A07 el pill cae **entero adentro** de `'Arrancar jornada'` (`[34,1242]-[686,1362]` vs. `[220,1244]-[500,1306]`), y en web era el elemento topmost sobre `"Ir a Animales"` (Inicio), `"Eliminar campo"` (Más) y tres maniobras de `/maniobra/jornada` etapa 2. La banda de abajo está disputada por diseño → no hay posición en x donde un pill flotante y tocable sea seguro. **RMV3.6 queda como estaba** (ver su nota, que conserva los números). Se fueron con la reversión el `onPress`, el `pressStyle`, el rol de botón y el `minHeight="$chipMin"`; quedó el aire de `$4`, que se sostiene solo.]**

## Fase MV.5 — `adapter-spp-android` ESCRITO (código + tests puros hoy; conexión real GATED)

> **Depende de** MV.1 (driver RS420). El **código se escribe hoy**; la **conexión SPP real** y el **dev build** quedan GATED por hardware. El config plugin se **veta** sin instalar la dependencia.

- [x] T-MV.5.1 — **VETO (prerrequisito, gated):** probar la compatibilidad del config plugin de `react-native-bluetooth-classic` con Expo SDK 56 + permisos Android 12+ (`BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`, `neverForLocation`), **sin instalar la dep ni prebuildear el dev build**. Si incompatible, **PARAR y reportar al leader**. **Aceptación**: dev build documentado como viable o el bloqueo reportado. Cubre: RMV5.8. **[Veto liviano 2026-07-20 (sin instalar): stack = Expo ~56.0.15 + RN 0.85.3 NEW ARCHITECTURE/bridgeless (no apagable — reanimated@4 la exige). RIESGO ALTO NO RESUELTO: `react-native-bluetooth-classic` (kenjdavidson) usó históricamente el bridge viejo (RCTBridgeModule/RCTEventEmitter) y tiene issues abiertas de New Architecture; es la MISMA clase de fallo que bloqueó `react-native-quick-sqlite` (JSI bindings no instalados bajo bridgeless RN 0.85 → migración a op-sqlite). El config plugin (permisos Android 12+) es lo de MENOS; el bloqueante probable es la compat new-arch del módulo nativo. NO se puede confirmar viable desde docs → el gate REAL es el dev build Android. NO bloquea este run (el código usa import perezoso → importa/testea sin la lib). El leader/dev-build debe tratar esto como GATE explícito antes de comprometer la conexión SPP real.]**
  **[VETO CERRADO — 2026-07-29 (Gate 0 de la unidad «bastón Android SPP»): COMPATIBLE. El riesgo del veto liviano NO se materializó y la analogía con `react-native-quick-sqlite` era incorrecta.** Evidencia contra el código INSTALADO + un build REAL, no contra docs: (1) la lib es un NativeModule **legacy** (`ReactPackage.createNativeModules()`, sin `codegenConfig`), y RN 0.85.3 lo soporta bajo bridgeless por el **interop de módulos legacy** — `ReactPackageTurboModuleManagerDelegate.shouldSupportLegacyPackages()` ⇐ `enableBridgelessArchitecture() && useTurboModuleInterop()`, y `ReactNativeNewArchitectureFeatureFlagsDefaults.kt:35` define `useTurboModuleInterop() = newArchitectureEnabled`, o sea el interop está **ON justamente cuando `newArchEnabled=true`**. (2) quick-sqlite fallaba por **bindings JSI** instalados a mano (para eso NO hay interop); esta lib **no tiene una línea de JSI ni de C++** (el tarball no trae `cpp/` ni `CMakeLists`) → clase de fallo distinta. (3) Los 17 imports `com.facebook.*` de su código nativo existen en RN 0.85.3. (4) Su `implementation 'com.facebook.react:react-native:0.71.0-rc.0'` (coordenada muerta) lo resuelve el `DependencyUtils.kt` del gradle-plugin de RN, que sustituye a `react-android` y fuerza la versión sobre `rootProject.allprojects`. (5) **Compila de verdad**: `./gradlew :react-native-bluetooth-classic:assembleDebug` → BUILD SUCCESSFUL (Gradle 9.3.1 + AGP 8.12.0 + compileSdk 36 + JDK 17). (6) Autolinkea: `autolinking.json` trae `new RNBluetoothClassicPackage()`. (7) **Config plugin: la lib NO trae ninguno** (86 archivos publicados, sin `app.plugin.js`) → se escribió uno propio, `app/plugins/with-bluetooth-classic.js`, que además **topea el `ACCESS_FINE_LOCATION` que la lib declara SIN tope**. Detalle y tabla de evidencia en `progress/impl_baston-android-spp.md`.]**
- [x] T-MV.5.2 — `adapter-spp-android.ts` — **reescribir el placeholder** a `StickAdapter` real (`kind:'spp-android'`): abre RFCOMM SPP (UUID del driver, `SPP_UUID`) vía `react-native-bluetooth-classic` con **import perezoso** (require dentro de la I/O, patrón `feedback.ts`); parametrizado por `RS420_DRIVER` (sppUuid/pin/frameParser, RMV5.2); framing por línea (`LineFramer`, reuso, RMV5.3) → línea cruda al contrato; baud-independiente (RMV5.7). Cubre: RMV5.1, RMV5.2, RMV5.3, RMV5.7.
  **[RECONCILIACIÓN 2026-07-30 (🟡-4 del review) — el enunciado de esta task quedó viejo en dos puntos y hay que leerlo con las notas de RMV5.2/RMV5.3:** (1) *"framing por línea (`LineFramer`, reuso)"* es exactamente lo que el as-built **NO** hace, y no por gusto: era el bug que devolvía `[]` para siempre (cero lecturas aun con el bastón enchufado). El framing lo hace el nativo con el delimitador **del driver** y acá solo se separa defensivamente (`splitSppPayload`). (2) *"parametrizado por `RS420_DRIVER` (sppUuid/pin/frameParser)"*: el `sppUuid` se **contrasta** (la lib lo hardcodea), el `pin` va a la UI y no al transporte, el `frameParser` **no se usa en producción** (`contract.ts` llama `parseRs420Line` hardcodeado) — y el que sí se agregó como parámetro real del driver es el **`delimiter`**. RMV5.3 ya tenía su nota desde el 2026-07-29; esta task no, y por eso se leía como si el `LineFramer` estuviera en el camino.]**
- [x] T-MV.5.3 — `adapter-spp-android.ts` — pairing SPP (slave, PIN del driver `1234`) + persistencia del device (`remembered-device.ts`, RMV5.4); reconexión con `backoffDelayMs` (reuso), foreground-only (RMV5.5). **Aceptación (código)**: la máquina de estados compila y las partes puras se testean; la conexión real queda gated. Cubre: RMV5.4, RMV5.5.
- [x] T-MV.5.4 — `adapter-spp-android.test.ts` (node:test, **partes puras**): resolución driver→sppUuid/pin/frameParser (RMV5.2); framing por línea → EID correcto por el parser (RMV5.3); backoff (RMV5.5); **`import('./adapter-spp-android')` NO tira** en node/CI sin la lib nativa (RMV5.6). Cubre: RMV5.2, RMV5.3, RMV5.5, RMV5.6.
- [x] T-MV.5.5 — Montar `adapter-spp-android` en el provider para Android device (`instantiateTransport('spp-android')` → `new SppAndroidAdapter()`; `selectTransportAdapter({android,auto})` → `'spp-android'`). Sin tocar el contrato ni los otros adaptadores. **[as-built 2026-07-29 (unidad «bastón Android SPP»)**: montaje HECHO + `BUILT_ADAPTERS` += `'spp-android'` en `StickConnectionScreen`. Una desviación deliberada del enunciado: `instantiateTransport('spp-android')` devuelve el adapter **solo si `isSppNativeAvailable()`** (Platform android + `NativeModules.RNBluetoothClassic != null`), si no `null`. Motivo: `require('react-native-bluetooth-classic')` resuelve desde `node_modules` aunque el binario NO esté en el APK (dev build anterior a la dep) → montar igual sería un transporte fantasma, o sea el mismo CTA-que-no-cumple que cerró el bugfix del chip del 2026-07-29. Con el guard, un build viejo queda manual-first y el chip/CTA se ocultan solos.**]** Cubre: RMV2.7 (montaje).
- [ ] T-MV.5.6 — *(GATED por hardware — RS420 físico)* **Prueba de las idiosincrasias del RS420 real**: su emparejamiento (diálogo, timeouts, rarezas del bonding del lector), su semántica de desconexión (cuándo corta solo, si se duerme, si mantiene el RFCOMM abierto sin datos), sus tiempos (latencia bastoneada→trama, jitter, throughput con el rodeo entrando), su buffer interno / "sessions", y si la trama cambió con la actualización de firmware pendiente. **Nadie del equipo tiene un RS420 hoy.** **Aceptación**: documentado en `progress/impl_baston-android-spp.md` + `field-findings.md`. Cubre: RMV5.9 (parte irreductible).
  **[RECONCILIACIÓN 2026-07-29 — esta task SE ACOTÓ: ya no cubre stream/dedup/reconexión.** El enunciado original era *"stream de lecturas, dedup por-TAG sobre lecturas reales, asignación masiva, corte y reconexión con el bastón físico"* y decía que hacía falta un RS420 físico para todo eso. **Ya no es cierto**: el ESP32 que estaba de bridge de la balanza se reprogramó como **emulador de bastones** (`firmware/baston-emulator/`, `MODO_SPP`) y emite la trama capturada en campo con fidelidad de byte, más los escenarios que rompen (repetidos, ráfagas, corte del link, radio abajo, flap, malformadas, mudez). Esa parte pasó a **T-MV.5.7**, que NO está gated por hardware. Acá queda solo lo que un emulador no puede imitar: las mañas del lector real. Lista completa de lo que el emulador NO valida en `firmware/baston-emulator/README.md` §"Qué NO valida".]**
- [x] T-MV.5.7 — **Prueba con el emulador ESP32** (`firmware/baston-emulator/`, `MODO_SPP`) — *NO gated por hardware: el ESP32 ya está en la mesa*. Con el dev build Android + el emulador emparejado (PIN `1234`): (a) `read`/`seq on` → stream de lecturas distintas ingeridas; (b) `same 5` → **una sola** ingesta (dedup por ventana); (c) `burst 8` → nada perdido ni duplicado; (d) `drop` → `disconnected` + backoff + reconexión; (e) `off 8000` → la cadena de reintentos **sobrevive al primer fallo** (regresión del bug 3 de `dad711f`); (f) `flap 4 3000` → backoff creciente sin trabarse ni duplicar suscripciones; (g) `mute 30` → sigue `connected` con cero ingestas; (h) las 10 `bad <caso>` → descartadas en silencio, sin crash ni tag inválido en la UI; (i) `split`/`double` → una y dos lecturas respectivamente; (j) `term lf` y `stx off` → mismo resultado. ~~Pendiente de flasheo~~ → **FLASHEADO Y CORRIDO el 2026-07-30** (Raf autorizó: *"mandale automático, ya está respaldado"*; el firmware de la balanza sigue respaldado en `firmware/backup/` con su SHA-256). **Aceptación**: resultado documentado en `progress/impl_baston-emulator-esp32.md` + `field-findings.md`. Cubre: RMV5.9 (la parte que el emulador destraba), RMV1.5/RMV2.8 (matching por nombre real anunciado: el emulador se anuncia `RS420-EMU`, que matchea el `deviceMatch` del driver).
  **[CERRADA — 2026-07-30 (🟡-E del review): ya se corrió y estaba marcada `[ ]`.** El ESP32 se flasheó, se emparejó con el A07 y los 10 sub-escenarios (a…j) se corrieron. La aceptación apuntaba al informe equivocado: el resultado quedó en **`progress/bench_baston-spp-emulador.md`** (16 escenarios a mano + un baseline automatizado de **18/21** con `firmware/baston-emulator/bench/run-bench.py`), no en `impl_baston-emulator-esp32.md`. **La app ingirió su primera trama real** (`982000364696050`, byte por byte igual a la captura de campo). Dos sub-escenarios dieron resultados distintos de lo esperado y quedaron documentados como hallazgos, no como verdes: (f) el `flap` NO daba backoff creciente (§4.3 → lo arregló el dwell de esta unidad) y (g) `mute` sale igual que un terminador equivocado (§4.4 → de ahí el watchdog de mudez). Una task abierta que ya está cerrada es tan mala señal como una cerrada que no lo está.]**

## Fase MV.5-bis — BLOQUEANTES del camino SPP (review adversarial de `dad711f` + banco en device)

> Unidad del **2026-07-30**. No es scope nuevo: son los defectos que la review adversarial (`progress/review_baston-android-spp.md`, 2 🔴 · 5 🟠 · 5 🟡 · 5 ⚪) y el banco contra el ESP32 (`progress/bench_baston-spp-emulador.md`) encontraron en el as-built de T-MV.5.2/5.3. **Tres de ellos están reproducidos en el A07 real**, no deducidos. Informe: `progress/impl_baston-spp-bloqueantes.md`.
>
> **R6.4 / 🟠-3** (auto-conectar al ABRIR la app) se hizo en una **segunda pasada** de la misma unidad → **T-MV.5.19**. 🟡-2, 🟡-3 (parcialmente hecho acá: el dwell), 🟡-5 y las ⚪ → `docs/backlog.md`.

- [x] T-MV.5.8 — **Liveness: segunda fuente de verdad del link** (🔴 BENCH-1, banco §4.1, 3/3 repro en device). `verifyLiveness()` en `adapter-spp-android.ts` consulta `isDeviceConnected(address)` del nativo (i) al volver a **foreground** y (ii) por un **poll periódico** (`livenessPoll`, 15 s) que no depende de ningún evento ni de `AppState`; si el socket está muerto → teardown + `disconnected` + backoff. Fail-closed si la sonda no está o rechaza; se loguea `liveness_probe_unavailable` UNA vez por conexión en vez de fingir cobertura. Cubre: RMV5.5 (nota 1).
- [x] T-MV.5.9 — **`bridge-timeout.ts`: ningún await del puente sin presupuesto** (🔴-1, banco §4.2 — 2 min 40 s sin un evento en device). `withTimeout`/`withTimeoutOr` + `BridgeTimings` (call 10 s / prompt 30 s / connect 20 s); latch de conexión liberado en el `finally` **y** en `disconnect()`; generación de intento (`connectGeneration`) para que un intento viejo que despierta no pise al nuevo ni deje un socket abierto; `requestBluetoothEnabled` **coalescido** a nivel módulo (dos pedidos solapados pisaban el `mEnabledPromise` del nativo y dejaban una promesa huérfana para siempre). Cubre: RMV5.5 (nota 2).
- [x] T-MV.5.10 — **Filtrar por dirección el evento de desconexión del SO** (🔴-2). `onDeviceDisconnected` se suscribe a `DEVICE_DISCONNECTED` **pelado** y lo alimenta `ActionACLReceiver`, un BroadcastReceiver de `ACTION_ACL_DISCONNECTED` de **todos** los devices Classic del teléfono: unos auriculares que se apagan cerraban el socket del bastón. Comparación case-insensitive contra la MAC de la sesión (y contra la que reporta el device); un evento sin dirección legible se acepta y queda documentado por qué. **Device-test pendiente**: necesita un 2º device Classic emparejado (unos auriculares; 1 minuto de Raf). Cubre: RMV5.5.
- [x] T-MV.5.11 — **Gate de foreground al DISPARAR el timer** (🟠-1, banco §4.6 — confirmado en device: un connect nativo ejecutado con la app minimizada) + **el diálogo del sistema nunca sale de un timer** (§9.3 del review: un reintento automático con el BT apagado no pide activarlo y sigue reintentando en silencio) + **dwell del backoff** (🟡-3, banco §4.3: `flap` daba `attempt:0` las 4 veces; ahora el reset exige que el link dure `LINK_DWELL_MS` = 30 s). Cubre: RMV5.5 (notas 3/4/5), R6.9 del core.
- [x] T-MV.5.12 — **Un `connect()` a otro bastón no se descarta mudo** (🟠-2): se **encola** el último target pedido y se atiende al terminar el intento en curso, con log `connect_superseded`. Antes el operario tocaba el bastón A, se daba cuenta de que era el otro, tocaba el B, no pasaba **nada** (ni estado ni log) y terminaba conectado a A. Cubre: RMV5.5.
- [x] T-MV.5.13 — **El terminador de trama sale del DRIVER, con chequeo honesto** (🟠-5 + BENCH-2, banco §4.4). `delimiter` en el `TransportCapability` de kind `spp`; el RS420 lo declara explícito (`\n`); un delimitador que este adapter no puede framear (vacío) **corta con log** en vez de conectar y quedarse mudo. Más el **watchdog de mudez**: `connected_silent` en el log cuando hace ≥45 s que no llega un byte — no desconecta (el silencio es normal si el operario no bastonea) pero deja rastro, que es lo que permite distinguir en logcat las tres causas que hoy dan el mismo síntoma. Cubre: RMV5.2, RMV5.3.
- [x] T-MV.5.14 — **Modo de ingesta declarado por adapter, con guard sobre la ausencia** (🟡-1): `ADAPTER_INGEST_MODE` en `adapter-selection.ts` (`satisfies Record<AdapterKind, IngestMode>`) + `ADAPTER_KINDS` anclado al union con un chequeo de exhaustividad **en el módulo** (no en el test: `app/tsconfig.json` excluye `**/*.test.ts`, así que una aserción de tipos en un test no la chequea nadie). El provider delega en `ingestModeFor`. Cubre: RMV5.3, RMV1.1.
- [x] T-MV.5.15 — **`/baston` deja de consumir cada lectura dos veces** (🟠 BENCH-3, banco §4.5): la pantalla toma la propiedad exclusiva del bastón con `acquireScopedScanner()` dentro de un `useFocusEffect`. Justificación de por qué el scanner acotado y no `BLE_OWNED_ROUTES`: nota de RMV3.1. Cubre: RMV3.1, RMV4.8.
- [x] T-MV.5.16 — **Guard de re-entrada + awaits acotados en la lista de emparejados** (🟠-4): coalesce en `listPairedSppDevices` + guard en la pantalla + caída a `error` (que sí tiene CTA). Cubre: RMV3.2.
- [x] T-MV.5.17 — **Tests + guards estáticos de todo lo anterior**: `adapter-spp-android.test.ts` (**39 → 62** en esta pasada), `bridge-timeout.test.ts` (**12**), `adapter-ingest-mode.test.ts` (**5**, incluye el guard de que el provider delegue), `spp-bridge-timeout-guard.test.ts` (**4**, falla si aparece un await del puente sin presupuesto), + los nuevos casos de `spp-protocol.test.ts` y `wiring.test.ts`. Los tres archivos nuevos **registrados** en la lista explícita de `scripts/run-tests.mjs`. Los dos guards estáticos se verificaron **mutando el código** (sin la mutación no probarían nada).
  **[MÉTODO de los números, y su corrección — 🟡-E del review del 2026-07-30.** Todos los conteos de esta fase se miden con la línea `ℹ pass` de `node --test` **por archivo**, no con `grep`. Esta task decía *"39 → 71"* y *"bridge-timeout (13)"*: el 71 mezclaba las pasadas (era el total DESPUÉS de R6.4, no lo que agregó esta) y el 13 estaba mal medido (son 12). Totales al cierre de las tres pasadas + el fix-loop: `adapter-spp-android` **102** · `connection-view` **30** · `spp-protocol` **17** · `wiring` **17** · `permissions-android` **13** · `bridge-timeout` **12** · `connect-trigger` **6** · `adapter-ingest-mode` **5** · `spp-bridge-timeout-guard` **4**.]** Cubre: RMV5 (todo el as-built de esta unidad).
- [x] T-MV.5.19 — **R6.4: reconexión automática al ABRIR la app** *(segunda pasada, 2026-07-30 — decisión de Raf: "que se reconecte sola al abrir, sí")*. `SppAndroidAdapter.autoConnect()` + `autoConnect?()` opcional en `StickAdapter` + la llamada del provider al montar el transporte + `checkAndroidBluetoothPermissions()` (consultar sin pedir, sobre `PermissionsAndroid.check`) + el campo **obligatorio** `checkPermissions` en `SppEnv`. Regla: **el arranque no pide nada** (cuatro gates, ningún estado emitido al saltear, `autoconnect_skipped` con seis motivos en el log). Ampliación gratis del mismo principio: los **reintentos** también consultan el permiso en vez de pedirlo. **13 casos** nuevos en `adapter-spp-android.test.ts` (R6.4 no tenía ninguno: el camino no existía) + **6** en `permissions-android.test.ts` + **2 guards** en `wiring.test.ts` (que el provider LLAME a `autoConnect`; que los otros cuatro adapters NO lo implementen, con el motivo escrito). Cubre: **R6.4** (core), R6.3, R6.9, RMV5.4, RMV5.5.
- [x] T-MV.5.20 — **TOPE de la cadena de reintentos que NADIE pidió** *(tercera pasada, 2026-07-30 — decisión del leader: no es preferencia de UX, es un defecto que introduce R6.4)*. `connect-trigger.ts` (nuevo, PURO): `ConnectTrigger` (`operator` / `autoconnect` / `retry`) + `CONNECT_TRIGGER_POLICY` `satisfies Record<…>` con las dos políticas por trigger (¿puede mostrar diálogos del SO? ¿qué le hace a la cadena?) + `CONNECT_TRIGGERS` anclado al union en el módulo + `UNPROMPTED_RETRY_BUDGET_MS` = **120 s** (justificado contra la escalera de backoff: cubre "lo prendí un minuto después" con el doble de margen y topea el martilleo por apertura en 2 min). Reemplaza el booleano `auto` de `doConnect`. Al agotarse: deja de reintentar, emite `'off'` (que **sí** tiene CTA), **no olvida** el device recordado, y el backoff vuelve al piso; el tap del CTA entra como `operator` → cadena sin tope. El chequeo del presupuesto va **antes** del gate de foreground en los DOS lugares donde se evalúa (si no, un timer que dispara en background se parquea esperando el retorno **sin pasar por el tope**: el tope sería evitable guardando el teléfono). Copy honesto en la pantalla vía `autoConnectExhausted` (`connectionStatusView`), sin tomarle el chrome a nadie. **8 casos** nuevos en `adapter-spp-android.test.ts`, **6** en `connect-trigger.test.ts` (incluye el GUARD de call sites: todo `runConnect(` pasa un trigger literal conocido → un camino nuevo que arranque reintentos sin declarar su origen nace en rojo), **4** en `connection-view.test.ts` y **1** guard de coherencia en `wiring.test.ts`. Cubre: R6.4, R6.9, RMV5.5, RMV3.4.
- [x] T-MV.5.21 — **FIX-LOOP del review + Gate 2** *(2026-07-30; `progress/review_baston-spp-bloqueantes.md` CHANGES_REQUESTED 🔴-A + `progress/security_code_04-spp-bloqueantes.md` FAIL HIGH-1 — los dos encontraron el MISMO bloqueante, cada uno con su propio probe)*. Cinco arreglos: **(1) 🔴** el presupuesto del tope **muere cuando el link se establece** (`retryBudgetUntil = null` en el punto de éxito). Sin eso no acotaba la cadena sino **los primeros 120 s de vida de la app**: el primer corte posterior mataba la reconexión automática de toda la sesión, con 0 reintentos, el pill del chrome oculto y un diagnóstico inventado en la pantalla — incumpliendo la segunda cláusula de R6.4 (*"vuelve a estar en rango"*). El invariante, ahora escrito: el presupuesto pertenece a la CADENA, y una cadena que llegó a `'connected'` TERMINÓ. **(2) 🟠** un `connect()` del **operario** con el latch tomado re-aplica la política de su cadena (**destopa**) y **siempre** deja log (`connect_reasserted`) — el camino del chip del header, que llama `connect()` sin target, era un no-op mudo que se comía el destope. **(3) 🟠** `canCloseOrphanSocket(gen)`: el cleanup del connect vencido cerraba **por dirección** (`disconnectFromDevice(this.address)` en la lib), así que un intento vencido podía cerrarle el socket al que ya había reconectado; ahora distingue las dos razones por las que la generación avanzó (`closed` → cerrar; otro intento vigente → no tocar). **(4) 🟠** el **device recordado**: no se persiste más antes de conectar (lo hace el adapter al llegar a `'connected'`), **R6.6 CABLEADA** (`forgetRememberedDevice` tenía cero call sites; ahora hay CTA "Olvidar el bastón guardado", condicionado a que haya algo guardado) y se limpia en `signOut` y en la baja de cuenta — el scope por usuario queda argumentado, no omitido: la vida de la clave pasa a ser la de la sesión, que es lo que compra el scope para el caso del teléfono compartido. **(5) ⚪** el guard de timeouts escaneaba por línea y un await partido en dos se le escapaba (⚪-H). **Tests**: 7 nuevos en `adapter-spp-android.test.ts` (5 del 🔴-A —que **sí distinguen** el bug del arreglo: el mutante M7 del reviewer pasaba 104/104 y ahora falla 3— y 2 de 🟠-B), 1 de MEDIUM-1, 2 guards en `wiring.test.ts` (que `forgetRememberedDevice` esté cableado en los TRES call sites; que la pantalla no persista antes de conectar) y 1 test propio corregido (el que daba por bueno cerrar el socket ajeno). **5 mutantes** corridos para probar que cada arreglo tiene un test que lo caza. Cubre: R6.4 (las dos cláusulas), R6.6, RMV5.5, RMV3.2.
- [x] T-MV.5.22 — **El invariante del techo, FUERA del adapter** *(pasada final, 2026-07-30; ⚪-L del review, subido a 🟠 por el leader: "son call sites nuevos que introdujo esta unidad y caen justo afuera de donde llega el guard que la unidad construyó")*. (a) Las tres funciones de `remembered-device.ts` acotan **en el borde** (`DEFAULT_BRIDGE_TIMINGS.storage` = 2 s, mucho menor que los 10 s de una llamada genérica: un logout que tarda 10 s es un logout roto), así que todo caller queda protegido y uno nuevo **nace protegido** — un `.catch()` en el caller cubría el rechazo y NO el colgado, que dejaba al operario sin poder cerrar sesión. (b) El guard dejó de mirar una carpeta y mira el **invariante**, en dos mitades que se cierran entre sí: los bordes declarados acotan de verdad (la tabla no puede mentir), y en el territorio de la unidad nadie awaitea una primitiva nativa (`SecureStore`/`PermissionsAndroid`/`AsyncStorage`/`NativeModules`) sin techo salvo las excepciones **nombradas con su motivo** — más la contraparte que exige que cada excepción exista y siga siendo excepción. (c) **LOW-5**: la limpieza del bastón recordado se agregó al branch `SIGNED_OUT` de `onAuthStateChange` (fin de sesión involuntario; `delete_account` revoca global, así que en los otros teléfonos la sesión muere por ahí) y **los dos comentarios se corrigieron**: afirmaban "la vida de la clave es la de la sesión" con solo el logout explícito puesto, que era falso. `spp-bridge-timeout-guard.test.ts` pasó de **4 a 8** casos; **2 mutantes** nuevos (sacar el techo del borde; un archivo nuevo con un await pelado) para probar que las dos mitades caen. Cubre: R6.3, R6.6, RMV5.5 (el mecanismo del techo).
- [ ] T-MV.5.18 — *(pendiente de device — lo corre el leader)* **Re-correr el banco automatizado** (`firmware/baston-emulator/bench/run-bench.py`) contra el build con estos fixes y comparar contra el baseline **18/21** de `dad711f`. Oráculos que tienen que darse vuelta: **BENCH1** (hoy: *"app dice 'Bastón conectado' · emulador link=libre · lee=False"*) y **LATCH** (hoy: *"NO reconectó"*). Predicciones falsables adicionales: (a) `flap 4 3000` ahora tiene que dar `attempt:0 → 1 → 2 → 3` en logcat (antes `attempt:0` las cuatro veces); (b) **arranque en frío** con el bastón recordado y el emulador arriba → conecta sola en ~1-2 s, sin ningún gesto ni diálogo; (c) con el emulador **apagado** (`off 120000` o directamente desenchufado) y un device recordado → la app **deja de reintentar** y muestra CTA a los ~2 min (`autoconnect_exhausted` en logcat), en vez de seguir para siempre. **Aceptación**: 21/21, o el rojo restante explicado. Cubre: RMV5.9, R6.4.

## Fase MV.6 — MFi/EA + GATT (fuera de este delta; arquitectura preparada)

> **Gated por negocio (MFi, canal Facundo) / futuro (GATT).** No se implementa el adapter; se deja la arquitectura declarable.

- [x] T-MV.6.1 — *(arquitectura, buildable hoy)* Confirmar que un `ReaderDriver` puede declarar un `TransportCapability` `kind:'mfi'` con `protocolString`, y que `adapterForTransport('mfi',…)` devuelve `null` (no-buildable) → binding con manual como piso. Test unit del punto de extensión. **El driver RS420 NO popula un `protocolString` real** (desconocido hasta Facundo). Cubre: RMV6.1, RMV6.2.
- [ ] T-MV.6.2 — *(GATED por negocio — NO en este delta)* Implementar `adapter-ea-ios` (External Accessory / MFi) cuando Facundo consiga la autorización + `protocolString` de Allflex. Extiende `AdapterKind`/`adapterForTransport` sin tocar el contrato. **No arranca en este delta.** Cubre: RMV6.2 (futuro).
- [ ] T-MV.6.3 — *(futuro — NO en este delta)* Implementar `adapter-ble-gatt` si aparece un lector con GATT abierto real, declarando `TransportCapability` `kind:'ble-gatt'`, sin tocar el contrato (core R11.3). Cubre: RMV6.3 (futuro).

## Fase MV.7 — Tests, QA e integración

- [x] T-MV.7.1 — Suite node:test del delta (`driver-registry.test.ts`, `selection-priority.test.ts`, `adapter-simulator.test.ts`, `demo-gate.test.ts`, `connection-view.test.ts`, `adapter-spp-android.test.ts`) enganchada en `scripts/run-tests.mjs`; `node scripts/check.mjs` verde end-to-end. **[as-built 2026-07-29: los 6 ya estaban enganchados; la unidad del SPP sumó 3 más a la lista explícita — `spp-protocol.test.ts`, `permissions-android.test.ts` y `plugins/with-bluetooth-classic.test.ts`.]** Cubre: RMV1, RMV2, RMV3 (puro), RMV4, RMV5 (puro).
- [x] T-MV.7.2 — E2E/component de la `StickConnectionScreen` contra el **simulador**: descubrir→elegir→conectar→lectura DEMO marcada→confirmación pre-commit→find-or-create; estados con CTA; no bloqueante; available=false no conecta. Reusa el patrón de gate del bridge E2E. Cubre: RMV3, RMV4.6, RMV4.8. **[as-built 2026-07-20 — Gate 2.5 / ADR-029: DOS artefactos. (1) Suite de regresión `app/e2e/baston-multivendor.spec.ts` (4 casos, 4/4 verde): (a) la pantalla monta bajo demo (2 marcas `__RAFAQ_BLE_E2E__`+`__RAFAQ_BLE_DEMO__` → `isDemoMode()` → `mode='demo'`), RS420 reconocido en web + "Simular lectura" + manual no bloqueante; (b) lectura simulada → find-or-create (confirmación pre-commit del EID) + marca DEMO en la lista; (c) estados off→conectado→desconectado con CTA; (d) REGRESIÓN: E2E no-demo (solo mock) NO monta DemoControls ni el indicador global (`isNonDemoE2E`) y el bridge mock sigue abriendo el overlay. (2) Capture de veto `app/e2e/captures/baston-multivendor.capture.ts` (6 shots: 01 pantalla off, 02 DemoControls, 03 lectura DEMO, 04 find-or-create, 05 desconectado, 06 conectado). NOTAS: `available:false` (RMV3.7) y `unrecognized` (RMV3.8) NO son alcanzables en el E2E web (el RS420 resuelve `recognized-available` en web; exigen mockear `Platform.OS`/inyectar device sintético) → quedan cubiertos por `connection-view.test.ts` (node:test, T-MV.4.6); documentado como N/A del E2E web. La lectura simulada se dispara con RETRY-tap: tras el reload a `/baston` (deep-link) el listener global se re-habilita recién cuando el rodeo activo re-resuelve (warm-up de PowerSync/contextos post-reload); reintentar es colisión-safe (cada emisión del simulador es un EID fresco). Bajo demo, el texto de estado aparece 2 veces (card + indicador global) → asserts con `.first()`. CERO cambio de producción (todos los testIDs/anclas ya existían).]**
  **[RECONCILIACIÓN 2026-07-30 (BENCH-3) — los dos artefactos asertaban el BUG y se corrigieron.** (a) La spec de regresión: los casos (b) y (c) esperaban que un bastonazo en `/baston` **abriera** el overlay global. Eso era la doble ingesta medida en device. Ahora (b) asserta la invariante nueva —la lectura entra UNA vez, en la lista de la pantalla, y el overlay **no** se abre (por ausencia del testID exclusivo, no por ausencia de un texto: la pantalla de fondo sigue montada detrás de cualquier scrim)— y (c) ya no necesita cerrar ningún sheet. **4/4 verde re-corrida contra el build nuevo.** (b) La capture: el shot `04-find-or-create` pasó a `04-lectura-sin-sheet-encima`, y el shot **`07-indicador-global-chrome` se CAYÓ**: la única navegación client-side que salía de `/baston` con la conexión viva era el *"Dar de alta"* de ese overlay — o sea, existía solo gracias al bug (`/baston` se alcanza por deep-link porque la fila de "Más" no está cableada, así que no tiene back-stack in-app y cualquier `goto` remonta el provider y apaga la conexión). RMV3.5 sigue cubierto por `connection-view.test.ts` + la spec E2E; su evidencia VISUAL vuelve cuando "Más" tenga la fila a `/baston` (anotado en `docs/backlog.md`). Se agrega la capture dedicada de esta unidad: `app/e2e/captures/baston-spp-bloqueantes.capture.ts` (4 shots, corrida y verde).]**
  **[RECONCILIACIÓN 2026-08-05 (unidad «acceso in-app», T-MV.4.7) — los números de arriba quedaron viejos; estos son los reales.** La suite `app/e2e/baston-multivendor.spec.ts` pasó de **4 a 6 casos** (**6/6 verde**): se sumaron **(e)** «la fila "Bastón" del tab "Más" navega a `/baston`, y el chevron vuelve a "Más"» —la ruta REAL del operario, no un `page.goto`— y **(f)** el contrafáctico sin transporte (`__RAFAQ_BLE_E2E_MANUAL__` → la fila SIGUE, dice "No disponible" y navega igual), que es lo que impide que (e) pase con un trailing hardcodeado. La capture `baston-multivendor.capture.ts` pasó de **6 a 10 shots** en **2 tests**: el **`07-indicador-global-chrome` VOLVIÓ** —ya no hace falta esperar a nada: la fila existe, así que la captura entra a `/baston` por ella (navegación client-side) y vuelve por el chevron (`backOr`) con la conexión viva, que es exactamente la nav que faltaba— sobre el tab **"Más"** (no `/crear-animal`); más `08-mas-fila-baston` (pantalla completa, para vetar la UBICACIÓN de la sección), `09-fila-mas-baston-conectado` (trailing en vivo) y `10-fila-mas-baston-sin-transporte` (la decisión de NO ocultar la fila + el único estado del trailing con descendente, para el veto del recorte g/q/p/j/y). Los shots 01–06 conservan su intención; sólo cambia que ahora se llega a ellos entrando por la fila.]**
- [ ] T-MV.7.3 — *(GATED por hardware)* QA de campo con el RS420 real (dev build Android): pairing/stream/reconexión/dedup/asignación masiva/manual al desconectar; validación del matching device→driver por canal real (RMV1.5, RMV2.8). **[Reconciliación 2026-07-29: el ensayo end-to-end de este circuito ya se puede correr SIN el RS420 con el emulador ESP32 (T-MV.5.7). Esta task queda como el QA de campo contra el lector real — con animales, barro y una sola mano — que ningún emulador reemplaza.]** **Aceptación**: documentado en `progress/impl_04-multivendor.md` + `field-findings.md`. Cubre: RMV5.9, RMV1.5, RMV2.8 (real).
- [ ] T-MV.7.4 — Documentación de cierre: reconciliar `requirements-multivendor.md`/`design-multivendor.md`/`tasks-multivendor.md` al as-built; foldear al baseline (índice "Deltas posteriores" del `design.md` core) un puntero + nota as-built bajo el/los `R<n>` afectados (R9 del core aterriza; R6/R11 extendidos por el registry); actualizar `field-findings.md` (resultado del veto del config plugin / gate device si se corrió). **Aceptación**: docs reflejan el estado real. Cubre: housekeeping.

---

## Resumen de dependencias críticas

```
MV.0 (setup + uniones aditivas + tipos)
   → MV.1 (registro RS420)  →  MV.2 (selección por capacidad)     ← TODO buildable HOY, puro
        ↓                          ↓
   MV.3 (simulador + gate)   →  MV.4 (pantalla + indicador + demo) ← buildable HOY (mock/sim/web-serial)
        ↓
   MV.5 código adapter-spp-android + tests PUROS (buildable HOY)
        ↓
⏸ VETO config plugin (MV.5.1) → ⏸ dev build + Android → MV.5.5 (device-test SPP real)  ← GATED hardware
        ↓
⏸ Facundo (MFi authz + protocolString) → MV.6.2 (adapter-ea-ios)                        ← GATED negocio
⏸ Lector GATT abierto real → MV.6.3 (adapter-ble-gatt)                                   ← futuro
```

## Trazabilidad RMV → tasks

| Requirement | Tasks | Bloqueo |
|---|---|---|
| RMV1.1, RMV1.2 | T-MV.0.3 | OK (buildable hoy) |
| RMV1.3 | T-MV.1.1 | OK |
| RMV1.4, RMV1.5, RMV1.7 | T-MV.1.2, T-MV.1.3 | OK |
| RMV1.6 | T-MV.1.3 (aditividad) | OK |
| RMV2.1, RMV2.2 | T-MV.2.1 | OK |
| RMV2.3, RMV2.4, RMV2.5, RMV2.6, RMV2.8 | T-MV.2.2, T-MV.2.4 | OK (matching real → gated) |
| RMV2.7 | T-MV.0.2, T-MV.2.3, T-MV.2.4 | OK |
| RMV3.1, RMV3.2, RMV3.3, RMV3.4, RMV3.6 | T-MV.4.1, T-MV.4.2, T-MV.4.5, T-MV.4.7, T-MV.4.8 | OK UI / conexión real gated. **RMV3.6 NO cambió**: T-MV.4.8 intentó volver tocable el pill y lo revirtió el mismo día (ver su nota); hoy lo hace cumplir el guard `(E)` + `e2e/fab-target-geometry.spec.ts` |
| RMV3.5 | T-MV.4.3, T-MV.4.5, T-MV.4.8 | OK |
| RMV3.7, RMV3.8 | T-MV.4.1, T-MV.4.3, T-MV.4.6 | OK |
| RMV4.1, RMV4.2, RMV4.8 | T-MV.3.2, T-MV.3.3, T-MV.3.4 | OK |
| RMV4.3 | T-MV.2.3, T-MV.3.4 | OK |
| RMV4.4, RMV4.5 | T-MV.3.1, T-MV.3.3, T-MV.4.4 | OK |
| RMV4.6 | T-MV.4.1, T-MV.4.4, T-MV.4.6 | OK |
| RMV4.7 | T-MV.3.1, T-MV.3.3 (triple-guard) + frontera spec 09/08 | OK (parar/reportar si toca spec 09/08) |
| RMV5.1, RMV5.2, RMV5.3, RMV5.7 | T-MV.5.2, T-MV.5.4 | OK código / conexión real gated |
| RMV5.4, RMV5.5 | T-MV.5.3, T-MV.5.4 | OK código / real gated |
| RMV5.6 | T-MV.5.2, T-MV.5.4 | OK |
| RMV5.8 | T-MV.5.1 | ⏸ veto (parar si incompatible) |
| RMV5.9 | T-MV.5.5, T-MV.5.7 (emulador ESP32), T-MV.5.6 + T-MV.7.3 (RS420 real) | ⚠️ gate ACOTADO: stream/dedup/reconexión con emulador; solo las idiosincrasias del lector real siguen GATED |
| RMV6.1, RMV6.2 | T-MV.2.1, T-MV.6.1 | OK arquitectura / adapter EA gated |
| RMV6.3 | T-MV.2.1, T-MV.6.3 | futuro |

## Notas de ejecución

- **NO reescribir** el core (contrato, tipos de spec 09, adaptadores existentes). Extensiones **aditivas** (`'simulator'`, `'demo'`) + reuso (`parser-rs420`, `LineFramer`, `backoffDelayMs`, `remembered-device`, `permissions`, `EidIngestEngine`).
- El `adapter-spp-android` se **escribe** con import perezoso; **no** se instala `react-native-bluetooth-classic` ni se prebuildea el dev build en este delta (RMV5.8). La conexión real es device-gated.
- El simulador es **dev/demo-only** (triple-guard endurecido por `__DEV__`); un EID demo marcado "DEMO" **nunca** se declara como real.
- **Colisión-safe:** trabajar en `app/src/services/ble/*` + `app/src/features/ble-stick/*`; único toque host-level = 1 línea del `mode` en `_layout.tsx` (coordinar). No tocar teléfono/auth.
- Si aparece la necesidad de cambiar un contrato de spec 09 (indicador global, confirmación del simulador, no-declaración SENASA), **PARAR y reportar al leader** — no parchear desde 04.
- **Recomendación de ADR** (design §12): el patrón driver-registry + tabla de selección probablemente merece una **enmienda a ADR-024** — la decide y redacta el **leader** en la Puerta 1, no el spec_author.
- Commits en español, presente, descriptivo (`agrega registro de drivers de bastón`, `crea motor de selección por capacidad`, `escribe adapter-spp-android`, etc.).
