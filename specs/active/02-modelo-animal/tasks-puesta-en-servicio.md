# Spec 02 — Stream A: modelo de puesta en servicio (delta backend) — Tasks

**Status**: `spec_ready` (pendiente **Gate 1 OBLIGATORIO** + Puerta 1). Implementa `requirements-puesta-en-servicio.md` (RPS.x) + `design-puesta-en-servicio.md`.

> 🔴 **Gate 1 OBLIGATORIO** antes de la Puerta 2 (backend deployado: `compute_category` + `rodeos`). **NO aplicar ninguna migración** (numeración ≥ 0102; el deploy lo gatea el leader post-Gate-2). El implementer escribe los `.sql` + tests; el leader aplica.
>
> Orden por dependencia. Tests = runners Node nativos de backend (`supabase/tests/...`, ver `docs/verification.md`); el cliente Expo aún no tiene suite (convenciones §Tests). Cada `RPS.n` mapea a ≥1 test (trazabilidad, regla dura).

## Fase 0 — Confirmar el techo de numeración

- [ ] **TPS.0** — Confirmar el número de migración libre real (Glob `supabase/migrations/*.sql`; as-built llega a `0101` → arrancar en `0102`) **y** chequear que ninguna terminal paralela tenga migraciones en vuelo que muevan el techo. Ajustar los números de TPS.1–TPS.4 contiguos ≥ 0102 sin reabrir spec. — *infra / RPS.7.2.*

## Fase 1 — Columna `service_months` (schema + validación)

- [ ] **TPS.1** — `0102_rodeo_service_months.sql` (NUEVO): `ALTER TABLE public.rodeos ADD COLUMN service_months smallint[]` (nullable, **sin default** → backfill NULL, DD-PS-3) + `CONSTRAINT rodeos_service_months_valid CHECK` (rango 1–12 / sin duplicados / ≤12 elementos; NULL pasa) + `comment on column`. Sin tocar RLS (hereda `rodeos` `0017`). — *cubre RPS.1.1, RPS.1.2, RPS.1.3, RPS.1.4, RPS.1.5, RPS.2.1.*
- [ ] **TPS.2** — Test DB del CHECK (`supabase/tests/animal/` o suite equivalente): rechaza `{0}`/`{13}` (RPS.1.3), `{10,10}` (RPS.1.4), un array de 13 elementos (RPS.1.5); acepta `NULL`, `{}`, `{10,11,12}`; verifica que `NULL` y `{}` son distinguibles (RPS.1.2) y que un rodeo creado por el camino viejo queda `NULL` tras el ALTER (RPS.2.1). — *cubre RPS.1.2, RPS.1.3, RPS.1.4, RPS.1.5, RPS.2.1.*

## Fase 2 — Camino de escritura (RPC offline + helper + authz)

- [ ] **TPS.3** — `0103_create_rodeo_service_months.sql` (NUEVO): helper `assert_service_months_valid(smallint[])` (IMMUTABLE, revoke de public/anon/authenticated; lo invocan las RPC definer). — *cubre RPS.3.5 (helper).*
- [ ] **TPS.4** — `0103` (continúa): `CREATE OR REPLACE create_rodeo(...)` con `p_service_months smallint[] default null` → si NULL aplica default primavera `{10,11,12}` (RPS.1.6), valida con el helper (RPS.3.5), persiste en el INSERT del rodeo. Resolver el cambio de firma (DROP de la firma vieja `(uuid,uuid,text,uuid,uuid,jsonb)` + CREATE de la nueva, default de diseño) + revoke/grant + smoke-check de grants (patrón `0081`). Conservar owner-only + idempotencia natural por `id` de cliente + anti-IDOR c-bis. — *cubre RPS.3.1, RPS.1.6, RPS.3.3, RPS.3.5.*
- [ ] **TPS.5** — `0103` (continúa): `CREATE set_rodeo_service_months(p_rodeo_id, p_service_months)` (gemela de `set_rodeo_config` `0082`): deriva `establishment_id` del rodeo (anti-IDOR hermético, RPS.3.4), guard `is_owner_of(v_est)` (RPS.3.3), valida con el helper (RPS.3.5), `UPDATE rodeos SET service_months` idempotente (RPS.3.6); rodeo inexistente/soft-deleted → `P0002`; revoke/grant + smoke-check. — *cubre RPS.3.2, RPS.3.3, RPS.3.4, RPS.3.5, RPS.3.6.*
- [ ] **TPS.6** — Test DB de las RPC (`supabase/tests/animal/`): `create_rodeo` sin `p_service_months` → rodeo con `{10,11,12}` (RPS.1.6); con `{6,7}` → ese valor; con `{13}` → rechazo (RPS.3.5). `set_rodeo_service_months`: owner edita OK e idempotente (re-aplicar = no-op, RPS.3.6); **field_operator/vet → 42501** (RPS.3.3); **owner del tenant A con `p_rodeo_id` del tenant B → 42501 sin tocar nada** (RPS.3.4, anti-IDOR); `NULL` vuelve a "sin configurar"; rodeo inexistente → `P0002`. — *cubre RPS.3.1, RPS.3.2, RPS.3.3, RPS.3.4, RPS.3.6, RPS.1.6.*

## Fase 3 — Reconciliar `compute_category` (quitar `service`)

- [ ] **TPS.7** — `0104_compute_category_drop_service.sql` (NUEVO): `CREATE OR REPLACE compute_category(uuid)` = copia **literal** de `0062` **menos** la declaración `v_has_service`, su `SELECT EXISTS ... event_type='service'`, y el término `or v_has_service` de la rama `vaquillona`. Conservar `SECURITY DEFINER STABLE` + `search_path=public` + grant a `authenticated` (RPS.4.6) + precedencia LOAD-BEARING + rama macho + cortes de edad + tacto+ vigente. **No** tocar `0063` (guard incremental: dejar `'service'`, recompute idempotente — DD-PS-4 recomendación firme). — *cubre RPS.4.1, RPS.4.2, RPS.4.3, RPS.4.6, RPS.4.7.*
- [ ] **TPS.8** — Test DB `compute_category` post-cambio (`supabase/tests/animal/`): (a) hembra con **solo** un evento `service` (natural) y sin destete, <1 año → categoría **`ternera`** (NO `vaquillona` por el evento, RPS.4.1); (b) hembra con destete → `vaquillona` (RPS.4.2); (c) hembra ≥1 año con `birth_date` → `vaquillona` por edad (RPS.4.4); (d) tacto+ → `vaquillona_prenada`, 1 parto → `vaca_segundo_servicio`, ≥2 → `multipara`, aborto revierte tacto+, castración intacta (RPS.4.3); (e) recompute de un perfil con `service` histórico (al editar/borrar otro evento) = misma categoría que sin él (RPS.4.5). — *cubre RPS.4.1, RPS.4.2, RPS.4.3, RPS.4.4, RPS.4.5.*
- [ ] **TPS.9** — Test DB de la **IA** (RPS.4.8): insertar `reproductive_events` con `event_type='service'` + `service_type='ai'` sobre una `ternera` (en rodeo con `inseminacion` habilitado, gating `0054`) → la categoría **sigue `ternera`** (la IA ya no promueve por el solo evento); y verificar que esa hembra **sí** aparece en `rodeo_serviced_females` por la rama `ai` (cruce con Fase 4). — *cubre RPS.4.8.*
- [ ] **TPS.10** — Test DB de la **red de seguridad de edad** (RPS.4.4): correr `refresh_age_categories()` (como `service_role`, no como cliente — está revocada) sobre una hembra `ternera` con `category_override=false` y `birth_date` de ≥365 días → su categoría pasa a `vaquillona` (history `auto_transition`). Confirma que el corte hembra del cron (`0066`) cubre "se olvidaron de destetar" tras quitar `service`. — *cubre RPS.4.4.*

## Fase 4 — Contrato de derivación servidas/entoradas

- [ ] **TPS.11** — `0105_repro_denominator.sql` (NUEVO): `rodeo_service_campaign(p_rodeo_id, p_year)` (ventana + `is_configured` + `n_months` + `months`; SECURITY DEFINER STABLE + guard `has_role_in(est)`; cota de `p_year`). — *cubre RPS.5.8, RPS.2.3, RPS.5.6 (parte).*
- [ ] **TPS.12** — `0105` (continúa): `rodeo_serviced_females(p_rodeo_id, p_year)` — unión DISTINCT natural∪IA (RPS.5.7): rama natural = vacas + vaquillonas APTAS (último `heifer_fitness='apta'`, RPS.5.3) + fallback por edad sin veredicto (RPS.5.4), solo si `service_months` no vacío (RPS.2.2); rama IA = `service`+`ai` no borrado con `event_date` en la campaña (RPS.5.1). Guard de tenant (RPS.5.6); read-only/STABLE (RPS.5.9). — *cubre RPS.5.1, RPS.5.2, RPS.5.3, RPS.5.4, RPS.5.7, RPS.5.9.*
- [ ] **TPS.13** — `0105` (continúa): `rodeo_repro_denominator(p_rodeo_id, p_year)` → `(serviced, retired, entoradas)` con **entoradas = serviced − retired** (RPS.5.5). Guard de tenant. — *cubre RPS.5.5.*
- [ ] **TPS.14** — `0105` (continúa): revoke `execute … from public, anon` + grant a `authenticated` para las 3 funciones + smoke-check fail-closed (patrón `0066`/`0074`). — *cubre RPS.5.6.*
- [ ] **TPS.15** — Test DB del denominador (`supabase/tests/animal/`): sembrar un rodeo con `service_months={11}`, vacas + vaquillonas (apta / no_apta / diferida / sin-veredicto-pero-de-edad) + una hembra con IA en Nov + una hembra con IA fuera de campaña + una baja: verificar que `rodeo_serviced_females` incluye vacas + apta + sin-veredicto-de-edad + IA-en-campaña, **excluye** no_apta/diferida (RPS.5.3) y IA-fuera-de-campaña; **distinct** (hembra en ambas ramas cuenta 1, RPS.5.7); `rodeo_repro_denominator.entoradas = serviced − retiradas` (RPS.5.5); rodeo `service_months=NULL` → rama natural vacía (RPS.2.2) pero IA igual cuenta; `rodeo_service_campaign.is_configured=false` para NULL (RPS.2.3); **caller de otro tenant → 42501** en las 3 (RPS.5.6, anti-IDOR); las 3 son read-only (no mutan, RPS.5.9). — *cubre RPS.2.2, RPS.2.3, RPS.5.1, RPS.5.3, RPS.5.4, RPS.5.5, RPS.5.6, RPS.5.7, RPS.5.8, RPS.5.9.*

## Fase 5 — Verificación de `heifer_fitness` (sin migración)

- [ ] **TPS.16** — Test DB del contrato `heifer_fitness` (verificación, NO migración — `0053` ya tiene el enum): el enum `heifer_fitness_result` acepta `'apta'`/`'no_apta'`/`'diferida'` y **rechaza** un 4º valor (RPS.6.1, RPS.6.4); insertar `tacto_vaquillona` con cada veredicto sobre una vaquillona → su `category_id` **no** cambia (RPS.6.3); una vaquillona con último veredicto `'diferida'` **no** entra al denominador por aptitud (RPS.6.2, cruce con TPS.15) y **no** se marca CUT ni se saca del padrón (RPS.6.2). — *cubre RPS.6.1, RPS.6.2, RPS.6.3, RPS.6.4.*

## Fase 6 — Enganche de tests + cierre

- [ ] **TPS.17** — Enganchar los tests nuevos en el runner correspondiente (`scripts/run-tests.mjs` / la suite `supabase/tests/animal/run.cjs`) para que `node scripts/check.mjs` los corra (los runners DB no toman por glob — paths/casos explícitos). **Cuidado colisión** con terminales paralelas en `run-tests.mjs` (memoria `feedback_parallel_terminals`): inserción quirúrgica. — *infra de tests.*
- [ ] **TPS.18** — **Gate 1 (`security_analyzer` modo `spec`)** sobre este delta (OBLIGATORIO): owner-only + anti-IDOR de `service_months`; SECURITY DEFINER de las 3 funciones de derivación (revoke/grants, IDOR, cota de `p_year`, read-only); `compute_category` preserva grants/revokes/search_path y no abre cross-tenant; cambio de firma de `create_rodeo`. Reflejar findings en la spec (preservando IDs `RPS.x`) antes de la Puerta 1. — *cubre RPS.7.5; gate.*
- [ ] **TPS.19** — Reconciliar specs con el as-built si algo cambió en implementación (regla dura `feedback_correcciones_en_specs`) + dejar **anotada** la dependencia frontend del espejo (RPS.7.4: quitar `hasService` de `app/src/utils/animal-category.ts`, lo ejecuta Stream B / slice C6, **no** este delta) + `node scripts/check.mjs` verde + reviewer + Gate 2 antes de la puerta de código humana. **NO aplicar migraciones** (las aplica el leader post-Gate-2). — *cierre; RPS.7.3, RPS.7.4.*

## Notas de ejecución

- **Backend deployado, NO aplicar:** las 4 migraciones (`0102`–`0105`) se escriben pero **no** se aplican al remoto en este chunk; hasta entonces el test de la suite `animal` que las ejercita **FALLA** — es **ESPERADO** (mismo patrón que `0075`–`0082`). El leader las aplica por Management API tras Gate 1 (spec) + reviewer + Gate 2 (código) + Puerta 2.
- **Encadenamiento con Stream B/C:** Stream B (frontend) agrega el **selector de 12 meses** en el wizard de alta/edición de rodeo (consume `create_rodeo`/`set_rodeo_service_months`) + **deprecada la maniobra de servicio manual** + alinea el **espejo client-side** (quita `hasService`). Stream C (spec 07) consume el **denominador** de Fase 4. Stream A no implementa nada de eso.
- **Aristas [TENTATIVO] de §5 del design** (ventana de "retirada", cruce de fin de año, umbral de edad del fallback, membresía histórica): Stream A fija defaults razonables y los marca; si Stream C/Facundo pide refinarlos, entra como reconciliación de esta spec (IDs `RPS.x` estables).
- **Orden mínimo viable**: TPS.0 → TPS.1–2 (columna) → TPS.3–6 (RPC) → TPS.7–10 (compute_category) → TPS.11–15 (denominador) → TPS.16 (heifer_fitness) → TPS.17–19 (enganche + Gate 1 + cierre).
