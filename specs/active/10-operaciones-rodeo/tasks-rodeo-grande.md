# Spec 10 — DELTA «rodeo grande» — Tasks

**Tipo**: delta-spec (ADR-028 Nivel B). El `tasks.md` baseline NO se toca (es el ledger histórico); este delta trae el suyo.
**Status**: `spec_ready`. Gate 1: **NO aplica** (design §7). ⏸ Puerta 1 pendiente.
**Fuente**: `requirements-rodeo-grande.md` + `design-rodeo-grande.md` (2026-07-18).

> Reglas (`docs/specs.md`): pasos discretos en orden, cada uno con `[ ]` + los `RG<n>` que cubre. El implementer marca `[x]`; el reviewer rechaza `[ ]` sin justificación. Cada `RG<n>` mapea a ≥1 test.
>
> **Fases**: (1) builders puros en `local-reads.ts` (testeables contra SQLite in-memory, sin SDK — pre-requisito de todo). (2) services (`group-page.ts`, fix de `management-groups`/`group-data`). (3) hooks (`useGroupView` paginado + estado buscador/chips). (4) UI (FlatList en `GroupViewScreen` + buscador/chips + wiring de las 2 rutas + baja en tanda del lote). (5) E2E. **NO se toca la tab Animales ni `buildAnimalsListQuery`** (RG7.1). **Sin backend** (design §7).

---

## Fase 1 — Builders puros (`local-reads.ts`) + tests SQLite in-memory

- [x] T-RG.1 — `buildGroupAnimalsPageQuery(establishmentId, group, filter, cursor, pageSize)` (design §3.1): reusa `LOCAL_LIST_SELECT` + la inyección de `in_treatment` + el UNION synced/overlay del baseline; agrega **scope de grupo** (`ap.rodeo_id = ?` | `ap.management_group_id = ?`, y su análogo `pap.*`), **filtro opcional** categoría (`c.code = ?`) y sexo (`ap.animal_sex = ?`), **predicado keyset** `(in_treatment DESC, created_at DESC, id DESC)` en el SELECT externo que envuelve el UNION, y `LIMIT pageSize`. La 1ª página omite el keyset. **`buildAnimalsListQuery` NO se toca** (RG7.1). Cubre: RG1.1, RG1.2, RG1.4, RG1.7, RG1.8, RG3.5.
- [x] T-RG.2 — Test (SQLite in-memory, patrón `local-reads.test.ts`) de la paginación keyset: sembrar N filas con `created_at`/`id`/`in_treatment` conocidos → página 1 (sin cursor) devuelve las `pageSize` primeras en el orden `(in_treatment DESC, created_at DESC, id DESC)`; página 2 (cursor = última de la 1) devuelve las siguientes **sin saltos ni duplicados**, incluso con `created_at` repetidos (desempate por `id`); el overlay (`pending_*`) del grupo aparece intercalado por su `created_at`; el scope excluye filas de otro rodeo/lote. Cubre: RG1.4, RG1.7, RG1.8, RG6.3.
- [x] T-RG.3 — `buildAllGroupMembersQuery(establishmentId, group)` (design §5.3): misma query scopeada que T-RG.1 **sin** keyset ni LIMIT (o LIMIT de guarda alto), overlay-aware → set completo del grupo. Cubre: RG5.3, RG5.4.
- [x] T-RG.4 — Test de `buildAllGroupMembersQuery`: devuelve **todos** los miembros activos del grupo (rodeo y lote), incluyendo los que caerían fuera de un LIMIT 200 del campo (regresión del bug del lote); excluye `status != active` / `deleted_at` / exits pendientes; incluye overlay. Cubre: RG5.3, RG5.4.
- [x] T-RG.5 — `buildGroupCandidateCountsQuery(group, { weaningEnabledRodeoIds? })` (design §5.2): COUNT overlay-aware de candidatos **castrar** (`animal_sex='male' AND is_castrated=0`, exacto) y **destetar** (`c.code IN ('ternero','ternera') AND NOT EXISTS(weaning vivo)`, restringido a rodeos con destete en lote cross-rodeo). Cubre: RG5.2.
- [x] T-RG.6 — Test de `buildGroupCandidateCountsQuery`: cuenta candidatos sobre el **grupo entero** (no una página); castrar exacto (hembras/castrados no cuentan); destetar excluye ya-destetados y (en lote) rodeos sin destete; overlay incluido. Cubre: RG5.2.
- [x] T-RG.7 — `buildGroupCategoryOptionsQuery(establishmentId, group)` (design §4.2): DISTINCT de `c.code` (+ nombre) de los animales activos del grupo, scopeado + overlay-aware, para las opciones del chip de categoría. Cubre: RG3.9.
- [x] T-RG.8 — Extender `buildSearchUnion` (y por ende `buildSearchByTagQuery`/`buildSearchByIdvQuery`/`buildSearchLikeQuery`/`buildApodoSearchQuery`) con un **`groupScope` opcional** (`ap.rodeo_id`/`ap.management_group_id` + `pap.*` en ambas ramas), **sin** cambiar el comportamiento cuando `groupScope` es ausente (la tab Animales lo llama sin scope → idéntico, RG7.1). Cubre: RG3.1, RG3.2.
- [x] T-RG.9 — Test de la búsqueda scopeada: `groupScope` presente → solo matchea animales del grupo (excluye un match de otro rodeo/lote del mismo campo); `groupScope` ausente → resultado idéntico al as-built (no-regresión de la tab). Cubre: RG3.2, RG7.1.

## Fase 2 — Services

- [x] T-RG.10 — `app/src/services/group-page.ts`: `fetchGroupAnimalsPage(est, group, { filter, cursor })` → `{ items: AnimalListItem[], nextCursor, reachedEnd }`. Corre `buildGroupAnimalsPageQuery`, aplica `computeMirrorOverrides` + `computeReproStatuses` **solo a la página** (design §2), mapea con `toLocalListItem`. `reachedEnd = items.length < GROUP_PAGE_SIZE`; `nextCursor` = clave `(in_treatment, created_at, id)` de la última fila. Lee del SQLite local (offline). Cubre: RG1.1, RG1.2, RG1.3, RG1.5, RG6.4.
- [x] T-RG.11 — `group-page.ts`: `searchGroupAnimals(est, group, q)` — espeja `searchAnimals` as-built pero pasando el `groupScope` a los builders (T-RG.8); mismo dedup por profileId + espejo + repro sobre el set deduplicado. Cubre: RG3.1, RG3.2.
- [x] T-RG.12 — `group-page.ts`: `fetchGroupMemberCount(est, group)` (design §3.2) reusando `fetchRodeoHeadCounts`/`fetchGroupHeadCounts` as-built → total real overlay-aware del grupo; `fetchGroupCategoryOptions(est, group)` (T-RG.7). Cubre: RG2.1, RG2.2, RG3.9.
- [x] T-RG.13 — Tests de `group-page.ts` (contra SQLite in-memory): `fetchGroupAnimalsPage` pagina y anexa sin duplicar; `reachedEnd` correcto; count real ≥ filas de la primera página; `searchGroupAnimals` scopeado. Cubre: RG1.5, RG2.1, RG3.2.
- [x] T-RG.14 — `management-groups.ts`: reescribir `fetchGroupMembers` a `buildAllGroupMembersQuery(est, {type:'lote', id:groupId})` (scope `management_group_id`, sin tope) — **fix del bug** (deja de traer 200-del-campo-y-filtrar). Exponer `fetchAllGroupMembers` (o el propio `fetchGroupMembers`) como el set completo para las masivas. Cubre: RG5.3, RG5.4.
- [x] T-RG.15 — Test de regresión del lote: `fetchGroupMembers` devuelve **todos** los miembros del lote aun con >200 animales en el campo (antes devolvía un subconjunto arbitrario). Cubre: RG5.4.
- [x] T-RG.16 — `group-data.ts`: `fetchRodeoGroupActions`/`fetchLoteGroupActions` dejan de contar candidatos sobre la lista mostrada y usan `buildGroupCandidateCountsQuery` (T-RG.5) sobre el **grupo entero** (design §5.2). El gating por CONFIG (`rodeo_data_config` → `resolveGroupActions`/`applyCandidateGating`) no cambia. Ajustar las firmas (ya no necesitan `animals`). Cubre: RG5.2.
- [x] T-RG.17 — Test de `group-data.ts`: el gating ofrece/oculta Castrar/Destetar según los candidatos del **grupo entero**, no de una página parcial; lote cross-rodeo respeta destete por rodeo. Cubre: RG5.2.
- [x] T-RG.18 — `bulk-selection-data.ts`: verificar/ajustar que `fetchGroupSelectionProfiles` para **rodeo** use el set completo (`fetchAllGroupMembers`, no `fetchAnimals({rodeoId})` LIMIT 200) y para **lote** herede el `fetchGroupMembers` corregido → las masivas operan sobre el grupo entero (design §5.1/§5.3). Cubre: RG5.1, RG5.3.

## Fase 3 — Hooks

- [x] T-RG.19 — `useGroupView.ts` al contrato paginado (design §6.2): estado `animals` (páginas acumuladas), `actions`, `totalCount`, `loading`/`loadingMore`/`reachedEnd`/`error`, cursor interno. Carga inicial = meta (gating + count) + primera página. `loadMore()` con guard de-un-solo-en-vuelo (RG1.6) y corte por `reachedEnd` (RG1.5). **As-built**: firma `useGroupView({ establishmentId, group })` (ya no un `loader`); las acciones se resuelven por `group.type` (rodeo→`fetchRodeoGroupActions`, lote→`fetchLoteGroupActions`). Lógica DECIDIBLE (canLoadMore, dedup) extraída a `utils/group-view-model.ts` (PURA, testeada). Cubre: RG1.3, RG1.5, RG1.6, RG2.1.
- [x] T-RG.20 — `useGroupView.ts`: refresh silencioso (foco / `lastSyncedAt`) recarga la **ventana cargada** (`LIMIT loadedCount` desde el tope, `fetchGroupWindow` nuevo), reemplaza `animals` en el lugar con keys estables (`profileId`), **sin** resetear a página 1 ni tocar `loading`; fallo transitorio conserva la lista; refresca `totalCount` + `actions`. `reachedEnd` previo se preserva si la ventana vuelve llena. Cubre: RG6.1, RG6.2, RG6.3.
- [x] T-RG.21 — Estado buscador/chips **FOLDED en `useGroupView`** (design §6.3): texto con debounce 250ms + chips categoría/sexo; modo lista paginada (sin texto, chips en SQL) vs modo búsqueda (con texto, intersecta chips client-side vía `intersectSearchWithChips` puro); guard de secuencia; reset de cursor al cambiar criterio (RG3.7); volver a lista completa al limpiar (RG3.8). Opciones de SEXO por query propia (`buildGroupSexOptionsQuery`/`fetchGroupSexOptions` nuevos, RG3.9). Cubre: RG3.4, RG3.5, RG3.6, RG3.7, RG3.8.

## Fase 4 — UI

- [x] T-RG.22 — `GroupViewScreen.tsx`: reestructurar `ScrollView`+`.map()` → **`FlatList`** (design §6.1): `ListHeaderComponent` = `GroupMetaHeader` + card de acciones; `renderItem` = `renderRow`; `keyExtractor` = `profileId`; `onEndReached` = `loadMore` (threshold 0.5); `ListFooterComponent` = spinner "cargando más" cuando `loadingMore`; `ListEmptyComponent` = loading/empty/no-match. **FlatList, no FlashList** (RG4.2). **As-built**: dejó de ser genérico `<T>` → tipa `AnimalListItem` (ambas rutas lo pasan). Cubre: RG4.1, RG4.2, RG4.3, RG4.5, RG4.6, RG4.7.
- [x] T-RG.23 — Buscador + chips **FIJOS arriba** (fuera de la FlatList, RG4.4): `GroupSearchBar` (nuevo) = buscador + FilterChip (categoría/sexo) + FilterPopover, **CLONANDO** el patrón de `animales.tsx` — tap nativo `onPress`+`buttonA11y` en la MISMA pieza Tamagui con `pressStyle`, **sin `<Pressable>` de RN envolviendo** (RG3.10, CRÍTICO). **As-built**: se CLONA (no se extrae) para NO tocar la tab Animales (RG7.1). Opciones de categoría de `fetchGroupCategoryOptions`; sexo solo si ambos presentes (RG3.9, vía `fetchGroupSexOptions`+`sexFilterAvailable`). Cubre: RG3.1, RG3.3, RG3.9, RG3.10, RG4.4.
- [x] T-RG.24 — `GroupViewBits.tsx`: `GroupMetaHeader` muestra `totalCount` real (RG2.1) — "…" mientras `null` (RG2.3), formato es-AR `toLocaleString('es-AR')` (RG2.4); `lineHeight` matching en el heading del nombre + en el conteo (RG7.3). `GroupAnimalsList` absorbido en la FlatList (eliminado el componente `Card`+`.map`). Cubre: RG2.1, RG2.3, RG2.4, RG7.3.
- [x] T-RG.25 — `rodeo/[id].tsx`: usa `useGroupView({establishmentId, group:{type:'rodeo', id}})` (service paginado) en vez de `fetchAnimals({rodeoId})`; el buscador/chips los cablea `GroupViewScreen` desde `view`; `renderRow` = `AnimalRow` compacto (as-built). Cubre: RG1.1, RG4.7.
- [x] T-RG.26 — `lote/[id].tsx`: ídem (`group:{type:'lote', id}`). **Modo selección de baja en tanda** reconciliado (design §6.4): la lista de selección VIRTUALIZA (FlatList) y carga el **conjunto completo del lote** (`fetchAllGroupMembers`) al entrar en modo selección — "seleccionar todos"/`resolveSelectedIds` operan sobre TODOS los miembros. **Edge del nav-param gigante RESUELTO** (ver design §6.4 as-built): `goToVenta` pasa `groupId` + `mode` ('all'|'subset') + el csv MÁS CHICO entre seleccionados/excluidos → "todos" (miles) va con csv VACÍO; `venta.tsx` resuelve el set del lado destino contra `fetchGroupMembers` (anti-IDOR). Cubre: RG1.1, RG4.7, RG5.6.
- [x] T-RG.27 — Pasada es-AR voseo + descendentes sobre las piezas nuevas (placeholder del buscador, empty-states, footer "cargando más", labels de chips): `lineHeight` matching en headings ≥`$6` y textos con `numberOfLines` (RG7.3); copy voseo (RG7.2). Cubre: RG7.2, RG7.3.

## Fase 5 — E2E (Playwright, corre en web)

- [x] T-RG.28 — E2E scroll infinito (design §8 caso 1): seed de `> GROUP_PAGE_SIZE` animales en un rodeo (batch); la lista inicial trae ~`GROUP_PAGE_SIZE`; al scrollear al fondo, un animal seedeado fuera de la 1ª página se vuelve visible (`onEndReached` cargó más). Cubre: RG1.2, RG1.3, RG1.5. **As-built** (`e2e/rodeo-grande.spec.ts`): 65 hembras con `created_at` EXPLÍCITO (el keyset ordena por `created_at DESC, id DESC`; un batch comparte `now()` → sin control el orden sería por UUID no-determinístico); el más viejo queda fuera de la 1ª página → `toHaveCount(0)` antes de scrollear, visible tras `onEndReached` → `loadMore`. Seed REAL (DG8, no se reduce `GROUP_PAGE_SIZE`).
- [x] T-RG.29 — E2E buscar dentro del grupo (caso 2): buscar la caravana de un animal del grupo más allá de la 1ª página → aparece (búsqueda sobre el set completo, RG3.2). Cubre: RG3.1, RG3.2. **As-built**: 65 hembras; se busca el idv de la fila ~63 (fuera de la 1ª página) → aparece + el animal de la 1ª página (que no matchea) desaparece (probando que se filtró por la query scopeada, no que quedó la lista completa).
- [x] T-RG.30 — E2E filtrar por categoría/sexo (caso 3): activar un chip → subconjunto correcto; combinar dos chips; **ENSANCHAR re-puebla la lista** (regresión del race). Cubre: RG3.3, RG3.4, RG3.5 + regresión del race de `useGroupView.refreshWindow`. **As-built**: 6 animales (2 vaquillona F, 2 ternera F, 2 torito M) con `created_at` EXPLÍCITO (orden determinístico: tor > ter > vaq, así las vaquillonas son las hembras más VIEJAS). Secuencia: NARROW (6→4→2: sexo=Hembras → +categoría=Ternera) + **WIDEN (2→4→6: limpiar categoría → limpiar sexo)** asertando que las filas filtradas-fuera RE-pueblan (`toHaveCount(1)` — con el bug quedaban en 0, la lista pegada en el tamaño angosto). Oráculo por PRESENCIA (`toHaveCount`, robusto a virtualización + card de acciones). El tramo de ensanchar dispara el `refreshWindow` concurrente (efectos foco+sync) → cubre el race. **Verificado que CAE con el bug** (guard deshabilitado → falla en el step de ensanchar) y verde con el fix.
- [x] T-RG.31 — E2E count real en el header (caso 4): el header muestra el total del grupo (> filas de la 1ª página). Cubre: RG2.1, RG2.2. **As-built**: 65 hembras → header muestra "65 animales activos" (> 60 = filas de la 1ª página), NO `list.length`.
- [x] T-RG.32 — E2E lote muestra TODOS sus miembros — regresión del bug (caso 5): seed de un lote cuyos miembros quedarían fuera del viejo "200-del-campo" → la vista de lote / su count los incluye a todos. Cubre: RG5.4. **As-built**: 205 filler en el rodeo (más NUEVOS, sin lote) + 5 miembros del lote (más VIEJOS → fuera de los 200-más-nuevos del campo). La vista de lote muestra los 5 + header "5 animales activos" (con el bug mostraría 0). Seed >200 REAL (el LIMIT 200 de `buildAnimalsListQuery` NO es reducible — RG7.1).
- [x] T-RG.33 — E2E acción masiva sobre el grupo entero (caso 6): con un grupo de > una página, la vista ofrece la acción (gating por COUNT del grupo) y la selección masiva lista candidatos de más allá de la 1ª página. Cubre: RG5.1, RG5.2, RG5.3. **As-built**: 61 hembras (1ª página, NO candidatas) + 5 machos enteros (fuera de la 1ª página, candidatos). "Castrar" se OFRECE (gating por COUNT del grupo entero, aunque la página cargada = 60 hembras sin candidatos → con gating por página estaría oculto); la pantalla de selección (set completo, RG5.3) lista un macho candidato de más allá de la 1ª página. Todos verdes: 6/6 (`e2e/rodeo-grande.spec.ts`).

## Mapa de cobertura RG → tasks

| RG<n> | Tasks |
|---|---|
| RG1.1 | T-RG.1, T-RG.10, T-RG.25, T-RG.26 |
| RG1.2 | T-RG.1, T-RG.10, T-RG.28 |
| RG1.3 | T-RG.10, T-RG.19, T-RG.22, T-RG.28 |
| RG1.4 | T-RG.1, T-RG.2 |
| RG1.5 | T-RG.10, T-RG.13, T-RG.19, T-RG.28 |
| RG1.6 | T-RG.19 |
| RG1.7 | T-RG.1, T-RG.2 |
| RG1.8 | T-RG.1, T-RG.2 |
| RG2.1 | T-RG.12, T-RG.19, T-RG.24, T-RG.31 |
| RG2.2 | T-RG.12, T-RG.31 |
| RG2.3 | T-RG.24 |
| RG2.4 | T-RG.24 |
| RG3.1 | T-RG.8, T-RG.11, T-RG.23, T-RG.29 |
| RG3.2 | T-RG.8, T-RG.9, T-RG.11, T-RG.13, T-RG.29 |
| RG3.3 | T-RG.23, T-RG.30 |
| RG3.4 | T-RG.21, T-RG.30 |
| RG3.5 | T-RG.1, T-RG.21, T-RG.30 |
| RG3.6 | T-RG.21 |
| RG3.7 | T-RG.21, T-RG.30 (ensanchar → lista desde el tope, re-poblada) |
| RG3.8 | T-RG.21, T-RG.30 (limpiar chips → lista completa) |
| RG3.9 | T-RG.7, T-RG.12, T-RG.23 |
| RG3.10 | T-RG.23 |
| RG4.1 / RG4.2 / RG4.3 / RG4.5 / RG4.6 / RG4.7 | T-RG.22 |
| RG4.4 | T-RG.23 |
| RG5.1 | T-RG.18, T-RG.33 |
| RG5.2 | T-RG.5, T-RG.6, T-RG.16, T-RG.17, T-RG.33 |
| RG5.3 | T-RG.3, T-RG.4, T-RG.14, T-RG.18, T-RG.33 |
| RG5.4 | T-RG.3, T-RG.4, T-RG.14, T-RG.15, T-RG.32 |
| RG5.5 | (baseline R3.4 intacto — verificado en T-RG.18/T-RG.33; sin cambio de modelo) |
| RG5.6 | T-RG.26 |
| RG6.1 | T-RG.20 |
| RG6.2 | T-RG.20 |
| RG6.3 | T-RG.2, T-RG.20 |
| RG6.4 | T-RG.10 |
| RG6.5 | (invariante multi-tenant: todas las queries scopean `establishment_id` — verificado en T-RG.1/T-RG.8/T-RG.14) |
| RG7.1 | T-RG.1, T-RG.8, T-RG.9 |
| RG7.2 | T-RG.27 |
| RG7.3 | T-RG.24, T-RG.27 |
| RG7.4 | (invariante de scope: sin sorting configurable ni otras listas — no se agregan tasks) |

## Changelog

- **2026-07-18 — Capture file de Gate 2.5 (ADR-029) ENTREGADO** (impl `progress/impl_10-rodeo-grande-captures.md`). Deliverable de veto visual del leader: `app/e2e/captures/rodeo-grande.capture.ts` (2 tests, viewport mobile 412×915, seed batch LOCAL namespaced RUN_TAG con `created_at`/`birth_date`/`category_override` explícitos, `afterAll(cleanupAll)`). Recorre el flujo del feature y saca **10 capturas NOMBRADAS** a `e2e/captures/__shots__/rodeo-grande/` (gitignored — el `.capture.ts` SÍ se commitea, los `.png` NO): `01-rodeo-grande-lista` (HERO: count real "65 animales activos" + buscador fijo + chips + primeras filas), `02-buscador-activo` (match de un idv de más allá de la 1ª página — búsqueda sobre el set completo), `03-chip-categoria-abierto` (popover Todas las categorías/Ternera/Torito/Vaquillona), `04-chip-sexo-abierto` (Ambos sexos/Machos/Hembras), `05-filtro-combinado` (Hembras+Ternera, chips activos + subconjunto), `06-busqueda-sin-resultados` (empty state), `07-scroll-segunda-pagina` (loadMore → filas de la 2ª página), `08-lote-todos-miembros` (lote con sus 8 miembros + count real + "Vender / Descartar"), `09-castrar-ofrecido` (actions bar, gating por el grupo entero), `10-seleccion-masiva` (13 candidatos del set completo, incluye toritos de más allá de la 1ª página, tildados + CTA "Castrar 13 animales"). Corrida: **2/2 verde** contra el dist de prod (:8099) + Supabase remoto + PowerSync (`playwright.capture.config.ts`). Verificado el tsc targeted (0 errores propios) + inspección visual shot-por-shot. **CERO cambio de producción** (solo el `.capture.ts` nuevo). El leader corre este capture en el Gate 2.5 y veta el diseño contra las capturas antes de la Puerta 2.
- **2026-07-18 — FIX del race de `useGroupView.refreshWindow` (ensanchar-filtro)** (impl `progress/impl_10-rodeo-grande-race-fix.md`). Cierra el FINDING de Fase 5 (ya NO es "candidato a backlog"): el race es REAL y determinístico (al ensanchar un filtro, un `refreshWindow` de fondo con `loadedCount` STALE bumpeaba `listSeq` último y clobber-eaba la 1ª página fresca → lista pegada en el tamaño angosto). **Approach (a)**: `refreshWindow` CEDE (bail al inicio, antes de bumpear `listSeq`/leer `loadedCount`) si hay una carga de foreground en vuelo (1ª página/refresh vía `loadingRef`, o `loadMore` vía `loadingMoreRef`). Guard PURO `shouldYieldWindowRefresh` extraído a `utils/group-view-model.ts` (+ 3 unit tests). Se prefirió (a) sobre `max(loadedCount, GROUP_PAGE_SIZE)` (deja `prevReachedEnd` stale → bloquearía `loadMore`) y sobre `criterionVersion` (no cubre la traza: el criterio ya es el nuevo cuando el refresh lee el `loadedCount` stale). Tocó SOLO `useGroupView.ts` (+ guard/test en `group-view-model.*`) + `e2e/rodeo-grande.spec.ts` (T-RG.30 ensanchar). `tsc` verde + unit **184/184** + **E2E 6/6** (T-RG.30 verificado que CAE con el bug y verde con el fix). Reconciliado en design §8 (RESUELTO) + §6.2 (punto 4).
- **2026-07-18 — Fase 5 (E2E) implementada** (T-RG.28..33 `[x]`; impl `progress/impl_10-rodeo-grande-fase5.md`). Un solo spec nuevo `app/e2e/rodeo-grande.spec.ts` (6 tests, patrón as-built de la suite: `test`/`expect` de `./helpers/fixtures`, seed namespaced RUN_TAG, `afterAll(cleanupAll)`). Seed BATCH LOCAL (`batchSeedAnimals`, usa el `admin` exportado — NO se toca `e2e/helpers/admin.ts`) con `created_at` EXPLÍCITO por perfil (el keyset ordena por `created_at DESC, id DESC` y un batch comparte `now()`). Seed REAL (DG8): 65 (paginación), 210 (regresión del lote, el LIMIT 200 de la tab NO es reducible), 66 (masiva). **Smoke-run: 6/6 verde** (targeted, contra dist de prod + Supabase remoto). **CERO testID nuevo** (todos los selectores ya existían: buscador `getByLabel`, chips `Filtrar por categoría`/`Filtrar por sexo`, filas por idv `role=button`, count `N animales activos`, `Castrar`, checkbox de selección por idv). **FINDING (no de Fase 5): race en `useGroupView.refreshWindow`** — al ENSANCHAR un filtro (subconjunto chico → grande), un refresh silencioso concurrente (foco/avance de sync) con `limit = loadedCount` (stale, chico) y un `listSeq` más nuevo puede clobber-ear la 1ª página fresca de `loadFirstPage`, dejando la lista "pegada" en el tamaño angosto anterior. Reproducido por el E2E al filtrar durante el churn del first-sync. El E2E se diseñó NARROW-only (6→4→2), donde el race NO aplica (`limit ≥ result`). Documentado en design §8 + reportado al leader. **→ RESUELTO** el 2026-07-18 (ver la entrada de más arriba del changelog + design §8): `refreshWindow` cede ante el foreground (`shouldYieldWindowRefresh`), y T-RG.30 se extendió con un tramo de ENSANCHAR que asere la re-población.
- **2026-07-18 — Fases 3+4 implementadas** (T-RG.19..27 `[x]`; impl `progress/impl_10-rodeo-grande-fase3-4.md`). Reconciliaciones as-built: (1) `useGroupView({ establishmentId, group })` — firma nueva (ya NO un `loader`); folda buscador+chips (design §6.3, "folded" tomada); las acciones se resuelven por `group.type`. (2) **Capa de datos completada** para el chip de SEXO (RG3.9): Fase 1+2 construyó opciones de CATEGORÍA pero no de SEXO → se agregaron `buildGroupSexOptionsQuery` (local-reads.ts) + `fetchGroupSexOptions` (group-page.ts), query PROPIA scopeada (robusta ante categorías sex-neutras como `cut`, a diferencia de derivar category→sex). (3) `fetchGroupWindow` (group-page.ts) para el refresh silencioso de la ventana (`LIMIT loadedCount`, design §6.2). (4) `utils/group-view-model.ts` (PURO) + test: `canLoadMore`/`dedupById`/`intersectSearchWithChips`/`isSearchActive`/`sexFilterAvailable` — la lógica decidible del hook (los hooks importan el SDK, no corren en node:test — patrón as-built). (5) `GroupSearchBar` **CLONA** el patrón buscador/chips de `animales.tsx` (no lo extrae) para NO tocar la tab Animales (RG7.1). (6) `GroupViewScreen` dejó de ser genérico `<T>` (tipa `AnimalListItem`); `GroupAnimalsList` eliminado (absorbido en la FlatList). (7) **Edge del nav-param gigante** de la baja en tanda (design §6.4): `lote/[id].tsx`→`venta.tsx` pasa `mode`+`ids` (complemento/subconjunto, el csv más chico) en vez de enumerar cada UUID; `venta.tsx` resuelve el set del lado destino (anti-IDOR intacto). Verificación network-free: `tsc` verde + `local-reads.test.ts`/`group-page-cursor.test.ts`/`group-view-model.test.ts` **181/181**. Fase 5 (E2E) = otro run.
- **2026-07-18 — Fases 1+2 implementadas** (T-RG.1..18 `[x]`; impl `progress/impl_10-rodeo-grande-fase1-2.md`). Reconciliaciones as-built: (1) `buildGroupCandidateCountsQuery` ganó `establishmentId` en la firma (regla dura multi-tenant) + se agregó `buildGroupRodeoIdsQuery` (rodeos representados del lote sin cargar miembros) — design §5.2. (2) `enrichLocalRows`/`LocalListRow` exportados de `animals.ts` (aditivo) + util puro `group-page-cursor.ts` (`deriveNextCursor`) — design §1. (3) Firmas de gating `fetchRodeoGroupActions(est, rodeoId)` / `fetchLoteGroupActions(est, groupId)` (sin `animals`); 2 call sites de Fase 4 con ajuste MÍNIMO de la llamada (no rompen tsc; el loader→FlatList sigue Fase 4). (4) **Tests de servicio (T-RG.13/15/17)**: los services importan el SDK transitivamente (no corren en node:test — patrón as-built de `sigsa-export-service.test.ts`) → su semántica se cubre con tests in-memory de los builders (`local-reads.test.ts`) + la lógica pura del cursor (`group-page-cursor.test.ts`) + composición COUNT→`applyCandidateGating`. Verificación: `tsc` verde + `local-reads.test.ts`/`group-page-cursor.test.ts` 170/170 + utils de gating 50/50. Fases 3-5 (hooks/UI/E2E) pendientes.
- **2026-07-18** — Tasks iniciales del delta desde `requirements-rodeo-grande.md` + `design-rodeo-grande.md`. 5 fases: builders puros (keyset/scope/count/candidatos/búsqueda scopeada) + tests SQLite in-memory → services (`group-page.ts`, fix `management-groups`/`group-data`) → hooks (`useGroupView` paginado + buscador/chips) → UI (FlatList + buscador/chips fijos + wiring rodeo/lote + baja en tanda) → E2E (6 casos). Sin backend (Gate 1 no aplica). Tab Animales intacta.
