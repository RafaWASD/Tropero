# Spec 10 — Operaciones masivas por rodeo + vista de grupo — Tasks

**Status**: reconciliado 2026-06-11 (Gate 0 v2 + staleness). Gate 1 re-corrido 2026-06-11: **PASS 0H/2M/3L**; M1/M2/L1 foldeados (T-DB.4 e/f + prosa de design/requirements). **Puerta 1 APROBADA por Raf (2026-06-11) — LIM-1=mitigar con observación / LIM-2=tolerar-y-saltear foldeadas** (R13.7 + design §3.5/§4.2(4)). ⚠ Re-chequeo puntual de Gate 1 sobre el delta de LIM-2 pendiente (lo lanza el leader — design §6).
**Fuente**: `requirements.md` + `design.md` reconciliados (2026-06-11).

> Reglas (`docs/specs.md`): pasos discretos en orden, cada uno con `[ ]` + los `R<n>` que cubre. El implementer marca `[x]`; el reviewer rechaza `[ ]` sin justificación. Cada `R<n>` mapea a ≥1 test.
>
> **Fases**: el backend (Fase 1) es ejecutable ya y es **pre-requisito del cliente** (la denorm debe existir antes de cablear castración offline y el espejo). El cliente construye SOBRE el as-built DONE de spec 02 C1–C4 + spec 09 (`AnimalRow`) + spec 15 (PowerSync). La estética fina de UI es TENTATIVA (design system); el **modelo de interacción NO** (lockeado por Gate 0 v2). Numeración de migraciones: TBD al implementar (as-built en disco = 0083 → ≥0084; coordinar con el leader/terminales paralelas, design §9 D6).

---

## Fase 1 — Backend: delta `future_bull` + denorm `is_castrated` + recompute simétrico (pre-requisito)

- [x] T-DB.1 — Migration **`0085_future_bull.sql`** (design §4.1): columna `animal_profiles.future_bull boolean not null default false` + comment + trigger `animal_profiles_normalize_future_bull` (BEFORE INSERT OR UPDATE OF `future_bull, is_castrated, animal_sex`; normaliza a `false` si no-macho o castrado) + `revoke execute` de la función. Verificar el orden alfabético respecto del force de 0079. Cubre: R12.1, R12.4. **AS-BUILT: número final `0085`** (va DESPUÉS de la denorm 0084 — el trigger lee `new.is_castrated`; dependencia real, design §4 nota as-built). Orden de triggers verificado contra `pg_trigger` (T-DB.4f).
- [x] T-DB.2 — Migration **`0084_denormalize_is_castrated.sql`** (design §4.2): columna espejo + backfill idempotente + force-INSERT (`animal_profiles_force_is_castrated`) + write-through up (`animal_profiles_is_castrated_writethrough`, guard `IS DISTINCT FROM`) + propagación down (`animals_propagate_is_castrated`, guard `IS DISTINCT FROM` + **pre-filtro de rodeo vivo espejo de `rodeo_check` 0021 + `RAISE LOG` del skip** — tolerar-y-saltear, LIM-2/Puerta 1, design §4.2(4)) + `revoke execute` en las 3 funciones. Cubre: R13.3, R13.4. **AS-BUILT: número final `0084`** (se aplica ANTES que future_bull). Pre-filtro verificado literal contra 0021 (T-DB.4 pre-filtro test).
- [x] T-DB.3 — Migration **`0086_castration_recompute_symmetric.sql`** (design §4.3): `create or replace` SOLO del cuerpo de la función REAL `tg_animals_apply_castration` (0064) con guard dirección-agnóstico (`IS NOT DISTINCT FROM` → return); conservar perfil-activo-único, respeto de override, delegación a `compute_category` y `apply_auto_transition`. NO re-crear el trigger `animals_apply_castration`. Re-emitir `revoke execute` idempotente. Cubre: R13.5, R5.7. **AS-BUILT: número final `0086`.** Supersede RT2.2.6 de spec 02 → 1 aserción del test as-built de spec 02 (`tests/animal/run.cjs` T2.27 "des-castración NO revierte") reconciliada a `torito` (la nota DOC en specs de spec 02 la coordina el LEADER, D10).
- [x] T-DB.4 — Test (runner ADR-012) del **write-through y la fidelidad**: (a) UPDATE `animal_profiles.is_castrated true/false` se refleja en `animals.is_castrated`; (b) un INSERT de perfil nuevo nace fiel a `animals` aunque el payload mienta (force-INSERT); (c) la propagación down actualiza TODOS los perfiles del animal; (d) **no-loop**: la cadena completa termina (el UPDATE no rebota; contadores de history sin duplicados); (e) **propagación con rodeo muerto = SKIP esperado (LIM-2 resuelta: tolerar-y-saltear, Puerta 1 2026-06-11)**: animal con dos perfiles, uno cuyo rodeo está inactivo o soft-deleted → el UPDATE de `is_castrated` desde el otro perfil **NO aborta**: el perfil propio queda aplicado (y `animals.is_castrated` actualizado vía write-through), el perfil huérfano queda **salteado** por el pre-filtro (conserva su valor stale — y su `future_bull`, que tampoco se auto-limpia) y el trigger emite `RAISE LOG`; sub-aserción de convergencia: reactivar el rodeo huérfano + nuevo flip de `is_castrated` del animal ⇒ el perfil antes salteado se actualiza (design §4.2(4), requirements §Limitaciones LIM-2); (f) **orden de triggers contra `pg_trigger` (Gate 1 v2 L1)**: aserción de que los BEFORE relevantes de `animal_profiles` quedan en orden alfabético `animal_profiles_force_animal_identity` → `animal_profiles_force_is_castrated` → `animal_profiles_identity_check` → `animal_profiles_normalize_future_bull` (consultando `pg_trigger`, no solo por convención de nombres — blinda contra un rename futuro silencioso). Cubre: R13.3, R13.4. **AS-BUILT: el "segundo perfil" del animal compartido se modela NO-activo (`status='transferred'`) porque `animal_profiles_active_animal_unique` (unique parcial `(animal_id) WHERE status='active'`) permite a lo sumo UN perfil activo por animal global; la propagación (sin filtro de status, estilo 0079) lo alcanza igual.**
- [x] T-DB.5 — Test del **recompute simétrico**: macho >1 año `torito` → `is_castrated=true` ⇒ `novillito`; revert `false` ⇒ `torito`; macho ≥730d `toro` → castrar ⇒ `novillo`, revert ⇒ `toro`; en `ternero` no transiciona en ninguna dirección; con `category_override=true` no transiciona y el history no registra; cada transición queda en `animal_category_history` como `auto_transition`. Cubre: R13.5, R5.6, R5.7.
- [x] T-DB.6 — Test de **`future_bull`**: (a) set `true` sobre macho persiste; (b) sobre hembra se normaliza a `false`; (c) al castrar (UPDATE `is_castrated=true`, por cualquier path incl. propagación) `future_bull` queda `false` en todos los perfiles del animal; (d) default `false` en alta. Cubre: R12.1, R12.4.
- [x] T-DB.7 — Test RLS / tenant: un usuario sin rol activo en el establishment NO puede (a) UPDATEar `is_castrated`/`future_bull` del perfil, (b) insertar vacunación/destete sobre animales de ese establishment. Un usuario con rol sí (cualquier rol activo — paridad con la mutación individual). Cubre: R9.1, R9.2.
- [x] T-DB.8 — Test del **destete as-built sobre el que esta spec se apoya** (regresión de sustrato, no re-implementación): INSERT `weaning` sobre `ternera` ⇒ `vaquillona`; sobre `ternero` castrado ⇒ `novillito`; con override ⇒ no transiciona; soft-delete del weaning ⇒ recalcula (0046/0063). Cubre: R5.5.
- [x] T-DB.9 — Test de **no-regresión del gating** (delta viejo eliminado): `tg_sanitary_events_gating` NO tiene rama de castración (un `treatment` con `product_name='Castración'` NO se gatea por ningún data_key); `vaccination` sigue fail-closed contra rodeo sin `vacunacion`; NO existe `castracion` en `field_definitions`. Cubre: R7.3, R9.4.
- [x] T-DB.10 — Test de superficie (R9.4): las 4 funciones nuevas con `revoke execute` efectivo para `public/authenticated/anon` (patrón smoke-check 0055/0066); ninguna policy RLS nueva, ninguna RPC nueva expuesta. Cubre: R9.4.

## Gate 1 — Security (spec) — RE-CORRIDA obligatoria antes de Puerta 1

- [x] T-G1.1 — Correr `security_analyzer` modo `spec` sobre el delta reconciliado (design §4/§6): no-loop, análisis de poder del write-through (vs 0071), orden de triggers BEFORE, revokes, eliminación completa del delta viejo, columnas nuevas en el wire de sync (ADR-025), reemplazo de 0064 sin abrir cross-tenant. Output: `progress/security_spec_10-operaciones-rodeo.md` (nueva corrida; el PASS 2026-06-01 quedó obsoleto). **HECHO 2026-06-11: PASS 0H/2M/3L; Puerta 1 aprobada por Raf 2026-06-11 con D8/D9 validadas.**
- [ ] T-G1.2 — **Re-chequeo puntual de Gate 1 (lo lanza el leader)** sobre el delta de LIM-2 (Puerta 1): el pre-filtro de la propagación (design §4.2(4)) — espeja `rodeo_check` 0021 sin desviación, el skip no abre escritura nueva, el `RAISE LOG` no filtra datos cross-tenant (solo ids + count). Alcance acotado a `tg_propagate_is_castrated_to_profiles`; la observación automática (§3.5) no agrega superficie (as-built 0034). Cubre: condición de cierre del fold LIM-2.

## Fase 2 — Cliente: utils puros (sin design system; requiere Fase 1 para los tipos)

- [x] T-CL.1 — `bulk-candidates.ts`: base activos del grupo (rodeo/lote) + candidatos por operación: vacunación (todo + filtro categoría/sexo), castración (machos `is_castrated=false`, terneros+adultos), destete (`ternero`/`ternera` sin weaning no borrado; en lote cross-rodeo excluye rodeos sin `destete` con contador). Cubre: R1.3, R4.1, R11.2, R11.4, R7.2.
- [x] T-CL.2 — Tests de candidatos: solo `status='active'`/no-soft-deleted; hembras/castrados fuera de castración; ya-destetados fuera de destete; exclusión cross-rodeo reportada; mellizos aparecen cada uno como candidato. Cubre: R1.3, R11.2, R11.4, R7.2, R3.5.
- [x] T-CL.3 — `bulk-selection.ts`: secciones por categoría (castración: Terneros/Adultos; destete: Terneros/Terneras), defaults pre-tildados (castración: solo `ternero` con `future_bull=false`; destete: todos), todos/ninguno por sección, contador vivo, desglose por categoría + conteo ⭐ + conteo override para el bottom-sheet. Cubre: R11.3, R11.4, R11.5, R11.7, R11.8, R5.6. **AS-BUILT: estado INMUTABLE (`selected: ReadonlySet`, toggles devuelven Set nuevo). El conteo override del sheet lee `GroupProfile.categoryOverride` (campo OPCIONAL nuevo en `bulk-candidates.ts` — NO afecta la candidatura, solo el aviso R5.6; default `false`). Vacunación cae a una sola sección defensiva (no usa selección por checkbox — design §3.1).**
- [x] T-CL.4 — Tests de selección: defaults exactos por operación (⭐ y adultos sin tildar en castración; todos tildados en destete); contadores correctos al togglear sección; CTA count == seleccionados; desglose del sheet suma bien. Cubre: R11.3, R11.4, R11.5, R11.7. **AS-BUILT: + invariante `selectedCount == summarize().total`; `sectionCheckState` all/none/some; futureBullCount/overrideCount de los seleccionados.**
- [x] T-CL.5 — `bulk-idempotency.ts` (solo ops de evento): clave `(animal_profile_id, tipo, fecha)` + `id` UUIDv5 determinístico sobre namespace fijo. Test: misma clave ⇒ mismo id; claves distintas ⇒ ids distintos. Cubre: R6.1. **AS-BUILT: UUIDv5 (RFC 4122 §4.3) sobre SHA-1 PURO en JS (sin `node:crypto` ni deps nativas → corre en Hermes/web/Node, como el resto del repo que solo usa `globalThis.crypto.randomUUID`). Namespace fijo CONGELADO `BULK_EVENT_NAMESPACE`. `filterNewEventKeys(keys, existingIds)` para la exclusión local. La castración NO usa este módulo (UPDATE de estado, idempotencia semántica).**
- [x] T-CL.6 — Tests de exclusión/no-duplicación local: re-ejecutar la misma op excluye los ya procesados (vacunación: skip `already_applied`; destete/castración: ya no son candidatos); el conteo del preview/CTA refleja solo mutaciones nuevas. Cubre: R6.2, R6.3, R4.3, R4.4. **AS-BUILT: tests del UUIDv5 contra el vector canónico RFC 4122 (DNS+www.example.com) + SHA-1 contra vectores FIPS 180-1 (""/"abc"); dedup intra-batch; re-ejecución completa ⇒ 0 nuevos. (R4.3/R4.4 = el lado vacunación/skip `already_applied` se ejercita en el service de Fase 3 + UI T-UI.6; acá la barrera idempotente pura.)**
- [x] T-CL.7 — `animal-category.ts` (espejo C6): cablear el input real `isCastrated` desde `animal_profiles.is_castrated` local (retirar/degradar la inferencia RC6.2.1 con nota); test: castración offline ⇒ espejo da `novillito` sin sync; revert ⇒ `torito`. Cubre: R13.6, R10.6. **AS-BUILT: el input real se HABILITA en la función pura (`MirrorRowInput.isCastrated?: boolean | null`, con PRECEDENCIA sobre `inferIsCastrated`); la inferencia RC6.2.1 queda DEGRADADA a fallback documentado (solo cuando el caller no provee el real). El CABLEADO en los call-sites de `animals.ts` + la proyección de la columna en `local-reads.ts`/schema es **Fase 3 (T-CL.12)** — hasta entonces el caller no pasa el real → fallback (cero regresión de C6, 75/75 verde). Banner ANTI-DRIFT del header actualizado.**

## Fase 3 — Cliente: services + hooks (PowerSync as-built de spec 15)

- [x] T-CL.8 — `bulk-operations.ts`: genera y encola N mutaciones locales en batches (~100, sin bloquear UI): vacunación → INSERTs `sanitary_events 'vaccination'` (id UUIDv5, `campaign_id NULL`, patrón `events.ts`); destete → INSERTs `reproductive_events 'weaning'` (id UUIDv5, uno por ternero); castración → UPDATEs `animal_profiles SET is_castrated=1, future_bull=0` **+ observación automática "Castrado" por animal** (reuso `addObservation`/`buildAddObservationInsert`, `establishment_id` del perfil, id random — design §3.5) → **2 CrudEntries por animal**. Cubre: R3.1, R3.2, R3.3, R3.4, R3.5, R13.7, R10.1, R10.5. **AS-BUILT: la DECISIÓN (qué statements, batches, idempotencia, 2-CrudEntries) vive en `app/src/utils/bulk-operations-plan.ts` (PURO, testeable sin SDK — `planVaccination`/`planWeaning`/`planCastration` + `drainBulkPlan`); el service `app/src/services/bulk-operations.ts` es el wrapper de I/O (`runLocalWrite` + `InteractionManager.runAfterInteractions` entre batches). Builders nuevos en `local-reads.ts`: `buildAddVaccinationInsert` (campaign_id NO se setea → NULL), `buildAddWeaningInsert` (created_at de cliente, desempate del mismo event_date), `buildExistingVaccinationIdsQuery`/`buildExistingWeaningIdsQuery` (barrera idempotente local), `buildProfileEstablishmentsQuery` (establishment del perfil batched, R13.7). La observación reusa `buildAddObservationInsert` (id RANDOM — design §3.5, NO el UUIDv5 de evento).**
- [x] T-CL.9 — `bulk-operations.ts`: progreso "X de N sincronizados" + rechazos por animal con motivo, sobre el canal de status/error de `uploadData` (spec 15); mutaciones independientes (sin rollback de exitosas). Cubre: R10.2, R10.3, R10.4. **AS-BUILT: el ENCOLADO local reporta progreso por `onProgress({done,total})` + rechazos LOCALES por animal (`DrainRejection`); el "X de N SINCRONIZADOS" y los rechazos de SYNC por animal los superficia `uploadData` as-built (spec 15 R8.1) — el service devuelve el `BulkPlan` (mutaciones por animal) para que la UI (Fase 4) ate un rechazo de CrudEntry a su profileId. NO se reimplementa el canal de uploadData.**
- [x] T-CL.10 — Test: fallo de sync a mitad ⇒ exitosas persisten, fallidas reportadas por animal; re-intento no duplica (UUIDv5 / no-op de estado). Cubre: R10.2, R10.3, R6.1, R6.3. **AS-BUILT: `bulk-operations-plan.test.ts` ejercita `drainBulkPlan` con un writer que falla a mitad ⇒ las exitosas encolan (sin rollback), la fallida se reporta por animal; castración con la 2da CrudEntry (observación) fallando ⇒ el UPDATE no se re-ejecuta + animal reportado; re-ejecución con ids ya presentes ⇒ plan vacío ⇒ drenado sin writes (R6.3). El fallo de SYNC real es el camino as-built de uploadData (spec 15, ya testeado allá).**
- [x] T-CL.11 — `animals.ts`: `setCastrated(profileId, value)` (incluye `future_bull=0` cuando `value=true`; **encadena la observación automática de design §3.5 — UPDATE + INSERT, 1+1 CrudEntries**) + `setFutureBull(profileId, value)` (sin observación). Tests unit de los statements generados. Cubre: R13.1, R13.4, R13.7, R12.2, R12.4. **AS-BUILT: `setCastrated` resuelve el `establishment_id` del PERFIL (`buildProfileEstablishmentQuery`) → UPDATE (`buildSetCastratedUpdate`) → INSERT observación (`buildAddObservationInsert`, texto de `castration-copy.ts`, author_id OMITIDO). `setCastrated(true)` → `is_castrated=1, future_bull=0`; `setCastrated(false)` → solo `is_castrated=0` (NO toca future_bull). `setFutureBull` = UN UPDATE de future_bull, sin observación. Statements testeados en `local-reads.test.ts` (incl. ejecución real contra SQLite in-memory).**
- [x] T-CL.12 — `powersync/schema.ts`: declarar `is_castrated` y `future_bull` en `animal_profiles` local; verificar que `est_animal_profiles` (SELECT *) las trae (nota operativa design §4.4). Cubre: R13.3, R12.1. **AS-BUILT: declaradas en `schema.ts` (`column.integer`); el GUARD de `schema.test.ts` (`COLUMNS_READ_BY_BUILDERS.animal_profiles`) actualizado + test dedicado. Los SELECT de lista/detalle/búsqueda (`LOCAL_LIST_SELECT`/detail synced) proyectan `is_castrated` (espejo C6 real) + `future_bull` (detalle, badge ⭐); las ramas overlay proyectan `0` constante (alta optimista nace entero). **Esto COMPLETA el cableado de T-CL.7**: `computeMirrorOverrides` ahora pasa el `is_castrated` REAL a `computeDisplayOverrides` con precedencia (antes caía al fallback). `est_animal_profiles` hace SELECT * (sync-streams/rafaq.yaml) → las columnas bajan sin tocar el YAML. `AnimalDetail` expone `isCastrated`/`futureBull` (para la ficha de Fase 4).**
- [x] T-CL.13 — Tests de la **observación automática** (R13.7, design §3.5): (a) castrar (vía `setCastrated(true)` y vía la masiva) ⇒ se encola un INSERT `animal_events` `event_type='observacion'` con texto "Castrado", `establishment_id` del perfil, **sin** `author_id` en el payload (lo fuerza el trigger 0034 al subir → autor = usuario actual); (b) revertir (`setCastrated(false)`) ⇒ observación "Corrección: marcado como no castrado" (simetría); (c) la observación aparece en el timeline local (fetchTimeline) offline; (d) masiva de N animales ⇒ exactamente N observaciones + N UPDATEs; (e) `setFutureBull` NO genera observación. Cubre: R13.7, R13.2. **AS-BUILT: (a)/(b) `castration-copy.test.ts` pinea el copy simétrico + `local-reads.test.ts` verifica que `buildAddObservationInsert` NUNCA manda author_id; (c) la observación se inserta en `animal_events` (la MISMA tabla que `buildTimelineQuery`/`fetchTimeline` lee con `deleted_at IS NULL`) → aparece local offline (verificado con SQLite in-memory en el plan-test); (d) `bulk-operations-plan.test.ts` ejecuta la masiva de N=3 contra SQLite ⇒ N UPDATEs + N observaciones queryables; (e) `setFutureBull`/`buildSetFutureBullUpdate` NO toca `animal_events` ni `is_castrated` (verificado en `local-reads.test.ts`).**

## Fase 4 — Cliente: UI (interacción lockeada Gate 0 v2; estética con design system)

- [x] T-UI.1 — Vista de grupo (`rodeo/[id]`, `lote/[id]`): metadatos + config + lista (reusa `AnimalRow`) + `GroupActionsBar` (Vacunar/Destetar gated por `rodeo_data_config`; lote: "algún rodeo"; Castrar siempre; verbos pelados). Cubre: R1.1, R1.2, R1.4, R1.5, R1.6, R7.1. **AS-BUILT (chunk UI-A, 2026-06-11):** `app/app/rodeo/[id].tsx` + `app/app/lote/[id].tsx` (rutas finas) sobre un scaffold presentacional compartido `app/src/components/GroupViewScreen.tsx` + `GroupViewBits.tsx` (`GroupMetaHeader`/`GroupAnimalsList`) + el hook `app/src/hooks/useGroupView.ts` (orquesta load + foco + sync-reload). Gating: util PURO `app/src/utils/group-actions.ts` (`resolveGroupActions`/`buildRodeoGating`, fail-closed; lote = "algún rodeo" via `resolveGroupActions(gatings[])`) + service `app/src/services/group-data.ts` (`fetchRodeoGroupActions`/`fetchLoteGroupActions`, lee `field_definitions` data_key→field_id + `rodeo_data_config` enabled del SQLite local, offline). Componente `app/src/components/GroupActionsBar.tsx`: las 3 acciones outline, orden Vacunar→Destetar→Castrar (verbos pelados, ninguna primary — Castrar deliberada). Las acciones navegan vía `app/src/utils/group-nav.ts` (`navigateToGroupAction`) a las rutas STUB `seleccion-masiva`/`vacunacion-masiva` (la selección/vacunación reales = chunks siguientes). Tests: `group-actions.test.ts` (gating, 10). Rutas registradas en `app/app/_layout.tsx` (+ `GROUP_DESTINATIONS` para que el RootGate no las expulse en estado 'active').
- [x] T-UI.2 — Inicio rodeo-céntrico (evolución de `(tabs)/index.tsx`): cards de rodeo + cards de lote → vista de grupo. Tab `Animales` intacta (verificación explícita). Cubre: R2.1, R2.2, R2.3. **AS-BUILT:** secciones "Mis rodeos" (de `RodeoContext.available`) + "Lotes" (de `fetchManagementGroups`) con `app/src/components/GroupSummaryCard.tsx` (nombre + cabezas activas + chevron, tap → `/rodeo/[id]` | `/lote/[id]`). Conteos por grupo: `fetchRodeoHeadCounts`/`fetchGroupHeadCounts` (group-data.ts) sobre los builders nuevos `buildRodeoHeadCountsQuery`/`buildGroupHeadCountsQuery` (local-reads.ts, GROUP BY + UNION overlay + oculta exits, tests de comportamiento contra SQLite in-memory). El Stepper de onboarding queda DEBAJO de los rodeos y se OCULTA cuando los 3 pasos están completos (iter2: `app/src/utils/onboarding.ts` `allOnboardingStepsDone` puro + test, render condicional en index.tsx). La tab `Animales` (`animales.tsx`) NO se tocó (verificado). El **nombre del sistema productivo** del rodeo (R2.1) SÍ se muestra en la card ("Cría · N cabezas", prop `meta` de `GroupSummaryCard` ← `fetchProductionSystems` local; iter2 2026-06-12; solo rodeos, no lotes). La **"señal de atención"** (R2.1) se difiere (analytics = feature 07). Íconos unificados (iter2): rodeo=`Boxes`, lote=`Layers` en todo `app/`.
- [x] T-UI.3 — `AnimalRow` variante compacta: ≥56px, checkbox, "categoría · edad" (de `animal_birth_date`), badge ⭐ (solo positivo; oculto si categoría `toro`). Sin redefinir el componente. Cubre: R11.9, R12.3. **AS-BUILT:** props nuevas en `app/src/components/AnimalRow.tsx` (`compact`/`age`/`categoryCode`/`futureBull`/`checked`/`onToggle`) — la fila grande default queda intacta. Compacto: ≥`$touchMin` (56), avatar 40, subtítulo "categoría · edad", badge ⭐ `FutureBullBadge` (terracota, helper exportado `shouldShowFutureBullBadge` — solo positivo + oculto en `toro`, R12.3), slot `RowCheckbox` (para la selección futura, no usado en este chunk). Edad: util PURO `app/src/utils/animal-age.ts` (`formatAnimalAge` desde `animal_birth_date`, años/meses, tests 11). El dato por animal: `AnimalListItem` ahora expone `animalBirthDate` + `futureBull`, y `LOCAL_LIST_SELECT` proyecta `future_bull` (synced `ap.future_bull`; overlay `0`) además del `animal_birth_date` que ya traía.
- [x] T-UI.4 — Pantalla de selección (`seleccion-masiva`): secciones por categoría + todos/ninguno + contador vivo en header + orden por ID + búsqueda solo si >~20 + resaltado terracota de ⭐ tildado SIN modal (solo castración) + CTA fijo con número vivo (disabled en 0). Cubre: R11.1, R11.5, R11.6, R11.7, R11.9. **AS-BUILT (chunk UI-B, 2026-06-12): `app/app/seleccion-masiva.tsx` REEMPLAZA el stub.** Datos: `app/src/services/bulk-selection-data.ts` (`fetchGroupSelectionProfiles`, rodeo→`fetchAnimals` / lote→`fetchGroupMembers`, mergea los 3 flags que `AnimalListItem` no expone vía el builder nuevo `buildGroupCandidateFlagsQuery` en `local-reads.ts` — `is_castrated`/`category_override`/`has_weaning`=EXISTS weaning vivo synced|pending). Candidatos+selección REUSAN Fase 2 (`buildBulkCandidates`/`buildBulkSelectionState`, defaults R11.3/R11.4 intactos). Presentación PURA nueva: `app/src/utils/selection-display.ts` (`sortByIdentifier` numérico/es-AR + desempate por profileId, `shouldShowSearch`/`SEARCH_THRESHOLD=20`, `filterBySearch`, `pluralCategoryLabel`) + test. **Resaltado ⭐** = prop nueva `highlight` de `AnimalRow` (borde izq terracota + fondo $surface, lenguaje terracota-signal as-built; SIN token nuevo) cuando `operation==='castrate' && futureBull && checked`. CTA fijo abajo (thumb-zone) con número vivo (Button primary, disabled en 0). **R7.2 cableado**: para wean+lote resuelve `rodeoWeaningEnabled` (`fetchRodeoGroupActions` por rodeo distinto) → `buildBulkCandidates` excluye + cuenta; la pantalla muestra "N terneros excluidos por config del rodeo" (InfoNote). Empty-state es-AR por operación.
- [x] T-UI.5 — Bottom-sheet de confirmación: desglose por categoría + "⚠ N futuros toritos incluidos" (castración) + aviso override con acción de revertir (patrón C6) + CONFIRMAR/Volver + **copy reversible** ("Podés corregirlo después desde la ficha de cada animal") — prohibido "no se puede deshacer". Cubre: R11.8, R5.6. **AS-BUILT (chunk UI-B): `app/src/components/BulkConfirmSheet.tsx`** (presentacional, scrim `$scrim` token NUEVO en `tamagui.config.ts` = textPrimary @ 45%, sheet anclado abajo con grip). Desglose `summarizeSelection` (Fase 2) → `pluralCategoryLabel`; ⚠ futuros toritos solo castración + `futureBullCount>0`; aviso override + "Quitar la fijación…" → `revertCategoryOverride` as-built (C6, offline, RLS al subir) por animal con override, luego recarga; copy reversible literal; CONFIRMAR/Volver. Al CONFIRMAR → `applyBulkCastration`/`applyBulkWeaning` (Fase 3, REUSADOS — `author_id` NUNCA en payload) → **`app/src/components/BulkProgressPanel.tsx`** (in-screen): "Castrando X de N…" (onProgress local) → "N listos, se sincronizan en segundo plano" + rechazos LOCALES por animal (R10.3; el "X de N sincronizados" por animal lo superficia uploadData as-built) → Listo. Retry en error.
- [ ] T-UI.6 — Vacunación masiva (`vacunacion-masiva`): pre-config + filtro + preview "N eventos sobre M animales" + "K saltados (motivos)" + confirmación + pantalla de progreso. Cubre: R4.1, R4.2, R4.3, R4.4, R10.4.
- [ ] T-UI.7 — Ficha: fila "Castrado Sí/No" (solo machos) con confirmación que anticipa el recálculo (espejo C6) + toggle ⭐ futuro torito (solo ficha, no alta) + sin evento **tipado** en el timeline: el flip aparece como **observación automática** ("Castrado" / "Corrección: marcado como no castrado" — R13.7) y, cuando hubo transición, además como cambio de categoría. Cubre: R13.1, R13.2, R13.7, R12.2, R12.3.
- [ ] T-UI.8 — Corrección individual: evento de vacunación/destete editable/borrable por owner/autor desde la ficha (reuso spec 02 R6.8.1, recalcula categoría); castración corregible vía T-UI.7. Cubre: R4.5.
- [ ] T-UI.9 — E2E (Playwright, `app/e2e/`): castración masiva — selección (defaults: terneros comunes tildados, ⭐ y adultos no) → tildar un ⭐ (resaltado, sin modal) → CTA con número → bottom-sheet (desglose + ⚠ + copy reversible) → confirmar → categoría visible novillito **+ observación "Castrado" visible en el timeline del animal**; revert desde la ficha → torito **+ observación "Corrección: marcado como no castrado"**. Cubre: R11.x, R13.1, R13.5, R13.7, R10.6.
- [ ] T-UI.10 — E2E: destete masivo — todos los terneros/as pre-tildados, mellizos generan un weaning cada uno, transición visible (vaquillona/torito), animal con override avisado y sin transición. Cubre: R11.4, R3.2, R3.5, R5.5, R5.6.
- [ ] T-UI.11 — E2E: vacunación masiva — preview → confirmar → N eventos; re-ejecutar ⇒ 0 nuevos (skip). Cubre: R3.1, R4.2, R6.3.

## Mapa de cobertura R → tasks

| R<n> | Tasks |
|---|---|
| R1.1 / R1.2 / R1.4 / R1.5 / R1.6 | T-UI.1 |
| R1.3 | T-CL.1, T-CL.2 |
| R2.1 / R2.2 / R2.3 | T-UI.2 |
| R3.1 | T-CL.8, T-UI.11 |
| R3.2 / R3.5 | T-CL.8, T-CL.2, T-UI.10 |
| R3.3 / R3.4 | T-CL.8 |
| R4.1 / R4.2 | T-CL.1, T-UI.6, T-UI.11 |
| R4.3 / R4.4 | T-CL.6, T-UI.6 |
| R4.5 | T-UI.8 |
| R5.5 | T-DB.8, T-UI.10 |
| R5.6 | T-DB.5, T-CL.3, T-UI.5, T-UI.10 |
| R5.7 | T-DB.3, T-DB.5 |
| R6.1 / R6.2 / R6.3 | T-CL.5, T-CL.6, T-CL.10, T-UI.11 |
| R7.1 | T-UI.1 |
| R7.2 | T-CL.1, T-CL.2 |
| R7.3 | T-DB.9 |
| R9.1 / R9.2 | T-DB.7 |
| R9.3 | (heredado SEC-SPEC-03 — sin task nueva; verdad documentada en design §5) |
| R9.4 | T-DB.9, T-DB.10 |
| R10.1 / R10.5 | T-CL.8 |
| R10.2 / R10.3 / R10.4 | T-CL.9, T-CL.10, T-UI.6 |
| R10.6 | T-CL.7, T-UI.9 |
| R11.1 / R11.5 / R11.6 / R11.7 / R11.9 | T-CL.3, T-CL.4, T-UI.4 |
| R11.2 / R11.4 | T-CL.1, T-CL.2, T-UI.10 |
| R11.3 | T-CL.3, T-CL.4 |
| R11.8 | T-CL.3, T-UI.5 |
| R12.1 | T-DB.1, T-DB.6, T-CL.12 |
| R12.2 / R12.3 | T-UI.3, T-UI.7 |
| R12.4 | T-DB.1, T-DB.6, T-CL.11 |
| R13.1 / R13.2 | T-CL.11, T-CL.13, T-UI.7 |
| R13.3 | T-DB.2, T-DB.4, T-CL.12 |
| R13.4 | T-DB.2, T-DB.4, T-CL.11 |
| R13.5 | T-DB.3, T-DB.5, T-UI.9 |
| R13.6 | T-CL.7 |
| R13.7 | T-CL.8, T-CL.11, T-CL.13, T-UI.7, T-UI.9 |

## Changelog

- **2026-06-01 (s21)**: versión original (delta `castracion` + marcador + N eventos para las 3 ops) + fixes del Gate 1 s21.
- **2026-06-11 — RECONCILIACIÓN COMPLETA**: Fase 1 reescrita al delta nuevo (future_bull / denorm is_castrated / recompute simétrico; eliminadas T-DB.1–T-DB.3 viejas del data_key `castracion` — reemplazadas por test de no-regresión T-DB.9); eliminada la sección "TENTATIVO/DEFERIDO Facundo" (T-DEF.1/2 resueltas as-built 0059–0064; T-DEF.3 sigue fuera de MVP, nota en requirements US-2); Fase 2–4 reescritas al modelo de selección explícita (Gate 0 v2) + as-built (AnimalRow, Expo Router, spec 15, C6); Gate 1 marcado para re-corrida.
- **2026-06-11 — Fix-loop Gate 1 v2 (PASS 0H/2M/3L)**: T-DB.4 ampliada con (e) propagación con rodeo inactivo/soft-deleted → aborto fail-closed esperado, visible vía R10.3 (M1/LIM-2) y (f) aserción del orden de triggers BEFORE contra `pg_trigger` (L1). Sin tasks nuevas ni cambios de diseño.
- **2026-06-11 — Fix-loop #2 (Puerta 1 APROBADA por Raf — LIM-1/LIM-2)**: **LIM-1**: T-CL.8 y T-CL.11 ampliadas con la observación automática (2 CrudEntries/animal en masiva, 1+1 en ficha); nueva **T-CL.13** (tests: castrar → observación "Castrado" con autor; revert → "Corrección: marcado como no castrado"; N obs en masiva; `setFutureBull` sin obs); T-UI.7/T-UI.9 verifican la observación en el timeline; R13.7 en el mapa. **LIM-2**: T-DB.2 incorpora el pre-filtro + `RAISE LOG`; **T-DB.4(e) reescrita** de "aborto esperado" a "skip esperado + perfil propio aplicado + huérfano stale + log + convergencia al reactivar". T-G1.1 cerrada (PASS + Puerta 1); nueva **T-G1.2**: re-chequeo puntual de Gate 1 sobre el delta de LIM-2 (lo lanza el leader).
