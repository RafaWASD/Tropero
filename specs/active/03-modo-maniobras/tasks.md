# Spec 03 — MODO MANIOBRAS — Tasks

**Status**: `spec_ready` — **APROBADA por Raf (Puerta 1, 2026-05-30)**. Gate 1 PASS. **Backend DONE (`0050-0057`, Fases 1-2 `[x]`)**. Cliente (Fases 3/4) **reconciliado JIT contra as-built 2026-06-13** y decompuesto en chunks **M1-M4** (abajo). **+ Fold sesión 26 (2026-06-13/14): 2 maniobras nuevas (Antiparasitario R6.13/R6.14 + Antibiótico R6.15) en M3 + sub-chunk M3.0-BACKEND (delta de gating `0054` → Gate 1 OBLIGATORIO + decisión abierta D10). + Fold sesión 27 (2026-06-17): chunk M6 — Circunferencia escrotal (US-14, R14.1–R14.18) — M6-BACKEND (tabla typed + data_key + gating + RLS + sync → Gate 1 OBLIGATORIO) + M6-CLIENTE (rueda 🔴 manga, Gate 2 por sub-chunk).**
**Fecha**: 2026-05-30 (sesión 18) · **reconciliación JIT del cliente 2026-06-13** · **fold antiparasitario/antibiótico sesión 26 (2026-06-13/14)**.

Plan de implementación paso a paso. Cada tarea tiene su criterio de aceptación y los `R<n>` que cubre. El orden importa: dependencias hacia adelante. **El backend está implementado y done** (`0050_sessions` … `0057_soft_delete_maneuver_preset`); **el cliente NO requiere migraciones nuevas** (enum `tacto_vaquillona`, FK `session_id`, triggers de gating capa 2 INSERT + UPDATE dientes/CUT, tenant-check cross+intra, fail-closed — todo ya existe). Las Fases 3/4 (cliente) se reorganizan abajo en chunks **M1-M4**.

> **Postura de gates (2026-06-13).** El cliente es **frontend puro** (el backend ya está gateado y done) → **Gate 1 N/A por chunk**; **Gate 2 (code) SÍ por chunk** (reviewer + autorrevisión adversarial del implementer). Si en algún chunk apareciera la necesidad de una migración nueva (no debería: enum/FK/triggers ya existen `0050-0057`), se marca **explícito como hallazgo** para que el leader corra un **Gate 1 puntual** antes de aplicarla — no se aplica DDL sin pasar por el clasificador.

> **EXCEPCIÓN (sesión 26): M3.0-BACKEND reabre la capa de gating → Gate 1 OBLIGATORIO.** Las 2 maniobras nuevas (Antiparasitario R6.13/R6.14, Antibiótico R6.15) son `silent_apply` y el backend ya las soporta SIN schema change (`sanitary_event_type` `0027` ya tiene `deworming`/`treatment`; `field_definitions` `0018` ya tiene `antiparasitario_interno`/`antiparasitario_externo`/`antibiotico`). El **único delta backend** es extender `tg_sanitary_events_gating` (`0054`, hoy solo gatea `vaccination`) para gatear también `deworming`/`treatment` (R7.7) → **modifica un trigger de spec 02 + reabre la capa de gating → Gate 1 (security modo `spec`) OBLIGATORIO sobre el delta ANTES de aplicar**. Ver M3.0-BACKEND. **D10 CERRADO (Raf 2026-06-14): el antiparasitario NO distingue interno/externo de forma estructurada → gating OR pura del `deworming`** (basta `antiparasitario_interno` O `antiparasitario_externo` enabled), sin `route` ni columna nueva. La migración (`0091`, AS-BUILT) ya está escrita; el deploy a la DB compartida lo gatea el leader tras Gate 1 + OK de Raf.

> **Orden de arranque del cliente (decisión de Raf 2026-06-13): el chunk M2 arranca por un DESIGN SPIKE** de la pantalla de manga (carga rápida + paso de maniobra) con los **botones gigantes** (R5.2/R12.2/R12.5), **antes** de la plomería. Visual primero, vetado con design-review (análisis pro fundamentado: Fitts, jerarquía, densidad, contraste, recorte de descendentes en títulos), iterado con el implementer; a Raf solo se le muestra lo aceptable.

> **Antes de empezar (coordinación, ver design §9)**: **T1.3** (`session_id` sobre tablas de spec 02), **T1.4** (extensión enum repro de spec 02), **T1.5** (gating `BEFORE INSERT`/`BEFORE UPDATE` sobre tablas de spec 02) **modifican tablas/enums de otra spec**. Confirmar con Raf / la terminal de backend (decisiones abiertas D1, D2, D4, **D8** en design.md) antes de aplicarlas. Si Raf prefiere que `session_id` entre como delta de spec 02, se reasigna esa migración allá. **Coordinación (terminal paralela) de numeración de migraciones**: hay otra terminal trabajando en el repo; el bloque 0050+ debe coordinarse antes de crear archivos para evitar colisión de números entre migraciones concurrentes. El implementer reserva el rango contra el as-built **y** contra lo que la otra terminal tenga en vuelo, no solo contra el árbol.

> **⚠️ Pre-flight (CORREGIDO por Gate 1, SEC-SPEC-03-02).** Una versión previa de esta spec daba por **existentes as-built de spec 02** las funciones `public.current_animal_rodeo(uuid)` y `public.get_rodeo_data_keys(...)`. **ES FALSO**: verificado contra el árbol (migrations 0001-0049, 0 hits para ambas). El implementer **NO debe** asumir que existen ni pararse esperándolas. El rodeo real del animal se resuelve **inline** vía `select rodeo_id from public.animal_profiles where id = ? and deleted_at is null` (perfil activo) — esa resolución vive **dentro de** `assert_data_keys_enabled` (se crea en spec 03, T1.5), no en una función separada. Funciones que SÍ existen as-built y se reusan: `public.establishment_of_profile(uuid)` (`0023`), `public.tg_force_created_by_auth_uid()` (`0043`), `public.tg_set_updated_at_generic()`. El próximo número de migración libre es **0050** (último as-built: `0049`).

**Fases**:
- **Fase 1 — Schema + triggers + RLS**: migrations `0050..0057`. Backend puro. **DONE.**
- **Fase 2 — Tests reales contra DB remota**: suite `supabase/tests/maneuvers/`. **DONE.**
- **Fases 3/4 — Cliente**: decompuestas en chunks **M1-M4** (abajo). M2 arranca por **design spike**. Consume el as-built de spec 04 (BLE) / 09 (find-or-create) / 15 (PowerSync) / 02 (eventos, espejo de categoría) — ver `design.md` §1.1 para los nombres reales.
- **Chunk M5 — datos/maniobras CUSTOM** (foldado 2026-06-13 desde `context-m5-custom-maniobras.md`): **M5-BACKEND** (reabre schema/RLS → **Gate 1 OBLIGATORIO**) + **M5-CLIENTE** (Gate 2 por sub-chunk). US-13 (R13.1–R13.24). Detalle en `design.md` §11.

---

## Fase 1 — Schema, triggers y RLS

### [x] T1.1 — Migration `0050_sessions.sql` (entidad sesión)
Satisface: R1.1, R1.9, R1.10, R1.11, R10.6, R10.7, R11.1, R11.2, R11.3.
Detalle: enum `session_status ('active','closed')` + tabla `public.sessions` (design §2.1: `id`, `establishment_id`, `rodeo_id`, `config jsonb`, `status`, `work_lot_label`, `animal_count`, `event_count`, `notes`, `created_by`, `started_at`, `ended_at`, timestamps, `deleted_at`). **CHECK `sessions_config_size` (`octet_length(config::text) < 16384`)** — SEC-SPEC-03-06. Indexes `by_est`, `by_rodeo`, `active`. Triggers `sessions_force_created_by` (reusa `tg_force_created_by_auth_uid` de spec 02 `0043`), `sessions_set_updated_at`, `tg_sessions_rodeo_check` (rodeo activo del mismo establishment). RLS canónico (`has_role_in` SELECT/INSERT/UPDATE; sin DELETE). `grant select, insert, update to authenticated`.
Aceptación: owner crea sesión sobre rodeo propio → OK; sesión sobre rodeo de otro establishment → `23514`; `field_operator`/`veterinarian` activos crean sesión → OK; `userB` sin rol → 0 filas; `created_by` queda en `auth.uid()` aunque el payload mande otro uid; `config` > 16 KiB → falla el CHECK.
Archivos: `supabase/migrations/0050_sessions.sql`.

### [x] T1.2 — Migration `0051_maneuver_presets.sql` (presets por establishment)
Satisface: R2.1, R2.4, R2.5, R11.1, R11.3.
Detalle: tabla `public.maneuver_presets` (design §2.2): `id`, `establishment_id`, `name` (check no vacío), `config jsonb`, `created_by`, timestamps, `deleted_at`. **CHECK `maneuver_presets_config_size` (`octet_length(config::text) < 16384`)** — SEC-SPEC-03-06. Index `by_est`. Triggers `force_created_by` + `set_updated_at`. RLS por establishment (`has_role_in` SELECT/INSERT/UPDATE). Grants.
Aceptación: cualquier rol operativo activo crea/lee/edita presets del establecimiento; `userB` sin rol → 0 filas; name vacío → falla; `config` > 16 KiB → falla el CHECK.
Archivos: `supabase/migrations/0051_maneuver_presets.sql`.

### [x] T1.3 — Migration `0052_event_session_fk.sql` (FK de eventos → sesión) ⚠️ toca constraints de tablas de spec 02
Satisface: R5.11, R7.4, R11.4. (Resuelve C2; ver D1.)
Detalle: la **columna `session_id uuid` YA EXISTE** en las 5 tablas (0025-0029, sin FK, comentada "sessions no existe aún"). Esta migración **agrega la FK** (no la columna): `ALTER TABLE … ADD CONSTRAINT … FOREIGN KEY (session_id) REFERENCES public.sessions(id) ON DELETE SET NULL` en `weight_events`, `reproductive_events`, `sanitary_events`, `condition_score_events`, `lab_samples`. Index parcial `by_session` por tabla. Función `tg_event_session_tenant_check` (SECURITY DEFINER, search_path public): si `session_id` no es null valida (i) **cross-tenant**: `sessions.establishment_id == establishment_of_profile(animal_profile_id)` (`23514` si difiere, `23503` si la sesión no existe/está borrada); (ii) **intra-tenant (SEC-SPEC-03-04)**: `sessions.status='active'` (`23514` si no) y el rodeo del animal (`animal_profiles.rodeo_id` del perfil activo) == `sessions.rodeo_id` (`23514` si difiere — R1.1 "una sesión = un rodeo"; el flujo R4.4 mueve el animal de rodeo ANTES de cargar eventos). `revoke execute … from public, authenticated, anon`. Trigger `BEFORE INSERT OR UPDATE OF session_id` en las 5 tablas.
Aceptación: evento con `session_id` de la misma sesión/establishment/rodeo y sesión activa → OK; con `session_id` de otro establishment → `23514`; con `session_id` inexistente → `23503`; con `session_id` de una sesión `closed` → `23514`; con animal de otro rodeo del mismo establishment → `23514`; evento sin `session_id` → OK; la función no es invocable como RPC por `authenticated`.
Archivos: `supabase/migrations/0052_event_session_fk.sql`.

### [x] T1.4 — Migration `0053_tacto_vaquillona.sql` (extensión enum repro) ⚠️ toca enum de spec 02
Satisface: R5.13, R6.3.
Detalle: `ALTER TYPE public.repro_event_type ADD VALUE IF NOT EXISTS 'tacto_vaquillona'`; enum `heifer_fitness_result ('apta','no_apta','diferida')`; `ALTER TABLE reproductive_events ADD COLUMN heifer_fitness heifer_fitness_result`. Aislar el `ADD VALUE` en su propia migración si el motor lo exige (no transaccionable con otro DDL).
Aceptación: insertar `reproductive_events (event_type='tacto_vaquillona', heifer_fitness='apta')` → OK; valor de `heifer_fitness` fuera del enum → falla.
Archivos: `supabase/migrations/0053_tacto_vaquillona.sql`.

### [x] T1.5 — Migration `0054_gating_db_layer.sql` (gating capa 2, ADR-021) ⚠️ toca tablas de spec 02
Satisface: R7.1, R7.2, R7.3, R7.4, R7.5, R7.6.
Detalle: función `assert_data_keys_enabled(p_animal_profile_id uuid, p_data_keys text[])` (SECURITY DEFINER, search_path public) que **resuelve el rodeo INLINE** (`select rodeo_id from public.animal_profiles where id = p_animal_profile_id and deleted_at is null` — NO usa `current_animal_rodeo`, que no existe; SEC-SPEC-03-02) y verifica que **todos** los `data_keys` estén `enabled=true` en `rodeo_data_config` (join a `field_definitions`); rechaza `23514` si falta alguno. **FAIL-CLOSED explícito (SEC-SPEC-03-03)**: si `v_rodeo IS NULL` (perfil inexistente/soft-deleted) → `raise exception … errcode '23514'`; **prohibido** un `if v_rodeo is null then return;` (fail-open). `revoke execute … from public, authenticated, anon`.
Triggers `BEFORE INSERT` por tabla que ramifican por `event_type`/`sample_type` → `data_key(s)` según el mapeo R5.4 (design §4): `condition_score_events`→`condicion_corporal`; `weight_events`→`peso`; `lab_samples` `blood`→`brucelosis`, `scrape_*`→`raspado_toros`; `sanitary_events` `vaccination`→`vacunacion`; `reproductive_events` `tacto`→`['prenez','tamano_prenez']`, `tacto_vaquillona`→`tacto_vaquillona`, `service`+IA→`inseminacion`. Parto/aborto/destete NO se gatean.
**Gating del destino UPDATE dientes/CUT (SEC-SPEC-03-01, R7.5)**: función `tg_animal_profiles_teeth_gating()` (SECURITY DEFINER, EXECUTE revocado) que invoca `assert_data_keys_enabled(NEW.id, array['dientes'])`; trigger `BEFORE UPDATE OF teeth_state, is_cut, category_id ON public.animal_profiles` con guarda `WHEN (new.teeth_state IS DISTINCT FROM old.teeth_state OR new.is_cut IS DISTINCT FROM old.is_cut)` para NO gatear UPDATE de lote (R9.2) ni de rodeo (R4.4). Modifica una tabla de spec 02 → coordinación + D8 (default = enforce; Raf puede excluir, documentar).
Aceptación: ver T2.4/T2.4b/T2.5/T2.11; la función no es RPC público.
Archivos: `supabase/migrations/0054_gating_db_layer.sql`.

### [x] T1.6 — Migration `0055_check_grants.sql` (housekeeping)
Satisface: housekeeping, R11.4.
Detalle: consolidar grants de `sessions`, `maneuver_presets` y confirmar `revoke execute` de los helpers internos (`assert_data_keys_enabled`, `tg_event_session_tenant_check`). Patrón de `0038_check_grants.sql` / `0042_revoke_internal_function_grants.sql` de spec 02.
Aceptación: `node scripts/check.mjs` verde; SELECT desde cliente authenticated sobre `sessions`/`maneuver_presets` funciona; los helpers internos NO son invocables por `authenticated`.
Archivos: `supabase/migrations/0055_check_grants.sql`.

---

## Fase 2 — Tests reales contra DB remota

> Patrón heredado de spec 01/02: tests Node nativo en `supabase/tests/`, login con users de prueba, ejercen policies y triggers, limpian al final. Setup reusa helpers de `supabase/tests/animal/` (createAnimal, createRodeo).

### [x] T2.1 — Suite `supabase/tests/maneuvers/run.cjs` (esqueleto)
Satisface: housekeeping.
Detalle: runner Node nativo siguiendo `supabase/tests/animal/run.cjs`. Setup: `userA` (owner estA) + `userB` (owner estB) + un rodeo `(bovino,cría)` por establishment (con `rodeo_data_config` pre-poblado por el trigger de spec 02). Helpers: `createSession(client, {establishmentId, rodeoId})`, `createPreset(client, {establishmentId, name, config})`.
Aceptación: skeleton corre con 0 tests y termina limpio.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.2 — Tests: RLS de `sessions`
Satisface: R1.1, R1.3, R10.7, R11.1, R11.3.
Detalle: owner crea sesión OK; sesión sobre rodeo ajeno → `23514`; `userB` sin rol no la ve (0 filas) ni la crea; `field_operator` activo crea sesión OK; cerrar sesión (`status='closed'`) por rol activo OK; no hay DELETE de cliente.
Aceptación: 6 tests verdes.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.3 — Tests: `created_by` forzado server-side en `sessions`/`presets`
Satisface: R11.2.
Detalle: insertar `session` y `preset` pasando `created_by = <uid de otro usuario>` en el payload → con `service_role`, la fila resultante tiene `created_by = <uid del caller>` (trigger `tg_force_created_by_auth_uid`).
Aceptación: 2 tests verdes.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.4 — Tests: gating capa 2 — accept/reject por data_key (INSERT a eventos)
Satisface: R7.1, R7.3, R5.4.
Detalle: por cada maniobra gateada, insertar el evento tipado sobre un animal cuyo rodeo tiene el/los `data_key` **enabled** → OK; y sobre un rodeo con ese `data_key` **disabled** (owner hace `update rodeo_data_config set enabled=false`) → `23514`. Cubrir: `condicion_corporal`, `peso`, `brucelosis` (blood), `raspado_toros` (scrape), `vacunacion`, `prenez`+`tamano_prenez` (tacto), `tacto_vaquillona`, `inseminacion`. Caso multi-key: tacto con `prenez` enabled pero `tamano_prenez` disabled → falla (requiere ambos). Caso bypass: insertar evento gateado directo por PostgREST (saltando la UI) sobre rodeo disabled → falla (defensa en profundidad).
Aceptación: ~10 tests verdes (accept + reject por maniobra + multi-key + bypass).
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.4b — Tests: gating capa 2 **fail-closed** (no-bypass ante rodeo no resoluble)
Satisface: R7.6 (SEC-SPEC-03-03).
Detalle: (a) insertar un evento gateado (p. ej. `condition_score_events`) sobre un `animal_profile_id` **soft-deleted** (`deleted_at IS NOT NULL`) → la resolución inline de rodeo da NULL → `assert_data_keys_enabled` debe **rechazar** (`23514`), NUNCA pasar; (b) insertar sobre un `animal_profile_id` **inexistente** → rechazo (`23514`); (c) caso de control: mismo evento sobre perfil activo con `data_key` enabled → OK. Verifica que NO hay early-return fail-open.
Aceptación: 3 tests verdes (soft-deleted → reject; inexistente → reject; control → OK).
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.5 — Tests: binding data_key↔destino no roto
Satisface: R7.2 (riesgo de binding ADR-021).
Detalle: test que verifica que **cada `data_key` literal** usado en los triggers de gating (`condicion_corporal`, `peso`, `brucelosis`, `raspado_toros`, `vacunacion`, `prenez`, `tamano_prenez`, `tacto_vaquillona`, `inseminacion`, **`dientes`** — este último del trigger `BEFORE UPDATE` de SEC-SPEC-03-01) **existe** en `field_definitions`. Si un `data_key` se renombrara, este test falla. Complementa T2.4 (comportamiento) verificando el contrato del mapeo.
Aceptación: 1 test que recorre los data_keys del mapeo y confirma presencia en `field_definitions`.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.6 — Tests: tenant-check de `session_id` en eventos (cross + intra-tenant)
Satisface: R5.11, R7.4, R1.1 (SEC-SPEC-03-04).
Detalle: insertar `weight_event` con `session_id` de la misma sesión/establishment/rodeo y sesión **active** → OK; con `session_id` de una sesión de otro establishment → `23514` (cross-tenant); con `session_id` inexistente → `23503`; evento sin `session_id` → OK. **Intra-tenant (SEC-SPEC-03-04)**: con `session_id` de una sesión `status='closed'` del mismo establishment → `23514`; con un animal cuyo `rodeo_id` ≠ `sessions.rodeo_id` (mismo establishment) → `23514`. Repetir el cross-tenant para al menos otra tabla (`sanitary_events`). **Ordenamiento de cierre (R10.8 / orden de cierre offline, design §5)**: crear eventos con `session_id` de una sesión **active**, luego cerrarla (`status='closed'`); verificar que el patrón create-events→close **NO rechaza** los eventos ya creados (el rechazo solo aplica a insertar un evento NUEVO contra una sesión ya cerrada).
Aceptación: 8 tests verdes (OK, cross-tenant, inexistente, sin session, closed, rodeo-mismatch, + 1 cross-tenant en otra tabla, + ordenamiento de cierre).
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.7 — Tests: transición de categoría disparada en maniobra + ortogonalidad
Satisface: R8.1, R8.2, R8.3.
Detalle: insertar `reproductive_events (event_type='tacto', pregnancy_status='medium', session_id=<sesión>)` sobre una vaquillona cuyo rodeo tiene `prenez`+`tamano_prenez` enabled → la categoría pasa a `vaquillona_prenada` (trigger spec 02) Y queda en `animal_category_history`; verificar que `rodeo_id` y `management_group_id` NO cambiaron (ortogonalidad). Con `category_override=true` → no transiciona.
Aceptación: 3 tests verdes.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.8 — Tests: RLS de `maneuver_presets`
Satisface: R2.1, R2.4, R2.5, R11.1, R11.3.
Detalle: rol operativo activo crea/lee/edita preset del establecimiento; `userB` sin rol → 0 filas; name vacío → falla; soft-delete por rol activo deja de aparecer en SELECT.
Aceptación: 4 tests verdes.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.9 — Tests: append-only / corrección per-evento sigue funcionando
Satisface: R11.5.
Detalle: cargar un evento en una sesión (con `session_id`), corregirlo por edición (owner o `created_by`, sin ventana de tiempo — spec 02 R6.8.1) y por soft-delete → OK; `userB` sin rol no puede; verificar que no hay camino de escritura cross-tenant sobre el evento.
Aceptación: 3 tests verdes.
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [x] T2.11 — Tests: no-bypass del gating dientes/CUT (UPDATE `animal_profiles`)
Satisface: R7.5 (SEC-SPEC-03-01).
Detalle: sobre un animal cuyo rodeo tiene `dientes` **disabled** (`update rodeo_data_config set enabled=false where data_key='dientes'`): (a) `UPDATE animal_profiles SET teeth_state=...` (valor no-NULL, aditivo) directo por PostgREST/sync (saltando la UI) → **rechazado** (`23514`); (b) `UPDATE animal_profiles SET is_cut=true, category_id=..., category_override=true` (transición CUT, aditivo false→true) sobre ese rodeo → **rechazado** (`23514`). Controles: mismo rodeo con `dientes` **enabled** → ambos UPDATE OK. **Afinación ENFORCE AFINADO (D8) — sustractivos permitidos sobre rodeo SIN dientes**: (E) limpiar `teeth_state` a NULL (`UPDATE animal_profiles SET teeth_state=NULL`) → **aceptado**; (F) desmarcar `is_cut` (`UPDATE animal_profiles SET is_cut=false`, true→false) → **aceptado**. **Guarda WHEN**: `UPDATE animal_profiles SET management_group_id=...` (lote, R9.2) y `UPDATE … SET rodeo_id=...` (R4.4) sobre rodeo con `dientes` disabled **NO disparan** el gating (siguen su propio camino) → OK. Trazabilidad: R7.5, SEC-SPEC-03-01, trigger guard `IS DISTINCT FROM`.
Aceptación: ~8 tests verdes (2 reject aditivo + 2 control enabled + 2 sustractivo aceptado [Caso E, Caso F] + 2 guarda lote/rodeo no-gatea).
Archivos: `supabase/tests/maneuvers/run.cjs`.

### [ ] T2.12 — Verificación cross-spec del find-or-create inline (re-check en Gate 2 de M2.1-core)
Satisface: R4.6 (SEC-SPEC-03-05).
Detalle: **spec 09 YA está integrada** (as-built 2026-06): el find-or-create de la manga (R4.1) reusa el camino REAL — `lookupByTag` (`animals.ts`) para resolver, y el alta va a `/crear-animal` (spec 02/09) con el identificador precargado. **M2.1-core NO crea un alta propia**: navega a `crear-animal` con los params precargados (`resolvePrefilledCreateParams`: tag por BLE / idv|visual por manual); el INSERT real lo hace `crear-animal` (spec 02 `createAnimal`), que ya **fuerza el `establishment_id` activo** (del contexto, NO del payload — `animals.ts::createAnimal` toma `input.establishmentId` del campo activo), respeta el UNIQUE `tag_electronic` global y `(establishment_id, idv)` (`0020`), y **fuerza `created_by = auth.uid()` server-side** (`tg_force_created_by_auth_uid`, `0043`). El Gate 2 (`code`) de M2.1-core debe **re-verificar** que este call-site no introduce un bypass (no manda `establishment_id` en los params del find-or-create; el precargado es solo identificador). NO hay superficie de alta nueva en M2.1-core (frontend puro que delega al alta as-built). El test de no-bypass cross-tenant del alta vive en la suite de spec 02 (ya cubierto).
Aceptación: Gate 2 de M2.1-core confirma que el find-or-create de la manga delega al alta as-built (establishment del contexto, UNIQUE, created_by forzado) sin params de tenant.
Archivos: `progress/impl_03-m2.1-core.md` (nota de Gate 2) — sin migración nueva.

### [x] T2.10 — Hook al runner global
Satisface: housekeeping.
Detalle: agregar `maneuvers/run.cjs` a `scripts/run-tests.mjs`; `node scripts/check.mjs` ejecuta y reporta verde.
Aceptación: `check.mjs` corre Fase 2 entera verde (incluye T2.4b, T2.11).
Archivos: `scripts/run-tests.mjs`.

---

## Cliente — decomposición en chunks M1-M4 (orden de build, 2026-06-13)

> Estas son las Fases 3/4 del cliente, reorganizadas en 4 chunks de implementación. Cada chunk es una unidad de **Gate 2 (code)**. Consumen el as-built (ver `design.md` §1.1 para los nombres reales): NO se crea `app/src/features/maneuvers/` — pantallas en `app/app/**`, lógica en `app/src/{services,hooks,utils,contexts}`. NO se reimplementa BLE (se consume spec 04). El peso es **manual** (balanza diferida). Tests con node:test (lógica pura) + RTL/Detox (flujos), y la suite E2E `app/e2e/` se extiende por chunk (memoria: testear con Playwright, no a mano).

---

### M1 — Inicio + config de jornada

#### [x] M1.1 — `maneuver-gating.ts` (mapeo + resolución por rodeo real) + `useManeuverGating` — DONE (chunk M1-SERVICIOS, 2026-06-13; pendiente reviewer + Gate 2)
Satisface: R1.4, R1.5, R5.3, R5.4, R5.5, R5.6, R10.3.
Detalle: módulo PURO `app/src/utils/maneuver-gating.ts` con el mapeo `MANEUVER_DATA_KEYS` (design §3, R5.4) + la resolución por **rodeo real** del animal (lee `animal_profiles.rodeo_id` del perfil activo del SQLite local vía `buildActiveProfileRodeoQuery` — NO `current_animal_rodeo`, que no existe; SEC-SPEC-03-02). El hook `app/src/hooks/useManeuverGating.ts` lo combina con `fetchRodeoGating` (`app/src/services/rodeo-config.ts`, read-only: joina `rodeo_data_config` + `field_definitions` + `system_default_fields` cacheados → `RodeoDataKeyMap`). Determina maniobras que aplican por animal (R5.5), required vs opcional (R5.6, required de `system_default_fields`). **As-built**: `ManeuverKind` usa `tacto`/`raspado` (no `tacto_vaca`/`raspado_toros`) — ver design §3 reconciliado.
Aceptación: unit tests (node:test) con tabla de casos (rodeo con/sin data_key, multi-key tacto, required/optional) → `app/src/utils/maneuver-gating.test.ts`. Refleja el mapeo ADR-021.
Archivos: `app/src/utils/maneuver-gating.ts`, `app/src/hooks/useManeuverGating.ts`, `app/src/services/rodeo-config.ts` (`fetchRodeoGating`), `app/src/services/powersync/local-reads.ts` (`buildRodeoSystemQuery`/`buildActiveProfileRodeoQuery`), `app/src/hooks/index.ts`.

#### [x] M1.2 — Service `sessions.ts` (CRUD-plano offline) — DONE (chunk M1-SERVICIOS, 2026-06-13; pendiente reviewer + Gate 2)
Satisface: R1.9, R1.10, R1.11, R10.1, R10.2, R10.7, R9.4.
Detalle: `app/src/services/sessions.ts` — `createSession`, `closeSession`, `getActiveSession`/`getSessionById`, `setWorkLotLabel`, `setSessionCounts` (contadores app-maintained absolutos, D5). **CRUD-plano** (INSERT/UPDATE local sobre la tabla sincronizada `sessions` vía `runLocalWrite` → 1 CrudEntry → uploadData; mismo patrón que `events.ts::addWeight`). IDs de cliente (`crypto.randomUUID()`). `config` jsonb pass-through (no se le da forma rígida). `created_by`/`establishment_id` los fuerza el trigger `0050` al subir (no se mandan). `started_at` de cliente (persiste, default sin force) para reanudación offline determinística. RLS `has_role_in` es la barrera real.
Aceptación: unit (SQL builders ejecutados contra node:sqlite) → `app/src/services/powersync/maneuver-reads.test.ts`; la sesión se crea/cierra offline; rodeo-check (`0050`) y `created_by` forzado se observan al subir (cubierto por la suite backend Fase 2).
Archivos: `app/src/services/sessions.ts`, `app/src/services/powersync/local-reads.ts` (builders sessions), `app/src/utils/maneuver-config.ts` (parseo de config pass-through).

#### [x] M1.3 — Service `maneuver-presets.ts` (CRUD-plano) — DONE (chunk M1-SERVICIOS, 2026-06-13; pendiente reviewer + Gate 2)
Satisface: R2.1, R2.2, R2.3, R2.5.
Detalle: `app/src/services/maneuver-presets.ts` — CRUD de presets (createPreset/updatePreset CRUD-plano, scope establishment; soft-delete vía RPC `0057` por la OUTBOX, `entity:'maneuver_preset'` agregado a `outbox.ts`/`upload.ts`). `loadPreset(presetId, rodeo)` filtra maniobras con data_key OFF (reusa `maneuver-gating.ts` vía `fetchRodeoGating`) + devuelve la lista de omitidas para avisar (R2.3). `fetchPresets` UNIONa el overlay → un soft-delete optimista oculta el preset al instante (R6.11).
Aceptación: unit tests (builders ejecutados + overlay-hide) → `app/src/services/powersync/maneuver-reads.test.ts`; filtrado por gating del rodeo + parseo de config → `app/src/utils/maneuver-config.test.ts`; el soft-delete deja de aparecer en SELECT (overlay + RPC 0057).
Archivos: `app/src/services/maneuver-presets.ts`, `app/src/services/powersync/local-reads.ts` (builders presets), `app/src/services/powersync/outbox.ts` + `upload.ts` (op_type `soft_delete_maneuver_preset`).

#### [x] M1.4 — Pantalla de inicio + wizard 3 etapas (incluye drag-reorder de maniobras) — DONE (chunk M1-UI, 2026-06-13; pendiente reviewer + Gate 2)
Satisface: R1.2, R1.3, R1.4, R1.5, R1.7, R1.8, R1.9, **R1.12, R1.13**, R2.2, R2.3.
Detalle: expandir el stub `app/app/maniobra.tsx` (inicio: presets al tope R2.2 + "nueva jornada"; aviso de maniobras filtradas al elegir preset R2.3) + nueva ruta del wizard (`app/app/maniobra/jornada.tsx` o equivalente): etapa 1 rodeo activo (R1.3, reusa `rodeo-store`/`fetchRodeos`); etapa 2 maniobras gateadas capa 1 (R1.4/R1.5 vía `useManeuverGating`) + pre-config con texto libre + autocompletar de usadas antes (R1.7/R1.8) + **lista reordenable por drag con handles** de las maniobras elegidas (R1.12, design §6.bis.1; orden inicial = orden de selección o del preset; orden = presentación pura, no toca gating); etapa 3 resumen + arrancar → `createSession` persistiendo el **array ordenado** `config.maniobras` + `config.preconfig` (R1.13, shape design §2.1.1; el mismo array se persiste en `maneuver_presets.config` al guardar como preset en M1.3). Una decisión por pantalla. **Sin schema change — `config` ya es jsonb (`0050`/`0051`); Gate 1 N/A.**
Aceptación: el wizard solo ofrece maniobras habilitadas en el rodeo; las elegidas se reordenan arrastrando y el orden persiste en `sessions.config.maniobras` (y en `maneuver_presets.config` si se guarda como preset); reordenar NO cambia el gating; al confirmar crea la sesión local; presets al tope.
**As-built (chunk M1-UI)**: inicio `app/app/maniobra.tsx` (presets al tope + "Nueva jornada"); wizard `app/app/maniobra/jornada.tsx` (3 etapas por estado interno, una decisión por pantalla); drag-reorder `app/app/maniobra/_components/ManeuverReorderList.tsx` (gesture-handler `Gesture.Pan` en el handle + reanimated `useSharedValue`/`useAnimatedStyle`, handles `GripVertical` visibles, filas de alto fijo → índice destino por desplazamiento); lógica pura `app/src/utils/maneuver-wizard.ts` (labels es-AR + `moveManeuver`/`toggleManeuver`/`buildJornadaConfig`/`filterAutocomplete`) + tests `maneuver-wizard.test.ts` (16 casos, incl. round-trip de orden config→extractManeuvers). **DM1-UI-1 (decisión menor, reconciliada en design §6.bis.1)**: el autocompletar de preconfig (R1.8) se siembra de los valores de preconfig ya usados en los **presets** del campo (sin nuevo service de distinct-query sobre eventos tipados — fuera de scope M1-UI); helper source-agnóstico, una fuente más rica se enchufa en M2/M3.
**As-built v2 (iteración de diseño M1-UI, feedback de Raf, 2026-06-13; design §6.bis.1 reconciliado)**: la etapa 2 se rehízo como **LISTA UNIFICADA con drag "burbuja"** — `ManeuverReorderList.tsx` reescrito: una sola lista (Weverse "Change order"), seleccionadas-arriba (número + grip, reordenables) + pool-abajo (tap para sumar), tap = toggle. Drag burbuja (escala ~1.04 + sombra fuerte + esquinas redondas + 1:1 con el dedo + hermanos reflow por springs + spring al soltar + háptica), todo en el UI thread (reanimated worklets + gesture-handler, layout absoluto con mapa `positions`; NO `react-native-draggable-flatlist` — web pobre + peer-deps + postinstall). **Cierra el bug del CTA que tapaba la lista** (seleccionadas siempre arriba/visibles). Cambios adicionales: ícono de rodeo `Group` (no `Layers`, reservado a LOTE); resumen etapa 3 muestra el **detalle de preconfig** por maniobra (`maneuverDetail` puro, ej. "Vacunación · Brucelosis"); CTA "Arrancar jornada" con emphasis confiado (▶ leading + 64px, no gigante). Nuevos: `app/src/utils/haptics.ts` (`hapticPickUp`/`hapticDrop` vía `Vibration` de RN — no `expo-haptics`); `maneuverDetail` + tests en `maneuver-wizard`(+`.test.ts`, +6 casos = 22). Test hook `?dragFreeze=<i>` para la captura `etapa2-drag.png`. e2e actualizado (pool-rows/selected-rows, captura del estado burbuja, detalle en resumen).
**As-built v3 — "Guardar como rutina" CABLEADO (guardar-rutina, 2026-06-16; design §6.bis.10; R2.1).** M1.4 prometía "el mismo array se persiste en `maneuver_presets.config` al guardar como preset" pero esa ACCIÓN de UI NUNCA se había construido (createPreset existía sin call-site → "Tus rutinas" siempre vacío; el empty-state del landing prometía algo inexistente — Raf lo cazó). Ahora la **etapa 3** del wizard tiene la acción SECUNDARIA "Guardar como rutina" (`Button variant="secondary"` outline, debajo del primario "Arrancar jornada" SIN degradarlo; **independiente de arrancar**) → nuevo `app/app/maniobra/_components/SavePresetSheet.tsx` (sheet de nombre, idiom lockeado `ManeuverConfigSheet`/`ExitJornadaSheet` + GUARD anti tap-through `readyToDismissRef` doble-rAF; input ≥56 + "Guardar" deshabilitado si vacío/whitespace + `MAX_PRESET_NAME_LEN`=60 cliente + error es-AR fail-closed) → `createPreset({ establishmentId, name, config })` con `buildCurrentConfig()` (mismo shape §2.1.1 que `createSession`, extraído a un solo lugar). OK → toast "Rutina guardada" (`preset-saved-toast`) + cierra; fail → error en el sheet sin perder lo tipeado. e2e: `maniobra-wizard.spec.ts` (guardar + oráculo server `waitForServerPreset` config.maniobras en orden + nombre vacío→deshabilitado) + `maniobra-config-sheet-race.spec.ts` (tap-through del scrim del SavePresetSheet, hasTouch+touchscreen.tap). Capturas 360/412 `tests/modo-maniobra/guardar-rutina-{etapa3,sheet}-{360,412}.png`. **Hardening pendiente (NO bloqueante)**: cap server-side de longitud de `maneuver_presets.name` (Gate 1 puntual, backlog 2026-06-14 — este es el call-site que lo anticipaba). Frontend puro → Gate 1 N/A; reviewer + Gate 2 después.
**As-built v3 (iteración UX 2, feedback de Raf, 2026-06-14; design §6.bis.1 reconciliado)** — 4 arreglos del wizard (NO toca servicios de M1 ni backend): (1) **Registro central de iconos** `app/src/theme/icons.ts` (single source of truth: `RodeoIcon=Boxes`, `LoteIcon=Layers`, `CampoIcon=Building2`, `AnimalIcon=PawPrint`, `MiembroIcon=Users`) — antes cada pantalla importaba el lucide suelto y se desincronizaban; consumidores migrados (solo swap de iconos, idéntico): `jornada.tsx`, `app/(tabs)/index.tsx`, `app/(tabs)/mas.tsx`, `lotes.tsx`. (2) **Ícono de rodeo** etapa 1 + resumen `Group`→`RodeoIcon`(=`Boxes`) (el `Group` de la v2 NO era el ícono de rodeo as-built — bug de Raf). (3) **Etapa 2 SCROLL + bounds + auto-scroll**: la lista absoluta rompía el scroll (con 8 maniobras la 9na + pool + preconfig + CTA quedaban inalcanzables); ahora la etapa va en un `Animated.ScrollView` (reanimated, web-safe) → swipe normal scrollea; el drag del grip es **grip-gated** (`activeOffsetY`/`failOffsetX`) para no robar el swipe; **auto-scroll** cerca del borde (`useFrameCallback` + `scrollTo`, viewport medido con `measureInWindow` y guardado); **bounds**: el ítem arrastrado se clampea a la región de las seleccionadas (no sube arriba del título ni baja al pool). (4) **Etapa 3 SCROLL**: el resumen va en el mismo `Animated.ScrollView` con el CTA pinneado. El orden persiste igual (round-trip ya testeado). Capturas regeneradas + nueva `etapa2-scroll.png` (8 maniobras, scrolleado al pool + CTA).
**As-built v4 (iteración UX 3, feedback de Raf, 2026-06-14; design §6.bis.1 reconciliado)** — preconfig de tanda INLINE + BOTTOM SHEET (NO toca servicios de M1 ni backend): se **eliminó la sección "Detalle de la tanda" del fondo** de la etapa 2 (el `FormField` suelto + chips, ya removidos junto con `PreconfigField`). El preconfig ahora vive **inline en la fila** de la maniobra configurable (Vacunación/Inseminación): **2da línea** con hint *"Tocá para elegir vacuna/pajuela"* (muted) o el valor cargado (énfasis); las no configurables no muestran 2da línea. **Zonas de toque** de la fila seleccionada: **badge = quitar**, **cuerpo = abrir sheet** (si configurable; si no, inerte), **grip = drag**. Nuevo **bottom sheet** `app/app/maniobra/_components/ManeuverConfigSheet.tsx` (patrón `BulkConfirmSheet`: backdrop `$scrim` + anclado abajo + grip + safe-area inferior): input grande (`$searchBarLg`) + autocompletar "Usadas antes" (`filterAutocomplete`); **Vacunación = multi** (chips, persiste coma-separado) / **Inseminación = single** (pajuela). Guardar → `config.preconfig[<maniobra>]` → inline + resumen (round-trip). `ROW_HEIGHT` 64→80 (cabe la 2da línea sin romper el drag). Nuevos helpers puros `splitMultiPreconfig`/`joinMultiPreconfig` en `maneuver-wizard.ts` (+6 tests). Bubble/scroll/bounds/auto-scroll/drag de v2/v3 intactos. Capturas: `etapa2.png` (Vacunación · Brucelosis inline) + nueva `etapa2-sheet.png` (sheet abierto + input + autocompletar).
**As-built v5 (BUGFIX, feedback de Raf en testing en vivo web, 2026-06-15; design §6.bis.1 v5 reconciliado)** — el `ManeuverConfigSheet` se abría y se cerraba al instante (~1ms) en web al tocar el cuerpo de Vacunación. Causa: el cuerpo lo abre un `Gesture.Tap()` de gesture-handler (driven por `pointerup`); en web táctil el `click` EMULADO del touch se dispara ~20ms después y cae sobre el `$scrim` recién montado (`onPress=onClose`) → cierra. Fix: guard `readyToDismissRef` en el sheet (false al montar → armado en el próximo frame vía doble `requestAnimationFrame`, fallback `setTimeout(0)`); el `onPress` del scrim hace early-return si no está armado → ignora el click huérfano del open, pero un tap deliberado posterior SÍ cierra. Solo el scrim pasa por el guard (Cancelar/Guardar/chips/sugerencias intactos). **`CutPromptSheet` (DientesStep, R6.8) NO lleva guard**: se abre con el `onPress` de Tamagui (driven por `click`, consumido) → sin click huérfano (verificado con repro táctil); solo una nota en el archivo. Regresión nueva `app/e2e/maniobra-config-sheet-race.spec.ts` (context `hasTouch:true` + `touchscreen.tap` — única forma fiel; el wizard spec con `locator.click()`/sin-touch daba falso verde aun CON el bug). Probado: revertir el fix hace fallar el race spec.
Archivos: `app/app/maniobra.tsx`, `app/app/maniobra/jornada.tsx`, `app/app/maniobra/_components/ManeuverReorderList.tsx`, `app/app/maniobra/_components/ManeuverConfigSheet.tsx` (NUEVO, bottom sheet de preconfig; v5 = guard del click huérfano), `app/app/maniobra/_components/DientesStep.tsx` (v5 = nota: por qué NO necesita el guard), `app/src/theme/icons.ts` (registro de iconos), `app/app/(tabs)/index.tsx` + `app/app/(tabs)/mas.tsx` + `app/app/lotes.tsx` (migrados al registro), `app/src/utils/maneuver-wizard.ts`(+`.test.ts`), `app/src/utils/haptics.ts`, `app/app/_layout.tsx` (registro de ruta `maniobra/jornada`), `app/e2e/maniobra-wizard.spec.ts` (capturas 412×915 incl. `etapa2-drag.png` + `etapa2-scroll.png` + `etapa2-sheet.png`), `app/e2e/maniobra-config-sheet-race.spec.ts` (NUEVO, regresión táctil del race), `app/e2e/helpers/admin.ts` (helper `seedManeuverPreset` para el historial de autocompletar).

---

### M2 — Carga rápida (PANTALLA CRÍTICA) — **ARRANCA POR DESIGN SPIKE**

#### [x] M2.0 — **DESIGN SPIKE** de la pantalla de manga (botones gigantes) — ✅ APROBADO por Raf (2026-06-13)
> Hecho + lockeado: `app/app/maniobra/carga.tsx` (Tacto binario) + `paso.tsx` (pesaje, grilla 3×4 simétrica medida ±1px + bordes + peso dominante + coma es-AR) + `_components/SpikeIdentityHeader.tsx` + `e2e/maniobra-spike.spec.ts`. Densidad ≥83% del alto útil; vetado contra 8 refs de Mobbin. Token `$11` (64px hero) agregado a `tamagui.config.ts`. (carga.tsx mostró solo el paso 1 del tacto; el paso 2 = tamaño cabeza/cuerpo/cola lo construye M3, R6.2.)
Satisface: R5.2, R12.2, R12.4, R12.5 (visual).
Detalle: spike visual de la **carga rápida** + un **paso de maniobra** representativo con los **botones gigantes** (R5.2): bloques de decisión full-width que se reparten el alto del viewport, header de identidad mínimo siempre visible (R12.4), cero espacio muerto (R12.5: bloques de decisión ≥ 60 % del alto útil). El leader **auto-revisa** el resultado con análisis de diseñador pro fundamentado (Fitts, jerarquía, Gestalt, contraste, regla de los tercios, densidad medida — no a ojo) y re-itera con el implementer; a Raf solo se le muestra lo aceptable. **Vetar SIEMPRE con un título que tenga descendente (g/q/p/j/y)** para confirmar el `lineHeight` matching (memoria: recorte de descendentes). Es visual-primero: sin plomería de datos todavía (datos mockeados).
Aceptación: pantalla de manga revisada y aprobada por Raf como base visual antes de cablear datos; medición de densidad (≥ 60 % del alto útil en bloques de decisión) registrada.
Archivos: `app/app/maniobra/carga.tsx`, `app/app/maniobra/paso.tsx` (mock data).

#### M2.1 — Identificación dual (BLE spec 04 + manual spec 09) — partida en CORE + EDGE
> El chunk M2.1 original se decompuso en **M2.1-spike** (design spike, DONE+aprobado), **M2.1-core**
> (plomería del camino feliz + edge cases que NO frenan la fila, DONE) y **M2.1-edge** (desambiguación
> manual + heurística de rodeo, diferido). El spike es exploratorio/visual; el cableado real es M2.1-core.

##### [x] M2.1-spike — DESIGN SPIKE de la pantalla de identificación (scan-first) — DONE + APROBADO por Raf (2026-06-14)
> 4 estados visuales (listening/found/manual/unknown), 100% mock, vetado (design-review, medido Pillow) y aprobado. Tokens `heroScan/heroRing/heroIcon` + `StickIcon` canonizados. Detalle: `progress/impl_03-m2.1-spike-identify.md`.

##### [x] M2.1-core — PLOMERÍA del identify (camino feliz + edge cases sin-frenar-la-fila) — DONE (implementer Opus, 2026-06-14; pendiente reviewer + Gate 2)
Satisface: R3.1, R3.2, R3.3, R3.4, R3.5, R3.6, R3.7, R3.8, R4.1, R4.3, R4.5, R10.4, R12.3, + R5.1 (destino auto-avance).
Detalle (as-built): la pantalla `maniobra/identificar.tsx` (de spike mock a REAL, manteniendo la dirección scan-first aprobada) **consume** `useBleStickListener({ enabled:true, onTagRead })` (manga-owned, R3.8 contrato transport-agnóstico de spec 04 — en web/e2e `adapter-mock`); el listener BLE global (`FindOrCreateOverlay`) se **suspende por RUTA** (`BLE_OWNED_ROUTES = {asignar-caravanas, maniobra}`, R3.2) → un solo consumidor del bastón (NO se usa `disableListener()`, que apagaría el transporte y la manga no recibiría la lectura — ver impl). `onTagRead(eid)` recibe el EID ya validado+dedupeado del provider (R3.3); el feedback inmediato (vibración+beep) lo dispara el provider, el "found" visual es el feedback visual (R3.4/R12.3). Lookup: `lookupByTag` (BLE) / `searchAnimals` (manual idv exacto + visual fuzzy, R3.5). La decisión found/other_establishment/unknown/ambiguous + el gate de auto-avance viven en el módulo PURO `app/src/utils/maniobra-identify.ts` (unit-testeado). Edge cases que NO frenan la fila: BLE desempata por tag único global (R4.3, nunca ambiguo por BLE); otro establecimiento → "Está en otro campo" + Saltar (R4.5, NO transfiere — feature 11); find-or-create inline (R4.1, → `/crear-animal` con el identificador precargado + rodeo de la sesión en el hero). Disconnect/reconnect (R3.6/R3.7) los garantiza el provider as-built; el chip `BleConnectionChip` refleja el estado en el header; el manual es manual-first (siempre disponible si se cae el bastón). **AUTO-AVANCE (decisión de Raf, default del leader no objetado)**: en match ÚNICO y claro (`found`), tras el flash de confirmación ~0,8s, navega automáticamente a la carga rápida (`/maniobra/carga?sessionId=…&profileId=…`, la pantalla del spike M2.0 — punto de integración M2.2 comentado en carga.tsx). El wizard `jornada.tsx` re-apunta a `/maniobra/identificar` (antes a `/maniobra/carga`).
Aceptación: identificación por ambas puertas; el listener global se suspende DENTRO de la manga (un solo consumidor) y se restaura al salir; los edge cases sin-frenar-la-fila resueltos; match único → auto-avance. **Tests**: `app/src/utils/maniobra-identify.test.ts` (9 casos PURO) + `app/e2e/maniobra-identify.spec.ts` (5 escenarios reales con adapter-mock/BleE2EBridge: found→auto-avance, unknown→find-or-create, otro-establecimiento→saltar, manual→found, disconnect→manual).
Archivos: `app/src/utils/maniobra-identify.ts`(+`.test.ts`), `app/app/maniobra/identificar.tsx`, `app/app/maniobra/_components/SpikeSessionHeader.tsx` (slot `right`), `app/app/_components/FindOrCreateOverlay.tsx` (BLE_OWNED_ROUTES), `app/app/maniobra/jornada.tsx` (re-point), `app/app/maniobra/carga.tsx` (nota de integración), `app/app/_layout.tsx` (sale de DEV_WEB_ROUTES), `app/e2e/maniobra-identify.spec.ts` + `app/e2e/maniobra-wizard.spec.ts` (smoke), `scripts/run-tests.mjs`.

##### [x] M2.1-edge — Desambiguación manual + heurística de rodeo — DONE (implementer Opus, 2026-06-14; fix-loop R4.4 2026-06-14; pendiente reviewer + Gate 2)
Satisface: R4.2, R4.4 (UI de **pasar-el-animal**/saltar de otro-rodeo-mismo-sistema), R4.7.
Detalle (as-built):
  - **R4.2 (picker de desambiguación)** — la búsqueda MANUAL por `visual_id_alt` que devuelve >1 candidato ya NO muestra el `AmbiguousHero` de aviso (M2.1-core, estado seguro): ahora monta el **`CandidatePicker`** (sheet manga-friendly anclado abajo, patrón `ManeuverConfigSheet`/`BulkConfirmSheet`) sobre el `ScanHero` (la pantalla sigue "escuchando" de fondo). Filas grandes (≥`$searchBarLg`=56) tappables con la jerarquía del header de identidad (visual DOMINANTE > electrónico): caravana visual grande + **N° interno (idv) como DESEMPATE** a la derecha (clave: cuando el visual está duplicado —el caso de R4.2— el idv es lo único que distingue dos animales que comparten visual+rodeo+categoría) + badge categoría + rodeo + tag electrónico muted. Elegir uno → se reusa el camino del `found` (pasa por la resolución de rodeo R4.4/R4.7). Salida find-or-create (R4.1): "Ninguno · dar de alta" → `/crear-animal` con el texto precargado. NO se construyó sobre `CandidateRow`/`CandidateSummary` del `FindOrCreateOverlay` (son funciones locales no exportadas, para un caso distinto —asignar caravana a animal sin tag—); se construyó un componente nuevo manga-friendly. La lógica de jerarquía (`candidateDominantId`/`candidateDistinguisher`) vive en el módulo PURO `app/src/utils/maniobra-edge.ts` (unit-testeado).
  - **R4.4 (otro rodeo del mismo campo) — HONRA el EARS (fix-loop 2026-06-14)** — tras un `found` (BLE o manual), antes de auto-avanzar, se resuelve el **rodeo REAL del animal** (`fetchAnimalDetail`, lectura local) y se compara con el rodeo de la sesión (`isOtherRodeo`, PURO). Si difiere → **NO se carga directo** (el tenant-check del DB `tg_event_session_tenant_check` rechazaría el evento con `23514`, "una sesión = un rodeo", design §2.3) → monta el **`OtherRodeoSheet`**: aviso con el **rodeo de origen visible** + dos acciones — **[Pasar el animal a este rodeo]** (`moveAnimalToRodeo(profileId, sessions.rodeo_id)` → UPDATE de `animal_profiles.rodeo_id` al rodeo de la sesión, solo si es del MISMO sistema, `canChangeSessionRodeo` PURO) o **[Saltar este animal]**. La validación de mismo sistema (R4.5.1) la hace el trigger `tg_animal_profiles_rodeo_same_system_check` (0047) + `tg_animal_profiles_rodeo_check` (0021) server-side al subir (el cliente NO replica validación). Si es de OTRO sistema, solo se ofrece saltar (pasar el animal cruzaría sistemas → dead-end de categoría R4.6, lo rechaza el trigger). El systemId de cada rodeo se resuelve contra `rodeo.available` (RodeoContext) — sin tocar backend, sin hardcode. **NOTA fix-loop**: una versión previa desvió R4.4 a "cambiar la jornada" (`setSessionRodeo`); se revirtió a mover el animal para honrar el EARS R4.4.
  - **R4.7 (heurística de rodeo mal elegido)** — un tracker PURO de racha (`SeenRodeoStreak` + `pushSeenRodeo`/`shouldWarnMisconfiguredRodeo`, umbral configurable `MISCONFIGURED_RODEO_THRESHOLD=3`) se alimenta con el rodeo de cada animal identificado: si los primeros ~3 CONSECUTIVOS son del MISMO otro-rodeo (un animal del rodeo correcto rompe la racha), se muestra el **`RodeoMismatchBanner`** — banner NO-BLOQUEANTE anclado debajo del header (la identificación sigue funcionando detrás), dismissable (× / "Ahora no") con confirmar (→ `setSessionRodeo` al rodeo de la racha — cambia el rodeo de la SESIÓN, no mueve animales: el caso real es "elegí mal el rodeo de la jornada"). No se apila sobre el sheet R4.2/R4.4. **Robustez de nombres largos**: el botón "Cambiar a {rodeo}" trunca el nombre (`numberOfLines={1}` + ellipsize) y los 2 botones se apilan vertical (full-width) → no clipean con nombres de rodeo largos.
  - **Servicios nuevos**: (1) `moveAnimalToRodeo` en `app/src/services/animals.ts` (+ `buildMoveAnimalToRodeoUpdate` en `local-reads.ts`): UPDATE local CRUD-plano del `rodeo_id` del perfil ACTIVO (R4.4, offline; `deleted_at IS NULL`); triggers 0047 + 0021 + RLS re-validan al subir. (2) `setSessionRodeo` (+ `buildSetSessionRodeoUpdate`): UPDATE local del `rodeo_id` de una sesión ACTIVA (R4.7, offline; `status='active'` + `deleted_at IS NULL`); `tg_sessions_rodeo_check` (0050) + RLS re-validan al subir.
Aceptación: ✅ manual multi-candidato → picker (sin duplicar, N° interno desempata); animal de otro rodeo del mismo campo → **pasar el animal a este rodeo** (UPDATE de `animal_profiles.rodeo_id`) o saltar (rodeo de origen visible); primeros ~3 de otro rodeo → aviso no-bloqueante de cambiar el rodeo de la sesión (nombres largos sin clipear).
**Tests**: unit `app/src/utils/maniobra-edge.test.ts` (16 casos PUROS: isOtherRodeo/canChange R4.4, tracker de racha R4.7 con umbral configurable, dominante/distinguidor R4.2 con desempate por idv) + `maniobra-identify.test.ts` ampliado (candidatos enriquecidos R4.2) + `maneuver-reads.test.ts` ampliado (`buildSetSessionRodeoUpdate` solo activas + `buildMoveAnimalToRodeoUpdate` mueve el perfil activo / no toca soft-deleted). e2e `maniobra-identify.spec.ts` 3 escenarios (f/g/h: visual dup → picker → elegir; otro rodeo mismo campo → **pasar el animal a este rodeo → carga + oráculo server-side `waitForServerProfileRodeo` confirma el UPDATE de `animal_profiles.rodeo_id`**; 3 consecutivos de otro rodeo → banner R4.7) + capturas 412×915 (mock limpio: caravana "0385", rodeos "Cría hembras"/"Vaquillonas"/"Toros") en `design/maniobra-identify/{candidate-picker,other-rodeo-sheet,rodeo-mismatch-warning}.png`.
Archivos: `app/src/utils/maniobra-edge.ts`(+`.test.ts`), `app/src/utils/maniobra-identify.ts` (outcome `ambiguous` enriquecido con `candidates`)(+`.test.ts`), `app/src/services/animals.ts` (`moveAnimalToRodeo`), `app/src/services/sessions.ts` (`setSessionRodeo`), `app/src/services/powersync/local-reads.ts` (`buildMoveAnimalToRodeoUpdate` + `buildSetSessionRodeoUpdate`)(+`maneuver-reads.test.ts`), `app/app/maniobra/identificar.tsx` (cableado R4.2/R4.4/R4.7), `app/app/maniobra/_components/{CandidatePicker,OtherRodeoSheet,RodeoMismatchBanner}.tsx`, `app/e2e/maniobra-identify.spec.ts` (f/g/h + capturas) + `app/e2e/helpers/admin.ts` (`waitForServerProfileRodeo`), `scripts/run-tests.mjs` (maniobra-edge.test.ts).
**Nota as-built sobre el cambio de rodeo de jornada + eventos en cola (R4.7 ↔ R10.8)**: confirmar R4.7 re-apunta `sessions.rodeo_id`; el tenant-check del DB valida `v_event_rod = v_session_rod` con el `sessions.rodeo_id` ACTUAL al momento del INSERT. Un evento offline de un animal de un rodeo VIEJO que suba DESPUÉS de cambiar la jornada sería rechazado (`23514`) → lo superficia R10.8 (M4.2). R4.7 (avisar temprano) MITIGA esto. R4.4 ya no toca `sessions.rodeo_id` (mueve el animal) → no genera este riesgo. Decisión consciente (design §2.3 Hardening R4.7).

##### [x] M2.1-exit-hero — Salida de la jornada (botón ‹ → sheet) + hero adaptativo por conexión — DONE (implementer Opus, 2026-06-16; pendiente reviewer + Gate 2)
Satisface: R10.7 (cerrar la jornada, surfacing), R10.5/R10.6 (salir sin terminar = reanudable), R3.6/R3.7 (hero adaptativo por estado de conexión del bastón).
Detalle (as-built):
  - **PARTE A — botón ‹ → ExitJornadaSheet (cierre de jornada).** El botón ‹ del `SpikeSessionHeader` antes era no-op (no se le pasaba `onBack`) → ahora abre un bottom sheet de SALIDA (NO navega atrás directo: no se "va atrás", se CIERRA una jornada que siempre hay que cerrar — decisión de Raf). Nuevo `app/app/maniobra/_components/ExitJornadaSheet.tsx` (patrón `ManeuverConfigSheet`/`OtherRodeoSheet`: backdrop `$scrim` tappable + sheet anclado abajo + grip + safe-area). Contexto arriba "Llevás **N** animales hoy" (N = `session.animalCount`, decisión informada). 3 acciones, **NADA rojo** (no hay destructivo: los eventos ya están guardados): (1) **Terminar jornada** (primaria `$primary`) → `closeSession(sessionId)` (R10.7, UPDATE offline) → al OK, paso de CONFIRMACIÓN dentro del sheet "Jornada terminada · Procesaste N animales" + un único "Listo" → `router.dismissAll()` (FUERA del flujo de maniobra → vuelve a `(tabs)`; fallback `router.replace('/(tabs)')` si no hay stack que dismissear); si `closeSession` devuelve `ok:false` → **fail-closed**: NO navega, superficia error accionable es-AR + deja reintentar. (2) **Salir sin terminar** (outline) → navega FUERA SIN cerrar (la sesión queda `active` + reanudable, R10.5/R10.6). (3) **Seguir en la jornada** (terciario texto) / tap en el scrim → cierra el sheet. **Guard tap-through** (regla `reference_rn_web_pitfalls`): el scrim lleva `readyToDismissRef` armado en el próximo frame (doble `requestAnimationFrame` + fallback `setTimeout(0)`), igual que `ManeuverConfigSheet` → el click huérfano del open (touch→mouse emulado) no auto-cierra el sheet.
  - **PARTE B — hero ADAPTATIVO por conexión (solo "escuchando", `outcome===null`).** El estado "escuchando" se ramifica en 3 sub-estados según `isConnected` (de `useBleStickListener`) + `conectable` (`useBleProviderApi()?.transport != null`), decisión PURA en `app/src/utils/maniobra-listen-state.ts` (`resolveListenConnState`/`isManualPromoted`, unit-testeado): (1) **CONECTADO** → `ScanHero` actual (sin cambios). (2) **DESCONECTADO + CONECTABLE** (`transport!=null`, web antes de elegir puerto / bastón caído) → nuevo **`ConnectHero`**: mismo tamaño/posición de disco que `ScanHero` (Jakob, sin salto de layout) pero el disco es un **BOTÓN ACTIVO** (`testID="connect-stick-disc"`, pressStyle + a11y de botón) con `StickIcon` + badge **Bluetooth** sobreimpreso; tap → `api?.transport?.connect()` (el tap ES el gesto que web-serial exige); título "Conectá el bastón" + "Tocá para conectar · o cargá la caravana abajo". (3) **DESCONECTADO + NO CONECTABLE** (`transport==null`, native manual-first hoy) → **MANUAL PROMOVIDO**: nuevo **`ManualPromptHero`** (sin disco, prompt "Ingresá la caravana del animal", tono NEUTRO + línea muted "El bastón no está disponible en este dispositivo") + el `ManualEntry` reusado con prop nueva `promoted` = expandido por default + sin "Cancelar→volver al escaneo" (no hay scan al cual volver). La lógica adaptativa aplica SOLO a `outcome===null`; las ramas de outcome (found/unknown/other/ambiguous) quedan IGUAL. Reactivo a `isConnected`/`transport` (conecta/cae en vivo → el hero reacciona sin recargar).
Aceptación: ✅ ‹ → sheet (no navega atrás directo); Terminar → `closeSession` (sesión cerrada server-side) + "Procesaste N animales" + navega fuera; Salir sin terminar → navega fuera SIN cerrar (sigue `active`); Seguir/scrim → cierra el sheet sin navegar; scrim con tap táctil (click huérfano del open) NO auto-cierra; hero: conectado=ScanHero / desconectado+conectable=ConnectHero (tap→connect) / transport null=manual promovido.
**Tests**: unit `app/src/utils/maniobra-listen-state.test.ts` (4 casos: los 3 sub-estados + `isManualPromoted`). e2e `app/e2e/maniobra-identify.spec.ts` (i: terminar → `waitForServerSessionClosed` + navega fuera; j: salir sin terminar → `readServerSessionStatus`==='active' + navega fuera; k: seguir → cierra el sheet sin navegar; l: tap-through regresión con `hasTouch:true`+`touchscreen.tap()`; m: hero adaptativo conectado↔conectable en vivo con tap→connect) + `app/e2e/helpers/admin.ts` (`waitForServerSessionClosed`/`readServerSessionStatus`/`waitForServerActiveSessionId`). El sub-estado manual-promovido (`transport==null`) NO es reproducible con el mock-adapter en web → se fuerza con el flag SECUNDARIO de E2E `__RAFAQ_BLE_E2E_MANUAL__` (doble-gateado por `__RAFAQ_BLE_E2E__`, sin superficie de prod → `mode='manual'`); capturas en `app/e2e/captures/maniobra-exit-hero.capture.ts` (5 estados × 360/412 en `tests/modo-maniobra/`).
Archivos: `app/app/maniobra/identificar.tsx` (cableado A+B + `ConnectHero`/`ManualPromptHero` + `ManualEntry` `promoted`), `app/app/maniobra/_components/ExitJornadaSheet.tsx` (NUEVO), `app/src/utils/maniobra-listen-state.ts` (NUEVO +test), `app/src/services/ble/adapter-selection.ts` (`ProviderMode` +`'manual'`), `app/app/_components/ble-e2e-flag.ts` (`isBleE2EManual`), `app/app/_layout.tsx` (mode `'manual'` bajo el flag secundario), `app/e2e/maniobra-identify.spec.ts` (i-m + `startManiobraSessionOnRodeo` conecta el mock) + `app/e2e/helpers/admin.ts` (oráculos de sesión), `app/e2e/captures/maniobra-exit-hero.capture.ts` (NUEVO), `scripts/run-tests.mjs` (maniobra-listen-state.test.ts).

##### [x] M2.2-continue-maniobra — Alta desde la manga → CONTINÚA la carga de la maniobra del animal nuevo (cierra el TODO de R4.1) — DONE (implementer Opus, 2026-06-16; pendiente reviewer + Gate 2)
Satisface: **R4.1** (cierra el "y al confirmar deberá continuar el wizard de maniobras para ese animal" que M2.1-core había diferido a M2.2). Frontend puro (navegación/flujo; no toca backend/schema/el alta).
Detalle (as-built): Raf lo cazó en vivo (`pnpm web`): el alta desde MODO MANIOBRAS aterrizaba en la FICHA (`/animal/[id]`) = **dead-end de la jornada** (sin volver a cargar la maniobra; back→pantalla stale "Animal nuevo"). NO es bug de conexión (`createAnimal` es offline-first, el animal se crea optimista). Fix: el contexto de la jornada (`sessionId`) viaja al alta y el alta, en contexto maniobra, continúa la carga.
  - **`app/app/maniobra/identificar.tsx`**: `onDarDeAlta` (outcome `unknown`) y `onCreateFromPicker` (outcome `ambiguous` → "dar de alta") pasan `sessionId` (guard: solo si existe) a `/crear-animal` — aplica a manual Y BLE (el `unknown` por bastón también continúa la maniobra). Comentario del TODO de `onDarDeAlta` actualizado (ya no "el alta es el destino").
  - **`app/app/crear-animal.tsx`**: lee `maneuverSessionId = params.sessionId`. Happy-path → con `maneuverSessionId`, `router.replace('/maniobra/carga', {sessionId, profileId})` (carga del nuevo animal), sin él → `/animal/[id]` (ficha, como hoy). Soft-fail (animal creado, evento post-create falló): se mantiene el aviso suave pero el CTA pasa a **"Continuar con la maniobra"** + rutea a `/maniobra/carga`; la rama de early-return de `onSubmit` (cuando `createdProfileId` ya seteado) también rutea a la carga en contexto maniobra. Label del CTA adaptativo (`maneuverSessionId ? 'Continuar con la maniobra' : 'Ver la ficha del animal'`). El alta cross-tenant-safe NO cambia; solo el destino post-create.
  - **Loop limpio (verificado)**: `carga.tsx` `onConfirmAnimal` → `router.replace('/maniobra/identificar', {sessionId})` = identify FRESCO para el siguiente animal. La pantalla `identificar(unknown)` stale queda abajo en el stack pero es inalcanzable (back→`ExitJornadaSheet`, ya no popea) → NO se fuerza gimnasia de stack para limpiarla (riesgosa e innecesaria).
Aceptación: ✅ desconocido en maniobras → dar de alta → wizard → "Crear animal" → cae en `/maniobra/carga` del nuevo animal (no `/animal/[id]`); alta NORMAL sin sessionId (desde la lista) → ficha (sin regresión); soft-fail en contexto maniobra → CTA "Continuar con la maniobra" → carga; `ambiguous` → "dar de alta" → también continúa.
**Tests**: e2e `app/e2e/maniobra-identify.spec.ts` (n: alta desde la manga → `/maniobra/carga` del nuevo animal, asserta display de peso + contador "· 1 de 1" + caravana nueva, NEGATIVA "Historial"; o: regresión alta sin sessionId → ficha `/animal/[id]` + `waitForServerAnimalProfile`, NEGATIVA display de peso) + helper local `walkCrearAnimalWizard`.
Archivos: `app/app/maniobra/identificar.tsx`, `app/app/crear-animal.tsx`, `app/e2e/maniobra-identify.spec.ts` (n,o + `walkCrearAnimalWizard`).

#### [x] M2.2 — Frame de carga rápida + resumen por animal + progreso — DONE (implementer Opus, 2026-06-14; pendiente reviewer + Gate 2)
Satisface: R5.1, R5.8, R5.9, R5.10, R5.11, R5.12, **R5.14**, R6.2 (tacto, paso 1 + tamaño), R6.9 (pesaje), R10.1 (offline), R12.1, R12.4.
Detalle (as-built): cableó el spike M2.0 a datos reales. El FRAME vive en `app/app/maniobra/carga.tsx` (NO `resumen.tsx` separado — el resumen es un MODO del frame para no perder el estado capturado al navegar; el `AnimalSummary` es un componente). Lee la sesión (`getSessionById`) + el animal (`fetchAnimalDetail`) + el gating por **rodeo REAL del animal** (`useManeuverGating(animal.rodeoId)`, R5.3/R5.5); construye la SECUENCIA = orden de `config.maniobras` ∩ maniobras aplicables (`buildSequence`, R5.14 + R5.5); header de identidad SIEMPRE visible (tag legible/idv/visual + rodeo + categoría, R5.1/R12.4) + línea "Tacto · 2 de 4" sobre la secuencia FILTRADA. **DISPATCHER GENÉRICO** (`stepKindFor` → `ManeuverStep` switch): tacto → `TactoStep` (binario PREÑADA/VACÍA **+ tamaño Cabeza/Cuerpo/Cola** condicional, R6.2 — un único `reproductive_events`), pesaje → `PesajeStep` (keypad es-AR, R6.9), resto → `PlaceholderStep` ("pendiente M3", no rompe la secuencia — se saltea sin persistir). PERSISTE cada maniobra al confirmar vía el ESQUELETO del orquestador `app/src/services/maneuver-events.ts` con `session_id` (R5.11; `created_by` por trigger R5.12), CRUD-plano offline (R10.1). RESUMEN corregible (`AnimalSummary`, R5.9): tocar una fila → vuelve a su paso (UPDATE del evento por el mismo id, no duplica). Confirmar → contador `setSessionCounts` (animales++ R5.10) → siguiente animal (`/maniobra/identificar`).
Aceptación: ✅ identidad+rodeo+categoría siempre visibles; maniobras en orden de `config.maniobras` (omitiendo no-aplican, sin reordenar); resumen corregible; contador incrementa por animal; eventos aterrizan en el server CON `session_id`; todo OFFLINE.
**Tests**: unit `app/src/utils/{maneuver-step-kind,maneuver-sequence,maneuver-event-query}.test.ts` (40 casos: dispatcher/secuencia/orquestador) + `maneuver-config.test.ts` ampliado (round-trip server↔local del config jsonb) + `local-reads.test.ts` ampliado (session_id en los builders). e2e `app/e2e/maniobra-carga.spec.ts` (3 escenarios: flujo completo identify→carga→tacto→pesaje→resumen→siguiente con persistencia verificable por oráculo de server `waitForServerWeightEventWithSession`/`waitForServerTactoWithSession`; corrección desde el resumen; OFFLINE→reconexión→drenado). Capturas 412×915 en `design/maniobra-carga/`.
Archivos: `app/app/maniobra/carga.tsx` (frame), `app/app/maniobra/_components/{TactoStep,PesajeStep,PlaceholderStep,AnimalSummary}.tsx`, `app/src/services/maneuver-events.ts` (esqueleto orquestador), `app/src/utils/{maneuver-sequence,maneuver-step-kind,maneuver-event-query}.ts` (+tests), `app/src/utils/maneuver-config.ts` (fix doble-encoding), `app/src/services/powersync/local-reads.ts` (session_id en builders + UPDATE de corrección), `app/src/services/{sessions,maneuver-presets}.ts` (config: unknown), `app/app/_layout.tsx` (`maniobra/carga` sale de DEV_WEB_ROUTES), `app/e2e/{maniobra-carga,maniobra-identify,maniobra-spike}.spec.ts` + `app/e2e/helpers/admin.ts` (oráculos session_id), `scripts/run-tests.mjs` (3 tests nuevos enganchados).
**Diferido a M3** (seam dejado explícito): las otras 8 maniobras (tacto vaquillona, sangrado, vacunación, inseminación, condición corporal, dientes/CUT, pesaje ternero distinto, raspado) + antiparasitario/antibiótico → cada una agrega su renderer en el switch de `ManeuverStep` + su rama en `buildManeuverEventQuery` (el frame NO cambia); el gating per-animal de R6.12 (raspado solo machos), CUT/dientes; el preview de transición offline (R8.4); M3.0-BACKEND (gating `deworming`/`treatment`).

#### [x] BUGFIX-TACTO — 2 bugs de la pantalla de tacto reportados por Raf (web, 2026-06-15)
Satisface (reconciliación as-built): R3.5/R4.2 (auto-avance manual sólo con match EXACTO), R5.7/R5.8/R10.8 (persistencia fail-closed: error superficiado, no tragado).
Detalle: dos bugs reproducidos en web y arreglados.
- **(1) "otra caravana"**: la identificación manual auto-avanzaba a un ÚNICO match por **substring/fuzzy** (no exacto) → cargaba la caravana EQUIVOCADA (tecleo "42" → animal "1428"). Fix en `resolveManualIdentify` (`app/src/utils/maniobra-identify.ts`): el auto-avance manual exige match EXACTO (idv/visual/tag === texto, `isExactMatch`); un único match no-exacto → `ambiguous` → `CandidatePicker` de confirmación (copy adaptado para 1 candidato) o dar de alta. El flash al elegir muestra la caravana del animal ELEGIDO (`onPickCandidate`, `identificar.tsx`).
- **(2) "no avanza"**: el frame (`carga.tsx`, `captureAndAdvance`) tragaba el error del write LOCAL (sin chequear el ServiceResult + `void` sin try/catch) → tapear PREÑADA/VACÍA no hacía nada, sin feedback. Fix: fail-closed — chequea el resultado + try/catch; ante fallo NO avanza y superficia `ManeuverErrorBanner` (`testID="maneuver-capture-error"`, es-AR accionable); `setCaptured` corre tras el write confirmado; guard `capturingRef` anti doble-tap.
**Tests**: unit `app/src/utils/maniobra-identify.test.ts` (+5 casos: match exacto por idv/visual/tag → found; substring no-exacto → ambiguous; 1 candidato sin display → ambiguous seguro). e2e `app/e2e/maniobra-tacto-bugfix.spec.ts` (3 escenarios: substring NO auto-carga → picker → confirma → carga la correcta; match exacto → auto-avance preservado; persist falla → banner visible + NO avanza, reintento → avanza al resumen + persiste server-side con `waitForServerTactoWithSession`). La falla de persist se inyecta con la marca SOLO-E2E `window.__RAFAQ_MANEUVER_FAULT__` (`app/app/maniobra/_components/maneuver-e2e-fault.ts`, patrón gated de `ble-e2e-flag.ts`).
Archivos: `app/src/utils/maniobra-identify.ts` (+test), `app/app/maniobra/identificar.tsx` (`onPickCandidate` flash), `app/app/maniobra/_components/CandidatePicker.tsx` (copy 1-candidato), `app/app/maniobra/carga.tsx` (fail-closed + banner), `app/app/maniobra/_components/maneuver-e2e-fault.ts` (NUEVO, e2e-only), `app/e2e/maniobra-tacto-bugfix.spec.ts` (NUEVO).

---

### M3 — Las 10 maniobras (+ 2 nuevas sanitarias: Antiparasitario / Antibiótico, sesión 26)

> **Maniobras nuevas (sesión 26): Antiparasitario (R6.13/R6.14) + Antibiótico (R6.15).** Ambas `silent_apply` igual que la vacunación, escriben a `sanitary_events` (`deworming`/`treatment`). El **frontend** entra en M3.1/M3.2 (UI estilo vacunación). El **único delta backend** es el gating capa 2 de esos dos `event_type` → sub-chunk **M3.0-BACKEND** (migración + tests, **Gate 1 ANTES de aplicar**). Orden: **M3.0-BACKEND (Gate 1 + deploy gateado por el leader) antes** de habilitar el INSERT de `deworming`/`treatment` del cliente en producción.

#### [x] M3.0-BACKEND — Delta de gating capa 2 para `deworming` + `treatment` ⚠️ toca trigger de spec 02 → **Gate 1 OBLIGATORIO** — ⚠️ PENDIENTE DEPLOY (lo aplica el leader tras Gate 1 + OK de Raf)
Satisface: R7.7 (+ R6.13/R6.14/R6.15 en su parte de enforcement DB).
AS-BUILT (M3.0-BACKEND, 2026-06-15; **migración `0091`, escrita NO aplicada**): se extendió `tg_sanitary_events_gating` (`0054`, que **HOY solo gatea `event_type='vaccination'`**) por la **migración nueva `0091_sanitary_gating_deworming_treatment.sql`** (número re-confirmado contra el árbol: último era `0090`) para ramificar también: `event_type='deworming'` → antiparasitario con **gating OR pura** (D10 CERRADO Raf 2026-06-14: NO se distingue interno/externo; basta que el rodeo real tenga `antiparasitario_interno` **O** `antiparasitario_externo` enabled) vía un helper nuevo `assert_any_data_key_enabled` (mismo fail-closed que `assert_data_keys_enabled` pero `v_have >= 1`); `event_type='treatment'` → `['antibiotico']` (single key, vía `assert_data_keys_enabled`). La rama `vaccination` queda EXACTA (no se altera el gating existente); `test`/`other` no se gatean. Ambos helpers SECURITY DEFINER + search_path=public + EXECUTE revocado de public/authenticated/anon. **NO ramifica por `route` ni agrega data_key nueva** (D10). **Deploy a la DB compartida GATEADO por el leader** (memoria: Supabase MCP en modo escritura) tras **Gate 1 (security modo `spec`) sobre el delta** — reabre la capa de gating.
Tests (suite `supabase/tests/maneuvers/run.cjs`, bloque `T2.4c`, patrón T2.4/T2.4b — **no-bypass**): INSERT `deworming` → 4 combinaciones de la OR (ninguno enabled → `23514`; solo interno → OK; solo externo → OK; ambos → OK); INSERT `treatment` → `antibiotico` enabled OK / disabled `23514`; **no-bypass** (INSERT directo por `service_role` sobre rodeo sin el data_key → `23514`); **fail-closed** (perfil soft-deleted → `23514` para `deworming` y `treatment`); **regresión** (vaccination sigue OK; `event_type='other'` no se gatea); binding-test (R7.2, T2.5) extendido a `antiparasitario_interno`/`antiparasitario_externo`/`antibiotico` presentes en `field_definitions`.
⚠️ **PENDIENTE DEPLOY**: los tests `T2.4c` (los de RECHAZO) **PASAN recién POST-DEPLOY de `0091`**. Pre-deploy fallan por la razón correcta (el trigger viejo `0054` solo gatea `vaccination` → el INSERT de `deworming`/`treatment` que esperamos rechazado se acepta → assert de `23514` falla). NO es bug del test.
Aceptación: `deworming` y `treatment` quedan gateados fail-closed por rodeo real (no-bypass por PostgREST/sync); la migración pasa Gate 1; tests verdes en `check.mjs` **post-deploy**.
Archivos: `supabase/migrations/0091_sanitary_gating_deworming_treatment.sql`, `supabase/tests/maneuvers/run.cjs`.

#### [x] M3.1 — Orquestador de escritura de eventos de maniobra generalizado + aplicabilidad per-animal (frontend puro)
Satisface: R5.8, R5.11, R5.12, R6.1, R6.2, R6.3, R6.4, R6.5, R6.6, R6.7, R6.8, R6.9, R6.10, R6.11, R6.12, **R6.13, R6.14, R6.15**, R8.1, R8.2, R8.3.
AS-BUILT (M3.1, 2026-06-15; **frontend puro sobre backend done + `0091` aplicada**; Gate 1 N/A): generalizó la CAPA DE ESCRITURA + el mapeo + los "kinds" del dispatcher para que M3.2 enchufe las pantallas (NO se construyeron pantallas). Piezas:
  - **`app/src/utils/maneuver-gating.ts`**: `ManeuverKind` extendido a 12 (10 fábrica + `antiparasitario`/`antibiotico`); `MANEUVER_DATA_KEY_REQS` (mapeo con `match` `all`/`any`) — el antiparasitario es `match:'any'` (OR de `antiparasitario_interno`/`antiparasitario_externo`, D10; NO un AND plano); `MANEUVER_DATA_KEYS` se DERIVA de él (compat); `resolveManeuverGating` respeta `all`/`any` (R5.5) + required de los enabled (R5.6). El wizard M1 (`jornada.tsx`) ahora ofrece las 2 nuevas cuando el rodeo las habilita (consecuencia esperada de iterar `ALL_MANEUVERS`).
  - **`app/src/utils/maneuver-step-kind.ts`**: `StepKind` real para CADA maniobra (`tacto`/`vaquillona`/`pesaje`/`score`/`silent_single`/`silent_multi`/`inseminacion`/`lab_single`/`lab_double`/`dientes`) — define los kinds; NO los componentes (M3.2). Sin `placeholder` (M3.1 cabló todas); default defensivo `silent_single`. `stepPersists` → siempre true.
  - **`app/src/utils/maneuver-sequence.ts`**: `StepValue` extendido (unión discriminada por kind para las 12) + `describeStepValue` es-AR del resumen (R5.9). Conserva `skipped` (M2.2 compat).
  - **`app/src/services/powersync/local-reads.ts`**: write-paths nuevos con `session_id` (R5.11) + sus UPDATE de corrección (R5.9): `buildAddManeuverSanitaryInsert`/`buildUpdateManeuverSanitary` (deworming/treatment, **SIN route** — D10), `buildAddManeuverVaccinationInsert` (multi), `buildAddManeuverConditionScoreInsert`/`buildUpdateManeuverConditionScore`, `buildAddManeuverTactoVaquillonaInsert`/`buildUpdateManeuverTactoVaquillona` (heifer_fitness 0053), `buildAddManeuverInseminationInsert`/`buildUpdateManeuverInsemination` (service ai, pajuela en notes), `buildAddManeuverLabSampleInsert`/`buildUpdateManeuverLabSample` (sample_type variable), `buildSetTeethStateUpdate` (propiedad, SIN session_id), `buildSetCutUpdate`/`buildUnsetCutUpdate` (R6.8 + revert consistente).
  - **`app/src/utils/maneuver-event-query.ts`**: `buildManeuverEventQueries` (devuelve **array** de writes — 0/1/2: raspado=2 lab_samples R6.11, vacunación multi=N R6.1, dientes+CUT=2 UPDATE) ramifica por `value.kind`, split INSERT/UPDATE de corrección (R5.9). `buildManeuverEventQuery` se conserva como COMPAT M2.2 (1 write o null).
  - **`app/src/services/maneuver-events.ts`**: `persistManeuverEvent` corre el ARRAY de writes (multi-write) con `runLocalWrite`; tacto/pesaje (M2.2) intactos.
  - **`app/src/utils/maneuver-applicability.ts` (NUEVO)**: aplicabilidad per-animal PURA — `appliesToAnimal`/`filterByAnimalApplicability` (R6.12: raspado solo machos → se salta para hembras; sexo desconocido → se salta, fail-safe) + `shouldOfferCutPrompt` (R6.8: prompt CUT solo en 1/2·1/4·sin_dientes, NO para terneros). **AS-BUILT v2 (bugfix 2026-06-16, frontend puro, Gate 1 N/A)**: `appliesToAnimal` se EXTENDIÓ a 3 ejes (las demás celdas de la matriz siguen `return true`, pendientes de validar con Facundo): (a) **tacto / tacto_vaquillona → solo HEMBRAS** (R6.2/R6.3 — un toro no se tacta; sexo `null` → se salta, fail-safe); (b) **pesaje vs pesaje_ternero MUTUAMENTE EXCLUYENTES por categoría** (R6.9/R6.10 — ambos usan el data_key `peso`; sin este split el animal pasaba por los DOS pasos de peso = el doble pesaje que reportó Raf): `pesaje_ternero` aplica SSI `categoryCode ∈ {ternero, ternera}`, `pesaje` aplica en caso contrario INCLUYENDO `categoryCode == null` (categoría desconocida → peso genérico de adulto, no de ternero — fail-safe); (c) raspado-solo-machos sin cambios. Tests: celdas nuevas + integración `buildSequence ∘ filterByAnimalApplicability` (ternero→pesaje_ternero sin pesaje; adulto→pesaje sin pesaje_ternero; null→pesaje; macho→ambos tactos saltados). e2e: `maniobra-sanitaria` test 3 ahora elige `pesaje_ternero` para la ternera (el `pesaje` genérico ya no aplica a una cría).
Tests: unit por rama del orquestador/builder (cada maniobra → tabla+campos+session_id; INSERT vs UPDATE de corrección; OR del antiparasitario; dientes=UPDATE no evento sin session_id; raspado=2 samples; aplicabilidad R6.12/R6.8) + node:sqlite (INSERT de deworming persiste sin route; corrección score = 1 fila con valor corregido). `check.mjs` RC=0 (typecheck + anti-hardcode + 207 unit de maniobras verdes + backend `T2.4c` deworming/treatment verde post-deploy de 0091).
**DIFERIDO A M3.2 (las pantallas)**: los COMPONENTES de cada StepKind (silent_apply/lab/score/vaquillona/inseminacion/dientes), el flujo de tacto 2-pasos UI, el prompt CUT UI, el 1-vs->1-pajuela UI, el autocompletar de producto, el preview de transición offline (R8.4), el lote opcional (R9.x), el orden de presentación R5.14, y el ajuste de label `humanizeSanitaryEventType` (`deworming`→"Antiparasitario"/`treatment`→"Antibiótico") si Raf lo prefiere. M3.1 dejó el SEAM listo: M3.2 lee `stepKindFor`, captura el `StepValue` y llama `persistManeuverEvent`; usa `appliesToAnimal`/`shouldOfferCutPrompt` al secuenciar.
Archivos: `app/src/services/maneuver-events.ts`, `app/src/utils/maneuver-event-query.ts`, `app/src/utils/maneuver-step-kind.ts`, `app/src/utils/maneuver-sequence.ts`, `app/src/utils/maneuver-gating.ts`, `app/src/utils/maneuver-applicability.ts` (nuevo), `app/src/services/powersync/local-reads.ts`, `app/src/utils/maneuver-wizard.ts` (2 labels). Tests: los `.test.ts` espejo + `local-reads.test.ts`.

> **Detalle ORIGINAL de M3.1 (pre-as-built, conservado como referencia del plan):**
Detalle: `app/src/services/maneuver-events.ts` orquesta los services de evento de spec 02 **inyectando `session_id`** (CRUD-plano):
  - reusa `addWeight` (R6.9 peso manual / R6.10 ternero con autocompletar categoría + `fetchMother`), `addConditionScore` (R6.6), `addTacto` (R6.2; dispara la transición server-side R8.1/R8.2/R8.3), `addService` (R6.5 inseminación, 1 vs >1 pajuela);
  - **write-paths NUEVOS** (no hay builder as-built): vacunación silenciosa multi-vacuna → `sanitary_events` (R6.1), sangrado → `lab_samples` `blood` con nº tubo (R6.4), raspado → 2× `lab_samples` `scrape_*` (R6.11, **solo machos** R6.12), tacto vaquillona → `reproductive_events` `event_type='tacto_vaquillona'` + `heifer_fitness` (R6.3, enum ya en `0053`), dientes → UPDATE `animal_profiles.teeth_state` (R6.7) + prompt CUT → UPDATE `is_cut`/`category_id`/`category_override` (R6.8, NO para terneros). El gating capa 2 (`0054`) ya valida estos INSERT/UPDATE server-side → el cliente solo necesita el write-path + el `session_id`.
  - **Maniobras nuevas (sesión 26)** — write-paths silent_apply a `sanitary_events`, mismo patrón que la vacunación (R6.1): **Antiparasitario** (R6.13/R6.14) → `sanitary_events` `event_type='deworming'` con `product_name` (texto libre + autocompletar) — **una sola maniobra SIN sub-elección estructurada de interno/externo** (D10 CERRADO Raf 2026-06-14: la vía, si se anota, va como texto libre en `product_name`/notas; NO se persiste `route` ni columna nueva); **Antibiótico** (R6.15) → `sanitary_events` `event_type='treatment'` con `product_name`. Ambos llevan `session_id` (R5.11). El gating capa 2 de estos dos `event_type` lo agrega **M3.0-BACKEND** (migración `0091`, gating OR del `deworming`) — **ordenar M3.0-BACKEND (Gate 1 + deploy) antes** de habilitar el INSERT de `deworming`/`treatment` en producción.
  - Persiste al confirmar cada maniobra (R5.8); `session_id` en cada evento (R5.11); `created_by` por trigger (R5.12).
Aceptación: unit + integración; cada maniobra escribe en su tabla con `session_id`; antiparasitario escribe `deworming` (una sola maniobra, sin sub-elección estructurada interno/externo — D10) y antibiótico escribe `treatment`, ambos silent_apply; prompt CUT no aplica a terneros; raspado se saltea para hembras; el tacto dispara la transición al subir (verificable por re-fetch del detalle).
Archivos: `app/src/services/maneuver-events.ts`.

#### [x] M3.2a — Pantallas de "elegir un valor": tacto vaquillona + condición corporal + dientes/CUT (frontend puro)
Satisface: R5.2, R6.3, **R5.13**, R6.6, R6.7, R6.8, R5.7 (parcial: estos 3 no tienen campo opcional faltante), R12.5.
AS-BUILT (M3.2a, 2026-06-15; **frontend puro sobre M3.1 done + backend done incl. 0091**; Gate 1 N/A): las 3 pantallas de paso de las maniobras de "elegir un valor", enchufadas al `switch` del dispatcher `ManeuverStep` de `app/app/maniobra/carga.tsx` (el SEAM de M3.1 — el frame no cambió, se agregaron los `case 'vaquillona'`/`'score'`/`'dientes'`). Piezas:
  - **`app/app/maniobra/_components/TactoVaquillonaStep.tsx`** (R6.3/R5.13): 3 bloques GIGANTES full-width que se reparten el alto — **APTA** (verde `$primary`, ✓), **NO APTA** (terracota `$terracota`, ✗), **DIFERIDA** (ámbar `$amber`, ⏲ reloj). Un toque = `onConfirm(fitness)` → `reproductive_events` tacto_vaquillona + heifer_fitness (0053).
  - **`app/app/maniobra/_components/CondicionCorporalStep.tsx`** (R6.6): STEPPER gigante — valor hero `$11`/64px centrado (coma es-AR "3,00"), botones − / + GIGANTES (`$stepperBtn`=88px ≥80) a los lados, step **0,25**, rango 1,00–5,00, **default 3,00**, − / + deshabilitados en los límites. Pista de escala VISUAL (5 marcas con la actual resaltada + "1=flaca·5=gorda") para densidad R12.5. CTA "Confirmar" → `condition_score_events`. Aritmética en la util pura `app/src/utils/condition-stepper.ts` (clamp/snap a grilla de 0,25 sin drift de coma flotante, ±, formato es-AR).
  - **`app/app/maniobra/_components/DientesStep.tsx`** (R6.7/R6.8): bloques GIGANTES verticales con los 8 valores del enum `teeth_state_enum` (0020) en orden de boca (`app/src/utils/teeth-options.ts`, labels es-AR). Tocar uno = elige → UPDATE `animal_profiles.teeth_state` (propiedad, no evento). **Prompt CUT (R6.8)**: si el valor ∈ {1/2, 1/4, sin_dientes} (`CUT_PROMPT_TEETH`, reusado de `maneuver-applicability.ts`) Y el animal NO es ternero (`shouldOfferCutPrompt`) → SHEET de confirmación (patrón as-built `ManeuverConfigSheet`: scrim + sheet anclado + grip + safe-area) con **[Marcar CUT]** (terracota, destructivo) / **[No, solo registrar dientes]**; confirmar → UPDATE is_cut/category_id(CUT)/override reusando el camino de categoría as-built. Desmarcar / no-CUT no rompe (revierte a la categoría derivada).
  - **`app/src/services/animals.ts` → `resolveCutCategory(profileId)`** (NUEVO): resuelve del catálogo LOCAL (offline) el `category_id` de CUT (code='cut' del sistema del rodeo) + la categoría DERIVADA del revert (reusa `resolveRevertCategory`). El frame lo invoca al confirmar dientes y pasa el id correcto (CUT al marcar / derivada al desmarcar) a `persistManeuverEvent` (parámetro `cutCategoryId` que M3.1 ya soportaba). Fail-safe: sin id → solo teeth_state.
  - **`app/app/maniobra/carga.tsx`**: dispatcher `ManeuverStep` con los 3 `case` nuevos + paso del `animal` real (para el gate del prompt CUT, R6.8) + resolución de `cutCategoryId` en `captureAndAdvance` para la maniobra dientes.
  - **Token de color `$amber`/`$amberPress`** (DIFERIDA del tacto vaquillona) + token `$stepperBtn`=88 (botón − / + del stepper) en `app/tamagui.config.ts` — JIT, provisionales a canonizar al aprobar la dirección (mismo patrón que heroScan/StickIcon de M2.1).
Tests: unit puros `condition-stepper.test.ts` (clamp/snap/±/límites/formato es-AR, 18 casos) + `teeth-options.test.ts` (cobertura del enum + mapeo valor→CUT-trigger derivado de CUT_PROMPT_TEETH + labels, 9 casos). e2e `app/e2e/maniobra-elegir.spec.ts` (2/2): jornada con las 3 (apta + stepper 3,25 + dientes 1/2→prompt→Marcar CUT, verifica heifer_fitness/score/teeth_state+is_cut+categoría='cut' server-side) + el prompt CUT NO aparece para un ternero (R6.8). 4 capturas 412×915 en `design/maniobra-elegir/`. `check.mjs` RC=0.
**DIFERIDO A M3.2b** (el resto de M3.2 — el seam de M3.1 sigue listo): tacto vaca 2-pasos (ya en M2.2 `TactoStep`) + sanitarias silent_apply (vacunación multi `silent_multi` / antiparasitario / antibiótico `silent_single`) + tubos (sangrado `lab_single` / raspado `lab_double`) + inseminación + pesaje ternero (autocompletar categoría R6.10) + frame: preview de transición offline (R8.4), lote opcional (R9.1/R9.2/R9.3), required vs opcional por rodeo (R5.6/R5.7 para las de campo opcional), label timeline `humanizeSanitaryEventType`.

#### [x] M3.2b — Pantallas paso-por-maniobra restantes (sanitarias silent_apply + tubos + pesaje ternero + **inseminación R6.5**)
Satisface: R5.2, R5.4, R5.5, R5.7, **R5.14**, R6.1, R6.4, **R6.5**, R6.9, R6.10, R6.11, R6.12, **R6.13, R6.14, R6.15**, R12.1, R12.5.

AS-BUILT (M3.2b, 2026-06-15; **frontend puro sobre M3.1 + M3.2a + backend done `0091`**; Gate 1 N\A): construyó las pantallas de paso que faltaban + sus `case` en el dispatcher de `carga.tsx` (SEAM de M3.1 — el frame NO se reescribió). Con esto quedan renderizables **las 12 maniobras**. Piezas:
  - **`SilentSanitaryStep.tsx` (NUEVO)** — silent_apply de UN producto (Antiparasitario→`deworming` R6.13, Antibiótico→`treatment` R6.15). Con producto preconfig (R1.7) → HERO del producto + "Aplicar y seguir" (1 toque); SIN producto → input grande + autocompletar "Usadas antes" (R1.8, reusa `filterAutocomplete`); producto EDITABLE ("Cambiar producto"). CTA SIEMPRE habilitado (silent: la maniobra "se aplicó" persiste un sanitary_event aunque el product_name quede vacío). El `eventType` lo fija la maniobra (no el usuario). D10: SIN route/interno-externo.
  - **`SilentVaccinationStep.tsx` (NUEVO)** — silent_apply MULTI (Vacunación R6.1, N vacunas → N `sanitary_events vaccination`). Chips con × (reusa el patrón multi de `ManeuverConfigSheet`) + input + "Agregar" + autocompletar. Vacunas de la tanda (preconfig) pre-cargadas. CTA deshabilitado con CERO vacunas (R6.1 escribe 1 fila por vacuna → no se "aplica" cero).
  - **`LabSampleStep.tsx` (NUEVO)** — Sangrado brucelosis (R6.4): 1 número de tubo → `lab_samples blood`. Input de TEXTO grande ($tubeText=24), NO keypad: `lab_samples.tube_number` es `text` (0029) y los códigos de lab son ALFANUMÉRICOS (ej. "A-104"); SIN formato es-AR (código de máquina). Tubo REQUERIDO (NOT NULL + R5.7 → CTA bloqueado si vacío). `maxLength=64` (cap server-side 0070).
  - **`LabDoubleStep.tsx` (NUEVO)** — Raspado de toros (R6.11): 2 números de tubo (Tricomoniasis + Campylobacteriosis, ambos visibles, claramente etiquetados) → 2 `lab_samples scrape_*`. Ambos REQUERIDOS (CTA bloqueado hasta los 2). Solo machos (R6.12): el skip de hembras lo aplica el frame (ver abajo).
  - **Pesaje de ternero (R6.10)** — reusa `PesajeStep` (ya cae en `case 'pesaje'`); la categoría ternero/ternera la autocompleta el espejo C6 (header), el vínculo con la madre viene de `birth_calves` (no se re-captura). Sin componente nuevo.
  - **FIX del frame (R6.12, cazado en autorrevisión)**: la `sequence` de `carga.tsx` aplicaba SOLO el gating por rodeo (R5.5), NO la aplicabilidad per-animal (`filterByAnimalApplicability` de M3.1). El raspado NO se saltaba para hembras (bug). Cerrado: la secuencia ahora es `orden de config ∩ gating del rodeo ∩ aplicabilidad por atributos del animal` (R5.14 + R5.5 + R6.12). Pasó a depender de `animal` (sexo). e2e verifica: hembra con {raspado, antiparasitario} → · 1 de 1 (raspado saltado, 0 scrape_* en el server).
  - **eventIds multi-write** (frame): el orquestador (M3.1) usa `eventId` para el 1er write y `eventIds[i-1]` para los siguientes (raspado 2; vacunación N); su fallback (`${eventId}-${i}`) NO es UUID → rompería al subir (22P02). El frame genera UUIDs ESTABLES adicionales (`extraIdsFor`) para los writes extra. + soft-delete de huérfanos al corregir una vacunación con MENOS vacunas (de 2→1: la fila extra ya escrita no se pisa por el re-INSERT → `softDeleteManeuverEvents`).
  - **`maneuver-config.ts`**: helpers PUROS nuevos `preconfigStringFor` (producto de tanda por maniobra, tolerante a string/objeto) + `preconfigHistory` (histórico aplanado para el autocompletar, source-agnostic, espeja DM1-UI-1) — con tests.
  - **`maneuver-events.ts`**: `softDeleteManeuverEvents` (retira las filas huérfanas de una corrección multi-write; RLS owner|autor es la barrera real).
  - **Tokens nuevos (JIT)**: `$tubeText`=24 (número de tubo grande). En `tamagui.config.ts`.
Tests: unit `maneuver-config.test.ts` (preconfigStringFor + preconfigHistory, 8 casos) + e2e `maniobra-sanitaria.spec.ts` (3/3: macho con las 5 → server-side deworming SIN route/treatment/2 vaccination/blood/2 scrape_*; hembra → raspado saltado + 0 scrape_*; ternera → pesaje + categoría) con oráculos server `waitForServerSanitaryWithSession`/`waitForServerLabSampleWithSession`/`countScrapeSamples`. 5 capturas 412×915 en `design/maniobra-sanitaria/` (vacunacion, sanitaria-silent [hero antiparasitario], sangrado, raspado, pesaje-ternero). `check.mjs` RC=0. Detalle/mapa R→test/autorrevisión: `progress/impl_03-m3.2b.md`.

**FIX-LOOP M3.2b (2026-06-15; frontend puro, Gate 1 N\A)** — veto del leader de las capturas, 2 cambios:
  - **(1) CERO ESPACIO MUERTO (Gate 0)**: las pantallas no-keypad (silent sanitario / vacunación / sangrado / raspado) dejaban ~50-60% vacío → se pasó el contenido a una **CARD DOMINANTE de superficie** (`flex={1}`, patrón CondicionCorporalStep figura-fondo) que ocupa el alto; CTA gigante abajo DISJUNTO ("Cambiar producto" dentro de la card → sin mis-tap). Lab inputs centrados verticalmente en la card. Comportamiento funcional intacto.
  - **(2) INSEMINACIÓN (R6.5)** — `InseminacionStep.tsx` (NUEVO) + `case 'inseminacion'` en el dispatcher. El write-path YA existía en M3.1 (`buildAddManeuverInseminationInsert` → `reproductive_events` service ai, pajuela en `notes`; data_key/StepKind/StepValue mapeados) → solo UI + case. 1 pajuela → confirmar de un toque (reusa `SilentSanitaryStep` con copia parametrizada de inseminación); >1 → SELECTOR (bloques + "Otra pajuela"). Pajuela texto libre + autocompletar (R1.8). Helper PURO nuevo `pajuelasFor` (`maneuver-config.ts`, 1 vs >1) + `preconfigHistory` enriquecido con pajuelas multi. **Inseminación nace deshabilitada en cría (0018) → el e2e prende el data_key (`setRodeoDataKey`)**; el server re-valida (capa 2, 0054). Corrección (R5.9): UPDATE de notes (no 2do INSERT).
  - Tests del fix: unit `maneuver-config.test.ts` (`pajuelasFor` + `preconfigHistory` enriquecido, 8 casos nuevos, 28 total) + e2e `maniobra-sanitaria.spec.ts` (5/5: las 3 previas + inseminación 1 pajuela → service ai en server con session + pajuela en notes; >1 pajuela → selector → elige "Toro 456" → server). Oráculo server `waitForServerInseminationWithSession`. **5 capturas re-hechas (alto LLENO)**: sanitaria-silent, vacunacion, sangrado, raspado, **inseminacion** (nueva). `check.mjs` RC=0. Detalle/autorrevisión/reconciliación: `progress/impl_03-m3.2b.md` (sección fix).

#### [~] M3.2c — Frame: preview transición R8.4 (DONE) + lote R9.x (DONE) + label timeline (pendiente)
Satisface: R8.4 **(as-built chunk R8.4, 2026-06-17 — DONE; pendiente reviewer + Gate 2)** · R9.1, R9.2, R9.3 **(as-built chunk R9.x, 2026-06-19 — DONE; pendiente reviewer + Gate 2)** · R9.4 (deferido/estructural, ver abajo).
Detalle: lo que QUEDÓ fuera de M3.2b (decisión: agrupar con offline/reanudación de M4). **Preview de transición de categoría offline** (R8.4) reusando `computeCategoryCode` de `app/src/utils/animal-category.ts` (NO `transitions.ts`, que no existe). **Lote manual opcional** desde el RESUMEN del wizard (R9.2 vía `assignAnimalToGroup`); nunca auto-asigna lote (R9.1/R9.3) — **DONE**. **R9.4 (`work_lot_label`)**: DEFERIDO/fuera de scope — metadata no-autoritativa "o se omite" (context §Lote); satisfecho ESTRUCTURALMENTE (columna texto sin FK asignadora; sin UI). **Timeline label (R6.13/R6.15)**: verificar/ajustar `humanizeSanitaryEventType` (`event-timeline.ts`) — **pendiente** (no-bloqueante: el evento YA aparece con su label actual). *(La inseminación R6.5 se hizo en el fix-loop M3.2b.)*
Aceptación: **R8.4 cubierto** — preview de transición visible antes de sincronizar (caso canónico tacto+/vaquillona → vaquillona preñada), display-only, offline, fail-safe (no se muestra si override/macho/sin-cambio/tacto-vacío/sin-catálogo). **R9.x cubierto** — afordancia "Lote (opcional)" en el resumen → sheet (Sin lote primero + grupos) → asigna/cambia/quita (null) offline, optimista, oráculo server `management_group_id`; NO auto-asigna (contraprueba e2e). Label timeline queda pendiente.
Archivos R8.4 (as-built): `app/src/utils/maneuver-category-preview.ts` (+`.test.ts`, 19, incl. round-trip antidrift), `app/src/services/animals.ts` (`fetchRodeoCategoryCatalog`, append-only), `app/app/maniobra/carga.tsx` (catálogo + `transitionPreview`), `app/app/maniobra/_components/AnimalSummary.tsx` (`CategoryPreviewBanner`, testID `summary-category-preview`), `app/e2e/maniobra-preview-transicion.spec.ts` (2/2: canónico + negativo).
Archivos R9.x (as-built): `app/src/utils/lote-picker.ts` (+`.test.ts`, 6), `app/app/maniobra/_components/LotePickerSheet.tsx` (NUEVO), `app/app/maniobra/_components/AnimalSummary.tsx` (`LoteAffordance`, props loteName/onOpenLote, testID `summary-lote-row`), `app/app/maniobra/carga.tsx` (groups + sheet + `onAssignLote` + loteError), `app/e2e/maniobra-lote.spec.ts` (2/2: R9.2 offline + R9.1/R9.3 no-auto), `app/e2e/helpers/admin.ts` (append: `seedManagementGroup`, `waitForServerProfileManagementGroup`, `readServerProfileManagementGroup`), `scripts/run-tests.mjs` (registra el unit). Archivo pendiente (label timeline): `app/src/utils/event-timeline.ts`.

---

### M4 — Offline / reanudación / surfacing

#### [~] M4.1 — Reanudación en el landing (R10.5/R10.6 DONE; R8.4 movido a M3.2c — DONE 2026-06-17)
Satisface: R10.5, R10.6 **(as-built M4-reanudación, 2026-06-16)** · R8.4 **(movido a M3.2c, as-built chunk R8.4 2026-06-17 — DONE; pendiente reviewer + Gate 2)**.
> **As-built (2026-06-16) — reconciliado a un patrón MÁS SIMPLE que el `ManeuverSessionContext` planeado.** NO se construyó un `ManeuverSessionContext` nuevo: la reanudación se resuelve en el **landing** (`app/app/maniobra.tsx`) reusando los servicios que YA existían (M1): `getActiveSession(establishmentId)` (lectura LOCAL offline) en el `useFocusEffect` + `closeSession`. Una sesión `active` del dispositivo → **tarjeta "Retomar la jornada de hoy"** (rodeo via RodeoContext + maniobras + N animales + fecha si no es hoy) → tap → `/maniobra/identificar?sessionId` (R10.5). "Nueva jornada" con una abierta → **`NuevaJornadaConfirmSheet`** (empezar una nueva [el cierre de TODAS las activas lo hace `createSession` al arrancar — §6.bis.12 / M4.1.1] / retomar la abierta / cancelar) — "una sola sesión activa por dispositivo" con guard de carrera (R10.6). Lógica pura en `app/src/utils/maniobra-resume.ts` (+test). Ver `design.md` §6.bis.11 + notas as-built bajo R10.5/R10.6. **R8.4 (preview de transición de categoría offline) NO entró en este chunk** — se construyó después en **M3.2c (chunk R8.4, 2026-06-17)**: un banner display-only en el RESUMEN del animal (`AnimalSummary`) que anticipa la transición que el server aplicará, reusando el espejo C6 `computeCategoryCode`. Ver M3.2c arriba.
Aceptación (cubierta): e2e `maniobra-reanudar.spec.ts` 4/4 (abierta→tarjeta retoma→identificar; "Nueva jornada"+abierta→sheet→"Empezar una nueva"→wizard [el cierre lo hace createSession al arrancar, oráculo server]; sin abierta→directo; sheet→"Retomar la abierta"→identificar) + regresión tap-through en `maniobra-config-sheet-race.spec.ts` + unit `maniobra-resume.test.ts`.
Archivos: `app/app/maniobra.tsx`, `app/app/maniobra/_components/NuevaJornadaConfirmSheet.tsx`, `app/src/utils/maniobra-resume.ts` (+`.test.ts`), `app/e2e/maniobra-reanudar.spec.ts`, `app/e2e/captures/maniobra-reanudar.capture.ts`.

#### [x] M4.1.1 — Enforzar ≤1 sesión activa por establishment (R10.6, bugfix 2026-06-16)
Satisface: R10.6 **(enforcement as-built, 2026-06-16)**.
> **Bug (reportado por Raf, confirmado en la DB):** R10.6 estaba documentado pero **nada lo enforzaba**. Cada "Arrancar jornada" hacía `createSession` (INSERT de una `session` `active`) SIN cerrar las anteriores, y "Salir sin terminar" deja la sesión `active` → se ACUMULABAN sesiones `active` huérfanas (en la DB de prueba: 4 activas + 2 cerradas). Síntoma: tras "Terminar jornada", `getActiveSession` (`ORDER BY started_at DESC LIMIT 1`) devolvía la SIGUIENTE activa huérfana → la tarjeta "Retomar la jornada de hoy" seguía apareciendo → parecía que "no terminó". El `NuevaJornadaConfirmSheet` "Empezar una nueva" cerraba SOLO UNA → no resolvía las huérfanas previas.
> **Fix (as-built):** `createSession` cierra **TODAS** las activas del establishment ANTES de insertar la nueva → tras cualquier `createSession` queda EXACTAMENTE 1 activa (la nueva). Es el ÚNICO punto de enforcement (único call-site de creación del flujo) → el invariante vale sin importar por dónde se llegó (wizard "Arrancar", futuros call-sites). Builder nuevo `buildCloseActiveSessionsUpdate(establishmentId, endedAt)` (`UPDATE sessions SET status='closed', ended_at=? WHERE establishment_id=? AND status='active' AND deleted_at IS NULL`) + service `closeActiveSessions(establishmentId)` (CRUD-plano local offline) llamado por `createSession` (fail-closed: si el close-all falla, NO inserta). El `closeSession(open.id)` explícito del `NuevaJornadaConfirmSheet` quedó REDUNDANTE → simplificado (un solo camino de cierre, sin doble-close): "Empezar una nueva" solo navega al wizard; el cierre lo hace `createSession` al arrancar. Ver `design.md` §6.bis.12 + nota as-built bajo R10.6.
Aceptación (cubierta): unit `maneuver-reads.test.ts` (buildCloseActiveSessionsUpdate: cierra todas las activas del est; NO toca otros est / cerradas / soft-deleted) + e2e `maniobra-single-active.spec.ts` (2 activas sembradas server → arrancar nueva → oráculo SERVER `count(*) active`=1, distinta de las 2 sembradas → "Terminar" → 0 activas → landing ya no ofrece retomar) + `maniobra-reanudar.spec.ts` (b) actualizado (cierre vía createSession al arrancar).
Archivos: `app/src/services/powersync/local-reads.ts`, `app/src/services/sessions.ts`, `app/app/maniobra.tsx`, `app/src/services/powersync/maneuver-reads.test.ts`, `app/e2e/maniobra-single-active.spec.ts`, `app/e2e/maniobra-reanudar.spec.ts`, `app/e2e/helpers/admin.ts`.

#### [x] M4.2 — Surfacing de rechazos de sync (R10.8) — DONE (implementer Opus, 2026-06-17; pendiente reviewer + Gate 2)
Satisface: R10.8 **(as-built M4.2-rechazos-sync, 2026-06-17)**.
> **As-built (2026-06-17) — el canal NO era un store, había que materializarlo.** El design (§5/§642) decía "el canal ya existe, 03 lo consume" — pero `connector.surfaceUploadRejection` solo hacía `console.warn` (un LOG, no un store observable). Este chunk **construye el store** + le da UI de manga. **Store** `app/src/services/powersync/upload-rejections.ts`: in-memory observable (`useSyncExternalStore`); cada rechazo = `{ id, table, op, code, at }` — **NUNCA `opData`** (privacidad, datos de campo); lista ACOTADA (cap 50, descarta los más viejos) + DEDUP por id (op re-rechazada no duplica). API: `recordUploadRejection(op, error)` (lo llama el connector), `useUploadRejections()`, `acknowledgeUploadRejections(ids?)`/`clearUploadRejections()`. Helpers PUROS es-AR: `rejectionReason(table, code)` (`23514`→rodeo dejó de habilitar/animal movido; `42501`→sin permiso; otro→servidor rechazó; prefijo = tipo de maniobra de la tabla, R5.4), `rejectionWhenLabel(at)` (recién/hace N min/h/dd-mm), `rejectionBannerTitle(n)` (pluralización sustantivo+verbo), `isManeuverRejection(table)` (filtra a las 5 tablas de evento). **Connector** (`connector.ts`): `surfaceUploadRejection` llama `recordUploadRejection` en su PROPIO try/catch — **best-effort, NUNCA throw** (no rompe el drenado de la upload queue); cero cambio a la clasificación de errores ni al flujo de `uploadData`. **UI** (`app/app/maniobra.tsx`): banner **terracota** (`$terracota`, aviso del DS, NO rojo) "N maniobra(s) no se sincronizó/sincronizaron" ARRIBA de la tarjeta de retomar / "Tus rutinas" cuando hay rechazos de maniobra → tap → **`SyncRechazoSheet`** (idiom lockeado ExitJornadaSheet/SavePresetSheet + guard tap-through doble-rAF) que lista cada rechazo (tipo + motivo + cuándo) + **"Entendido"** (acknowledge + cierra). **SIN "rehacer"** (manual: el operario rehace en su próxima jornada; el motivo le dice qué pasó). El camino de producción real está cableado (no solo E2E): los maniobra-events son CRUD-plano → `23514`/`42501` clasifican permanente (`isPermanentServerCode`) → `surfaceUploadRejection`. Ver `design.md` §5 (as-built R10.8) + §6.bis nota.
Aceptación (cubierta): unit `upload-rejections.test.ts` 17/17 (motivo es-AR por code/tabla, cap 50, dedup, NO opData, best-effort no-throw, acknowledge/clear, filtro maniobra, when/título pluralización) + e2e `maniobra-rechazo-sync.spec.ts` 3/3 (rechazo de maniobra armado [hook SOLO-E2E `__RAFAQ_SYNC_REJECT_E2E__`, gated fuera de prod] → banner → sheet con motivo → "Entendido" lo limpia; sin rechazo → sin banner; rechazo de otra tabla → no dispara el banner de manga) + 4 capturas web táctil 360/412 en `tests/modo-maniobra/sync-rechazo-{banner,sheet}-{360,412}.png`.
Archivos: `app/src/services/powersync/upload-rejections.ts` (+`.test.ts`), `app/src/services/powersync/connector.ts`, `app/app/maniobra.tsx`, `app/app/maniobra/_components/SyncRechazoSheet.tsx`, `app/app/maniobra/_components/sync-rechazo-e2e.ts`, `app/e2e/maniobra-rechazo-sync.spec.ts`, `app/e2e/captures/sync-rechazo-banner.capture.ts`, `scripts/run-tests.mjs`.

#### [x] M4.3 — Offline (PowerSync) + cierre (verificación) — DONE (verificación E2E, 2026-06-19)
Satisface: R10.1, R10.2, R10.3, R10.7, R12.1.
Detalle: `sessions`/`maneuver_presets` ya están en las sync rules + `AppSchema` (M1, scope establishment). Verificar carga 100 % offline de una jornada y sync posterior (R10.1/R10.2); cache de `rodeo_data_config` para gating offline (R10.3, ya sincroniza). Cierre explícito de sesión (`status='closed'`, R10.7) **ya done en M2.1-exit-hero** (ExitJornadaSheet → `closeSession`). Orden de cierre offline (eventos antes del close, design §5). Este chunk es de **verificación/cobertura** (el grueso del substrato ya existe).
Aceptación: jornada completa offline → sincroniza al recuperar señal; gating offline correcto.
Archivos: `app/src/services/powersync/schema.ts` (verificar buckets), tests en `app/e2e/*`.
> **AS-BUILT (M4.3, 2026-06-19 — verificación/cobertura, frontend/test puro, Gate 1 N/A).** E2E nuevo `app/e2e/maniobra-offline.spec.ts` (1 test, **VERDE**) que camina una **jornada completa OFFLINE por el camino MANUAL** (sin BLE, manual-first): config de jornada {vacunación, pesaje} → `setOffline(true)` (corte de red CDP, mismo idiom que `animals-offline.spec.ts`) → **createSession offline** (R1.11) → identificación manual (lookup LOCAL, R3.5) → carga de **2 maniobras de DOS tablas de evento distintas** (vacunación silent_apply → `sanitary_events` + pesaje → `weight_events`) → **cierre de la jornada** (ExitJornadaSheet → "Terminar jornada" → `closeSession`) → `setOffline(false)` → **oráculo SERVER-SIDE** (service_role). **Cubre**: R10.1 (sesión + identificación + ≥2 eventos cargados 100% offline); R10.2 (sync posterior + cero `upload rechazado` en consola del page); **R10.3** (gating offline desde el cache local de `rodeo_data_config`: el pool del wizard ofrece solo las HABILITADAS — pesaje + vacunación presentes, `inseminacion` AUSENTE por off-by-default en cría 0018 — y el frame de carga resuelve la secuencia OFFLINE a exactamente esas 2, `· 1 de 2`/`· 2 de 2`); **R10.7** (cierre `status='closed'` + **orden offline events-before-close**). **Prueba del FK + orden de cierre**: el oráculo cruza el `session_id` de AMBOS eventos (deben ser EL MISMO) y verifica que esa sesión quedó **`closed` server-side** (`waitForServerSessionClosed`) — la sesión cerrada CONSERVA sus eventos con su `session_id` FK (no huérfanos) → el close subió DESPUÉS de los eventos; si el close hubiera subido antes, el tenant-check `0056` habría rechazado los eventos por sesión 'closed' → habría `upload rechazado` (assert `=== []`). **Oráculos**: REUSO de `admin.ts` (`waitForServerWeightEventWithSession`, `waitForServerSanitaryWithSession`, `waitForServerSessionClosed`) — **0 helpers nuevos**. **Orden de cierre**: NO hay lógica pura de ordenamiento de la upload queue — el orden lo da el **FIFO de inserción de la CRUD queue** (cada maniobra hace `runLocalWrite` ANTES del `closeSession`, documentado en `app/src/services/sessions.ts:164-169`); el E2E es la prueba end-to-end (la suite de DB T2.6 ya prueba server-side que crear-eventos→cerrar NO rechaza los eventos ya creados). Verificado: E2E `1 passed`; `check.mjs` rojo = SOLO el flake `animals_tag_unique` (23505) de la suite backend `animal/run.cjs` (colisión cross-terminal con spec-08 SIGSA en la DB compartida) — NO regresión (cambio test-only/frontend). Mapa R→test + autorrevisión: `progress/impl_03-m4.3-offline.md`.

---

## M5 — Datos y maniobras CUSTOM (chunk nuevo, foldado 2026-06-13 desde `context-m5-custom-maniobras.md`)

> **A diferencia de M1-M4 (frontend puro, Gate 1 N/A), M5-BACKEND REABRE schema/RLS** (`field_definitions` de spec 02 + 2 tablas nuevas + triggers de gating + sync delta) → **Gate 1 (security modo `spec`) OBLIGATORIO sobre el delta backend ANTES de la Puerta 1**. Cubre US-13 (R13.1–R13.24). Especificación en `design.md` §11. **No se aplica DDL en esta pasada** — el deploy a la DB compartida lo gatea el leader (memoria: Supabase MCP en modo escritura). **M5-CLIENTE** consume el as-built (CRUD-plano feature 15) → **Gate 2 (code) por sub-chunk**.

> **Numeración de migraciones (M5-BACKEND) — RE-CONFIRMAR antes de crear archivos.** Último as-built = `0089_assign_tag_to_animal_rpc.sql` → próximo libre **0090**. Otra terminal trabaja spec 08 (SIGSA, capa pura) y a la fecha **no agregó migraciones**, pero el implementer **re-confirma el rango libre contra el árbol Y lo que la otra terminal tenga en vuelo** justo antes de crear los `.sql`. Propuesta (sujeta a re-confirmación): `0090_field_definitions_custom` / `0091_custom_measurements` / `0092_custom_attributes` / `0093_custom_gating` / `0094_check_grants`. El `ALTER`/drop-constraint que no sea transaccionable se aísla en su propia migración si el motor lo exige.

### M5-BACKEND — delta schema + gating + sync (⚠️ Gate 1 ANTES de aplicar)

#### [~] M5-B.1 — Migración `field_definitions` custom (ALTER) ⚠️ toca tabla de spec 02 — ESCRITA (`0093`), pendiente apply+tests remoto
Satisface: R13.1, R13.2, R13.3, R13.4, R13.17, R13.19, R13.22, R13.25, R13.26, R13.27.
Detalle (design §11.1): `add column establishment_id uuid references establishments(id) on delete cascade` (NULL=global) + `add column deleted_at` (soft-delete); **relajar el UNIQUE global de `data_key`** a UNIQUE parcial doble (`field_definitions_data_key_global where establishment_id is null` + `field_definitions_data_key_per_est where establishment_id is not null`) — `rodeo_data_config`/`system_default_fields` FK-ean por `id`, no se rompen (DM5-1, default = doble parcial); CHECK de caps (`label` ≤ 80, `octet_length(config_schema::text) < 4096`, R13.17); **CHECK de dominio de `ui_component` para custom** (los 7 de R13.8; globales exentas — §11.1d.1, R13.25); **caps/sets INPUT-1** `data_key` (≤64 + slug `^[a-z0-9_]+$`), `description` (≤500), `data_type` (set cerrado `maniobra/evento_individual/evento_grupal/propiedad`), `category` custom (≤32) — §11.1d.2, R13.27; `tg_field_definitions_custom_guard` (SECURITY DEFINER, EXECUTE revocado: si `auth.uid()` no-NULL → bloquea `establishment_id IS NULL` con `42501` R13.4 y exige `is_owner_of(establishment_id)` R13.2/R13.3; **inmutabilidad en UPDATE** de `establishment_id`/`data_type`/`data_key`/`ui_component` con `42501` — R13.26; **`data_type` de cliente ∈ (`maniobra`,`propiedad`)** R13.27; **cardinalidad ≤50 / largo ≤60 de `config_schema.options`** para enum — R13.17/M5-SEC-04); **reabrir RLS** (SELECT globales-a-todos + custom-con-rol R13.22; INSERT/UPDATE `establishment_id is not null and is_owner_of`); `grant select, insert, update to authenticated`. Verificar que `tg_rodeos_seed_data_config` (0018) sigue intacto (solo seedea fábrica) y que los CHECKs no abortan al validar las 26 globales as-built (data_key slug, data_type ∈ set, category ≤14, description <500, ui_component custom-only).
Aceptación: owner crea custom en su campo → OK; `authenticated` intenta crear con `establishment_id NULL` → `42501`; non-owner intenta crear custom → `42501`/0 filas; SELECT de un user sin rol NO ve las custom de otro campo pero SÍ las 26 globales; soft-delete (deleted_at) deja de ofrecerla; las 26 de fábrica intactas; crear custom con `ui_component` fuera de los 7 → `23514`; `UPDATE` de `establishment_id`/`data_type`/`data_key`/`ui_component` de una custom → `42501`; `data_key` no-slug o >64 / `description` >500 / `category` custom >32 → reject; enum con >50 opciones o una opción >60 → reject.
Archivos: `supabase/migrations/0093_field_definitions_custom.sql` (as-built; 0090/0091 = sanitary, 0092 reservada spec-08 → M5 desde 0093).

#### [~] M5-B.2 — Migración `custom_measurements` (append-only) + audit forzado + RLS — ESCRITA (`0094`), pendiente apply+tests remoto
Satisface: R13.11, R13.13, R13.22, R13.23.
Detalle (design §11.2): tabla genérica append-only (`id`, `animal_profile_id`, `field_definition_id`, `establishment_id` denorm, `value jsonb`, `session_id` FK `ON DELETE SET NULL`, `recorded_by`, `recorded_at`, `notes`, `deleted_at`, CHECK `octet_length(value::text) < 4096`, **CHECK `notes is null or char_length(notes) <= 500`** — M5-SEC-05, paridad `0070`); índices `(field_definition_id, animal_profile_id, recorded_at)`, `by_session`, `by_est`; `tg_custom_measurements_force_audit` (SECURITY DEFINER, EXECUTE revocado: fuerza `recorded_by=auth.uid()` R13.23 + `establishment_id=establishment_of_profile(...)` anti-spoof); RLS canónico (`has_role_in` SELECT/INSERT/UPDATE, R13.13/R13.22); grants.
Aceptación: cualquier rol operativo inserta una captura → OK; `recorded_by`/`establishment_id` quedan forzados aunque el payload mande otros; `userB` sin rol → 0 filas; `value` > 4 KiB → falla CHECK; `notes` > 500 chars → falla CHECK.
Archivos: `supabase/migrations/0094_custom_measurements.sql`.

#### [~] M5-B.3 — Migración `custom_attributes` (current-value upsert) + audit forzado + RLS — ESCRITA (`0095`), pendiente apply+tests remoto
Satisface: R13.12, R13.13, R13.22, R13.23.
Detalle (design §11.3): tabla genérica current-value (PK `(animal_profile_id, field_definition_id)`, `establishment_id` denorm, `value jsonb`, `updated_by`, `updated_at`, `created_at`, CHECK de tamaño); índices `by_field`/`by_est`; `tg_custom_attributes_force_audit` (BEFORE INSERT/UPDATE: fuerza `updated_by`/`establishment_id`/`updated_at`); RLS canónico. (DM5-2: EDIT de propiedad = cualquier rol por default; afinado a owner+creador si Raf lo pide.)
Aceptación: upsert de propiedad por rol operativo → OK; re-upsert (editable anytime, R13.12) actualiza el current-value; `updated_by` forzado; `userB` sin rol → 0 filas.
Archivos: `supabase/migrations/0095_custom_attributes.sql`.

#### [~] M5-B.4 — Migración gating genérico custom + validación de value ⚠️ corazón de seguridad de M5 — ESCRITA (`0096`), pendiente apply+tests remoto
Satisface: R13.14, R13.15, R13.16, R13.24, R13.25.
Detalle (design §11.4): `assert_custom_field_enabled(p_animal_profile_id, p_field_definition_id)` (SECURITY DEFINER, EXECUTE revocado) — resuelve rodeo inline + valida `enabled=true` del `field_definition_id` en `rodeo_data_config`; **fail-closed** (rodeo no resoluble → `23514`, SEC-SPEC-03-03). `assert_custom_value_valid(p_field_definition_id, p_value)` (SECURITY DEFINER, EXECUTE revocado) — valida `value` por `ui_component` (numeric→number; enum_single/multi ∈ options de `config_schema`; boolean→bool; text/date→string + parse de fecha; R13.16) con **rama `else raise 23514` fail-closed** ante un `ui_component` no reconocido (M5-SEC-01a, R13.25) y **array de `enum_multi` ≤50** elementos seleccionados (M5-SEC-04b, R13.17). Triggers `tg_custom_measurements_gating` (BEFORE INSERT) y `tg_custom_attributes_gating` (BEFORE INSERT/UPDATE) que invocan ambas. Ninguna es RPC público (R13.24).
Aceptación: captura custom sobre rodeo con el dato enabled → OK; sobre rodeo con el dato disabled → `23514` (R13.15, defensa en profundidad por PostgREST directo); sobre perfil soft-deleted/inexistente → `23514` (fail-closed); `value` que viola el `ui_component` (texto en numeric; opción fuera del enum) → `23514`; **`ui_component` no reconocido → `23514` (fail-closed, R13.25)**; `enum_multi` con >50 elementos seleccionados → `23514`; las funciones NO invocables por `authenticated`.
Archivos: `supabase/migrations/0096_custom_gating.sql`.

#### [~] M5-B.5 — Migración housekeeping (grants/revokes) + delta de sync rules ⚠️ frontera WAL (Gate 1) — ESCRITA (`0097` + `rafaq.yaml`/`schema.ts`), pendiente apply+deploy YAML
Satisface: R13.20, R13.21, R13.24.
Detalle (design §11.5): consolidar grants de `custom_measurements`/`custom_attributes`/`field_definitions` (insert/update a authenticated) + confirmar `revoke execute` de todos los helpers internos (`tg_field_definitions_custom_guard`, `tg_custom_*_force_audit`, `assert_custom_field_enabled`, `assert_custom_value_valid`, `tg_custom_*_gating`). **Sync rules** (`sync-streams/rafaq.yaml`, NO `app/powersync/sync-rules.yaml`): restringir `catalog_field_definitions` a `WHERE establishment_id IS NULL` (R13.21) + 3 streams nuevas scope establishment (`est_field_definitions_custom`, `est_custom_measurements`, `est_custom_attributes`, `WHERE establishment_id IN org_scope`, JOIN-free). **El YAML NO se deploya desde el repo**: se pega en el dashboard → Validate → Deploy (lo gatea el leader). Actualizar `app/src/services/powersync/schema.ts` (columnas `establishment_id`/`deleted_at` en `field_definitions` + dos `Table` nuevas).
Aceptación: `node scripts/check.mjs` verde; helpers internos NO invocables por `authenticated`; (post-deploy del YAML) un device de un campo NO recibe filas custom de otro campo (frontera WAL) y SÍ recibe las 26 globales.
Archivos: `supabase/migrations/0097_check_grants.sql`, `sync-streams/rafaq.yaml`, `app/src/services/powersync/schema.ts` (+`schema.test.ts` guard de 28 tablas).

#### [~] M5-B.6 — Suite de tests no-bypass (DB remota) — ESCRITA (`supabase/tests/custom/run.cjs`, casos a–n), pendiente apply+run remoto
Satisface: R13.2, R13.3, R13.4, R13.13, R13.14, R13.15, R13.16, R13.19, R13.21, R13.22, R13.23, R13.25, R13.26, R13.27.
Detalle: extender `supabase/tests/maneuvers/run.cjs` (o suite nueva `supabase/tests/custom/`): (a) **RLS tenant** de `custom_measurements`/`custom_attributes` (userB sin rol → 0 filas); (b) **owner-only creación** de `field_definitions` custom (non-owner → reject; `establishment_id NULL` desde cliente → `42501`; `establishment_id` forzado al del owner); (c) **gating genérico fail-closed** (dato disabled en rodeo → `23514` por PostgREST directo; perfil soft-deleted → `23514`; control enabled → OK) + **R13.13** un rol operativo NO-owner (`field_operator`) captura sobre el dato enabled → OK con audit forzado; (d) **validación de value** (numeric con texto → reject; enum fuera de options → reject; control válido → OK); (e) **frontera WAL (R13.21)** — test REAL espejo de `supabase/tests/sync_streams/run.cjs`: aplica el PREDICADO de cada stream con `service_role` + el `org_scope` de cada actor (la frontera del SYNC STREAM/WAL, capa distinta de la RLS de (a)/R13.22). (e.1) `catalog_field_definitions` emite solo `establishment_id IS NULL` (la custom de estA NO entra; las globales sí); (e.2) `est_field_definitions_custom`, (e.3) `est_custom_measurements`, (e.4) `est_custom_attributes` scopean `IN org_scope` → `userA` con rol SÍ las ve, `userC` sin rol NO. Corre POST-APPLY como el resto de la suite (no a confirmar contra el dashboard); (f) **audit forzado** (`recorded_by`/`updated_by`/`establishment_id` no spoofeables).
Detalle — **casos no-bypass del fix-loop de Gate 1 M5 (2026-06-13)**:
- (g) **value fail-closed por ui_component desconocido** (M5-SEC-01a / R13.25): una captura cuyo `field_definitions.ui_component` no sea uno de los 7 → `assert_custom_value_valid` rechaza (`23514`), no acepta el value. (Si no se puede insertar la fila con ui_component raro por (h), simular con un field global de ui_component `composite` referenciado por la captura, o probar la función directamente bajo el rol de servicio.)
- (h) **creación custom con ui_component fuera de los 7** (M5-SEC-01b / R13.25): `INSERT field_definitions` custom con `ui_component='composite'`/`'whatever'`/`NULL` → rechazo por el CHECK de dominio (`23514`); control con uno de los 7 → OK; una fila **global** (`establishment_id NULL`, vía service_role) con `composite` → OK (exenta del CHECK).
- (i) **inmutabilidad post-creación** (M5-SEC-02 / R13.26): `UPDATE field_definitions SET establishment_id = <otro>` sobre una custom → `42501`; idem cambiando `data_type`, `data_key`, `ui_component` → `42501`; `UPDATE` de `label`/`config_schema`/`active`/`deleted_at` (soft-delete) → OK. Caso clave: owner de A+B intenta mudar una custom de A a B → `42501` (no fuga vía WAL). **R13.19** (bloque dedicado): tras cargar una medición sobre un field nuevo, su soft-delete (UPDATE `deleted_at`) → la `custom_measurements` ya cargada SOBREVIVE con su value intacto (FK `field_definition_id` = RESTRICT, no hard-delete).
- (j) **data_type fuera del set / fuera del alta de cliente** (M5-SEC-03 / R13.27): `INSERT` custom con `data_type='evento_individual'` → rechazo por el guard (`42501`); `data_type='maniobra'`/`'propiedad'` → OK; un `data_type` totalmente inválido a nivel tabla → rechazo por CHECK.
- (k) **caps INPUT-1** (M5-SEC-03 / R13.27): `data_key` > 64 chars o con mayúsculas/espacios (no-slug) → reject; `description` > 500 → reject; `category` custom > 32 → reject; controles dentro del cap → OK.
- (l) **cardinalidad/largo de options de un enum** (M5-SEC-04 / R13.17): crear un `enum_single`/`enum_multi` con `config_schema.options` de >50 opciones → reject; una opción > 60 chars → reject; opciones no-string o `options` ausente/no-array → reject; ≤50 opciones válidas → OK. Y un `enum_multi` capturado con un array de >50 elementos seleccionados → `assert_custom_value_valid` rechaza (`23514`).
- (m) **cap de notes** (M5-SEC-05): `custom_measurements.notes` > 500 chars → falla el CHECK; ≤500 → OK.
Mapa `R<n> → archivo:test` en `progress/impl_03-modo-maniobras.md`.
Aceptación: suite verde; cada R13.x de backend con ≥1 test (incl. R13.25/R13.26/R13.27 y los caps de R13.17/notes); `check.mjs` corre la suite.
Archivos: `supabase/tests/custom/run.cjs` (suite nueva dedicada), `scripts/run-tests.mjs` (engancha COMENTADA — el LEADER la descomenta tras aplicar 0093–0097 al remoto; corre contra la DB remota → fallaría antes del apply).

### M5-CLIENTE — UI de creación + render genérico + services de captura (Gate 2 por sub-chunk)

#### [x] M5-C.1 — Services `custom-measurements.ts` + `custom-attributes.ts` (CRUD-plano feature 15)
Satisface: R13.11, R13.12, R13.13.
Detalle (design §11.6): `app/src/services/custom-measurements.ts` (INSERT append-only con `session_id`, R13.11) y `app/src/services/custom-attributes.ts` (upsert por `(animal_profile_id, field_definition_id)`, R13.12). CRUD-plano (`runLocalWrite` → 1 CrudEntry → `uploadData`, mismo patrón que `events.ts`). `recorded_by`/`updated_by`/`establishment_id` los fuerza el trigger al subir (no se mandan). El gating capa 2 (M5-B.4) re-valida server-side.
Aceptación: unit + integración liviana; captura offline → sincroniza; el gating/audit forzado se observan al subir.
Archivos: `app/src/services/custom-measurements.ts`, `app/src/services/custom-attributes.ts`.
AS-BUILT (implementer 2026-06-17): hechos los 2 services + el helper PURO `app/src/utils/custom-value.ts` (`serializeCustomValue`/`CustomValue`, único origen) + builders `buildAddCustomMeasurementInsert`/`buildSetCustomAttributeUpsert` en `local-reads.ts`. Dos gotchas de CRUD-plano de PowerSync resueltos en `connector.ts` + helpers puros `buildCrudUpsert`/`buildCrudPatch`/`decodeJsonbColumns` en `upload-classify.ts` (ver design §11.6 AS-BUILT): (1) PK COMPUESTA de `custom_attributes` (id sintético → 42703 si se sube como `id`) → upsert/patch por la PK natural; (2) TIPO jsonb de `value` (anti doble-encoding) → parse a nativo antes de subir. Tests: `custom-value.test.ts` (12), `maneuver-reads.test.ts` (+8 builders), `upload-classify.test.ts` (+18 connector). typecheck + client unit verdes (el rojo de check.mjs = flake `animals_tag_unique` de la suite backend spec-02, terminales paralelas — NO este chunk). Pendiente reviewer + Gate 2. **NO marcar feature done.**

#### [x] M5-C.2 — UI de creación custom (ambos `+` con/sin clasificación)
Satisface: R13.5, R13.6, R13.7, R13.8, R13.9.
Detalle (design §11.6): sheet/pantalla "nuevo dato custom" desde (a) el `+` de config de datos del rodeo → **pregunta de clasificación** (R13.6) → propiedad o maniobra; (b) el `+` de la lista de maniobras → **solo maniobra** (R13.7), + habilita en el rodeo en el mismo paso. Form: `label`, tipo de input (`ui_component` de R13.8: numeric/numeric_stepped/enum_single/enum_multi/text/boolean/date), opciones si es enum. Escribe a `field_definitions` (owner-only por RLS) + (caso b) `rodeo_data_config` (toggle, reusa el `set_rodeo_config`/`setRodeoConfig` de spec 02/feature 13). 1 dato = 1 campo (R13.9; multi-campo deshabilitado en UI).
Aceptación: el `+` de config crea propiedad o maniobra con la pregunta; el `+` de maniobras crea solo maniobra y la prende en el rodeo; los 7 tipos de input se ofrecen; owner-only (un non-owner no ve el `+` de creación).
Archivos: `app/app/maniobra/*` (config de rodeo + lista de maniobras), `app/src/services/field-definitions.ts` (o el service de rodeo-config existente).
AS-BUILT (implementer 2026-06-17): hecho. (1) Helper PURO `app/src/utils/custom-field.ts` (`slugifyDataKey` único [a-z0-9_]/≤64 con sufijo + `validateCustomFieldDraft` caps label≤80/options≤50·≤60 + `UI_COMPONENT_OPTIONS` los 7 es-AR + `buildCreateCustomFieldPayload` shape EXACTO de 0093 + `custom-field.test.ts` 23). (2) Service `app/src/services/custom-fields.ts` (`createCustomField` CRUD-plano offline — INSERT a field_definitions, id cliente, establishment_id del contexto, config_schema {options} para enums; `enableCustomFieldInRodeo` REUSA `enqueueSetRodeoConfig`/outbox; `fetchCustomDataKeys` / `fetchEnabledCustomManeuvers`) + builders `buildCreateCustomFieldInsert`/`buildCustomDataKeysQuery`/`buildEnabledCustomManeuversQuery` en `local-reads.ts` (+ 6 tests en `maneuver-reads.test.ts`). (3) Form `app/app/maniobra/_components/CustomFieldSheet.tsx` (sheet idiom + guard tap-through doble-rAF; modo `classify` = pregunta propiedad/maniobra primero / modo `maniobra` = sin pregunta, data_type fijo R13.7; label + picker de los 7 + editor de opciones enum). (4) Los 2 `+`: en `editar-plantilla.tsx` (config de datos del rodeo → CustomFieldSheet `classify`, owner-only) y en `jornada.tsx` etapa 2 (lista de maniobras → CustomFieldSheet `maniobra` + `enableCustomFieldInRodeo`, owner-only). (5) Tweak M1: `fetchEnabledCustomManeuvers` lista las custom `data_type='maniobra'` enabled del rodeo (overlay-aware) → `CustomManeuverSection` en etapa 2 (la lista = fábrica + custom enabled). **GOTCHA jsonb cazado en autorrevisión**: `field_definitions.config_schema` (jsonb) sumado a `JSONB_TEXT_COLUMNS` (`upload-classify.ts`) → el connector lo decodifica a jsonb nativo antes de subir (sin él, el guard 0093 `config_schema -> 'options'` daría NULL sobre un jsonb-string → 23514). **DECISIÓN DE SCOPE (reconciliada en design §11.6/§11.7)**: el `+` CREA + HABILITA la maniobra custom y la lista la EXHIBE; la SELECCIÓN/secuencia + el RENDER per-animal de una maniobra custom (escribir a `custom_measurements`) es del chunk **M5-C.3** (renderer genérico desde `ui_component`) — `buildJornadaConfig` filtra hoy a `ManeuverKind` conocidos, así que sumar custom al config requiere el renderer (C.3). Tests: `custom-field.test.ts` (23), `maneuver-reads.test.ts` (+6), `upload-classify.test.ts` (+2), e2e `maniobra-custom.spec.ts` 2/2 (config `+` clasificación→form→enum→crear→oráculo server; maniobra `+` sin pregunta→crear→aparece+oráculo server) + helper `waitForServerCustomField`. typecheck + 1243 client unit verdes; el rojo de check.mjs = flake `animals_tag_unique` de la suite backend spec-13 (terminales paralelas), NO este chunk (frontend puro). 10 capturas web táctil 360/412 en `tests/modo-maniobra/custom-*.png`. Pendiente reviewer + Gate 2. **NO marcar feature done.**

#### [x] M5-C.3 — Render genérico del paso desde `ui_component` + propiedad en alta/ficha
Satisface: R13.8, R13.10.
Detalle (design §11.6/§11.7): una **maniobra custom** enabled aparece en la lista de maniobras (tweak M1) y su paso se renderiza con el **renderer genérico por `ui_component`** (tweak M3 — el mismo que dibuja las de fábrica), escribiendo a `custom_measurements`. Una **propiedad custom** enabled aparece en el **form de alta + ficha** del animal del rodeo (reusa el form dinámico de spec 09), escribiendo a `custom_attributes`. NO se duplica el renderer por-maniobra.
Aceptación: una maniobra custom enum_single "adentro/afuera" se carga con botones gigantes (mismo renderer); una propiedad custom aparece en alta + ficha y se edita; sin código por-maniobra nuevo.
Archivos: `app/app/maniobra/paso.tsx`, form de alta/ficha de spec 09 (`app/app/**`), `app/src/utils/maneuver-gating.ts` (incluir custom maniobra enabled en la lista).
AS-BUILT (implementer 2026-06-17): hecho. **PARTE A (R13.8) — maniobra custom seleccionable + secuenciada + renderer genérico.** Shape ADITIVO: el config jsonb suma una clave PARALELA `customManiobras: string[]` (field_definition_id en orden) a las 12 de fábrica de `maniobras` (IDÉNTICAS — cero regresión). Helpers PUROS nuevos: `extractCustomManiobras` (`maneuver-config.ts`), `toggleCustomManiobra` + `buildJornadaConfig(maneuvers, preconfig, customManiobras?)` (`maneuver-wizard.ts`). La secuencia (`maneuver-sequence.ts`) pasó a un ítem DISCRIMINADO `SequenceItem` (`source:'factory'|'custom'`): `buildSequence(ordered, applicable, customEnabled?)` concatena fábrica (en su orden ∩ gating ∩ aplicabilidad) + custom enabled (en su orden), contador COMBINADO; `summaryRows`/`isSequenceComplete`/`firstUncapturedIndex` toman un `CustomCaptureMap` paralelo. Helper PURO de RENDER `custom-render.ts` (`parseCustomOptions` config_schema→options TOLERANTE, `parseCustomValueJson` value jsonb→tipado por ui_component, `describeCustomValue` es-AR, `isCustomValueComplete`, `captureKindFor`, `toCustomValue`). Componente `CustomManeuverStep.tsx` (renderer de manga por ui_component, REUSA idioms lockeados: numeric→keypad de PesajeStep, numeric_stepped→stepper, enum_single→bloques tipo TactoVaquillona, enum_multi→multi-select tipo SilentVaccination, text→input, boolean→2 bloques Sí/No, date→máscara es-AR AAAA-MM-DD). `jornada.tsx`: `CustomManeuverSection` pasó de visual-only a TOGGLE (✓ verde, entra a `chosenCustom`→`config.customManiobras`); el CTA habilita con fábrica O custom; StageSummary lista las custom; loadPreset restaura `chosenCustom`. `carga.tsx`: dispatcher discrimina `source` → `CustomManeuverStep` para custom → `addCustomMeasurement` (value tipado, session_id, R13.11). **PARTE B (R13.10) — propiedad custom en alta + ficha.** Componente `CustomFieldInput.tsx` (input por ui_component en estilo FORM, reusa idioms compactos) + `CustomPropertiesSection.tsx` (2 vistas: `CustomPropertiesForm` para el alta — estado local + ref `collectValues` → el submit de `crear-animal` persiste post-create con soft-fail como condición/preñez; `CustomPropertiesFicha` para la ficha — ver + editar in-place → `setCustomAttribute`, editable anytime R13.12, archivado=solo lectura). Services: `fetchEnabledCustomManeuvers` (extendido a ui_component/options) + `fetchEnabledCustomProperties` (`custom-fields.ts`) + `fetchCustomAttributes` (`custom-attributes.ts`). Builders `buildEnabledCustomFieldsQuery(rodeoId, dataType)` + `buildCustomAttributesQuery` + `buildUpdateCustomMeasurement` + `buildInsertCustomAttribute`/`buildUpdateCustomAttribute` (`local-reads.ts`). **GOTCHA "cannot UPSERT a view" cazado en e2e + resuelto**: PowerSync expone las tablas sincronizadas como VIEW → un `INSERT … ON CONFLICT(id) DO UPDATE` falla ("cannot UPSERT a view"). Se REVIRTIÓ el upsert que C.1 había puesto en `custom_attributes` (latente — C.1/C.2 nunca ejercieron la ESCRITURA del atributo end-to-end): `setCustomAttribute` ahora hace UPDATE-luego-INSERT-si-0-filas (`runLocalWriteCount` reporta rowsAffected); `custom_measurements` es plain INSERT (append-only) + `buildUpdateCustomMeasurement` para la corrección R5.9 (id estable). El connector (buildCrudUpsert/buildCrudPatch, PK natural) NO cambió. Tests: `custom-render.test.ts` (22), unit de sequence/config/wizard extendidos (+custom), `maneuver-reads.test.ts` (+buildEnabledCustomFieldsQuery/buildCustomAttributesQuery/buildUpdateCustomMeasurement/buildInsert+UpdateCustomAttribute). **typecheck + 1455 client unit + RLS 22 + Edge 42 VERDES**; **e2e `maniobra-custom-render.spec.ts` 3/3** (enum_single→bloques→custom_measurements [oráculo server con session_id]; numeric→keypad→número jsonb; propiedad en alta→custom_attributes + ficha ver/editar) con `seedCustomField`/`waitForServerCustomMeasurement`/`waitForServerCustomAttribute`/`adminQueryProfileByVisual`. **Regresión VERDE**: maniobra-carga 3/3, maniobra-elegir 2/2, maniobra-wizard 1/1, maniobra-custom 2/2, animals 14/14. El rojo de check.mjs = flake `animals_tag_unique` (23505 dup) de la suite backend animal (terminales paralelas) — NO este chunk (frontend puro). 8 capturas web táctil 360/412 en `tests/modo-maniobra/custom-render-{enum,numeric}-*` + `custom-prop-{alta,ficha}-*`. Reconciliado design §11.6/§11.7 + requirements R13.8/R13.10. Pendiente reviewer + Gate 2. **NO marcar feature done.**

#### [x] M5-CUSTOM-BUGFIX — 2 defectos visuales de la maniobra custom (cazados EN VIVO por Raf)
Satisface: R13.8 (presentación; sin cambio de comportamiento/contrato — el value y la captura no se tocan).
Frontend PURO. **Bug 1**: el TÍTULO del paso de maniobra custom (label largo) se RECORTABA a 1 línea silenciosa en la LÍNEA DE MANIOBRA del frame (`carga.tsx`, `numberOfLines={1}` a `$5`). **Bug 2**: en las listas enum_single/enum_multi no se notaba que se podía SCROLLEAR con muchas opciones (el operario creía que las visibles eran todas); además el enum_single NO scrolleaba (YStack de bloques `flexGrow`, no ScrollView).
AS-BUILT (implementer 2026-06-18): hecho. **Bug 1** → helper PURO `app/src/utils/maneuver-title-size.ts` (`maneuverTitleFontToken`, length-aware step-down `$5`/`$4` >56ch, lineHeight matching) + el label de la línea de maniobra (`carga.tsx`) pasa a `numberOfLines={2}` + word-break (web) + fila `alignItems="flex-start"` (contador pinneado en la 1ra línea) → título completo en ≤2 líneas. **Bug 2** → helper PURO `app/src/utils/scroll-affordance.ts` (`scrollFades`: decide fade top/bottom por la geometría del scroll, EPS + defensivo) + componente `ScrollAffordanceList` en `CustomManeuverStep.tsx` (ScrollView con scrollbar + fade-gradiente `expo-linear-gradient` en el borde con contenido oculto + chevron ▾ + PEEK de un ítem parcial + posicionamiento por tokens `$0`); enum_single lo usa con `fillHeight` (pocas→bloques gigantes idiom DientesStep / muchas→scrollea con affordance), enum_multi lo usa dentro de su card (fade `$surface`); + sub-header "Elegí las que correspondan" del multi `numberOfLines={1}`→`{2}` (descendentes g recortadas). Con pocas opciones (sin overflow) NO aparece fade ni chevron. Tests: `maneuver-title-size.test.ts` (6), `scroll-affordance.test.ts` (6); e2e `maniobra-custom-bugfix.spec.ts` 2/2 (web TÁCTIL hasTouch+isMobile: título largo completo + fade abajo en reposo → scroll → fade arriba + sin fade abajo, llega a la última opción). typecheck + anti-hardcode 0 + 1519 client unit verdes; regresión maniobra-custom-render (enum/numeric) 2/2 + carga 3/3 + elegir 2/2. check.mjs rojo SOLO por el flake cross-terminal `animals_tag_unique` (backend, no este bugfix). Capturas ANTES/DESPUÉS 360/412 en `tests/modo-maniobra/custom-bug-*`. Reconciliado design §11.6. **NO marcar feature done.**

#### [x] M5-CUSTOMFIELDSHEET-FIX — el sheet de CREACIÓN de dato/maniobra custom recortaba el título + error a nivel de campo (cazado EN VIVO por Raf)
Satisface: R13.5–R13.8 (presentación; **NO toca backend ni `validateCustomFieldDraft` — solo CÓMO se muestra el error y el layout del sheet**). Frontend PURO. Distinto del BUGFIX de arriba: ese es el PASO DE CARGA de una maniobra custom; este es el FORM de CREACIÓN (`CustomFieldSheet.tsx`). Bug (evidencia `tests/modo-maniobra/error-maniobra-custom.png`): el título "Nueva maniobra" se RECORTABA contra el tope, peor con el error "Agregá al menos una opción" (banner `FormError` al fondo que crecía y empujaba el contenido por encima de `maxHeight:90%` → el clip caía sobre el TOPE porque el sheet ancla abajo).
AS-BUILT (implementer 2026-06-18): hecho. **FIX 1 (layout robusto)** → header fijo (`YStack flexShrink:0`: grip + título) + cuerpo `ScrollView flex:1 minHeight:0` (absorbe el alto, scrollea interno) + footer fijo (`YStack flexShrink:0`: Crear/Cancelar); el paso `classify` también pasó a `ScrollView flex:1` → el título queda SIEMPRE completo. **FIX 2 (error a nivel de campo)** → helper PURO nuevo `customFieldErrorTarget(draft)` en `custom-field.ts` (mapea draft inválido → campo culpable `'label'|'options'|null` con la MISMA precedencia que `validateCustomFieldDraft`, sin re-validar; test de consistencia target===null⟺válido) → al tocar "Crear" inválido: scroll **geometría-medido** al campo culpable (cada sección reporta `{y,height}` por `onLayout` —que se re-dispara después de que el mensaje inline crece el contenido— + el alto del viewport del `ScrollView` → `runScrollToField` computa el `y` que trae el campo COMPLETO [input + borde + mensaje] a la vista, align-top si entra / align-bottom contra el viewport si no; doble-rAF de fallback; el error residual del server encadena a `onContentSizeChange`, no a un defer fijo) **robusto a 360 y 412** (fix-loop scroll-360 del veto del leader: el `scrollToEnd` diferido 1 frame original se quedaba corto a 360 → reemplazado por la medición real) + borde `$terracota` (token, borderWidth 2) en el input/editor culpable + mensaje INLINE (`FieldError`, terracota, lineHeight matching) en ese campo; el banner `FormError` del fondo se ELIMINÓ; el error residual del server (sin target) va al FINAL del cuerpo + scrollToEnd; limpieza al editar el campo. **FIX 3 (sweep)** → `ManeuverConfigSheet.tsx` tocado (mismo patrón: header fijo + ScrollView flex:1 + footer fijo); `ExitJornadaSheet`/`NuevaJornadaConfirmSheet`/`OtherRodeoSheet`/`SavePresetSheet`/`SyncRechazoSheet`/`CandidatePicker`/age-sheet de CE auditados y YA OK (contenido fijo o ScrollView hard-capeado → título no clipeable). Tests: `custom-field.test.ts` 30 (23+7 de `customFieldErrorTarget`); e2e nuevo `maniobra-customfield-validacion.spec.ts` 1/1 (web TÁCTIL: reposo título completo + error enum_multi-0-opciones → editor resaltado + scrolleado a la vista + mensaje inline visible + título COMPLETO con error + limpieza; título-no-recortado verificado por bounding-box `titleBox.y ≥ sheetBox.y`). typecheck + anti-hardcode 0 + custom unit (59 custom-*) verdes; regresión e2e `maniobra-custom`/`maniobra-wizard`(ManeuverConfigSheet)/`maniobra-custom-render`(enum/numeric)/`maniobra-custom-bugfix` verdes. **Nota de entorno**: la otra terminal tiene spec-08 + un delta spec-03 (custom-attributes ficha-render) sin commitear → `maniobra-custom-render.spec.ts:195` (propiedad "Manchado" en la ficha) falla por ESE delta ajeno (no usa `CustomFieldSheet`/`ManeuverConfigSheet`/`custom-field.ts` — los 3 archivos de este fix), NO es regresión de este chunk. Capturas 360/412 en `tests/modo-maniobra/customfield-{reposo,error}-*`. Reconciliado design §11.6 + esta entrada de tasks (AS-BUILT scroll geometría-medido). El e2e suma el oráculo `expectInvalidFieldFullyInScrollViewport` (input inválido + error dentro del rect REAL del `ScrollView`, forzando **360** antes de "Crear" = el ancho del bug). **reviewer APRUEBA tras esta reconciliación (2026-06-18)** (`progress/review_03-m5-customfieldsheet-fix.md`; su único bloqueo era esta línea de tasks, ya igualada). Gate 2 = N/A (presentación; sin cambio de data-path/inputs/auth/schema). **NO marcar feature done.**

### Tweaks forward-looking a M1 / M3 (NO se rehacen — anotados para cuando se construyan)
- **M1.1 / M1.4 (tweak)**: la lista de maniobras de un rodeo = 10 de fábrica gateadas (capa 1) **+ las `field_definitions` custom `data_type='maniobra'` enabled** en ese rodeo; el `+` de la lista crea una (R13.7). `maneuver-gating.ts` incluye las custom enabled como fuente de maniobras (no solo el `MANEUVER_DATA_KEYS` hardcodeado). Se folda al construir M1.
- **M3.2 (tweak)**: el paso-de-maniobra renderiza **desde `ui_component`** (generaliza R5.4) → una maniobra custom es un `ui_component` más, sin renderer nuevo; las custom escriben a `custom_measurements`. **M2.0 (design spike) NO se bloquea** (puede arrancar con las de fábrica; conviene render genérico desde el spike). Se folda al construir M3.

### Gates de M5
- **Gate 1 (security modo `spec`) OBLIGATORIO** sobre el **delta backend M5-BACKEND** (M5-B.1..B.6: reabre RLS de un catálogo global de spec 02 + 2 tablas nuevas + gating SECURITY DEFINER + frontera WAL del sync) **ANTES de la Puerta 1**. Output en `progress/security_spec_03-modo-maniobras.md` (o `_m5`).
- **Gate 2 (code) por sub-chunk** sobre M5-CLIENTE (M5-C.1..C.3) y sobre cada migración aplicada (reviewer + autorrevisión adversarial del implementer).
- **Preguntas abiertas DM5-1..DM5-3** (design §11.8) a confirmar con Raf antes de cerrar M5-BACKEND (default propuesto en cada una; no las cerró el spec_author).

---

## M6 — Circunferencia escrotal (chunk nuevo, foldado 2026-06-17 desde `context-m6-circunferencia-escrotal.md`)

> **Maniobra TIPADA de fábrica nueva (NO custom de M5).** Cubre **US-14 (R14.1–R14.18)**. **M6-BACKEND REABRE schema/RLS** (tabla typed `scrotal_measurements` + data_key global `circunferencia_escrotal` + seed cría + trigger de gating capa 2 + RLS + grants + sync rules) → **Gate 1 (security modo `spec`) OBLIGATORIO sobre el delta backend ANTES de la Puerta 1**. Especificación en `design.md` §12. **No se aplica DDL en esta pasada** — el deploy a la DB compartida lo gatea el leader (memoria: Supabase MCP en modo escritura). **M6-CLIENTE** consume el as-built (CRUD-plano feature 15 + StepKind/dispatcher de M3.1) → **Gate 2 (code) por sub-chunk**.

> **Numeración de migraciones (M6-BACKEND) — RE-CONFIRMAR antes de crear archivos.** Último as-built = `0097_check_grants.sql` (M5) → próximo libre **0098** (`0092` quedó reservada spec-08, ya saltada por M5). El implementer **re-confirma el rango libre contra el árbol Y lo que la otra terminal tenga en vuelo** justo antes de crear los `.sql`. Propuesta (sujeta a re-confirmación): `0098_scrotal_measurements` / `0099_scrotal_data_key_and_seed` / `0100_scrotal_gating`. El orden lógico importa (data_key debe existir antes de que el seed/gating lo referencien); se pueden colapsar en menos archivos si el implementer prefiere.

### M6-BACKEND — tabla typed + data_key + seed + gating + RLS + sync (⚠️ Gate 1 ANTES de aplicar)

#### [x] M6-B.1 — Migración `0098_scrotal_measurements.sql` (tabla typed nueva + audit forzado + RLS + session_id) ⚠️ tabla de evento nueva
Satisface: R14.8, R14.9, R14.10, R14.15, R14.17.
Detalle (design §12.3): tabla `scrotal_measurements` append-only longitudinal (`id`, `animal_profile_id`, `establishment_id` denorm, `session_id`, `circumference_cm numeric(4,1)` CHECK 20–50, `age_months int` nullable CHECK, `measured_at date`, `source` reusa `event_source` `0025`, `notes` CHECK ≤500, `recorded_by`, `created_at`, `deleted_at`); índices `(animal_profile_id, measured_at desc)`, `by_session`, `by_est`; `tg_scrotal_force_recorded_by` (SECURITY DEFINER, EXECUTE revocado: `recorded_by=auth.uid()`); **REUSA `tg_force_establishment_id_from_profile` (`0077`)** para forzar `establishment_id` anti-spoof + `SET NOT NULL`; FK `session_id → sessions ON DELETE SET NULL` + tenant-check (reusar `tg_event_session_tenant_check` `0056` o clonar — DM6, design §12.3 nota); RLS canónico (`has_role_in` SELECT/INSERT; UPDATE owner o `recorded_by`, R14.17); grants `authenticated`+`service_role`.
Aceptación: cualquier rol operativo inserta una CE → OK; `recorded_by`/`establishment_id` forzados aunque el payload mande otros; `userB` sin rol → 0 filas (SELECT) / reject (INSERT); `circumference_cm` fuera de 20–50 → falla CHECK; `notes` > 500 → falla CHECK; corrección por owner/`recorded_by` (UPDATE soft-delete) → OK, por tercero → reject.
Archivos: `supabase/migrations/0098_scrotal_measurements.sql` (re-confirmar número).

#### [x] M6-B.2 — Migración `0099_scrotal_data_key_and_seed.sql` (data_key global + seed cría habilitado por defecto)
Satisface: R14.1, R14.18.
Detalle (design §12.4a/b): INSERT del data_key global `circunferencia_escrotal` en `field_definitions` (`establishment_id NULL`, `data_type='maniobra'`, `ui_component='numeric_stepped'`, `category='reproductivo'`); seed en `system_default_fields` del sistema cría con `default_enabled=true`, `required_for_system=false`, `sort_order` = max+1. Verificar que NO rompe el UNIQUE parcial doble de `data_key` de M5 (`field_definitions_data_key_global where establishment_id is null`).
Aceptación: el data_key existe como global; un rodeo de cría NUEVO ofrece la maniobra de CE sin configurar (capa 1 lo lee de `system_default_fields`); el operario la puede deshabilitar por rodeo (toggle `rodeo_data_config`); el binding-test (M6-B.4) verifica que `circunferencia_escrotal` existe en el catálogo.
Archivos: `supabase/migrations/0099_scrotal_data_key_and_seed.sql`.

#### [x] M6-B.3 — Migración `0100_scrotal_gating.sql` (gating capa 2 fail-closed single-key) ⚠️ corazón de seguridad de M6
Satisface: R14.11, R14.12.
Detalle (design §12.4): `tg_scrotal_gating` (BEFORE INSERT en `scrotal_measurements`, SECURITY DEFINER, EXECUTE revocado) que invoca **`assert_data_keys_enabled(new.animal_profile_id, array['circunferencia_escrotal'])`** (REUSA el helper de `0054` — resolución inline del rodeo SEC-SPEC-03-02 + fail-closed SEC-SPEC-03-03; NO se crea helper nuevo, es single-key).
Aceptación: CE sobre rodeo con `circunferencia_escrotal` enabled → OK; sobre rodeo disabled → `23514` por PostgREST directo (R14.12, defensa en profundidad); sobre perfil soft-deleted/inexistente → `23514` (fail-closed); el trigger corre BEFORE INSERT independiente del rol (no-bypass).
Archivos: `supabase/migrations/0100_scrotal_gating.sql`.

#### [x] M6-B.4 — Sync rules + schema.ts + surfacing rechazos ⚠️ frontera WAL (Gate 1)
Satisface: R14.16, R10.8 (tweak).
Detalle (design §12.5): `sync-streams/rafaq.yaml` → stream nueva `ev_scrotal_measurements` (`WHERE establishment_id IN org_scope AND deleted_at IS NULL`, JOIN-free, paridad `ev_weight_events`). **El YAML NO se deploya desde el repo** (dashboard → Validate → Deploy, lo gatea el leader). `app/src/services/powersync/schema.ts` → `Table` nueva `scrotal_measurements` (CRUD-plano) + actualizar el guard de cantidad de tablas (`schema.test.ts`). `app/src/services/powersync/upload-rejections.ts` → `MANEUVER_TABLE_LABELS['scrotal_measurements'] = 'Circunferencia escrotal'` (surfacing R10.8).
Aceptación: `node scripts/check.mjs` verde; (post-deploy del YAML) un device de un campo NO recibe CE de otro campo (frontera WAL) y SÍ recibe las suyas; un rechazo permanente de CE aparece en el banner/sheet de manga con su tipo es-AR.
Archivos: `sync-streams/rafaq.yaml`, `app/src/services/powersync/schema.ts` (+`schema.test.ts`), `app/src/services/powersync/upload-rejections.ts` (+test).

#### [x] M6-B.5 — Suite de tests no-bypass (DB remota)
Satisface: R14.9, R14.11, R14.12, R14.15, R14.16, R14.17, R14.18.
Detalle: suite nueva `supabase/tests/scrotal/run.cjs` (espejo de `supabase/tests/maneuvers/run.cjs`): (a) **RLS tenant** (`userB` sin rol → 0 filas / reject); (b) **audit forzado** (`recorded_by`/`establishment_id` no spoofeables desde el payload, en **INSERT *y* UPDATE-path** — sub-assert del UPDATE-path agregado al cerrar **M6-CODE-01** de Gate 2 code: el owner intenta pisar `establishment_id` por UPDATE → el trigger `tg_force_establishment_id_from_profile` (cableado `before insert or update`) lo re-deriva del perfil real (inmutable) → no se puede pisar; verificado por `service_role`); (c) **gating capa 2 fail-closed** (CE sobre rodeo enabled → OK; disabled → `23514` por PostgREST directo, R14.12; perfil soft-deleted → `23514`; no-bypass por `service_role`); (d) **binding-test** (`circunferencia_escrotal` existe en `field_definitions`); (e) **seed cría** (un rodeo de cría nuevo tiene `circunferencia_escrotal` enabled por default, R14.18); (f) **CHECK de rango** (`circumference_cm` <20 o >50 → reject); (g) **frontera WAL (R14.16)** — espejo de `supabase/tests/sync_streams/run.cjs`: el predicado de `ev_scrotal_measurements` con `service_role` + `org_scope` → actor con rol ve sus CE, sin rol NO; (h) **corrección append-only (R14.17)** owner/`recorded_by` corrige/soft-deletea, tercero NO; (i) **`session_id` tenant-check no-bypass (M6-SEC-02, Gate 1)** — INSERT de CE con `session_id` de OTRO tenant / otra sesión / otro rodeo → reject, y que la forma `before insert or update` del trigger SÍ dispara en INSERT (espejo del precedente `0052→0056`, donde la forma del trigger causó un bypass total que solo cazó la suite). Mapa `R<n> → archivo:test` en `progress/impl_03-modo-maniobras.md`. **Verde post-apply (2026-06-18): 12/12 casos VERDE contra el remoto (0098–0100 aplicadas), confirmación empírica del no-bypass / gating capa 2 / RLS / fail-closed.**
Aceptación: suite verde; cada R14.x de backend con ≥1 test; `check.mjs` corre la suite.
Archivos: `supabase/tests/scrotal/run.cjs`, `scripts/run-tests.mjs` (hook **DESCOMENTADO** post-apply 2026-06-18).

### M6-CLIENTE — design spike de la rueda → render + write-path → ficha (Gate 2 por sub-chunk)

#### [x] M6-C.0 — **DESIGN SPIKE** de la rueda 🔴 manga (wheel picker inercial) — visual antes de plomería
Satisface: R14.5 (visual), R12.5, R5.2/R12.2 (densidad/botones gigantes en este step).
Detalle (design §12.2): componente visual `CircunferenciaEscrotalStep` — wheel picker inercial (Reanimated: momentum + snap + tick háptico), rango 20–50/0,5, edad en meses al lado, formato es-AR. **Vetado por design-review del leader** (análisis pro fundamentado: Fitts, jerarquía, contraste, densidad ≥60 % del alto útil, recorte de descendentes si hay título; **vetar en web táctil real** per `reference_rn_web_pitfalls`); iterado con el implementer; a Raf solo se le muestra lo aceptable. NO se cablea al frame todavía (solo el componente + un harness/preview).
Aceptación: la rueda se siente inercial (fling rápido pasa muchos valores, drag lento preciso), snapea, tickea háptico; el design-review del leader la aprueba antes de pasar a C.1.
Archivos: `app/app/maniobra/_components/CircunferenciaEscrotalStep.tsx` (+ harness/preview).
AS-BUILT (implementer Opus, 2026-06-17 — **pendiente veto design-review del leader + OK Raf**): `CircunferenciaEscrotalStep.tsx` (card $surface flex={1} con lectura grande "36 cm" fija `$wheelHero`, rueda CE dominante, edad pill secundario "≈ 24 meses" → tap-to-ajustar en bottom-sheet con rueda de meses, confirm gigante) + `WheelPicker.tsx` (drum inercial genérico reusado por CE y meses) + util PURA `wheel-picker.ts` (18 tests) + `hapticTick()` en `haptics.ts` (Vibration, NO expo-haptics — reconciliado, ADR-011) + tokens JIT `$wheelCell`/`$wheelHero`. Harness DEV `maniobra/rueda-ce.tsx` (DEV_WEB_ROUTES, paridad spike pesaje M2.0). **Densidad 79,6 % del alto útil** (R12.5 ✓). Capturas web TÁCTIL `hasTouch` 360/412: `tests/modo-maniobra/rueda-ce-{reposo,fling,edad}-{360,412}.png` (e2e `app/e2e/captures/rueda-ce.spec.ts`, 2/2). check.mjs: typecheck + anti-hardcode + client unit verdes (la única falla = flake del Animal suite por colisión `animals_tag_unique` en la DB compartida, ajeno a este spike frontend-only). Detalle/autorrevisión: `progress/impl_03-m6-c0-spike.md`.

#### [x] M6-C.1 — StepKind 'rueda' + aplicabilidad + dispatcher + write-path (frontend puro)
Satisface: R14.1, R14.2, R14.3, R14.4, R14.6, R14.7, R14.8, R14.10.
Detalle (design §12.1/§12.2/§12.4): (1) `maneuver-gating.ts` → `'circunferencia_escrotal'` en `ManeuverKind`/`ALL_MANEUVERS` + entrada `{ dataKeys:['circunferencia_escrotal'], match:'all' }` en `MANEUVER_DATA_KEY_REQS`. (2) `maneuver-step-kind.ts` → `StepKind 'rueda'` + mapeo `circunferencia_escrotal → 'rueda'` (factory-only). (3) `maneuver-applicability.ts` → regla per-animal (macho ENTERO no-ternero: `categoría ∈ {torito,toro} ∧ is_castrated !== true`; desconocida=null → incluye, R14.3; `is_castrated` denorm de spec 10) + agregar `isCastrated` a `AnimalApplicabilityInfo`. (4) `carga.tsx` (dispatcher `ManeuverStep`) → `case 'rueda':` renderiza `CircunferenciaEscrotalStep`, `onConfirm` emite `StepValue { kind:'scrotal', circumferenceCm, ageMonths }`; edad **prellenada** si `birth_date` existe (R14.6) y **siempre ajustable** por rueda de meses (R14.7, resuelto DM6-6: el as-built no distingue precisión); snapshot de edad confirmada (R14.8). (5) write-path: `app/src/services/scrotal.ts` (`addScrotalMeasurement`, CRUD-plano, `session_id`, sin RPC) + builders `buildAddScrotalInsert`/`buildScrotalHistoryQuery` en `local-reads.ts`. Orquestador `maneuver-events.ts` ramifica el `kind:'scrotal'`.
Aceptación: la CE solo se ofrece a toritos/toros enteros (saltada en hembra/ternero/castrado/novillo, R14.2/R14.4); castración desconocida → se ofrece (R14.3); la rueda arranca en la última medida o 36 cm; la edad sale auto o por rueda manual; confirmar persiste un `scrotal_measurements` offline con `session_id` (oráculo server post-sync); unit de los 3 módulos puros + e2e de la secuencia.
Archivos: `app/src/utils/{maneuver-gating,maneuver-step-kind,maneuver-applicability}.ts` (+tests), `app/app/maniobra/carga.tsx`, `app/src/services/scrotal.ts`, `app/src/services/powersync/local-reads.ts` (+tests), `app/src/services/maneuver-events.ts`.
AS-BUILT (implementer Opus, 2026-06-18 — pendiente reviewer + Gate 2): cableado EXACTO al plan. (1) `maneuver-gating.ts`: `circunferencia_escrotal` en `ManeuverKind`/`MANEUVER_DATA_KEY_REQS` (`{dataKeys:['circunferencia_escrotal'],match:'all'}`); `ALL_MANEUVERS` ahora = **13** (deriva). (2) `maneuver-step-kind.ts`: `StepKind 'rueda'` + mapeo factory-only. (3) `maneuver-applicability.ts`: `isCastrated:boolean|null` en `AnimalApplicabilityInfo` + `BULL_ENTIRE_CATEGORY_CODES={torito,toro}` + `case 'circunferencia_escrotal'` (categoría ∈ {torito,toro} ∧ isCastrated !== true; null → incluye, R14.3; categoría null → se salta). `toApplicabilityInfo` (carga.tsx) suma `isCastrated` de `AnimalDetail`. (4) `maneuver-sequence.ts`: variante `StepValue { kind:'scrotal'; circumferenceCm; ageMonths }` + `describeStepValue` es-AR ("36,5 cm · 24 meses"; edad null → solo CE). (5) `maneuver-event-query.ts`: `case 'scrotal'` → INSERT `buildAddScrotalInsert` (1ra captura) / UPDATE `buildUpdateManeuverScrotal` (corrección R14.17). (6) write-path: `scrotal.ts` (`addScrotalMeasurement` + lectura `fetchScrotalHistory`/`fetchLastScrotalCm`) + builders `buildAddScrotalInsert`/`buildUpdateManeuverScrotal`/`buildScrotalHistoryQuery` en `local-reads.ts`. (7) `carga.tsx`: `case 'rueda':` → `CircunferenciaEscrotalStep` con `initialCm`= corrección en curso, o última medida (`fetchLastScrotalCm`, leída en el frame al conocer el perfil), o `CE_DEFAULT_CM`=36; `ageMonths`= corrección en curso, o `prefillAgeMonths(animal.birthDate)` (R14.6). (8) **POLISH**: el `<input>` editable suprime el outline naranja del navegador en foco (`outlineStyle:'none'`/`outlineWidth:0` web-only) → el tratamiento de foco verde es el borde `$divider→$primary` de la caja (sin hardcode). **+1 builder no en el plan: `buildUpdateManeuverScrotal`** (corrección R14.17 desde el resumen, paridad con `buildUpdateManeuverWeight` — el plan solo listaba el INSERT + el history query). **Numeración**: `ALL_MANEUVERS.length` 12→13 (los tests exhaustivos actualizados). **Verificación**: typecheck + anti-hardcode 0 + 305 unit (gating/step-kind/applicability/sequence/event-query/wheel-picker/local-reads/wizard) verdes; e2e `maniobra-circunferencia-escrotal.spec.ts` **3/3** (web 412/360: TORITO entero → CE en secuencia → rueda + campo editable/teclado → server `scrotal_measurements` con session_id + establishment_id/recorded_by FORZADOS verificados; hembra/ternero → CE salteada; novillo castrado → salteada; entero → aparece + edad ajustable). Capturas web `tests/modo-maniobra/ce-flujo-{reposo,input}-{360,412}.png`. Detalle/mapa R→test/autorrevisión/reconciliación: `progress/impl_03-m6-c1.md`.
> **FIX SNAP/LOCK de la rueda (implementer Opus, 2026-06-18 — pedido por Raf "la rueda tiene que lockearse en el número que más cerca caiga"; reconciliado design §12.2 AS-BUILT snap/LOCK + nota R14.5.0):** la rueda quedaba descansando ENTRE dos valores porque `snapToInterval` NO lockea en react-native-web (verificado en `ScrollViewBase.js`: web solo emite `onScroll`, los handlers momentum/drag-end no disparan). **LOCK determinístico en JS** en `WheelPicker.tsx` (lo comparten la rueda de CE y la de meses → un solo fix): native = handlers nativos (momentum-end inmediato; drag-end diferido para ceder al momentum); web = debounce de settle desde `onScroll`; ambos `scrollTo` al offset exacto + commit del valor + tick háptico una vez; idempotente/anti-loop (`isOffsetSnapped`), sin tocar la sincronía campo→rueda. Helpers PUROS nuevos en `wheel-picker.ts` (`snapOffset`/`isOffsetSnapped`, +6 tests). e2e `maniobra-circunferencia-escrotal.spec.ts` suma el caso SNAP (suelta a mitad de camino → assert scrollTop múltiplo exacto + valor centrado en CE y edad; 4/4 verde). Capturas `tests/modo-maniobra/{ce,age}-snap-lock-{360,412}.png`. Detalle: `progress/impl_03-m6-wheel-snap-lock.md`.
> **FIX RECORTE del campo hero (implementer Opus, 2026-06-19 — 🔴 manga, reportado por Raf con captura `tests/modo-maniobra/40,5-sale-recortado.png`; reconciliado design §12.2 AS-BUILT BUGFIX de recorte):** el ancho FIJO `$stepperBtn`=88 del `<TextInput>` hero recortaba "40,5"/"49,5" (≈92px a `$wheelHero`=44 bold) de ambos lados. **Fix — ancho AUTO-FIT MEDIDO**: `<Text>` medidor oculto mide el peor caso del rango (`widestCmDisplay()`, nueva util pura en `wheel-picker.ts` que deriva "XX,X" de `wheelValues`+`formatCmAR`) a la fuente hero → `onLayout` reporta el ancho REAL → el input = `medido + buffer` (`clientWidth`=104 as-built, vs 88). + `maxLength=5` (draft no desborda al tipear). El número sigue grande (no se achica); la caja sigue hugging-content centrada; "cm"+ícono adentro. NO toca rueda/sincronía/sheet/snap-lock. `widestCmDisplay` testeada en `wheel-picker.ts` (peor caso "XX,X" + cubre 40,5/49,5; +3 tests). e2e `maniobra-circunferencia-escrotal.spec.ts` suma "ANTI-RECORTE" (40,5/49,5/50/20/20,5 + draft 49,5 + maxLength, a 360/412, `scrollWidth ≤ clientWidth`; 5/5 verde). Capturas `tests/modo-maniobra/ce-input-ancho-{40,5,49,5}-{360,412}.png`. Detalle: `progress/impl_03-m6-ce-input-ancho.md`.

#### [x] M6-C.2 — Tarjeta de tendencia en la ficha + timeline (frontend puro)
Satisface: R14.14.
Detalle (design §12.6): sección nueva "Circunferencia escrotal" en `app/app/animal/[id].tsx` con la serie de mediciones (cm + edad en meses + fecha, es-AR coma decimal) + mini-tendencia, leída con `buildScrotalHistoryQuery`; se muestra solo a machos enteros (paridad con la fila repro solo-hembras de `CurrentStateSection`). Integración al **timeline** de eventos del animal (default = composición en cliente desde la lectura local, NO toca la vista `animal_timeline`; **ver DM6 design §12.6** — si Raf prefiere extender la vista server, es otra migración + Gate 1). Label es-AR del evento en el riel = "Circunferencia escrotal".
Aceptación: la ficha de un toro con ≥1 CE muestra la tarjeta de tendencia (serie + tendencia, es-AR); la CE aparece en el timeline; una hembra/castrado no muestra la tarjeta; e2e ficha.
Archivos: `app/app/animal/[id].tsx`, `app/src/utils/event-timeline.ts` (label + composición en el riel, si aplica), capturas web táctil.
AS-BUILT (implementer Opus, 2026-06-18 — pendiente reviewer + Gate 2): **frontend PURO, display read-only** (reusa `fetchScrotalHistory`/`buildScrotalHistoryQuery` de C.1; NO toca la vista server `animal_timeline` — composición de la CE en el CLIENTE, DM6 §12.6 opción (a)). (1) `event-timeline.ts`: kind `'scrotal'` en `TimelineKind` + variante `TimelineItem` (`circumferenceCm`/`ageMonths`) + `isDateOnlyKind('scrotal')=true` (`measured_at` es columna `date`) + helpers PUROS `scrotalRowsToTimelineItems` (mapea las filas de la lectura local a TimelineItem; `eventId`=id real, `eventDate`=measured_at) + `describeScrotalTimeline` ("36,5 cm · 24 meses", reusa `formatCmAR`) + `formatAgeMonthsAR` (es-AR, NO snapea — snapshot histórico) + **`sortTimelineItems` EXTRAÍDO de `parseTimeline`** (mismo criterio día→createdAt→seq→eventId) para mergear la CE compuesta con el riel del server. (2) `TimelineEvent.tsx`: `case 'scrotal'` → ícono `Ruler` ($primary), título "Circunferencia escrotal", detalle = `describeScrotalTimeline`. (3) `maneuver-applicability.ts`: predicado PURO `isBullEntire(categoryCode, isCastrated)` (única fuente del set {torito,toro}; `appliesToAnimal('circunferencia_escrotal')` ahora lo reusa — DRY; null=desconocida → INCLUYE R14.3; null categoría → no). (4) `animal/[id].tsx`: estado `scrotalHistory` cargado en `load` (blando, paralelo) SOLO si `isBullEntire(detail.categoryCode, detail.isCastrated)` (→ null para el resto, la tarjeta no se renderiza); `composedTimeline` (memo) = `sortTimelineItems([...timeline, ...scrotalRowsToTimelineItems(scrotalHistory)])` → la CE aparece en el riel; `ScrotalTrendSection` (DetailSection ícono Ruler) = mini-tendencia (sparkline de BARRAS con tokens `$greenLight`/`$primary`, orden cronológico viejo→nuevo, la más reciente resaltada; sin lib de charts) + serie (más reciente primero, "38 cm"/"35,5 cm" es-AR + "edad · fecha") con **lista capeada a 4,5 filas + peek + fade de affordance** (`scrollFades`/`hasOverflow` reusados) cuando la serie es larga; empty cálido (macho entero sin mediciones → "Todavía no hay mediciones", no error). Tarjeta entre "Estado actual" y "Datos personalizados". (5) `e2e/helpers/admin.ts`: helper `seedScrotalMeasurement` (siembra una CE por service_role; `establishment_id` lo deriva el trigger 0077, `recorded_by` lo fuerza 0098). **Verificación**: typecheck + anti-hardcode 0 + 1559 client unit (incluye +9 `event-timeline.test.ts` scrotal + +3 `maneuver-applicability.test.ts` isBullEntire) verdes; e2e `ficha-circunferencia-escrotal.spec.ts` **4/4** (toro con 3 CE → tarjeta + serie es-AR + mini-tendencia + CE en el timeline; toro con 7 CE → lista scrollea con peek/fade; hembra → sin tarjeta; castrado con CE histórica → sin tarjeta). Capturas web táctil `tests/modo-maniobra/ficha-ce-{tarjeta,timeline,serie-larga}-{360,412}.png` (título sin recorte verificado por captura). **Gate 2 = N/A** (display read-only; sin cambio de data-path/inputs/auth/schema; lectura local de una tabla ya gateada/sincronizada). Detalle/mapa R→test/autorrevisión/reconciliación: `progress/impl_03-m6-c2.md`.

### FUERA de M6 (diferido a la capa de analytics/benchmarking — NO se construye acá)
- **Clasificación apto/dudoso/bajo (semáforo) + tabla de umbrales por (grupo racial, edad)** (R14.13). Diferida: ruidosa a edad joven, raza-dependiente (Bos indicus ~4-5 cm bajo Bos taurus → tabla única sobre-rechaza cebú), CE sola = screening, no veredicto BSE. El grupo racial es derivable de la raza (spec 02) → NO se snapshotea en MVP. **Riesgo (context-m6 §6)**: números AR (INTA/produccion-animal) en confianza MEDIA → leer full-text `repositorio.inta.gob.ar/handle/20.500.12123/12793` antes de hardcodear umbrales. Cuando se construya: modelo de 3 estados etiquetado "screening de CE" (no "aptitud reproductiva"), tabla de umbrales CONFIGURABLE por `(grupo racial, edad en meses)`, NO hardcodeada.

### Gates de M6
- **Gate 1 (security modo `spec`): ✅ PASS (2026-06-17, 0 HIGH)** — `progress/security_spec_03-m6-circunferencia.md`. Trazó cross-tenant / anti-spoof / fail-closed contra el as-built real; M6 reusa primitivos ya gateados (`0054`/`0077`/`0056`/`0043`) sin relajarlos. **2 MEDIUM foldeados** (no bloquean Puerta 1): M6-SEC-01 (test del loop gating→surfacing R10.8 — M6-B.4 acceptance / e2e M6-C) + M6-SEC-02 (test no-bypass del `session_id` tenant-check — M6-B.5 caso (i)). LOW: `measured_at` sin cota = paridad con weight/condition events (convención transversal, no de M6).
- **M6-BACKEND — reviewer ✅ (en el mérito) + Gate 2 (code) ✅ PASS 0 HIGH (2026-06-17)**: `progress/review_03-m6-backend.md` (CHANGES_REQUESTED **solo** por el flake ambiental `animals_tag_unique` de la otra terminal en la suite spec-02 — NO código M6; el mérito está aprobado) + `progress/security_code_03-m6-backend.md` (implementación fiel al diseño Gate-1; session-check sin `OF session_id` dispara en INSERT; RLS sin IDOR). **2 menores a foldear en el apply**: **M6-CODE-01** (Gate 2) — sumar al test (b) de `scrotal/run.cjs` el sub-assert del anti-spoof de `establishment_id` en el **UPDATE-path** (vector ya cerrado por el `USING` de la policy + `animal_profile_id` inmutable; falta el assert de regresión); LOW `measured_at` → backlog transversal. **Pendiente: autorización de deploy de Raf** → aplicar `0098→0099→0100` + deployar `rafaq.yaml` + descomentar el hook → suite `scrotal` verde POST-APPLY (confirmación empírica del no-bypass).
- **Gate 2 (code) por sub-chunk** sobre M6-CLIENTE (M6-C.0..C.2): reviewer + autorrevisión adversarial del implementer.
- **Decisiones de criterio propio DM6-1..DM6-5** (design §12.8): defaults del spec_author, **aceptados por el leader** (nombres `circunferencia_escrotal`/`rueda`, tabla/columnas `scrotal_measurements`/`circumference_cm`/`age_months`, `ui_component='numeric_stepped'`, seed cría `default_enabled=true`, CHECK 20–50). **DM6-6 (divergencia `birth_date` exacta vs solo-año): ✅ RESUELTA por el leader (2026-06-17)** — el as-built no distingue precisión → la edad se **prellena desde `birth_date` + es siempre ajustable** por rueda de meses (R14.6/R14.7 reescritas; design §12.8). Sin acción pendiente.

---

## Trazabilidad (resumen)

- **Backend (Fase 1/2, DONE)**: R1.x (sessions), R2.x (presets), R5.11 + R7.x (session_id + gating DB), R5.13/R6.3 (enum), R8.x (transición), R11.x (seguridad).
- **Cliente (M1-M4)**: M1 = R1.x/R2.x (inicio+wizard+services), M2 = R3.x/R4.x/R5.1/R5.9/R5.10/R12.x (carga rápida + identidad, **arranca por design spike M2.0**), M3 = R5.4-R5.8/R6.x/R8.4/R9.x (10 maniobras), M4 = R10.x (offline/reanudación/surfacing).
- **M5 — custom (foldado 2026-06-13)**: M5-BACKEND = R13.1-R13.4/R13.10-R13.24 en su parte de DB (schema + gating genérico + sync delta, **Gate 1 OBLIGATORIO**); M5-CLIENTE = R13.5-R13.9/R13.11-R13.13 (UI de creación + render genérico + services de captura, Gate 2 por sub-chunk). Tweaks a M1 (lista = fábrica + custom maniobra) y M3 (render desde `ui_component`) anotados, no rehechos.
- **M6 — circunferencia escrotal (foldado 2026-06-17)**: M6-BACKEND = R14.8-R14.12/R14.15-R14.18 (tabla typed `scrotal_measurements` + data_key + seed cría + gating capa 2 fail-closed + RLS + sync, **Gate 1 OBLIGATORIO**); M6-CLIENTE = R14.1-R14.7/R14.14 (M6-C.0 design spike de la rueda, M6-C.1 StepKind 'rueda' + aplicabilidad + write-path, M6-C.2 tarjeta de tendencia + timeline, Gate 2 por sub-chunk). Clasificación apto/no-apto (R14.13) = FUERA de M6 (analytics). Decisiones de criterio propio DM6-1..6 (design §12.8) a confirmar.
- Cada `R<n>` de `requirements.md` aparece en al menos una task ("Satisface:"). El implementer completa `progress/impl_03-modo-maniobras.md` con el mapa `R<n> → archivo:test` (regla dura de trazabilidad, `docs/specs.md`).

> **Decisiones abiertas D1-D9: TODAS resueltas (ver design §9).** D1 (FK session_id), D2 (no reversión de jornada), D3 (work_lot_label incluido), D4 (sin corrects_event_id), D5 (contadores app-maintained), D7 (preset por establishment), D8 (gating dientes/CUT = enforce afinado), **D6 (balanza = FUERA del MVP, RESUELTO por Raf 2026-06-13)**. D9 (re-verificación cross-spec del find-or-create) se cierra en el Gate 2 (code) de M2 — spec 09 ya está integrada, así que M2.1 debe verificar que el alta inline fuerza el `establishment_id` activo y respeta los UNIQUE (no es bloqueante; el enforcement real ya vive en spec 09 done).

> **Gates del cliente M1-M4 (2026-06-13): Gate 1 N/A por chunk** (frontend puro; el backend ya está gateado y done `0050-0057`). **Gate 2 (code) por chunk** (M1, M2, M3, M4). Si algún chunk necesitara una migración nueva (no debería: enum/FK/triggers ya existen), se marca **hallazgo para Gate 1 puntual** antes de aplicar DDL.

> **Gates de M5 (foldado 2026-06-13): Gate 1 (security modo `spec`) OBLIGATORIO** sobre **M5-BACKEND** (M5-B.1..B.6 — reabre RLS de `field_definitions` de spec 02 + 2 tablas nuevas + gating SECURITY DEFINER + frontera WAL del sync) **ANTES de la Puerta 1**. **Gate 2 (code) por sub-chunk** sobre M5-CLIENTE y cada migración aplicada. DM5-1..DM5-3 (design §11.8) a confirmar con Raf.

> **Cierre Gate 1 FAIL (sesión 18, backend)** — la pasada de s18 cerró los 6 findings de `progress/security_spec_03-modo-maniobras.md` (SEC-SPEC-03-01..06) → reflejados en las migrations `0050-0057` ya done.

---

## Historial de refinamiento (tasks)

- **2026-05-30 (sesión 18)** — Redacción inicial de las fases + cierre de Gate 1.
- **2026-06-13 — Reconciliación JIT del cliente.** Backend marcado DONE (`0050-0057`, Fases 1-2 `[x]`). Las Fases 3/4 del cliente se reescribieron como chunks **M1-M4** consumiendo el as-built real (BLE spec 04 `useBleStickListener`/`useStickListenerControls`/`useBusyMode`; find-or-create spec 09 `FindOrCreateOverlay`/`lookupByTag`; offline feature 15 CRUD-plano + outbox; eventos spec 02 `addWeight`/`addTacto`/`addService`/… + write-paths nuevos para vacunación/sangrado/raspado/tacto_vaquillona/dientes-CUT; espejo `animal-category.ts`). Layout reconciliado a `app/app/**` + `app/src/{services,hooks,utils,contexts}` (NO `app/src/features/maneuvers/`). **M2 arranca por design spike (M2.0)** con botones gigantes (decisión de Raf). **Balanza FUERA del MVP** → peso manual (M3.1 reusa `addWeight`). Gates del cliente: Gate 1 N/A, Gate 2 por chunk. No se tocó backend ni código de app/tests — solo este markdown.

- **2026-06-13 — Fold de M5 — datos/maniobras custom — desde `context-m5-custom-maniobras.md`.** Se agregó el **chunk M5** (US-13, R13.1–R13.24): **M5-BACKEND** (M5-B.1 ALTER `field_definitions` custom + RLS reabierta; M5-B.2 `custom_measurements`; M5-B.3 `custom_attributes`; M5-B.4 gating genérico + validación de value; M5-B.5 housekeeping + delta de sync rules frontera WAL; M5-B.6 suite no-bypass) y **M5-CLIENTE** (M5-C.1 services de captura; M5-C.2 UI de creación ambos `+`; M5-C.3 render genérico + propiedad en alta/ficha). **Tweaks forward-looking** a M1 (lista = fábrica + custom maniobra enabled) y M3 (render desde `ui_component`) anotados, NO rehechos; M2.0 design spike NO se bloquea. **Gate 1 (security modo `spec`) OBLIGATORIO** sobre M5-BACKEND (reabre schema/RLS de spec 02 + tablas nuevas + frontera WAL) ANTES de la Puerta 1; Gate 2 (code) por sub-chunk en M5-CLIENTE. Numeración propuesta `0090..0094` (último as-built `0089`), **a re-confirmar contra el árbol y las terminales paralelas** (spec 08 activa, sin migraciones a la fecha) antes de crear archivos; deploy gateado por el leader. 3 preguntas abiertas (DM5-1..3, design §11.8) para Raf. **No se aplicó DDL ni se tocó código de app/tests — solo markdown de la spec.** Status se mantiene en `spec_ready`.

- **2026-06-13 — Fix-loop Gate 1 M5: 2 HIGH + 3 MEDIUM foldeados — value fail-closed + dominio ui_component, inmutabilidad establishment_id/data_type/data_key/ui_component [decisión leader], caps INPUT-1 description/category/data_key/data_type/options/notes** (fuente: `progress/security_spec_03-m5-custom.md`). Endurecimientos **aditivos** del delta backend; NO se re-decidió el modelo (lockeado en `context-m5-custom-maniobras.md`), NO se tocó backend ni código de app/tests. Cambios en tasks:
  - **M5-B.1** (ALTER `field_definitions`): + CHECK de dominio de `ui_component` custom (R13.25), + caps/sets INPUT-1 `data_key`/`description`/`data_type`/`category` (R13.27), + inmutabilidad en UPDATE de `establishment_id`/`data_type`/`data_key`/`ui_component` en el guard (R13.26), + `data_type` de cliente ∈ (maniobra,propiedad), + cardinalidad/largo de `config_schema.options` (R13.17). "Satisface:" extendido a R13.25/R13.26/R13.27; aceptación ampliada.
  - **M5-B.2** (`custom_measurements`): + CHECK `notes ≤500` (M5-SEC-05).
  - **M5-B.4** (gating + validación de value): + rama `else raise 23514` fail-closed por `ui_component` no reconocido (R13.25) + array `enum_multi` ≤50 (R13.17). "Satisface:" + R13.25.
  - **M5-B.6** (suite no-bypass): + casos (g)..(m) que prueban cada fix — value con ui_component desconocido → reject; crear custom con ui_component fuera de los 7 → reject; UPDATE de establishment_id/data_type/data_key/ui_component → reject; data_type fuera del set → reject; options > cap → reject; description/data_key/category/notes > cap → reject. "Satisface:" + R13.25/R13.26/R13.27.
  - **Status se mantiene en `spec_ready`.** Re-correr Gate 1 sobre el delta editado antes de la Puerta 1.

- **2026-06-17 (sesión 27) — Fold de M6 — Circunferencia escrotal — desde `context-m6-circunferencia-escrotal.md`.** Se agregó el **chunk M6** (US-14, R14.1–R14.18): **M6-BACKEND** (M6-B.1 tabla typed `scrotal_measurements` + audit forzado + RLS + session_id; M6-B.2 data_key global `circunferencia_escrotal` + seed cría habilitado por defecto; M6-B.3 gating capa 2 fail-closed single-key reusando `assert_data_keys_enabled`; M6-B.4 sync rules `ev_scrotal_measurements` + schema.ts + surfacing de rechazos; M6-B.5 suite no-bypass) y **M6-CLIENTE** (M6-C.0 design spike de la rueda — visual antes de plomería, paridad M2.0; M6-C.1 StepKind 'rueda' factory-only + aplicabilidad per-animal macho-entero-no-ternero + dispatcher case + write-path CRUD-plano sin RPC; M6-C.2 tarjeta de tendencia en la ficha + timeline). **Clasificación apto/no-apto (R14.13) = FUERA de M6** (diferida a analytics, con su razón). **Gate 1 (security modo `spec`) OBLIGATORIO** sobre M6-BACKEND (tabla de evento nueva + data_key + gating capa 2 + RLS + frontera WAL) ANTES de la Puerta 1; Gate 2 (code) por sub-chunk en M6-CLIENTE. Numeración propuesta `0098..0100` (último as-built `0097`), **a re-confirmar contra el árbol y terminales paralelas** antes de crear archivos; deploy gateado por el leader. **6 decisiones de criterio propio (DM6-1..6, design §12.8)** para Raf/leader — la más sustantiva es **DM6-6 (divergencia context↔as-built del `birth_date`)**: el as-built NO distingue "fecha exacta" de "solo año" (un único `date` nullable, sin flag de precisión; year-only es edge ABIERTO de spec 02) → default conservador = `birth_date` no-NULL = exacta, rueda manual solo si NULL; NO se cerró por cuenta del spec_author. **No se aplicó DDL ni se tocó código de app/tests — solo markdown de la spec.** Status se mantiene en `spec_ready`.
