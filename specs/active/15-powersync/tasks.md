# Tasks — 15-powersync

> Checklist ejecutable. Cada tarea cubre `R<n>` de `requirements.md`. El implementer marca `[x]`.
> **Gate 1 (`security_analyzer` modo `spec`) es OBLIGATORIO sobre `sync-streams/rafaq.yaml` (T2) antes de cualquier deploy a la instancia y antes de la Puerta 1 humana.**
> Marcado de build target: `[web]` = buildable/verificable hoy en navegador; `[device]` = diferido al dev build Android.
> **Puerta 1 (2026-06-08, opción ii)**: el **alta de animal, parto, baja y soft-deletes** van OFFLINE vía outbox + RPC-mapping (R6.6, R6.8–R6.11; T6.2). Solo **`import_rodeo_bulk`** queda ONLINE (excepción: onboarding masivo; T6.1).

## Fase T1 — Cimientos del cliente (deps + env + connector + AppSchema + provider)

- [x] **T1.1** — Agregar deps: `@powersync/react-native@1.35.3`, `@powersync/web@1.38.2`, `@powersync/react@1.10.0` + el open-factory WASM para web `@journeyapps/wa-sqlite@1.7.0` (whitelisteado en pnpm `onlyBuiltDependencies` por su postinstall que baja el PowerSync core WASM — ADR-011). Cubre: R2.2. *(Run 1)*
- [x] **T1.2** — `app/src/utils/env.ts` (+ `env-resolve.ts` puro testeable): agregar `powersyncUrl` desde `EXPO_PUBLIC_POWERSYNC_URL`, con error accionable en español si falta. `.env.example` creado en la raíz con el placeholder. Cubre: R1.2, R1.3. *(Run 1)*
- [x] **T1.3** — `services/powersync/schema.ts`: `AppSchema` espejando las 26 tablas sincronizadas (§3 del design) + `op_intents` (insertOnly) + 5 overlay `pending_*` (localOnly). PK resuelta (T1.3): `id` por **alias en la stream** (`user_private`→`user_id AS id`; `rodeo_data_config`/`birth_calves`→`id` sintético `(a||':'||b)`); el SDK agrega el `id` implícito y prohíbe declararlo. Cubre: R2.1, decisión-abierta-PK. *(Run 1)*
- [x] **T1.4** — `services/powersync/database.ts` (+ `platform-select.ts` puro): factory por plataforma vía `require()` guardado por `Platform.OS` (`@powersync/web` WASM en web / `@powersync/react-native` en device) + instancia singleton lazy de `PowerSyncDatabase`. Cubre: R2.2. *(Run 1)*
- [x] **T1.5** — `services/powersync/connector.ts` (+ `upload-classify.ts` puro): `SupabaseConnector.fetchCredentials()` (`{ endpoint, token }` desde la sesión; **null si no hay sesión** — contrato del SDK, reconciliado vs design §4). Cubre: R3.1, R3.2. *(Run 1)*
- [ ] **T1.6** — `services/powersync/upload.ts` + `connector.uploadData()`: drenar la upload queue → upsert/update contra Supabase; transitorio → reintento (deja en cola), permanente → descarta + superficia. Cubre: R3.3, R3.4, R3.5, R8.1, R6.2. *(Run 1: la BASE de CRUD plano + clasificación transitorio/permanente + superficiado ya viven en `connector.uploadData()`; el `op_intents`→RPC/overlay/idempotencia queda STUB `// TODO Run T6`. `upload.ts`/`outbox.ts` son Run T6.)*
- [x] **T1.7** — `services/powersync/provider.tsx` + montaje en `app/app/_layout.tsx` (dentro de `AuthProvider`, envolviendo los providers de datos): `<PowerSyncProvider>` por encima del árbol; `connect(connector)` con sesión válida (authenticated+verified), `disconnect()` en logout. Cubre: R2.3. *(Run 1)*
- [x] **T1.8** — `services/powersync/status.ts` (+ `status-derive.ts` puro): estado de conexión/sync (conectado/sincronizando/pendientes en cola) consultable por la UI + copy es-AR. Cubre: R10.1, R10.2. *(Run 1)*

## Fase T2 — Sync streams (deploy GATEADO por Gate 1)

- [x] **T2.1** — `sync-streams/rafaq.yaml`: 26 streams completas (§2 del design) — globales, self-only (`user_private`, `user_roles`), per-establishment, eventos (vía perfil), `birth_calves`/`rodeo_data_config` (PK compuesta con `id` sintético aliased). Cada query scopea por rol activo / self-only / global y filtra `deleted_at IS NULL`. **NO deployado** (lo hace el leader/Raf, gateado). Cubre: R4.1–R4.11. *(Run 1)* **⚠️ REESCRITO 2026-06-08 al patrón `with:`/INNER JOIN** (CTE `org_scope`/`owner_scope` + INNER JOIN al padre, en vez de subquery anidada) por el límite de buckets de PowerSync (`[PSYNC_S2305]` too many buckets / regresión #611 — design §2.1-superseded, §2.2). **Frontera de autorización IDÉNTICA** (design §2.3-equivalencia); solo cambió la estructura SQL. Quedan 5 incertidumbres de sintaxis fina (design §2.2-incertidumbres) que confirma el validador del dashboard en el re-Gate 1 / deploy.
- [ ] **T2.2** — ⛔ **Gate 1 (re-Gate 1 tras la reescritura `with:`/JOIN)**: `security_analyzer` modo `spec` audita `rafaq.yaml`. Verifica equivalencia stream↔RLS, scoping por rol activo, `user_private` self-only, no-leak por WAL, `deleted_at` filtrado. **Foco del re-Gate 1: que el patrón `with:`/JOIN preservó EXACTAMENTE el set de filas del patrón anterior (no agrandó ni achicó la frontera)** — el cambio es estructural, no de autorización. **NO avanzar sin PASS.** Output: `progress/security_spec_15-powersync.md`.
- [ ] **T2.3** — Deploy de `rafaq.yaml` a la instancia `rafaq-beta` (SOLO tras T2.2 PASS + Puerta 1 humana). Al pegar el YAML en el dashboard: (a) el **validador** confirma la sintaxis fina (las 5 incertidumbres de design §2.2-incertidumbres — `with:` mapa, `queries:`, `(expr) AS id, t.*`, 2–3 INNER JOINs, short-hand `IN org_scope`); ajustar al fallback documentado si el validador rechaza alguna. (b) un **connect de prueba** confirma que el conteo de buckets quedó debajo de 1000 (idealmente con la beta limpia de la data de test — la contaminación de 106 establecimientos es un problema aparte). Cubre: R1.1 (consume el provisioning).

## Fase T3 — Swap de lectura: catálogos globales + contexto de establecimiento `[web]`

- [x] **T3.1** — `rodeo-config.ts` (`fetchFieldCatalog`/`fetchSystemDefaults`/`fetchRodeoConfig`) + `animals.fetchSystemCategories` + `rodeos.fetchProductionSystems`: leer catálogos (`categories_by_system`, `field_definitions`, `system_default_fields`, `rodeo_data_config`, `species`/`systems_by_species`) desde SQLite local vía `getPowerSync().getAll()` (one-shot; reactividad watchable diferida a un run posterior — decisión del leader). Degradación avisada si el catálogo aún no sincronizó (`kind:'network'` "Sincronizando…"). Las ESCRITURAS de rodeo-config (`toggleRodeoField`/`enableNonDefaultField`) quedan ONLINE sin tocar. Cubre: R4.4, R5.1, R5.4. *(Run 3)*
- [x] **T3.2** — `establishments.ts` (`loadMemberships`/`loadOwnProfile`/`loadFullProfile`/`loadEstablishmentDetail`/`countActiveMembers`) + `members.ts` (`loadMembers`/`countTeam`/`loadPendingInvitations`) + `profile.ts` (`loadProfileNamePhone`): lectura del contexto de establecimiento, miembros (perfil público de coworkers desde `users`+`user_roles`) y perfil self (`user_private`) desde SQLite local. Mutaciones admin/EF ONLINE sin tocar. Cubre: R5.1, R5.3, R7.2. *(Run 3)*
- [x] **T3.3** — `rodeos.ts`: `fetchRodeos` desde SQLite local. `createRodeo`/`softDeleteRodeo` siguen ONLINE (T5/T6); `createRodeo` usa un helper interno `fetchRodeosOnline` (PostgREST) para diffear su propio INSERT al instante (la fila vuelve al SQLite local async vía stream). Cubre: R5.1. *(Run 3)*

## Fase T4 — Swap de lectura: camino de datos `[web]`

- [ ] **T4.1** — `animals.ts`: `fetchAnimals`, `searchAnimals` (fuzzy → `LIKE` local; exacto TAG/IDV igual), `countAnimals`, `fetchAnimalDetail`, `findOrCreateLookup` → SQL local con JOINs SQLite. Cubre: R5.1, R5.2, R5.3.
- [ ] **T4.2** — `events.ts`: `fetchTimeline` (reconstruir el UNION ALL de los 7 orígenes como SQL local; sin las 2 queries suplementarias) + `fetchMother` (JOIN local sobre `birth_calves`). Cubre: R5.1, R5.3.
- [ ] **T4.3** — `management-groups.ts`: `fetchManagementGroups`, `fetchGroupMembers` → SQL local. Cubre: R5.1.

## Fase T5 — Swap de escritura offline: eventos simples + asignación/CRUD de lote `[web]`

- [ ] **T5.1** — `events.ts`: `addWeight`, `addConditionScore`, `addTacto`, `addService`, `addAbortion`, `addObservation` → INSERT local + upload queue; `id` cliente; **eliminar** el patrón split-insert (R6.3). Cubre: R6.1, R6.3, R6.4.
- [ ] **T5.2** — `management-groups.ts`: `assignAnimalToGroup` (UPDATE local), `createManagementGroup`, `renameManagementGroup` → local + upload; **eliminar** el `diff before/after` y el `count:'exact'` (ya no aplica el gotcha RLS-on-RETURNING). Cubre: R6.1, R6.3.
- [ ] **T5.3** — `sessions`/`maneuver_presets` (si la UI de spec 03 los consume): create/update local + upload. Cubre: R6.1.

## Fase T6 — Mutaciones RPC-bound OFFLINE vía outbox + RPC (Puerta 1, opción ii)

- [ ] **T6.1** — Mantener ONLINE **solo `import-rodeo.import_rodeo_bulk`** (excepción Puerta 1: onboarding masivo, no manga). Sin red → `kind:'network'` accionable, sin marcar como hecho. Cubre: R6.5, R6.6.
- [ ] **T6.2** *(PRIMARIA/REQUERIDA — opción ii de Puerta 1)* — Implementar el camino OFFLINE de las (b) RPC-bound de camino de campo:
  - **T6.2a** — `schema.ts`: declarar la tabla outbox `op_intents` (`insertOnly`, §5.3.2) con `id` = `client_op_id` de cliente **+ las tablas OVERLAY optimista `pending_*` (`localOnly: true`, §5.3.3) que NO generan CrudEntry** (`pending_animals`, `pending_animal_profiles`, `pending_reproductive_events`, `pending_birth_calves`, `pending_status_overrides`), cada una con `client_op_id`. Cubre: R6.8, R6.11, R6.12.
  - **T6.2b** — `outbox.ts`: helper que, en una **sola `writeTransaction` local**, encola la intención (`op_type` + `params_json` + `client_op_id`) **e** escribe el **efecto optimista en el overlay `localOnly`** (`pending_*`) — NO en las tablas sincronizadas (§5.3.3). Cubre: R6.8, R6.11, R6.12.
  - **T6.2c** — `upload.ts`/`connector.uploadData()`: en el drenado, mapear cada CrudEntry de `op_intents` a `supabase.rpc(...)` (§5.4.2), en orden de cola; pasar `p_client_op_id` **solo a `register_birth`** (las demás firmas no lo tienen). Cubre: R6.9.
  - **T6.2d** — Idempotencia (§5.4.3): dedup natural para `create_animal` (ids de cliente + ON CONFLICT), `soft_delete_*` (guarda `WHERE deleted_at IS NULL`) y **`exit_animal_profile` (transición de status idempotente — NO requiere delta, confirmado contra `0044`)**; para **`register_birth`** usar el `p_client_op_id` contra la columna/índice del delta (requiere T6.4). Cubre: R6.10.
  - **T6.2e** — ACK / rollback del overlay (§5.4.4): éxito → `tx.complete()` + `clearOverlay(client_op_id)` (las filas reales bajan por la stream; el UNION deja de mostrar el overlay); transitorio → reintento sin tocar el overlay; permanente → `rollbackOverlay(tx)` (borra el overlay `pending_*` por `client_op_id`) + descarte + superficia; **`P0002` (`not found`) de un `soft_delete_*` ya aplicado → éxito idempotente: descartar sin rollback + limpiar overlay** (NO restaurar la fila, que sí está borrada). Cubre: R6.11, R8.1, R10.2.
  - **T6.2f** — Swap de los 4 services a outbox + overlay: `animals.createAnimal` (intención `create_animal` + overlay `pending_animals`/`pending_animal_profiles`), `events.registerBirth` (intención `register_birth` + overlay terneros/parto), `exit-animal.exitAnimalProfile` (intención `exit_animal_profile` + overlay `pending_status_overrides` effect=`exited`), `soft_delete_*` de management-groups/rodeos/events (intención `soft_delete_<entity>` + overlay `pending_status_overrides` effect=`soft_deleted`). **Las lecturas afectadas (ficha/detalle, timeline, lista, miembros de lote) hacen UNION synced + overlay** (R6.11). Preservar las firmas públicas (R11.1). Cubre: R6.6, R6.8, R6.11.
- [ ] **T6.3** — Identidad/admin: confirmar que `establishments.create`, `members.invite`, edición de perfil y aceptación de invitación (EF) **siguen online** (sin tocar). Cubre: R7.1.
- [x] **T6.4** — ⚠️ **Delta de backend de idempotencia (APROBADO en Puerta 1, ADITIVO, schema-sensitive) — la migración la escribe/aplica el IMPLEMENTER (≥ `0075`) por Management API, GATEADA por el leader + Gate 1 (spec). El spec_author NO escribe migraciones.** *(SQL escrito en `supabase/migrations/0075_register_birth_idempotency.sql` — Run 2. La APLICA el leader por Management API tras gatear el SQL; NO aplicada al remoto desde el implementer.)* Alcance MÍNIMO (solo `register_birth`):
  - columna **nullable** `client_op_id uuid` en `reproductive_events`;
  - índice **UNIQUE parcial COMPUESTO** `... (animal_profile_id, client_op_id) WHERE client_op_id IS NOT NULL` (compuesto, **NO global**: cierra el oráculo de existencia cross-tenant — fix HIGH-D1, design §9-nota);
  - param `p_client_op_id uuid default null` en `register_birth` + **guard de idempotencia con SCOPING de tenancy + error genérico ante colisión ajena (fix HIGH-D1).** ⛔ **NO escribir el guard "literal" de versiones previas (lookup global `SELECT id WHERE client_op_id = p_client_op_id → RETURN`): saldría VULNERABLE a IDOR cross-tenant** (un atacante con un `client_op_id` colisionado recibiría el `id` de un parto de OTRO establecimiento por el canal RPC). El cuerpo correcto (ver design §5.4.3(1), R6.10):
    1. la authz as-built rige PRIMERO (`has_role_in(v_est)` sobre la fila real de la madre, `0045:213-225`) — sin cambios;
    2. el lookup del `client_op_id` está **scopeado al caller**: `WHERE re.client_op_id = p_client_op_id AND re.animal_profile_id = p_mother_profile_id AND animal_profiles.establishment_id = v_est AND re.deleted_at IS NULL` (JOIN a `animal_profiles` porque `reproductive_events` NO tiene `establishment_id` denormalizado, `0026`); si matchea → devolver ese `id` y RETURN (no-op legítimo del propio caller);
    3. si NO matchea (no existe para este caller, o colisión con un parto AJENO) → camino de creación → al INSERTAR el `client_op_id` choca contra el índice UNIQUE → **error genérico** (`unique_violation`/`23505`), **NUNCA** devolver el `id`/datos ajenos ni un oráculo de existencia.
  - **NO** se agrega delta a `exit_animal_profile` (idempotente natural, confirmado contra `0044`), ni a `create_animal`/`soft_delete_*` (dedup natural). Path online (`p_client_op_id` NULL) idéntico al as-built; índice parcial → no afecta partos históricos. **⛔ Gate 1 (spec) sobre el delta ANTES de aplicarlo** (R11.4) — debe verificar el scoping de tenancy del guard. Cubre: R6.10 (server-side), R11.3, R11.4.
- [x] **T6.6** — ⚠️ **2do delta de backend ADITIVO (la "Opción 2" de design §9, schema-sensitive) — trigger que mantiene `user_roles.active` coherente con el soft-delete de establecimientos.** La migración la escribe el IMPLEMENTER (≥ `0076`) y la APLICA el leader por Management API tras gatearla (Gate 1 spec + Gate 2 + reviewer); el implementer NO la aplica. *(SQL escrito en `supabase/migrations/0076_deactivate_roles_on_establishment_soft_delete.sql` — Run 4.)* Alcance:
  - función + trigger `AFTER UPDATE OF deleted_at ON establishments`: en la transición `NULL → NOT NULL`, `UPDATE user_roles SET active = false, deactivated_at = now() WHERE establishment_id = NEW.id AND active = true`;
  - **NO reactiva** en `NOT NULL → NULL` (restore): no se puede distinguir desactivado-por-borrado de desactivado-por-remoción-de-miembro (documentado como limitación deliberada; no hay flujo de restore en MVP);
  - **backfill** de los roles ya-activos que apuntan a campos ya-borrados (incluye los 3 espurios del user de prueba);
  - `security definer` + `set search_path = public` (precedente: `handle_new_establishment` 0011). **Verificado: NO hay otro trigger sobre `user_roles`** → cero loop / cero side-effect destructivo. **Aditivo**: NO cambia `has_role_in`/`is_owner_of` (ya filtraban `deleted_at`) → sin cambio observable de la app/RLS; sólo hace que `active` sea un proxy FIEL de "campo vivo" para el modelo de sync JOIN-free (org_scope). ⛔ Gate 1 (spec) sobre el delta ANTES de aplicarlo (R11.4 reconciliada). Cubre: R4.1, R4.2 (refuerza el cierre de HIGH-1), R11.3, R11.4.
  - **Test** en `supabase/tests/rls/run.cjs` (campo + usuario dedicados, autocontenido): soft-delete → ambos `user_roles` `active = false` + `deactivated_at` poblado → `has_role_in` sigue false. **FALLA hasta que el leader aplique `0076`** (esperado; mismo patrón que la suite `spec 15-powersync` del animal runner con `0075`).
  - **GUARD — 2da mitad del invariante (Run 4.1, Gate 1 HIGH-1 reabierto).** El trigger de deactivate cubre los roles EXISTENTES al borrar el campo, pero NO impide CREAR/activar un rol NUEVO para un campo YA borrado (vector verificado: `supabase/functions/accept_invitation/index.ts:93-101` inserta `active:true` sin chequear `establishments.deleted_at` → owner invita → owner borra el campo → el invitado acepta el link pendiente → rol activo sobre campo muerto → el sync JOIN-free le replicaría la data del campo borrado). **INVARIANTE DECLARADO: `user_roles.active = true` ⇒ el establecimiento está vivo (`deleted_at IS NULL`)**, enforced a NIVEL DB por las DOS mitades de `0076`: (a) `deactivate_roles_on_establishment_soft_delete` (deactivate-on-delete) + (b) `prevent_active_role_on_soft_deleted_establishment` (block-activate-for-deleted: trigger `BEFORE INSERT OR UPDATE OF active ON user_roles` que rechaza con `errcode 23514` cualquier INSERT/UPDATE que deje `active = true` apuntando a un campo `deleted_at IS NOT NULL`; `active = false` SIEMPRE permitido → no rompe el deactivate ni la remoción de miembros). **Compat verificada** (todos siguen pasando): creación de campo (`handle_new_establishment` 0011, campo nuevo deleted_at NULL), aceptar invitación a un campo VIVO, el deactivate (active=false) + backfill, y el fix R8.3/R8.4 (restore `deleted_at=null` ANTES de reactivar `active=true`). **Test** en `run.cjs` (`estG`/`userG`/`userH` dedicados, autocontenido): INSERT/UPDATE `active=true` sobre campo borrado → FALLA (guard 23514); sobre campo vivo y cualquier `active=false` → PASA. **FALLA hasta que el leader aplique `0076`** (esperado, mismo patrón que el test de deactivate). Cubre: R4.1, R4.2 (cierra HIGH-1 completo: roles existentes Y nuevos).

- [ ] **T6.5** — **No doble-upload (R6.12)**: verificar que el efecto optimista vive SOLO en el overlay `localOnly` y que NO se escribe en tablas sincronizadas en el camino (b); que `uploadData()` NO emite un INSERT/UPDATE plano paralelo de las filas que la RPC va a (re)crear. Test: encolar `register_birth` offline → exactamente UNA CrudEntry (`op_intents`) en la upload queue → la RPC corre una sola vez. Cubre: R6.12.

## Fase T7 — Tests

- [ ] **T7.1** — Unit de mappers/connector: `uploadData` traduce CrudEntry → upsert/update; transitorio reintenta, permanente descarta + superficia. Cubre: R3.3, R3.4, R3.5, R8.1.
- [ ] **T7.2** — Integración Node de **no-bypass por device** (espejo de los runners RLS de `supabase/tests/rls/`): por cada clase de stream, usuario A NO recibe filas del establecimiento de B; `user_private` de B NO llega a A; catálogos globales SÍ llegan a todos; soft-deleted NO entra al sync set. Cubre: R4.1–R4.11, R9.1, R9.2, R8.2.
- [ ] **T7.3** — `[web]` E2E Playwright: con sync activo, alta→ficha→evento simple offline (simulado)→reconexión→fila en Supabase; lectura offline de lista/ficha/timeline. Cubre: R5.1, R5.2, R6.1, R6.2, R11.2.
- [ ] **T7.4** — `[web]` Smoke de boot del DB local WASM: la app arranca, el DB local bootea sin crashear, una query local devuelve filas tras el primer sync. Cubre: R2.4.
- [ ] **T7.5** — Verificar que la suite backend (RLS + Edge) sigue verde y que NO se tocaron RLS/policies/triggers/EF existentes. El **único** cambio de schema permitido es el delta aditivo de idempotencia de T6.4 (`reproductive_events.client_op_id` + UNIQUE parcial + param en `register_birth`), gateado por Gate 1; verificar que el path online de `register_birth` (`p_client_op_id` NULL) sigue idéntico y que ninguna otra migración cambió. Cubre: R11.3, R11.4.
- [ ] **T7.6** — Verificar que la suite unit/E2E existente del cliente sigue verde tras el swap (firmas de services intactas). Cubre: R11.1, R11.2.
- [~] **T7.7** — Unit/integración de **idempotencia (no doble-apply, R6.10)**: simular at-least-once (la RPC ejecuta server-side pero se pierde el ACK → PowerSync reintenta la misma CrudEntry de `op_intents`); verificar que el reintento NO crea un segundo parto / segunda baja / segundo animal / segundo soft-delete. *(Run 2: cubierto el sub-bullet `register_birth` — los 3 casos en `supabase/tests/animal/run.cjs` :: "T2.20 register_birth idempotencia por client_op_id (delta T6.4)". Los sub-bullets `exit_animal_profile`/`create_animal`/`soft_delete_*` (dedup natural, sin delta) se testean en el run de T6.2 con la outbox.)* Casos con su modo de dedup verificado:
  - `register_birth` → dedup por `client_op_id` (requiere el delta T6.4): el reintento con el mismo `client_op_id` **del mismo caller, misma madre** devuelve el parto existente, NO crea un 2do evento ni N terneros nuevos. (Si el delta aún no está aplicado, se testea el contrato esperado contra el guard de `p_client_op_id` y se marca pendiente de T6.4.) **Caso negativo cross-tenant (fix HIGH-D1, obligatorio):** un usuario A invoca `register_birth` con una madre PROPIA + un `p_client_op_id` que colisiona con un parto del usuario B (otro establecimiento) → la RPC **NO devuelve** el `id`/datos del parto de B (no IDOR), responde con un error genérico (`unique_violation`/`23505`), y NO se filtra existencia/propietario del parto ajeno. El parto de B queda intacto. *(Run 2: T2.20 casos 1, 2, 3.)*
  - `exit_animal_profile` → dedup **natural** (sin delta): el reintento re-aplica el UPDATE de status idéntico (mismo end-state); NO crea una 2da fila en `animal_category_history` (el UPDATE no toca `category_id`).
  - `create_animal` → dedup natural por id de cliente + ON CONFLICT.
  - `soft_delete_*` → reintento levanta `P0002` (fila ya borrada) → tratado como éxito idempotente (NO segundo borrado, NO rollback). Cubre: R6.10.
- [ ] **T7.8** — Unit/integración de **rollback del overlay ante rechazo permanente (R6.11)**: encolar una intención que la RPC rechaza permanente (RLS 42501 por `active_lost`, o error de dominio); verificar que el **overlay local-only** (`pending_*`) se **borra** (el ternero/alta desaparece del UNION; la baja/soft-delete se "des-oculta"), que NO quedó nada escrito en tablas sincronizadas, la intención se descarta de la cola (no loop) y el rechazo queda observable. Contrapruebas: (a) error transitorio → NO se borra el overlay, la intención queda en cola; (b) `P0002` de `soft_delete_*` ya aplicado → éxito idempotente: descarta sin rollback + limpia overlay. Cubre: R6.9, R6.11, R8.1, R10.2.
- [ ] **T7.9** — `[web]` E2E de **parto offline end-to-end**: offline (simulado) → `registerBirth` escribe el parto + N terneros en el **overlay local-only** (visibles en la ficha de la madre vía UNION) y encola UNA intención `op_intents` → reconexión → drena la outbox → `register_birth` corre atómico server-side → en el ACK se limpia el overlay y las filas reales (ids server-side) bajan por la stream → los terneros aparecen en Supabase con **un solo** evento de parto (no duplicado) y la ficha NO muestra duplicados. Mismo patrón para `createAnimal` offline (alta find-or-create) y `exitAnimalProfile` offline (baja). Cubre: R6.6, R6.8, R6.9, R6.10, R6.11, R6.12.

## Fase T8 — Native (diferido al dev build Android) `[device]`

- [ ] **T8.1** — `[device]` Boot del DB local con `@powersync/react-native` en el dev build; veredicto de persistencia + performance del sync set en hardware de campo. Cubre: R2.5, R5.2, R8.3.
- [ ] **T8.2** — `[device]` Verificar coexistencia BLE (bastón/balanza) + DB local + sync en device. (Diferido; no bloquea el cierre de la spec en web.)

## Fase T9 — Paso 2: denormalización de `establishment_id` (tablas hijas + entidades compartidas)

> **El as-built del paso 1 es V3 JOIN-FREE** (`sync-streams/rafaq.yaml`, validado en vivo + Gate-1-PASS). El paso 2 suma al sync las tablas que el paso 1 difirió porque NO tienen `establishment_id` propio. Patrón en ADR-026 + design §2.4. **Cada migración es schema-sensitive → Gate 1 (spec) ANTES de aplicar por Management API + Gate 2 + reviewer (R11.4/R13).** El spec_author NO escribe migraciones ni el YAML del paso 2 — los escribe el implementer tras Gate 1 + aprobación de (B)/(C).

- [ ] **T9.0** — ⏸ **Decisiones de Raf (el leader las lleva a la Puerta):** **(B) animals → (b1)** (denormalizar identidad sobre `animal_profiles`, NO sincronizar `animals`) y **(C) users/nombres → (c1)** (nombres ONLINE). Ambas cambian el modelo de lectura (ADR-026 §"Decisiones que Raf debe aprobar"). Recomendación del spec_author: (b1)/(c1). NO avanzar T9.4/T9.5 sin la decisión.
- [x] **T9.1** — ⚠️ **Migración(es) de denormalización (A) — las escribe el IMPLEMENTER (≥ `0077`) por Management API, GATEADAS por el leader + Gate 1 (spec).** Por cada tabla hija (`weight_events`, `reproductive_events`, `sanitary_events`, `condition_score_events`, `lab_samples`, `animal_category_history`, `birth_calves`, `rodeo_data_config`): `ADD COLUMN establishment_id uuid` (nullable, FK a establishments) → **backfill** desde el padre → **trigger BEFORE INSERT que FUERZA `establishment_id` desde el padre** (anti-spoof, espejo de `tg_force_created_by_auth_uid` `0043` / `tg_animal_events_validate_est` `0034`) [+ `BEFORE UPDATE` en `rodeo_data_config`] → `SET NOT NULL` al final. Cadenas de derivación: eventos/`animal_category_history` ← `animal_profiles.establishment_id`; `birth_calves` ← `reproductive_events → animal_profiles.establishment_id`; `rodeo_data_config` ← `rodeos.establishment_id` (design §2.4 (A)). **NO modificar la RLS as-built** (la columna es solo para el stream). Cubre: R13.1, R13.2, R13.3, R13.4, R13.6. *(Run 5: SQL escrito en `0077_denormalize_establishment_id_event_children.sql` (5 eventos + `animal_category_history`, función force compartida `tg_force_establishment_id_from_profile`) + `0078_denormalize_establishment_id_birth_calves_rodeo_config.sql` (`birth_calves` cadena parto→madre + `rodeo_data_config` vía rodeo, `BEFORE INSERT OR UPDATE`). **RECONCILIADO**: el force se aplicó en INSERT **y UPDATE** a las 5 tablas de evento (no solo `rodeo_data_config`) — vector de spoofeo por UPDATE de cliente con `GRANT UPDATE`; ver R13.3 reconciliado. NO aplicadas al remoto: las aplica el leader por Management API tras Gate 1.)*
- [ ] **T9.2** — ⛔ **Gate 1 (spec) sobre cada delta de (A)**: verifica trigger-force fiel al padre, backfill correcto/idempotente, stream equivalente (no más permisiva) a la RLS de la tabla, `security definer`/`search_path` donde aplique. **NO aplicar la migración antes de PASS.** Output: `progress/security_spec_15-powersync.md`. Cubre: R11.4, R13.6.
- [ ] **T9.3** — **Streams nuevas del paso 2 en `rafaq.yaml`** (las escribe el implementer tras T9.2 PASS): `ev_weight_events`, `ev_reproductive_events`, `ev_sanitary_events`, `ev_condition_score_events`, `ev_lab_samples`, `est_animal_category_history`, `ev_birth_calves` (`id` sintético), `est_rodeo_data_config` (`id` sintético) — todas `SELECT ... WHERE establishment_id IN org_scope [AND deleted_at IS NULL]`, **JOIN-free, idénticas al paso 1**. Mover estas de la sección "DIFERIDAS A PASO 2" (comentario al final de `rafaq.yaml`) a streams activas. **NO reintroducir JOINs** (PSYNC_S2305). Re-Gate 1 + validador del dashboard + connect (bucket math design §2.4 ≈ 43 buckets para 2 campos << 1000). Cubre: R13.5.
  - *(Run 6 — sub-parte AppSchema, hecha)*: `services/powersync/schema.ts` (`AppSchema`) refleja las **columnas denormalizadas** que estas streams emiten, para que PowerSync las MATERIALICE en el SQLite local (sin esto, las columnas sincronizadas no se guardan y las lecturas que las usan devuelven `undefined`): `establishment_id` (`column.text`) en las 8 tablas hijas (en `birth_calves`/`rodeo_data_config` como columna normal, SIN `id` propio — lo porta el alias §PK); `animal_tag_electronic`/`animal_sex`/`animal_birth_date` (`column.text`) en `animal_profiles` (b1); `member_name` (`column.text`) en `user_roles` (c2). `schema.test.ts` extendido (3 tests del paso 2). El YAML de streams + el deploy + el re-Gate 1 quedan para el leader/Raf (esta task sigue abierta hasta eso). Ver design §3 (banner as-built `AppSchema` paso 2).
- [x] **T9.4** *(depende de T9.0 → (b1))* — ⚠️ **Migración(es) de identidad de `animals` (b1) — implementer, ≥ `0077`, gateada.** Columnas `animal_tag_electronic`/`animal_sex`/`animal_birth_date` en `animal_profiles` + trigger force en el INSERT del perfil (desde `animals`) + trigger de propagación `AFTER UPDATE OF tag_electronic, sex, birth_date ON animals` + backfill. **NO** se sincroniza `animals` (NO se agrega `est_animals`). Gate 1 sobre el delta. Cubre: R4.7, R13.7. *(Run 5: SQL en `0079_denormalize_animal_identity_on_profiles.sql` — force `BEFORE INSERT OR UPDATE OF` las 3 columnas (reconciliado: también en UPDATE, anti-spoof) + propagación `AFTER UPDATE OF tag_electronic, sex, birth_date ON animals` + backfill. Columnas nullables (identidad puede ser NULL). NO aplicada al remoto.)*
- [ ] **T9.5** — **Swap de lectura T4 alineado a (b1)**: `animals.ts` (`fetchAnimals`/`searchAnimals`/`fetchAnimalDetail`/`countAnimals`/`findOrCreateLookup`) lee identidad (`tag_electronic`/`sex`/`birth_date`) desde las columnas denormalizadas de `animal_profiles`, NO desde un JOIN a `animals`; la búsqueda exacta por TAG y el filtro `noTag` pasan a `animal_profiles.animal_tag_electronic`. (Esto reemplaza/ajusta T4.1.) Cubre: R5.1, R13.7.
- [x] **T9.6** *(Raf eligió (c2), NO (c1))* — **Nombres de coworkers OFFLINE vía `user_roles.member_name` (c2)**: denormalizar `name` sobre `user_roles` (migración `0080`: columna `member_name` + trigger force `BEFORE INSERT OR UPDATE OF member_name` desde `users.name` + propagación `AFTER UPDATE OF name ON users` + backfill) y leer local. `local-reads.buildMembersQuery`/`buildOwnNameQuery` pasan a `user_roles.member_name` (eliminado el JOIN/lectura de `users`, que el paso 2 NO sincroniza). `members.loadMembers`/`profile`/`establishments.loadFullProfile` no cambian (consumen los builders; el shape `role/name` se mantiene — R11.1). PII (email/phone) sigue en `user_private` self-only. NO sincronizar `users`. *(Run 5: `0080_denormalize_member_name_on_user_roles.sql` (NO aplicada al remoto) + ajuste de `local-reads.ts` (`buildMembersQuery`/`buildOwnNameQuery`) + sus unit tests. La task estaba redactada para (c1); el as-built es (c2) por decisión de Raf — ver R13.8 reconciliado.)* Cubre: R13.8.
- [ ] **T9.7** — **Tests de no-bypass por device del paso 2** (extiende T7.2): por cada tabla hija denormalizada, usuario A NO recibe eventos/terneros/config del campo de B; la identidad denormalizada de `animal_profiles` no cruza tenant; `animals` NO está en el sync set de nadie. Cubre: R13.5, R13.6, R9.2.

## Dependencias y orden

- **T1 → T2 → T3..T7** en web (T2 gateado por Gate 1; deploy T2.3 tras Puerta 1).
- **T3, T4 (lectura)** pueden ir antes que **T5 (escritura)**.
- **T6.2** (outbox + overlay + RPC-mapping) es **primaria** (opción ii de Puerta 1); depende de **T6.4** (delta de backend de idempotencia) **solo** para `register_birth`. `exit_animal_profile` (idempotente natural), `create_animal` (ids de cliente) y los `soft_delete_*` (guarda `deleted_at IS NULL`) NO dependen de T6.4. **T6.4 está GATEADA por Gate 1 (spec) sobre el delta + aprobación del leader** antes de aplicar la migración (≥ `0075`).
- **T6.5** (no doble-upload) depende de T6.2a/b (overlay `localOnly`).
- **T8** depende del dev build (no existe hoy) → diferido; el cierre de la spec se valida en web.
- **T9 (paso 2)** depende del paso 1 (V3 JOIN-FREE) ya en vivo. **T9.1 → T9.2 (Gate 1) → T9.3** (denormalización (A) → Gate 1 → streams). **T9.4/T9.6 dependen de T9.0** (decisiones (B)/(C) de Raf). **T9.5** (swap T4 a (b1)) depende de T9.4 y reemplaza T4.1. Cada migración del paso 2 es schema-sensitive → Gate 1 por delta antes de aplicar (mismo régimen que T6.4/T6.6).

## Resueltas en la Puerta 1 (2026-06-08)

1. **Estrategia de las (b) RPC-bound** — RESUELTA: **opción ii** (offline vía outbox + overlay local-only + RPC-mapping, T6.2). `import_rodeo_bulk` queda online (T6.1). La idempotencia (T6.2d) es el punto crítico.
4. **Roles offline** — RESUELTA: **owner-only** (ya aplicado en `est_members` por MED-1; T2.1). Sin cambio adicional.
5. **Delta de backend de idempotencia** — RESUELTA: **delta ADITIVO mínimo, solo `register_birth`** (T6.4), aprobado en Puerta 1. `exit_animal_profile`/`create_animal`/`soft_delete_*` son dedup-natural (sin delta). Schema-sensitive → Gate 1 (spec) antes de implementar (R11.4).
6. **Hueco DOUBLE-UPLOAD** — RESUELTO: **overlay LOCAL-ONLY (opción b)** (T6.2a/b, T6.5). El efecto optimista vive en tablas `localOnly` (no generan CrudEntry); la única CrudEntry de una op (b) es su `op_intent`; lecturas UNION; clear en ACK; rollback del overlay en rechazo. Evita el doble-apply y la colisión de ids server-side de `register_birth`.

## Decisiones abiertas (no bloqueantes para la aprobación de la spec)

2. Mecanismo de PK para `user_private` (PK `user_id`), `rodeo_data_config` y `birth_calves` (PK compuesta → `id` sintético) — decisión de sintaxis del SDK, se confirma en T1.3.
