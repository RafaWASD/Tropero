# Tasks — 15-powersync

> Checklist ejecutable. Cada tarea cubre `R<n>` de `requirements.md`. El implementer marca `[x]`.
> **Gate 1 (`security_analyzer` modo `spec`) es OBLIGATORIO sobre `sync-streams/rafaq.yaml` (T2) antes de cualquier deploy a la instancia y antes de la Puerta 1 humana.**
> Marcado de build target: `[web]` = buildable/verificable hoy en navegador; `[device]` = diferido al dev build Android.
> **Puerta 1 (2026-06-08, opción ii)**: el **alta de animal, parto, baja y soft-deletes** van OFFLINE vía outbox + RPC-mapping (R6.6, R6.8–R6.11; T6.2). Solo **`import_rodeo_bulk`** queda ONLINE (excepción: onboarding masivo; T6.1).

## Fase T1 — Cimientos del cliente (deps + env + connector + AppSchema + provider)

- [x] **T1.1** — Agregar deps: `@powersync/react-native@1.35.3`, `@powersync/web@1.38.2`, `@powersync/react@1.10.0` + el open-factory WASM para web `@journeyapps/wa-sqlite@1.7.0` (whitelisteado en pnpm `onlyBuiltDependencies` por su postinstall que baja el PowerSync core WASM — ADR-011). Cubre: R2.2. *(Run 1)*
- [x] **T1.2** — `app/src/utils/env.ts` (+ `env-resolve.ts` puro testeable): agregar `powersyncUrl` desde `EXPO_PUBLIC_POWERSYNC_URL`, con error accionable en español si falta. `.env.example` creado en la raíz con el placeholder. Cubre: R1.2, R1.3. *(Run 1)*
- [x] **T1.3** — `services/powersync/schema.ts`: `AppSchema` espejando las 26 tablas sincronizadas (§3 del design) + `op_intents` (insertOnly) + 5 overlay `pending_*` (localOnly). PK resuelta (T1.3): `id` por **alias en la stream** (`user_private`→`user_id AS id`; `rodeo_data_config`/`birth_calves`→`id` sintético `(a||':'||b)`); el SDK agrega el `id` implícito y prohíbe declararlo. Cubre: R2.1, decisión-abierta-PK. *(Run 1)*
- [x] **T1.4** — `services/powersync/database.ts` (+ `platform-select.ts` puro): factory por plataforma vía `require()` guardado por `Platform.OS` (`@powersync/web` WASM en web / `@powersync/react-native` en device) + instancia singleton lazy de `PowerSyncDatabase`. Cubre: R2.2. *(Run 1)*
- [x] **T1.5** — `services/powersync/connector.ts` (+ `upload-classify.ts` puro): `SupabaseConnector.fetchCredentials()` (`{ endpoint, token }` desde la sesión; **null si no hay sesión** — contrato del SDK, reconciliado vs design §4). Cubre: R3.1, R3.2. *(Run 1)*
- [ ] **T1.6** — `services/powersync/upload.ts` + `connector.uploadData()`: drenar la upload queue → upsert/update contra Supabase; transitorio → reintento (deja en cola), permanente → descarta + superficia. Cubre: R3.3, R3.4, R3.5, R8.1, R6.2. *(Run 1: la BASE de CRUD plano + clasificación transitorio/permanente + superficiado ya viven en `connector.uploadData()`; el `op_intents`→RPC/overlay/idempotencia queda STUB `// TODO Run T6`. `upload.ts`/`outbox.ts` son Run T6.)*
- [x] **T1.7** — `services/powersync/provider.tsx` + montaje en `app/app/_layout.tsx` (dentro de `AuthProvider`, envolviendo los providers de datos): `<PowerSyncProvider>` por encima del árbol; `connect(connector)` con sesión válida (authenticated+verified), `disconnect()` en logout. Cubre: R2.3. *(Run 1)*
- [x] **T1.8** — `services/powersync/status.ts` (+ `status-derive.ts` puro): estado de conexión/sync (conectado/sincronizando/pendientes en cola) consultable por la UI + copy es-AR. Cubre: R10.1, R10.2. *(Run 1)*

## Fase T2 — Sync streams (deploy GATEADO por Gate 1)

- [x] **T2.1** — `sync-streams/rafaq.yaml`: 26 streams completas (§2 del design) — globales, self-only (`user_private`, `user_roles`), per-establishment, eventos (vía perfil), `birth_calves`/`rodeo_data_config` (PK compuesta con `id` sintético aliased). Cada query scopea por rol activo / self-only / global y filtra `deleted_at IS NULL`. **NO deployado** (lo hace el leader/Raf, gateado). Cubre: R4.1–R4.11. *(Run 1)* **⚠️ REESCRITO 2026-06-08 al patrón `with:`/INNER JOIN** (CTE `org_scope`/`owner_scope` + INNER JOIN al padre, en vez de subquery anidada) por el límite de buckets de PowerSync (`[PSYNC_S2305]` too many buckets / regresión #611 — design §2.1-superseded, §2.2). **Frontera de autorización IDÉNTICA** (design §2.3-equivalencia); solo cambió la estructura SQL. Quedan 5 incertidumbres de sintaxis fina (design §2.2-incertidumbres) que confirma el validador del dashboard en el re-Gate 1 / deploy.
- [ ] **T2.2** — ⛔ **Gate 1 (re-Gate 1 tras la reescritura `with:`/JOIN)**: `security_analyzer` modo `spec` audita `rafaq.yaml`. Verifica equivalencia stream↔RLS, scoping por rol activo, `user_private` self-only, no-leak por WAL, `deleted_at` filtrado. **Foco del re-Gate 1: que el patrón `with:`/JOIN preservó EXACTAMENTE el set de filas del patrón anterior (no agrandó ni achicó la frontera)** — el cambio es estructural, no de autorización. **NO avanzar sin PASS.** Output: `progress/security_spec_15-powersync.md`.
- [ ] **T2.3** — Deploy de `rafaq.yaml` a la instancia `rafaq-beta` (SOLO tras T2.2 PASS + Puerta 1 humana). Al pegar el YAML en el dashboard: (a) el **validador** confirma la sintaxis fina (las 5 incertidumbres de design §2.2-incertidumbres — `with:` mapa, `queries:`, `(expr) AS id, t.*`, 2–3 INNER JOINs, short-hand `IN org_scope`); ajustar al fallback documentado si el validador rechaza alguna. (b) un **connect de prueba** confirma que el conteo de buckets quedó debajo de 1000 (idealmente con la beta limpia de la data de test — la contaminación de 106 establecimientos es un problema aparte). Cubre: R1.1 (consume el provisioning).

## Fase T3 — Swap de lectura: catálogos globales + contexto de establecimiento `[web]`

- [x] **T3.1** — `rodeo-config.ts` (`fetchFieldCatalog`/`fetchSystemDefaults`/`fetchRodeoConfig`) + `animals.fetchSystemCategories` + `rodeos.fetchProductionSystems`: leer catálogos (`categories_by_system`, `field_definitions`, `system_default_fields`, `rodeo_data_config`, `species`/`systems_by_species`) desde SQLite local vía `getPowerSync().getAll()` (one-shot; reactividad watchable diferida a un run posterior — decisión del leader). Degradación avisada si el catálogo aún no sincronizó (`kind:'network'` "Sincronizando…"). Las ESCRITURAS de rodeo-config (`toggleRodeoField`/`enableNonDefaultField`) quedan ONLINE sin tocar. Cubre: R4.4, R5.1, R5.4. *(Run 3)*
- [x] **T3.2** — `establishments.ts` (`loadMemberships`/`loadOwnProfile`/`loadFullProfile`/`loadEstablishmentDetail`/`countActiveMembers`) + `members.ts` (`loadMembers`/`countTeam`/`loadPendingInvitations`) + `profile.ts` (`loadProfileNamePhone`): lectura del contexto de establecimiento, miembros (perfil público de coworkers desde `users`+`user_roles`) y perfil self (`user_private`) desde SQLite local. Mutaciones admin/EF ONLINE sin tocar. Cubre: R5.1, R5.3, R7.2. *(Run 3)*
- [x] **T3.3** — `rodeos.ts`: `fetchRodeos` desde SQLite local. `softDeleteRodeo` → OUTBOX/RPC (HECHO en T6). **`createRodeo` OFFLINE vía RPC `create_rodeo` (0081) + outbox/overlay (HECHO en T9.8 — un-defer, Raf: offline-first sin excepciones)**: ver T9.8. Eliminado el helper `fetchRodeosOnline` (ya no hace falta: `buildRodeosQuery` UNIONa `pending_rodeos`). Cubre: R5.1. *(Run 3 + reconciliación T6 + un-defer T9.8)*

## Fase T4 — Swap de lectura: camino de datos `[web]`

- [x] **T4.1** — `animals.ts`: `fetchAnimals`, `searchAnimals` (fuzzy → `LIKE` local; exacto TAG/IDV igual), `countAnimals`, `fetchAnimalDetail`, `findOrCreateLookup` → SQL local con JOINs SQLite. Cubre: R5.1, R5.2, R5.3. *(Run 6 — alineado a (b1)/T9.5: la identidad —tag/sex/birth_date— se lee de las columnas denormalizadas de `animal_profiles`, NO de `animals`; el `noTag` y el TAG exacto/substring pasan a `animal_profiles.animal_tag_electronic`. `findOrCreateLookup` queda local por delegar en `searchAnimals`.)*
- [x] **T4.2** — `events.ts`: `fetchTimeline` (reconstruir el UNION ALL de los 7 orígenes como SQL local, fiel a `animal_timeline` 0069; payload via `json_object` → `JSON.parse` en el service; las 2 queries suplementarias —nombres de categoría + service_type— pasan a queries locales) + `fetchMother` (JOIN local `birth_calves`→`reproductive_events`→`animal_profiles`; tag de la madre desde `animal_profiles` b1). Cubre: R5.1, R5.3. *(Run 6)*
- [x] **T4.3** — `management-groups.ts`: `fetchManagementGroups` → SQL local; `fetchGroupMembers` lee local automáticamente (delega en `fetchAnimals`, ya swapeado). Cubre: R5.1. *(Run 6)*

## Fase T5 — Swap de escritura offline: eventos simples + asignación/CRUD de lote `[web]`

- [x] **T5.1** — `events.ts`: `addWeight`, `addConditionScore`, `addTacto`, `addService`, `addAbortion`, `addObservation` → INSERT local (`getPowerSync().execute(...)` vía `runLocalWrite`) + upload queue; `id` cliente; **eliminado** el split-insert (ya eran `.insert()` sin `.select()`; ahora es write local). Cubre: R6.1, R6.3, R6.4. *(Run 8. SQL builders puros en `local-reads.ts`. `establishment_id` de los eventos OMITIDO en el INSERT local — lo FUERZA el trigger 0077 al subir, anti-spoof, queda NULL local y el timeline T4 filtra por `animal_profile_id`; EXCEPCIÓN `animal_events`: trigger de VALIDACIÓN 0034 → SÍ se setea, derivado del perfil. created_by/author_id/source/edit_window_until por trigger/default al subir → no se mandan. Las 6 son INSERT plano single-tabla, NINGUNA RPC-bound. `registerBirth` queda RPC-bound = T6.)*
- [x] **T5.2** — `management-groups.ts`: `assignAnimalToGroup` (UPDATE local), `createManagementGroup` (INSERT local, devuelve el lote con `id` cliente sin re-leer), `renameManagementGroup` (UPDATE local) → local + upload; **eliminados** el `diff before/after` de create y el `count:'exact'` de assign/rename (ya no aplica el gotcha RLS-on-RETURNING; autorización owner-only validada al SUBIR por RLS, R8.1). `softDeleteManagementGroup` NO se tocó (RPC-bound = T6). Cubre: R6.1, R6.3. *(Run 8.)*
- [~] **T5.3** — `sessions`/`maneuver_presets`: **N/A en este run** — NO existe service cliente de sessions/maneuver_presets (el frontend de spec 03 está diferido; solo el `AppSchema` los declara). No hay create/update plano que swapear → nada que hacer hasta que el frontend de spec 03 los consuma. Documentado, no se inventó. Cubre: R6.1 (sin alcance hoy).

## Fase T6 — Mutaciones RPC-bound OFFLINE vía outbox + RPC (Puerta 1, opción ii)

- [x] **T6.1** — Mantener ONLINE **solo `import-rodeo.import_rodeo_bulk`** (excepción Puerta 1: onboarding masivo, no manga). Sin red → `kind:'network'` accionable, sin marcar como hecho. Cubre: R6.5, R6.6. *(Run T6: `import-rodeo.ts` SIN tocar — verificado.)*
- [x] **T6.2** *(PRIMARIA/REQUERIDA — opción ii de Puerta 1)* — Implementar el camino OFFLINE de las (b) RPC-bound de camino de campo: *(Run T6 — hecho; ver impl_15 §Run T6 + reconciliaciones de design §1.2/§5.3.3/§5.4.2.)*
  - **T6.2a** — `schema.ts`: declarar la tabla outbox `op_intents` (`insertOnly`, §5.3.2) con `id` = `client_op_id` de cliente **+ las tablas OVERLAY optimista `pending_*` (`localOnly: true`, §5.3.3) que NO generan CrudEntry** (`pending_animals`, `pending_animal_profiles`, `pending_reproductive_events`, `pending_birth_calves`, `pending_status_overrides`), cada una con `client_op_id`. Cubre: R6.8, R6.11, R6.12. *(Run T6: `pending_animal_profiles` completado al shape de lectura b1+detalle; `pending_reproductive_events` + `created_at`. GUARD en schema.test.)*
  - **T6.2b** — `outbox.ts`: helper que, en una **sola `writeTransaction` local**, encola la intención (`op_type` + `params_json` + `client_op_id`) **e** escribe el **efecto optimista en el overlay `localOnly`** (`pending_*`) — NO en las tablas sincronizadas (§5.3.3). Cubre: R6.8, R6.11, R6.12. *(Run T6: `enqueueCreateAnimal`/`enqueueRegisterBirth`/`enqueueExitAnimal`/`enqueueSoftDelete` + `clearOverlay`/`rollbackOverlay`.)*
  - **T6.2c** — `upload.ts`/`connector.uploadData()`: en el drenado, mapear cada CrudEntry de `op_intents` a `supabase.rpc(...)` (§5.4.2), en orden de cola; pasar `p_client_op_id` **solo a `register_birth`** (las demás firmas no lo tienen). Cubre: R6.9. *(Run T6: `mapIntentToRpc` (PURO) + `applyIntentTransaction` en connector. ⚠️ Run create-animal-rpc 2026-06-10: la variante de Run T6 "`create_animal` → 2 upserts idempotentes, NO RPC" quedó SUPERSEDIDA — perdía el alta bajo reintento (42501 contra la policy UPDATE de `animals`; backlog 2026-06-10 REABIERTO). As-built: `create_animal` TAMBIÉN mapea a RPC — la atómica `0083` — y la rama de 2 upserts se ELIMINÓ del connector; `mapIntentToRpc` traduce el shape histórico del intent `{animals, animal_profiles}` a los args `p_*` (compat con los intents ya encolados en devices). Reconciliado §5.4.2.)*
  - **T6.2d** — Idempotencia (§5.4.3): dedup natural para `create_animal` (ids de cliente + ON CONFLICT), `soft_delete_*` (guarda `WHERE deleted_at IS NULL`) y **`exit_animal_profile` (transición de status idempotente — NO requiere delta, confirmado contra `0044`)**; para **`register_birth`** usar el `p_client_op_id` contra la columna/índice del delta (requiere T6.4). Cubre: R6.10. *(Run T6: `classifyIntentUploadError` cubre los 4 modos + idempotent_discard de P0002/23505-MED-1. Run create-animal-rpc: la dedup natural de `create_animal` se materializa SERVER-SIDE en la RPC `0083` (ambos `ON CONFLICT (id) DO NOTHING` en UNA tx; el replay es no-op 2xx — sin rama `idempotent_discard`; los 23505 de dominio tag/idv SALEN como permanent_reject) + healing del half-state del camino viejo. Tests: `supabase/tests/animal/run.cjs` :: suite `create_animal RPC` (ROJOS hasta que el leader aplique `0083`) + `upload.test.ts`.)*
  - **T6.2e** — ACK / rollback del overlay (§5.4.4): éxito → `tx.complete()` + `clearOverlay(client_op_id)` (las filas reales bajan por la stream; el UNION deja de mostrar el overlay); transitorio → reintento sin tocar el overlay; permanente → `rollbackOverlay(tx)` (borra el overlay `pending_*` por `client_op_id`) + descarte + superficia; **`P0002` (`not found`) de un `soft_delete_*` ya aplicado → éxito idempotente: descartar sin rollback + limpiar overlay** (NO restaurar la fila, que sí está borrada). Cubre: R6.11, R8.1, R10.2. *(Run T6 + el 23505-MED-1 de register_birth → idempotent_discard; + `PermanentIntentError` para intent corrupto, anti-loop.)*
  - **T6.2f** — Swap de los 4 services a outbox + overlay: `animals.createAnimal` (intención `create_animal` + overlay `pending_animals`/`pending_animal_profiles`), `events.registerBirth` (intención `register_birth` + overlay terneros/parto), `animals.exitAnimalProfile` (intención `exit_animal_profile` + overlay `pending_status_overrides` effect=`exited`), `soft_delete_*` de management-groups/rodeos (intención `soft_delete_<entity>` + overlay `pending_status_overrides` effect=`soft_deleted`). **Las lecturas afectadas (ficha/detalle, timeline, lista, miembros de lote) hacen UNION synced + overlay** (R6.11). Preservar las firmas públicas (R11.1). Cubre: R6.6, R6.8, R6.11. *(Run T6 — RECONCILIADO: `exitAnimalProfile` vive en `animals.ts`; `softDeleteRodeo` pasó de UPDATE directo al RPC `soft_delete_rodeo`; los eventos tipados NO tienen UI de borrado → sin swap. design §1.2.)*
- [ ] **T6.3** — Identidad/admin: confirmar que `establishments.create`, `members.invite`, edición de perfil y aceptación de invitación (EF) **siguen online** (sin tocar). Cubre: R7.1.
- [x] **T6.4** — ⚠️ **Delta de backend de idempotencia (APROBADO en Puerta 1, ADITIVO, schema-sensitive) — la migración la escribe/aplica el IMPLEMENTER (≥ `0075`) por Management API, GATEADA por el leader + Gate 1 (spec). El spec_author NO escribe migraciones.** *(SQL escrito en `supabase/migrations/0075_register_birth_idempotency.sql` — Run 2. La APLICA el leader por Management API tras gatear el SQL; NO aplicada al remoto desde el implementer.)* Alcance MÍNIMO (solo `register_birth`):
  - columna **nullable** `client_op_id uuid` en `reproductive_events`;
  - índice **UNIQUE parcial COMPUESTO** `... (animal_profile_id, client_op_id) WHERE client_op_id IS NOT NULL` (compuesto, **NO global**: cierra el oráculo de existencia cross-tenant — fix HIGH-D1, design §9-nota);
  - param `p_client_op_id uuid default null` en `register_birth` + **guard de idempotencia con SCOPING de tenancy + error genérico ante colisión ajena (fix HIGH-D1).** ⛔ **NO escribir el guard "literal" de versiones previas (lookup global `SELECT id WHERE client_op_id = p_client_op_id → RETURN`): saldría VULNERABLE a IDOR cross-tenant** (un atacante con un `client_op_id` colisionado recibiría el `id` de un parto de OTRO establecimiento por el canal RPC). El cuerpo correcto (ver design §5.4.3(1), R6.10):
    1. la authz as-built rige PRIMERO (`has_role_in(v_est)` sobre la fila real de la madre, `0045:213-225`) — sin cambios;
    2. el lookup del `client_op_id` está **scopeado al caller**: `WHERE re.client_op_id = p_client_op_id AND re.animal_profile_id = p_mother_profile_id AND animal_profiles.establishment_id = v_est AND re.deleted_at IS NULL` (JOIN a `animal_profiles` porque `reproductive_events` NO tiene `establishment_id` denormalizado, `0026`); si matchea → devolver ese `id` y RETURN (no-op legítimo del propio caller);
    3. si NO matchea (no existe para este caller, o colisión con un parto AJENO) → camino de creación → al INSERTAR el `client_op_id` choca contra el índice UNIQUE → **error genérico** (`unique_violation`/`23505`), **NUNCA** devolver el `id`/datos ajenos ni un oráculo de existencia.
  - **NO** se agrega delta a `exit_animal_profile` (idempotente natural, confirmado contra `0044`), ni a `create_animal`/`soft_delete_*` (dedup natural). Path online (`p_client_op_id` NULL) idéntico al as-built; índice parcial → no afecta partos históricos. **⛔ Gate 1 (spec) sobre el delta ANTES de aplicarlo** (R11.4) — debe verificar el scoping de tenancy del guard. Cubre: R6.10 (server-side), R11.3, R11.4.
- [x] **T6.6** — ⚠️ **2do delta de backend ADITIVO (la "Opción 2" de design §9, schema-sensitive) — trigger que mantiene `user_roles.active` coherente con el soft-delete de establecimientos.** La migración la escribe el IMPLEMENTER (≥ `0076`) y la APLICA el leader por Management API tras gatearla (Gate 1 spec + Gate 2 + reviewer); el implementer NO la aplica. *(SQL escrito en `supabase/migrations/0076_deactivate_roles_on_establishment_soft_delete.sql` — Run 4.)* Alcance:
  - función + trigger `AFTER UPDATE OF deleted_at ON establishments`: en la transición `NULL → NOT NULL`, `UPDATE user_roles SET active = false, deactivated_at = now() WHERE establishment_id = NEW.id AND active = true`;
  - **NO reactiva** en `NOT NULL → NULL` (restore): no se puede distinguir desactivado-por-borrado de desactivado-por-remoción-de-miembro (documentado como limitación deliberada; no hay flujo de restore en MVP);
  - **backfill** de los roles ya-activos que apuntan a campos ya-borrados (incluye los 3 espurios del user de prueba);
  - `security definer` + `set search_path = public` (precedente: `handle_new_establishment` 0011). **Verificado: NO hay otro trigger sobre `user_roles`** → cero loop / cero side-effect destructivo. **Aditivo**: NO cambia `has_role_in`/`is_owner_of` (ya filtraban `deleted_at`) → sin cambio observable de la app/RLS; sólo hace que `active` sea un proxy FIEL de "campo vivo" para el modelo de sync JOIN-free (org_scope). ⛔ Gate 1 (spec) sobre el delta ANTES de aplicarlo (R11.4 reconciliada). Cubre: R4.1, R4.2 (refuerza el cierre de HIGH-1), R11.3, R11.4.
  - **Test** en `supabase/tests/rls/run.cjs` (campo + usuario dedicados, autocontenido): soft-delete → ambos `user_roles` `active = false` + `deactivated_at` poblado → `has_role_in` sigue false. **FALLA hasta que el leader aplique `0076`** (esperado; mismo patrón que la suite `spec 15-powersync` del animal runner con `0075`).
  - **GUARD — 2da mitad del invariante (Run 4.1, Gate 1 HIGH-1 reabierto).** El trigger de deactivate cubre los roles EXISTENTES al borrar el campo, pero NO impide CREAR/activar un rol NUEVO para un campo YA borrado (vector verificado: `supabase/functions/accept_invitation/index.ts:93-101` inserta `active:true` sin chequear `establishments.deleted_at` → owner invita → owner borra el campo → el invitado acepta el link pendiente → rol activo sobre campo muerto → el sync JOIN-free le replicaría la data del campo borrado). **INVARIANTE DECLARADO: `user_roles.active = true` ⇒ el establecimiento está vivo (`deleted_at IS NULL`)**, enforced a NIVEL DB por las DOS mitades de `0076`: (a) `deactivate_roles_on_establishment_soft_delete` (deactivate-on-delete) + (b) `prevent_active_role_on_soft_deleted_establishment` (block-activate-for-deleted: trigger `BEFORE INSERT OR UPDATE OF active ON user_roles` que rechaza con `errcode 23514` cualquier INSERT/UPDATE que deje `active = true` apuntando a un campo `deleted_at IS NOT NULL`; `active = false` SIEMPRE permitido → no rompe el deactivate ni la remoción de miembros). **Compat verificada** (todos siguen pasando): creación de campo (`handle_new_establishment` 0011, campo nuevo deleted_at NULL), aceptar invitación a un campo VIVO, el deactivate (active=false) + backfill, y el fix R8.3/R8.4 (restore `deleted_at=null` ANTES de reactivar `active=true`). **Test** en `run.cjs` (`estG`/`userG`/`userH` dedicados, autocontenido): INSERT/UPDATE `active=true` sobre campo borrado → FALLA (guard 23514); sobre campo vivo y cualquier `active=false` → PASA. **FALLA hasta que el leader aplique `0076`** (esperado, mismo patrón que el test de deactivate). Cubre: R4.1, R4.2 (cierra HIGH-1 completo: roles existentes Y nuevos).

- [x] **T6.5** — **No doble-upload (R6.12)**: verificar que el efecto optimista vive SOLO en el overlay `localOnly` y que NO se escribe en tablas sincronizadas en el camino (b); que `uploadData()` NO emite un INSERT/UPDATE plano paralelo de las filas que la RPC va a (re)crear. Test: encolar `register_birth` offline → exactamente UNA CrudEntry (`op_intents`) en la upload queue → la RPC corre una sola vez. Cubre: R6.12. *(Run T6: garantía ARQUITECTÓNICA — el overlay es `localOnly` (0 CrudEntry, verificado en schema.test) + el `op_intent` `insertOnly` (1 CrudEntry); el `enqueue` escribe ambos en 1 writeTransaction. El cierre E2E in-vivo "1 CrudEntry server-side" es T7.5/T7.9, run aparte.)*

## Fase T7 — Tests

- [x] **T7.1** — Unit de mappers/connector: `uploadData` traduce CrudEntry → upsert/update; transitorio reintenta, permanente descarta + superficia. Cubre: R3.3, R3.4, R3.5, R8.1. *(Cubierto por runs previos, confirmado en disco en el Run cierre-T7: `app/src/services/powersync/upload-classify.test.ts` (R3.4 transient / R3.5 permanent) + `upload.test.ts` (`mapIntentToRpc` CrudEntry→RPC + `classifyIntentUploadError` transient/idempotent_discard/permanent_reject, Run T6 + create-animal-rpc).)*
- [x] **T7.2** — Integración Node de **no-bypass por device** (espejo de los runners RLS de `supabase/tests/rls/`): por cada clase de stream, usuario A NO recibe filas del establecimiento de B; `user_private` de B NO llega a A; catálogos globales SÍ llegan a todos; soft-deleted NO entra al sync set. Cubre: R4.1–R4.11, R9.1, R9.2, R8.2. *(Run cierre-T7: `supabase/tests/sync_streams/run.cjs` — suite top-level `spec 15-powersync — no-bypass por device (sync streams, T7.2 + T9.7)`, 25 subtests, AUTOCONTENIDA, enganchada en `scripts/run-tests.mjs`. Simula el predicado de cada stream (`establishment_id IN org_scope/owner_scope`) contra Postgres con el `user_id` de cada actor — design §7 — vía service_role. MUTATION-PROBADA: con una stream permisiva 11 subtests FALLAN con los asserts cross-tenant correctos. Incluye R8.2/HIGH-1: soft-delete de un campo → el owner deja de recibir su data por el sync set (trigger 0076).)*
- [x] **T7.3** — `[web]` E2E Playwright: con sync activo, alta→ficha→evento simple offline (simulado)→reconexión→fila en Supabase; lectura offline de lista/ficha/timeline. Cubre: R5.1, R5.2, R6.1, R6.2, R11.2. *(Run cierre-T7: el alta offline + reconexión + oráculo server-side de `animal_profiles` + lectura offline de lista/ficha/búsqueda ya estaban en `app/e2e/animals-offline.spec.ts` tests 1+2 (Run create-animal-rpc). AGREGADO el camino faltante de EVENTO SIMPLE: test 3 "agregar un PESO offline → reconexión → el weight_event aterriza en Supabase" — lectura offline de ficha+timeline + escritura de evento simple offline (T5.1 CRUD plano) + oráculo `waitForServerWeightEvent`. 3/3 verdes contra el remoto con 0083 aplicada.)*
- [x] **T7.4** — `[web]` Smoke de boot del DB local WASM: la app arranca, el DB local bootea sin crashear, una query local devuelve filas tras el primer sync. Cubre: R2.4. *(Validado EN VIVO por Raf en `pnpm web` (hotfix Run 1.2: boot WASM + first sync + 1 cliente en el dashboard); los 3 E2E de `animals-offline.spec.ts` ejercitan el boot del DB WASM + lecturas locales tras el primer sync de punta a punta. NO hay smoke unit puro — el factory WASM no es cargable bajo node:test.)*
- [x] **T7.5** — Verificar que la suite backend (RLS + Edge) sigue verde y que NO se tocaron RLS/policies/triggers/EF existentes. El **único** cambio de schema permitido es el delta aditivo de idempotencia de T6.4 (`reproductive_events.client_op_id` + UNIQUE parcial + param en `register_birth`), gateado por Gate 1; verificar que el path online de `register_birth` (`p_client_op_id` NULL) sigue idéntico y que ninguna otra migración cambió. Cubre: R11.3, R11.4. *(Run cierre-T7: `node scripts/check.mjs` exit 0 — RLS/Edge/Animal/Maneuvers/User_private/Import TODAS verdes con 0075-0083 aplicadas; el path online de `register_birth` (3 args) sigue verde (animal suite caso 3). Este run SOLO agregó tests + reconcilió docs — cero cambio de RLS/policies/triggers/EF/streams/migraciones, verificado por `git status`.)*
- [x] **T7.6** — Verificar que la suite unit/E2E existente del cliente sigue verde tras el swap (firmas de services intactas). Cubre: R11.1, R11.2. *(Run cierre-T7: `cd app && pnpm typecheck` verde (firmas `ServiceResult<T>` intactas, R11.1); client unit tests verdes en `check.mjs`; la suite E2E `animals-offline.spec.ts` 3/3 verde + resto de la suite E2E intacto.)*
- [~] **T7.7** — Unit/integración de **idempotencia (no doble-apply, R6.10)**: simular at-least-once (la RPC ejecuta server-side pero se pierde el ACK → PowerSync reintenta la misma CrudEntry de `op_intents`); verificar que el reintento NO crea un segundo parto / segunda baja / segundo animal / segundo soft-delete. *(Run 2: cubierto el sub-bullet `register_birth` — los 3 casos en `supabase/tests/animal/run.cjs` :: "T2.20 register_birth idempotencia por client_op_id (delta T6.4)". Los sub-bullets `exit_animal_profile`/`create_animal`/`soft_delete_*` (dedup natural, sin delta) se testean en el run de T6.2 con la outbox.)* Casos con su modo de dedup verificado:
  - `register_birth` → dedup por `client_op_id` (requiere el delta T6.4): el reintento con el mismo `client_op_id` **del mismo caller, misma madre** devuelve el parto existente, NO crea un 2do evento ni N terneros nuevos. (Si el delta aún no está aplicado, se testea el contrato esperado contra el guard de `p_client_op_id` y se marca pendiente de T6.4.) **Caso negativo cross-tenant (fix HIGH-D1, obligatorio):** un usuario A invoca `register_birth` con una madre PROPIA + un `p_client_op_id` que colisiona con un parto del usuario B (otro establecimiento) → la RPC **NO devuelve** el `id`/datos del parto de B (no IDOR), responde con un error genérico (`unique_violation`/`23505`), y NO se filtra existencia/propietario del parto ajeno. El parto de B queda intacto. *(Run 2: T2.20 casos 1, 2, 3.)*
  - `exit_animal_profile` → dedup **natural** (sin delta): el reintento re-aplica el UPDATE de status idéntico (mismo end-state); NO crea una 2da fila en `animal_category_history` (el UPDATE no toca `category_id`).
  - `create_animal` → dedup natural por id de cliente + ON CONFLICT. *(Run create-animal-rpc: cubierto server-side por la suite `create_animal RPC` de `supabase/tests/animal/run.cjs` — replay no-op, half-state healing (el bug), cross-tenant 42501, 23505 de tag/idv. **VERDES** — el leader aplicó `0083` al remoto, confirmado Run cierre-T7.)*
  - `soft_delete_*` → reintento levanta `P0002` (fila ya borrada) → tratado como éxito idempotente (NO segundo borrado, NO rollback). Cubre: R6.10. *(Cubierto a nivel de clasificación en `upload.test.ts` :: "IDEMPOTENCIA: P0002 … → idempotent_discard (sin rollback)".)*
- [x] **T7.8** — Unit/integración de **rollback del overlay ante rechazo permanente (R6.11)**: encolar una intención que la RPC rechaza permanente (RLS 42501 por `active_lost`, o error de dominio); verificar que el **overlay local-only** (`pending_*`) se **borra** (el ternero/alta desaparece del UNION; la baja/soft-delete se "des-oculta"), que NO quedó nada escrito en tablas sincronizadas, la intención se descarta de la cola (no loop) y el rechazo queda observable. Contrapruebas: (a) error transitorio → NO se borra el overlay, la intención queda en cola; (b) `P0002` de `soft_delete_*` ya aplicado → éxito idempotente: descarta sin rollback + limpia overlay. Cubre: R6.9, R6.11, R8.1, R10.2. *(Run cierre-T7: cubierto a NIVEL DE CLASIFICACIÓN/DECISIÓN en `app/src/services/powersync/upload.test.ts` — `classifyIntentUploadError`: 42501/23503/23514/23505-dominio → `permanent_reject` (→ el connector llama `rollbackOverlay`); red/5xx/429 → `transient` (NO toca el overlay, queda en cola); `P0002` de `soft_delete_*` → `idempotent_discard` (clear sin rollback); intent corrupto → `PermanentIntentError` (no loop). La ACCIÓN de `rollbackOverlay`/`clearOverlay` la ejecuta `connector.applyIntentTransaction` según esa clasificación.)* **CIERRE IN-VIVO (Run T7.9, 2026-06-10):** el rollback se ejercita E2E contra el SQLite/PowerSync REAL en `app/e2e/animals-offline.spec.ts` — test "PARTO offline + madre soft-deleteada server-side → reconexión → rollback del overlay": se rompe la precondición server-side (soft-delete de la MADRE vía admin mientras el cliente está offline, camino MÁS DETERMINISTA que un 42501 de RLS) → al reconectar `register_birth` levanta `23503` → `classifyIntentUploadError` → `permanent_reject` → `rollbackOverlay`. Verifica IN-VIVO: (a) el overlay se BORRA (el ternero desaparece de la lista — verificado: sin re-fetch forzado el assert FALLA con count=1, prueba de que muerde), (b) NADA quedó escrito server-side (oráculo `getServerBirthState` = 0 partos / 0 terneros), (c) el rechazo es OBSERVABLE (warn `upload rechazado` code 23503), (d) no-loop (tras un ciclo de retry el overlay sigue vacío). **CONTRAPRUEBA in-vivo:** test "PARTO offline + sigue sin red (transitorio)" — un transitorio NO borra el overlay (el ternero PERSISTE en la lista) ni superficia rechazo, y la intención EN COLA drena al reconectar (no se perdió). 2/2 VERDE.
- [x] **T7.9** — `[web]` E2E de **parto offline end-to-end** + rollback in-vivo del overlay: offline (simulado) → `registerBirth` escribe el parto + N terneros en el **overlay local-only** (visibles en la ficha de la madre vía UNION) y encola UNA intención `op_intents` → reconexión → drena la outbox → `register_birth` corre atómico server-side → en el ACK se limpia el overlay y las filas reales (ids server-side) bajan por la stream → los terneros aparecen en Supabase con **un solo** evento de parto (no duplicado) y la ficha NO muestra duplicados. Mismo patrón para `createAnimal` offline (alta find-or-create) y `exitAnimalProfile` offline (baja). Cubre: R6.6, R6.8, R6.9, R6.10, R6.11, R6.12. *(Run T7.9, 2026-06-10 — HECHO. SOLO tests + helpers e2e (CERO cambio de `app/src/`/migraciones/EF/streams). `app/e2e/animals-offline.spec.ts` (5 tests nuevos): **PARTO mono y mellizos** (overlay parto + N terneros en la ficha/lista offline → reconexión → oráculo server-side `waitForServerBirth`: EXACTAMENTE 1 evento `birth` + N `birth_calves`, NO duplicado; la ficha NO muestra parto duplicado tras el ACK) cubre R6.6/R6.8/R6.12/R6.10; **BAJA (Venta)** (overlay `pending_status_overrides` oculta de la lista activa offline → reconexión → oráculo `waitForServerExit`: status='sold' + exit_reason real) cubre R6.10; **ROLLBACK in-vivo** + **contraprueba transitorio** (ver T7.8) cubre R6.9/R6.11/R8.1/R10.2. El **alta offline** end-to-end ya estaba (tests 1+2, Run create-animal-rpc). Helpers nuevos en `app/e2e/helpers/admin.ts`: `waitForServerBirth`/`getServerBirthState`/`waitForServerExit`/`getServerProfileStatus`/`softDeleteProfile` (todos miran el SERVER vía service_role, no el overlay/UI). 8/8 verdes en `animals-offline.spec.ts` contra el remoto con 0075-0083 aplicadas. `birth_calves` es server-only (sin GRANT INSERT) → un ternero server-side SOLO existe si la RPC corrió: el oráculo no puede confundir overlay con fila real.)*

## Fase T8 — Native (diferido al dev build Android) `[device]`

- [ ] **T8.1** — `[device]` Boot del DB local con `@powersync/react-native` en el dev build; veredicto de persistencia + performance del sync set en hardware de campo. Cubre: R2.5, R5.2, R8.3.
- [ ] **T8.2** — `[device]` Verificar coexistencia BLE (bastón/balanza) + DB local + sync en device. (Diferido; no bloquea el cierre de la spec en web.)

## Fase T9 — Paso 2: denormalización de `establishment_id` (tablas hijas + entidades compartidas)

> **El as-built del paso 1 es V3 JOIN-FREE** (`sync-streams/rafaq.yaml`, validado en vivo + Gate-1-PASS). El paso 2 suma al sync las tablas que el paso 1 difirió porque NO tienen `establishment_id` propio. Patrón en ADR-026 + design §2.4. **Cada migración es schema-sensitive → Gate 1 (spec) ANTES de aplicar por Management API + Gate 2 + reviewer (R11.4/R13).** El spec_author NO escribe migraciones ni el YAML del paso 2 — los escribe el implementer tras Gate 1 + aprobación de (B)/(C).

- [ ] **T9.0** — ⏸ **Decisiones de Raf (el leader las lleva a la Puerta):** **(B) animals → (b1)** (denormalizar identidad sobre `animal_profiles`, NO sincronizar `animals`) y **(C) users/nombres → (c1)** (nombres ONLINE). Ambas cambian el modelo de lectura (ADR-026 §"Decisiones que Raf debe aprobar"). Recomendación del spec_author: (b1)/(c1). NO avanzar T9.4/T9.5 sin la decisión.
- [x] **T9.1** — ⚠️ **Migración(es) de denormalización (A) — las escribe el IMPLEMENTER (≥ `0077`) por Management API, GATEADAS por el leader + Gate 1 (spec).** Por cada tabla hija (`weight_events`, `reproductive_events`, `sanitary_events`, `condition_score_events`, `lab_samples`, `animal_category_history`, `birth_calves`, `rodeo_data_config`): `ADD COLUMN establishment_id uuid` (nullable, FK a establishments) → **backfill** desde el padre → **trigger BEFORE INSERT que FUERZA `establishment_id` desde el padre** (anti-spoof, espejo de `tg_force_created_by_auth_uid` `0043` / `tg_animal_events_validate_est` `0034`) [+ `BEFORE UPDATE` en `rodeo_data_config`] → `SET NOT NULL` al final. Cadenas de derivación: eventos/`animal_category_history` ← `animal_profiles.establishment_id`; `birth_calves` ← `reproductive_events → animal_profiles.establishment_id`; `rodeo_data_config` ← `rodeos.establishment_id` (design §2.4 (A)). **NO modificar la RLS as-built** (la columna es solo para el stream). Cubre: R13.1, R13.2, R13.3, R13.4, R13.6. *(Run 5: SQL escrito en `0077_denormalize_establishment_id_event_children.sql` (5 eventos + `animal_category_history`, función force compartida `tg_force_establishment_id_from_profile`) + `0078_denormalize_establishment_id_birth_calves_rodeo_config.sql` (`birth_calves` cadena parto→madre + `rodeo_data_config` vía rodeo, `BEFORE INSERT OR UPDATE`). **RECONCILIADO**: el force se aplicó en INSERT **y UPDATE** a las 5 tablas de evento (no solo `rodeo_data_config`) — vector de spoofeo por UPDATE de cliente con `GRANT UPDATE`; ver R13.3 reconciliado. NO aplicadas al remoto: las aplica el leader por Management API tras Gate 1.)*
- [ ] **T9.2** — ⛔ **Gate 1 (spec) sobre cada delta de (A)**: verifica trigger-force fiel al padre, backfill correcto/idempotente, stream equivalente (no más permisiva) a la RLS de la tabla, `security definer`/`search_path` donde aplique. **NO aplicar la migración antes de PASS.** Output: `progress/security_spec_15-powersync.md`. Cubre: R11.4, R13.6.
- [ ] **T9.3** — **Streams nuevas del paso 2 en `rafaq.yaml`** (las escribe el implementer tras T9.2 PASS): `ev_weight_events`, `ev_reproductive_events`, `ev_sanitary_events`, `ev_condition_score_events`, `ev_lab_samples`, `est_animal_category_history`, `ev_birth_calves` (`id` sintético), `est_rodeo_data_config` (`id` sintético) — todas `SELECT ... WHERE establishment_id IN org_scope [AND deleted_at IS NULL]`, **JOIN-free, idénticas al paso 1**. Mover estas de la sección "DIFERIDAS A PASO 2" (comentario al final de `rafaq.yaml`) a streams activas. **NO reintroducir JOINs** (PSYNC_S2305). Re-Gate 1 + validador del dashboard + connect (bucket math design §2.4 ≈ 43 buckets para 2 campos << 1000). Cubre: R13.5.
  - *(Run 6 — sub-parte AppSchema, hecha)*: `services/powersync/schema.ts` (`AppSchema`) refleja las **columnas denormalizadas** que estas streams emiten, para que PowerSync las MATERIALICE en el SQLite local (sin esto, las columnas sincronizadas no se guardan y las lecturas que las usan devuelven `undefined`): `establishment_id` (`column.text`) en las 8 tablas hijas (en `birth_calves`/`rodeo_data_config` como columna normal, SIN `id` propio — lo porta el alias §PK); `animal_tag_electronic`/`animal_sex`/`animal_birth_date` (`column.text`) en `animal_profiles` (b1); `member_name` (`column.text`) en `user_roles` (c2). `schema.test.ts` extendido (3 tests del paso 2). El YAML de streams + el deploy + el re-Gate 1 quedan para el leader/Raf (esta task sigue abierta hasta eso). Ver design §3 (banner as-built `AppSchema` paso 2).
- [x] **T9.4** *(depende de T9.0 → (b1))* — ⚠️ **Migración(es) de identidad de `animals` (b1) — implementer, ≥ `0077`, gateada.** Columnas `animal_tag_electronic`/`animal_sex`/`animal_birth_date` en `animal_profiles` + trigger force en el INSERT del perfil (desde `animals`) + trigger de propagación `AFTER UPDATE OF tag_electronic, sex, birth_date ON animals` + backfill. **NO** se sincroniza `animals` (NO se agrega `est_animals`). Gate 1 sobre el delta. Cubre: R4.7, R13.7. *(Run 5: SQL en `0079_denormalize_animal_identity_on_profiles.sql` — force `BEFORE INSERT OR UPDATE OF` las 3 columnas (reconciliado: también en UPDATE, anti-spoof) + propagación `AFTER UPDATE OF tag_electronic, sex, birth_date ON animals` + backfill. Columnas nullables (identidad puede ser NULL). NO aplicada al remoto.)*
- [x] **T9.5** — **Swap de lectura T4 alineado a (b1)**: `animals.ts` (`fetchAnimals`/`searchAnimals`/`fetchAnimalDetail`/`countAnimals`/`findOrCreateLookup`) lee identidad (`tag_electronic`/`sex`/`birth_date`) desde las columnas denormalizadas de `animal_profiles`, NO desde un JOIN a `animals`; la búsqueda exacta por TAG y el filtro `noTag` pasan a `animal_profiles.animal_tag_electronic`. (Esto reemplaza/ajusta T4.1.) Cubre: R5.1, R13.7. *(Run 6 — hecho como parte de T4.1: el swap de lectura YA se implementó alineado a (b1) desde el arranque, sobre el AppSchema as-built del paso 2; no hubo una versión intermedia con JOIN a `animals`.)*
- [x] **T9.6** *(Raf eligió (c2), NO (c1))* — **Nombres de coworkers OFFLINE vía `user_roles.member_name` (c2)**: denormalizar `name` sobre `user_roles` (migración `0080`: columna `member_name` + trigger force `BEFORE INSERT OR UPDATE OF member_name` desde `users.name` + propagación `AFTER UPDATE OF name ON users` + backfill) y leer local. `local-reads.buildMembersQuery`/`buildOwnNameQuery` pasan a `user_roles.member_name` (eliminado el JOIN/lectura de `users`, que el paso 2 NO sincroniza). `members.loadMembers`/`profile`/`establishments.loadFullProfile` no cambian (consumen los builders; el shape `role/name` se mantiene — R11.1). PII (email/phone) sigue en `user_private` self-only. NO sincronizar `users`. *(Run 5: `0080_denormalize_member_name_on_user_roles.sql` (NO aplicada al remoto) + ajuste de `local-reads.ts` (`buildMembersQuery`/`buildOwnNameQuery`) + sus unit tests. La task estaba redactada para (c1); el as-built es (c2) por decisión de Raf — ver R13.8 reconciliado.)* Cubre: R13.8.
- [x] **T9.7** — **Tests de no-bypass por device del paso 2** (extiende T7.2): por cada tabla hija denormalizada, usuario A NO recibe eventos/terneros/config del campo de B; la identidad denormalizada de `animal_profiles` no cruza tenant; `animals` NO está en el sync set de nadie. Cubre: R13.5, R13.6, R9.2. *(Run cierre-T7: implementado JUNTO con T7.2 en la MISMA suite `supabase/tests/sync_streams/run.cjs`. Subtests del paso 2: `ev_weight_events`/`ev_sanitary_events`/`ev_condition_score_events`/`ev_lab_samples`/`ev_reproductive_events` (A no recibe eventos de B, disjunción + establishment_id ∈ scopeA), `ev_birth_calves` (el ternero del parto de A no entra al sync set de B — cadena denormalizada 0078), `est_rodeo_data_config` (config del rodeo de A no cruza a B), `est_animal_category_history` (historial no cruza); `b1 (T9.7)`: la identidad denormalizada de `animal_profiles` es fiel y NO cruza tenant; `animals (T9.7)`: NO figura como `FROM` en ninguna stream y NO existe `est_animals`; `c2 (T9.7)`: `member_name` viaja en `user_roles` (owner-only) y `users` NO se sincroniza. El parto de prueba se crea vía `register_birth` (birth_calves es server-only).)*

- [x] **T9.8** — ⚠️ **`createRodeo` OFFLINE vía RPC `create_rodeo` (0081) + outbox/overlay — un-defer (Raf: offline-first sin excepciones).** Cierra el ÚLTIMO write que faltaba offline. NO es CRUD plano single-tabla como `createManagementGroup` (T5): crear rodeo arma una PLANTILLA (`rodeo_data_config`) que el trigger server-side `tg_rodeos_seed_data_config` (0018) seedea + el diff de toggles, y `rodeo_data_config` tiene PK COMPUESTA (read-only-local). → RPC atómica + outbox + overlay (patrón T6). *(Run T9.8 — la migración la escribe el IMPLEMENTER (`0081`); la APLICA el leader por Management API tras Gate 1 spec + reviewer. El implementer NO la aplica.)* Alcance:
  - **`supabase/migrations/0081_create_rodeo_rpc.sql`**: RPC `create_rodeo(p_id, p_establishment_id, p_name, p_species_id, p_system_id, p_toggles jsonb)` returns uuid, SECURITY DEFINER + `search_path=public`. **Authz PRIMERO** (`is_owner_of(p_establishment_id)`, owner-only, espeja `rodeos_insert` 0017, 42501). Valida species/system activos + pertenencia (23503/23514). **INSERT del rodeo con el id de cliente `ON CONFLICT (id) DO NOTHING`** → idempotencia natural (replay → el rodeo ya existe → no-op; el trigger de seed NO re-dispara → la plantilla NO se duplica). **GUARD ANTI-IDOR** (autorrevisión): tras el INSERT, exige que el rodeo con `p_id` pertenezca a `p_establishment_id` (42501) — sin él, un owner de otro campo que colisione `p_id` con un rodeo ajeno UPSERTearía su `rodeo_data_config` (bypass de RLS, SECURITY DEFINER). **UPSERT idempotente de los toggles** en `rodeo_data_config` (`ON CONFLICT (rodeo_id, field_definition_id) DO UPDATE SET enabled`). Grants `revoke from public/anon` + `grant to authenticated` + `notify`. BEGIN/COMMIT. **NO necesita `client_op_id`** (dedup natural por el id de cliente; replay completo = no-op total). Cubre: R5.1, R6.4, R6.10, R11.3, R11.4.
  - **`schema.ts`**: 2 overlay `localOnly` — `pending_rodeos` (espejo de las cols que la lista lee: id, establishment_id, name, species_id, system_id, active, created_at) + `pending_rodeo_data_config` (rodeo_id, field_definition_id, enabled) — cada una con `client_op_id`. GUARD del schema actualizado (7 overlay; total 34 tablas). Cubre: R6.11, R6.12.
  - **`outbox.ts`**: `enqueueCreateRodeo` (1 writeTransaction: op_intent `create_rodeo` + overlay `pending_rodeos` + la plantilla COMPUTADA en el cliente en `pending_rodeo_data_config`). `PENDING_OVERLAY_TABLES` += las 2.
  - **`upload.ts`/`connector.ts`**: `'create_rodeo'` ∈ `RPC_OP_TYPES` → `supabase.rpc('create_rodeo', params)` SIN `p_client_op_id` (dedup natural). ACK → `clearOverlay`; permanente → `rollbackOverlay`. NO hay caso `idempotent_discard` (replay = 2xx por ON CONFLICT).
  - **`rodeos.ts`**: `createRodeo` → id de cliente + computa la plantilla DESDE LOCAL (`fetchSystemDefaults` local + `computeConfigDiff` → `p_toggles`; `buildEffectiveConfigRows` → overlay) + encola por outbox + escribe overlay. **Firma pública intacta** (R11.1: sigue devolviendo `ServiceResult<Rodeo>` con el rodeo optimista). Eliminado `fetchRodeosOnline`/diff online.
  - **`local-reads.ts`**: `buildRodeosQuery` UNION `pending_rodeos` (el rodeo offline aparece en la lista); `buildRodeoConfigQuery` UNION `pending_rodeo_data_config` (la plantilla offline aparece en "editar plantilla"/el form dinámico). **⚠️ El `UNION ALL` puro de `buildRodeoConfigQuery` lo superseden T9.9** (pasó a overlay-override: para ALTA el synced está vacío y UNION-ALL alcanza, pero EDICIÓN sobre un rodeo ya sincronizado duplicaría el `field_definition_id`). Builders del overlay + `buildEffectiveConfigRows` (PURO, en `rodeo-template.ts`, testeado). Firmas intactas.
  - **`rodeo-template.ts`**: `buildEffectiveConfigRows(toggles, diffOps)` PURO (la plantilla efectiva optimista) + unit tests.
  - **Test** en `supabase/tests/animal/run.cjs`: owner crea (rodeo + plantilla 0018 + toggle aplicado), idempotencia (replay = no-op total), authz (no-owner → 42501), anti-IDOR (p_id colisionado con rodeo ajeno → 42501, plantilla ajena intacta). **FALLA hasta que el leader aplique `0081`** (esperado, patrón 0075-0080). Cubre: R6.10.

- [x] **T9.9** — ⚠️ **`editar plantilla del rodeo` OFFLINE vía RPC `set_rodeo_config` (0082) + outbox/overlay — un-defer (Raf: offline-first sin excepciones).** Gemelo de T9.8 para el camino de EDICIÓN: `editar-plantilla.tsx` escribía ONLINE (`toggleRodeoField`/`enableNonDefaultField`, UPDATE/INSERT directos a `rodeo_data_config` vía PostgREST → fallaba sin red). `rodeo_data_config` tiene PK COMPUESTA (read-only-local por el camino CRUD-plano) → RPC atómica + outbox + overlay (patrón T6/T9.8). *(Run T9.9 — la migración la escribe el IMPLEMENTER (`0082`); la APLICA el leader por Management API tras Gate 1 code + reviewer. El implementer NO la aplica.)* Alcance:
  - **`supabase/migrations/0082_set_rodeo_config_rpc.sql`**: RPC `set_rodeo_config(p_rodeo_id uuid, p_toggles jsonb)` returns uuid, SECURITY DEFINER + `search_path=public`. **DERIVA el establishment del rodeo** (`select establishment_id from rodeos where id = p_rodeo_id and deleted_at is null`; NOT FOUND → `P0002`) y **authz PRIMERO** (`is_owner_of(v_est)`, owner-only, espeja `rodeo_data_config_update/insert` 0018, 42501). **ANTI-IDOR HERMÉTICO POR CONSTRUCCIÓN**: a diferencia de `create_rodeo` (donde el est entraba por parámetro y necesitó el guard `c-bis` post-INSERT), acá el est se DERIVA del rodeo → un `p_rodeo_id` ajeno da `is_owner_of(est ajeno)=false → 42501` sin tocar nada; es imposible que el tenant autorizado y el rodeo objetivo diverjan. **UPSERT idempotente del diff de toggles** (`ON CONFLICT (rodeo_id, field_definition_id) DO UPDATE SET enabled`; el trigger 0078 fuerza `establishment_id` en INSERT *y* UPDATE, anti-spoof). Grants `revoke public/anon` + `grant authenticated` + `notify`. BEGIN/COMMIT. **NO necesita `client_op_id`** (dedup natural por el UPSERT; replay = no-op total). *(Gate 1 code: PASS, cero HIGH/MED. Cap de `p_toggles` clasificado LOW/opcional — backlog 2026-06-09 para simetría con 0081.)* Cubre: R5.1, R6.4, R6.10.
  - **`upload.ts`**: `'set_rodeo_config'` ∈ `RPC_OP_TYPES` → `supabase.rpc('set_rodeo_config', params)` SIN `p_client_op_id`. Clasificación: `P0002 && set_rodeo_config → permanent_reject` (el rodeo desapareció → el overlay optimista se ROLLBACKea; **distinto** del `idempotent_discard` de los `soft_delete_*`, donde la baja real ya ocurrió). 42501/23503 → `permanent_reject` (fallback). Cubre: R6.10, R6.12.
  - **`outbox.ts`**: `enqueueSetRodeoConfig({ rodeoId, params, configRows })` (1 writeTransaction: op_intent `set_rodeo_config` + overlay `pending_rodeo_data_config` por cada fila del diff). Reusa el overlay y `PENDING_OVERLAY_TABLES` de T9.8 (sin tabla nueva). **Fix rowid (Run T9.9 follow-up, 2026-06-09):** por cada fila del diff hace un **DELETE-PRIOR** (`buildDeletePendingRodeoConfig`) ANTES del INSERT del overlay, para garantizar el invariante de ≤1 fila de overlay por (rodeo_id, field) que `buildRodeoConfigQuery` necesita. Cubre: R6.11, R6.12.
  - **`local-reads.ts`**: `buildRodeoConfigQuery` de `UNION ALL` → **overlay-override**: el synced excluye los `field_definition_id` presentes en el overlay (`NOT IN`) `UNION ALL` TODAS las filas del overlay del rodeo. Una sola fila por field, el overlay pisa al synced (edición); degrada a "solo overlay" cuando el synced está vacío (ALTA, T9.8 sigue OK). Todo parametrizado (`?`), sin inyección. **Reconciliación rowid (Run T9.9 follow-up, 2026-06-09):** la dedupe por `MAX(rowid)` del diseño T9.9 original se ELIMINÓ — `rowid` NO existe sobre las VIEWS de PowerSync (regresión: `getAll` tiraba "no such column: rowid" online y offline; el unit test, contra `node:sqlite` con tablas reales, no lo cazó). La unicidad por field la garantiza ahora el DELETE-PRIOR del enqueue (builder nuevo `buildDeletePendingRodeoConfig`). Tests de COMPORTAMIENTO (SQL real contra SQLite en memoria): sin overlay→synced; edición pisa; **doble-edición con delete-prior→UNA fila con el valor nuevo**; alta→solo overlay; + string-test de `buildDeletePendingRodeoConfig` (DELETE 2 placeholders) y assert `doesNotMatch /rowid/`.
  - **`editar-plantilla.tsx`**: `onSave` reemplaza el loop online por UN `enqueueSetRodeoConfig` (`p_toggles`/`configRows` desde `computeEditDiff`; no-op si el diff está vacío). Eliminados `OFFLINE_COPY` + la rama de error de red + los imports de `toggleRodeoField`/`enableNonDefaultField` (el encolado es local → siempre OK offline; el error queda solo para fallo de DB local). **UX del guardado (cierre de zona, 2026-06-09 — Raf, consistencia con `editar-campo.tsx` `onSaved → router.back()` + closure de la acción terminal "Guardar"):** al ÉXITO del encolado, `onSave` hace **`router.back()`** (vuelve a RodeosScreen, de donde se llega vía `router.push`), NO se queda en la pantalla. El caso **diff-vacío** (sin cambios) también hace `router.back()` (tocar "Guardar" sin cambios = un "Listo"). El caso **error de encolado** (fallo de DB local, raro) NO navega: se queda y muestra el error (`setSaveError`). **Confirmación breve:** se evaluó disparar un toast ("Plantilla guardada"); NO existe primitiva de toast/snackbar reusable en `@/components` → se tomó el back inmediato silencioso (mismo patrón que el equipo ya acepta en `editar-campo.tsx`; NO se construye una primitiva nueva, fuera de scope del cierre). **REMOVIDOS** (ya no hacen falta al irse de la pantalla): el estado `savedOk`, el texto inline "Plantilla guardada" y el helper `reloadBaseOnly` (no se refresca el `baseConfig` porque se navega afuera). *(En `rodeo-config.ts` las viejas escrituras ONLINE `toggleRodeoField`/`enableNonDefaultField` se REMOVIERON en este mismo cierre — 0 callers verificado por grep en todo `app/`; el módulo quedó read-only. La authz owner-only vive en la RPC 0082.)*
  - **Test** en `supabase/tests/animal/run.cjs`: owner edita (toggle aplicado, `establishment_id` correcto por 0078), idempotencia (replay = no-op total), authz (no-owner → 42501, plantilla sin cambios), anti-IDOR (rodeo ajeno → 42501, plantilla ajena intacta), rodeo soft-deleteado → P0002. **FALLA hasta que el leader aplique `0082`** (esperado, patrón 0075-0081). Cubre: R6.10.

- [x] **T10** — **Guard de conexión anti-cuelgue para writes ONLINE-only (follow-up de validación en vivo, 2026-06-09).** Bug observado por Raf: editar perfil OFFLINE dejaba la pantalla en "Guardando…" PARA SIEMPRE — `saveProfile` (y el resto de mutaciones de identidad/admin) hacen `supabase.from(...).update/insert` / `supabase.auth` / `functions.invoke` directo que offline NO resuelven (la promesa nunca settlea). Como esos paths son ONLINE-only por spec (R7.1/R9.2: identidad/admin/login/invitaciones online; lo demás —campo/animales— ya es offline-first vía outbox), se agrega un **fast-fail** de conexión: en vez de colgar, devuelven un error `kind:'network'` accionable ("Necesitás conexión"). NO es un cambio de autorización ni de scope offline — solo evita el cuelgue. Cubre: R7.1, R9.2. Alcance:
  - **`powersync/online-guard-pure.ts`** (PURO, testeable sin SDK): `offlineError(connected: boolean|undefined, message)` → `{kind:'network', message}` si `connected !== true` (fail-closed: `undefined`/status sin poblar = offline → fast-fail, no cuelgue), `null` si conectado. Mismo patrón de separación que `status-derive.ts` (puro) vs `status.ts` (I/O) para no arrastrar RN al grafo de node:test.
  - **`powersync/online-guard.ts`** (I/O): `assertOnline(message, db?)` lee `(db ?? getPowerSync()).currentStatus?.connected` y envuelve `offlineError` en el shape de result de los services (`{ok:false, error}`). `currentStatus.connected` = socket de PowerSync (offline → false); no reemplaza la RLS server-side, solo evita el cuelgue del cliente.
  - **Call-sites gateados** (fast-fail al inicio, antes del primer supabase/auth/edge call): `establishments.ts` (`saveProfile`, `saveOwnPhone`, `createEstablishment`, `updateEstablishment`, `softDeleteEstablishment`); `account.ts` (`changeEmail`, `deleteAccount` — usan su propio shape `reason:'network'`, así que llaman `offlineError` directo); `members.ts` (`invokeFn` — un guard cubre las 6 ops de equipo: invitar/cancelar/regenerar/remover/cambiar-rol/aceptar). **NINGÚN write offline-first se gatea** (animals/events/management-groups/rodeos/outbox/upload — verificado por grep): alta/parto/eventos/lotes/crear-rodeo/editar-plantilla siguen offline.
  - **`app/app/(tabs)/mas.tsx`** (pantalla de perfil): offline → se SIGUE VIENDO nombre/email/teléfono (lectura local del ProfileContext) + `InfoNote` "Sin conexión: no podés editar el perfil ahora" + botones "Editar perfil"/"Guardar" deshabilitados (proactivo); si la conexión se cae mid-edit, el fast-fail mapea `network` → `OFFLINE_COPY`. Hook `useIsOffline` sobre `subscribeSyncUiState`. Cero hardcode (ADR-023).
  - **Test** `powersync/online-guard.test.ts`: `offlineError(true)→null`, `(false)→network`, `(undefined)→network`. Reviewer: APPROVED (sin regresión a paths offline; el guard no toca ningún write offline-first).

- [x] **T11** — **Fix showstopper: la app aterriza en ONBOARDING / listas vacías (follow-up de validación en vivo, 2026-06-09).** Bug CRÍTICO del swap de lectura T3: el gate de establecimiento (`EstablishmentContext`) y las lecturas resuelven el SQLite local **one-shot ANTES de que el first-sync poblara la DB** y NO re-evalúan → la app rutea a ONBOARDING (o muestra listas vacías) aunque el campo sincronice medio segundo después (confirmado por E2E: `user_roles=1, establishments=1, rodeos=1` bajaban, pero el gate ya había decidido). `runLocalQuery` YA degradaba "vacío + `!hasSynced`" a `kind:'network'`/"Sincronizando…", pero el bootstrap colapsaba CUALQUIER `!ok` a `no_establishments`. 100% client-side (no toca streams/migraciones/connector/overlay/schema/local-reads). Cubre: R5.4 (consumo correcto de la degradación) + R11.2 (no romper los flujos gateados). Alcance:
  - **`powersync/first-sync.ts`** (nuevo) + `first-sync.test.ts` (8 unit, `db` fake): `waitForUsableSync({timeoutMs?, db?})` → `'cached'` instantáneo si `hasSynced===true` (offline/reload: sync persistido restaurado de disco → NO esperar red), si no `await db.waitForFirstSync(abortSignal)` con `AbortController`+`setTimeout` → `'synced'`/`'timeout'`. `isFirstSyncPending(db?)`. `FIRST_SYNC_TIMEOUT_MS=4500`. `getPowerSync` por require LAZY (no arrastra RN al grafo node:test; el test inyecta `db`).
  - **`EstablishmentContext.tsx`** (el fix): (1a) bootstrap `await waitForUsableSync()` antes de `loadMemberships`; (1c) helper `applyMembershipsResult` compartido por bootstrap+refresh+listener: `network && isFirstSyncPending()` → NO afirma `no_establishments` (loading/preserva estado); (1b) listener `statusChanged` re-resuelve `refreshEstablishments()` SOLO en la transición first-sync false→true (var `lastHasSynced`, sin loops/falsos active_lost).
  - **`RodeoContext.tsx`**: listener `statusChanged` que re-corre `load(...)` en la transición false→true si está en `loading` (`isWaitingRef`) → el rodeo no queda colgado tras el fix del establecimiento (el RootGate exige est:active Y rodeo resuelto para home).
  - **`_layout.tsx`** (RootGate): sin lógica de sync nueva — `SPLASH_FALLBACK_MS = FIRST_SYNC_TIMEOUT_MS + 500` (el splash se destapa DESPUÉS de que el contexto resuelve; el listener re-rutea si el sync llega tarde).
  - **`(tabs)/animales.tsx`** + **`(tabs)/index.tsx`** (stepper): `useStatus()` de `@powersync/react` + efecto que re-corre la carga al avanzar `lastSyncedAt` (primitivo ms, no loopea). La lista/conteos se rellenan al bajar el sync sin re-foco. `useFocusEffect` = red de seguridad. NO se migró a `useQuery`/`watch` (backlog 2026-06-09).
  - **E2E** (`animals.spec.ts` reforzado con un assert anti-flash de onboarding): `animals.spec:386` ROJO→VERDE (repro directa); `auth.spec` 4/4 (onboarding legítimo intacto); `establishments.spec:68` verde. Residuales PRE-EXISTENTES al fix (rojos en baseline, fuera de scope → backlog 2026-06-09): `animals.spec:52` tail (stepper post-alta-offline), `:500` (overlay de exit sin `exit_date`), `establishments.spec:29` (crear-campo lee local antes del sync-down). **✅ LOS 3 CERRADOS (2026-06-09, run residuales-offline):** #1 `createEstablishment` con `id` de cliente (sin read-back local que dependía del sync) + aterrizaje OPTIMISTA en `EstablishmentContext` (`applyCreatedEstablishment`, merge-until-confirmed) → `establishments:29` VERDE; #2 `exit_date` de cliente en el overlay `pending_status_overrides` + `COALESCE(pso.exit_date, ap.exit_date)` en `buildAnimalDetailQuery` → `animals:500` VERDE; #3 ya cubierto por el `useFocusEffect`+`lastSyncedMs` de este fix (count UNIONa el overlay) → `animals:52` VERDE corrido solo. Detalle en `progress/impl_15-powersync.md` (Run residuales-offline). La flakiness restante de los alta-tests bajo carga serial es ambiental (DB beta contaminada, backlog "aislamiento de tests").
  - **Run bugfix-overlay-list (2026-06-10) — bug "animal creado OFFLINE desaparece de la lista al navegar de tab" (backlog 2026-06-10):** el re-query manual de `animales.tsx` cubría la LISTA pero no la BÚSQUEDA activa → con un término en el buscador, el re-foco mostraba `searchResults` stale ("No encontramos «N»") aunque el alta offline SÍ estaba en el overlay. Fix: `runSearch` extraído a callback y re-corrido en `useFocusEffect` + efecto de `lastSyncedAt` (simétrico a `loadList`). Las hipótesis de rollback espurio del overlay / JOIN / contexto se DESCARTARON con evidencia (repro E2E instrumentado en export prod Y dev server Metro: overlay intacto, clasificación transient en 10+ retries, cero `upload rechazado`). E2E nuevos (primeros offline reales de la suite): `animals-offline.spec.ts` ×2 — repro literal (verde en baseline, red de regresión) + alta vía buscador no-match (ROJO baseline → VERDE con el fix, verificado por stash). Detalle en `progress/impl_15-powersync.md` (Run bugfix-overlay-list) y design §5.1 as-built.

## Dependencias y orden

- **T1 → T2 → T3..T7** en web (T2 gateado por Gate 1; deploy T2.3 tras Puerta 1).
- **T3, T4 (lectura)** pueden ir antes que **T5 (escritura)**.
- **T6.2** (outbox + overlay + RPC-mapping) es **primaria** (opción ii de Puerta 1); depende de **T6.4** (delta de backend de idempotencia) **solo** para `register_birth`. `exit_animal_profile` (idempotente natural), `create_animal` (ids de cliente) y los `soft_delete_*` (guarda `deleted_at IS NULL`) NO dependen de T6.4. **T6.4 está GATEADA por Gate 1 (spec) sobre el delta + aprobación del leader** antes de aplicar la migración (≥ `0075`).
- **T6.5** (no doble-upload) depende de T6.2a/b (overlay `localOnly`).
- **T8** depende del dev build (no existe hoy) → diferido; el cierre de la spec se valida en web.
- **T9 (paso 2)** depende del paso 1 (V3 JOIN-FREE) ya en vivo. **T9.1 → T9.2 (Gate 1) → T9.3** (denormalización (A) → Gate 1 → streams). **T9.4/T9.6 dependen de T9.0** (decisiones (B)/(C) de Raf). **T9.5** (swap T4 a (b1)) depende de T9.4 y reemplaza T4.1. Cada migración del paso 2 es schema-sensitive → Gate 1 por delta antes de aplicar (mismo régimen que T6.4/T6.6).

## Resueltas en la Puerta 1 (2026-06-08)

1. **Estrategia de las (b) RPC-bound** — RESUELTA: **opción ii** (offline vía outbox + overlay local-only + RPC-mapping, T6.2). `import_rodeo_bulk` queda online (T6.1). La idempotencia (T6.2d) es el punto crítico.
4. **Roles offline** — RESUELTA: **owner-only** (ya aplicado en `est_members` por MED-1; T2.1). Sin cambio adicional.
5. **Delta de backend de idempotencia** — RESUELTA: **delta ADITIVO mínimo, solo `register_birth`** (T6.4), aprobado en Puerta 1. `exit_animal_profile`/`create_animal`/`soft_delete_*` son dedup-natural (sin delta). Schema-sensitive → Gate 1 (spec) antes de implementar (R11.4).
6. **Hueco DOUBLE-UPLOAD** — RESUELTO: **overlay LOCAL-ONLY (opción b)** (T6.2a/b, T6.5). El efecto optimista vive en tablas `localOnly` (no generan CrudEntry); la única CrudEntry de una op (b) es su `op_intent`; lecturas UNION; clear en ACK; rollback del overlay en rechazo. Evita el doble-apply y la colisión de ids server-side de `register_birth`.

## Decisiones abiertas (no bloqueantes para la aprobación de la spec)

2. Mecanismo de PK para `user_private` (PK `user_id`), `rodeo_data_config` y `birth_calves` (PK compuesta → `id` sintético) — decisión de sintaxis del SDK, se confirma en T1.3.
