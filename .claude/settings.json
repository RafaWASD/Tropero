{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node scripts/check.mjs --fast",
            "description": "Guardrail rapido por turno: estructura + feature_list + lint anti-hardcode, SIN tests (offline, ~1-2s). El check COMPLETO con tests (typecheck + RLS remota) se corre manual al iniciar sesion y antes de commitear (CLAUDE.md)."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(node scripts/check.mjs)",
      "Bash(node *)",
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(npx expo *)",
      "Bash(supabase *)"
    ]
  }
}
